# PROJECT_STATE.md — AutoCreate 작업 상태

> 새 창(특히 메인)은 이 파일 + `CLAUDE.md` + `docs/DESIGN.md` §0 + `docs/active/LANES.md` 정독.
>
> 🔴 **이 파일은 2026-09-16 에서 2026-09-23 까지 511 커밋 동안 안 고쳐졌다.** 그동안 «라이브 = 아직 빈
> 사이트(README만)»라고 **거짓을 말하고 있었다.** 오늘 우리가 «규칙 문서는 돌려 보는 자가 없다»를 잡았는데
> (AC-175), 🔴 **이 파일은 «아무도 안 고치는 문서»였고 그쪽이 더 크다 — 새 창의 첫 인상이기 때문이다.**
> ⇒ **라운드가 끝나면 메인이 즉시 갱신한다.** 안 고칠 거면 이 파일을 지우는 게 낫다.

**마지막 갱신: 2026-09-23 (메인) — 아래는 전부 그날 실측이다. 못 잰 것은 «못 쟀음»으로 적었다.**

---

## 1. 개요
- **AutoCreate(AC)** — 다계정 글·영상 자동 생성·발행 + 수익 통합 SaaS(토스형). AM 엔진 이식.
- 라이브: https://autocreate-endyd.netlify.app (GitHub `main` push → 자동 배포)

## 2. 지금 (2026-09-23 실측)

| 잰 것 | 수 |
|---|---|
| 라이브 커밋 | `51062e5` |
| 고객 화면 / 운영 화면 | 20개 / 20개 |
| Netlify 함수 | 83개 |
| 검사기(자) | 189개 |
| 마지막 DDL | `drizzle/0091-ac201-account-identity.sql` |
| 함정 노트 | 163항목 |

- 그날 배포에서 라이브로 **재서 확인한 것**: `.group .fr{padding-left:16px}`(줄이 왼쪽으로 튀던 것) · 계정 목록이 화면 앞쪽 3.6% 지점(전엔 한참 내려야 보였다).
- 품질 등급 **19축 ✓19 · ✗0 · ⊘0 = 100점**. 🔴 **단서가 점수에 붙어 있다 — «이 점수의 절반은 «모의만큼만» 참이다»**(넘기는 축 7 중 5가 모의를 본다 · 라이브 채널 17 ↔ 모의 11).

## 3. 🔴 «다 됐나»에 대한 답 = **아니오** (2026-09-23 실측)

`node scripts/verify-api-surface.mjs` — 공개된 문 **219종** · 화면 호출 187종 · **아무도 안 부르는 것 17** ·
🔴 그중 **«기능은 있는데 화면이 없는 것» 7종.** `CLAUDE.md` §4.8(«완료 = 화면에서 쓸 수 있을 때»)로 보면 **미완이다.**

| 문 | 무엇이 없나 |
|---|---|
| `ops-price-event-cancel` | 🔴 **고객 앱은 «요금 변경이 취소됐어요» 알림을 받을 준비가 끝났는데**(`public/js/ui.js:553`) **보낼 문이 화면에 없다.** 게다가 `plans.html:44` 의 상태 사전에 `cancelled` 가 없어 **영어로 찍힌다** |
| `ops-ticket-claim` | 🔴 **라이브에 실물이 있다** — 주인 없는 문의(50번 «카카오 채널 문의»)에 고객을 붙일 손잡이가 없다 |
| `ops-backup-status` | 서버·스크립트 다 있고 **한 줄만 안 그렸다**(놓을 자리도 계약에 적혀 있다 — `operators.html`) |
| `photo-usage` | «이 사진이 어느 글에 쓰였나» — **«내 사진함» 화면 자체가 없다**(§5E 내리기의 첫 걸음) |
| `revenue-oauth-start` | 서버는 끝났고 화면이 없다 |
| `ops-ticket-create` | 근거가 약하다 — 설계·계약에 없다. 쓸 자리 못 찾으면 **«유물»로 내려도 된다** |
| `ops-features` | 일부러 안 만든다(BYO 키를 안 판다 · 사장님 2026-09-16). **다시 팔기로 하면 그날 화면이 필요하다** |

- 그 밖 **중복문 3종**(`ops-audit` · `ops-ticket-update` · `support-ticket-create`) — 같은 일을 하는 문이 이미 있고 화면은 그쪽을 쓴다.

### ⊘ 못 쟀음 — 여기가 이 문서의 한계다
🔴 **«설계에는 있는데 아예 안 만든 것»은 못 쟀다.** 위 표는 **«문은 있는데 화면이 없는 것»만** 잰 것이다.
설계 대비 전수조사는 `docs/active/2026-09-15-DESIGN-AUDIT.md` 가 마지막이고 **그 뒤로 511 커밋이 지났다.**
⇒ **다시 돌리기 전에는 «전부»라고 말할 수 없다.** (2026-09-23 사장님께 여쭘 · 답 대기)

## 4. 사장님 손이 필요한 것 — «날 잡는 날»로 미뤄 두심 (2026-09-23)
구글 OAuth 앱 · Pexels/Pixabay 키 · Meta/틱톡/X 앱 · KICC 실결제 3종(부분 취소 포함) · 티스토리 로그인 1회(카카오 2FA) ·
러너 새 판 올리기 · 영상 레퍼런스 월 한도 결재 · Netlify 토큰 교체 · 약관 법률 검토 · 통신판매업 신고 · 사업자 정보 · admin 비밀번호 변경.
- **결정 대기 2건**: ① 설계 대비 전수조사 다시 돌릴까 ② `CLAUDE.md` §4.5 의 DDL 칸 표가 지금 칸이 아닌데 고칠까(정본은 `docs/active/LANES.md`).

## 5. 운영 메모
- Netlify site id `a14524de-ebfa-46e8-a116-dbc87343f276` · 이름 `autocreate-endyd`.
- Neon: 프로젝트 `old-tree-90235056`(us-east-2 · pg18 · db neondb) · DDL 은 `node scripts/neon-migrate.mjs <sql>`.
- 🔴 **라이브 키는 넷리파이에 있다. 로컬 `.env` 를 보고 «없다»고 말하지 마라** — API 로 읽으면 **마스킹된다**(별표16+뒤4자 = 20자 · AC-172).
- 초기 운영자: `admin@ops.local`(아이디 `admin`) · 시드 `scripts/seed-admin.mjs`.
- 화면 생성: `node scripts/build-pages.mjs`(템플릿 `public/app/_tpl.txt`·`public/ops/_tpl.txt`) — 생성물 커밋.
- push 규칙: push 1회 = 배포 = 비용. 검증 끝난 묶음일 때만(≤2~3회/일). docs·소변경은 commit 만.
