{
  "_설명": "🔴 «서버는 여는데 리포 어디에서도 안 부르는 API» 의 까닭 목록. `scripts/verify-api-surface.mjs` 가 이 파일을 읽고, **여기 안 적힌 orphan 이 생기면 빨강**이다.",
  "_왜 생겼나": "2026-09-20 — 그 자의 △ 줄이 «안 부르는 API 44종»을 **한 줄에 붙여** 찍고 58자에서 잘렸다. 화면에 보이는 것이 셋이었고, 묻힌 것 안에 «영상 돈 멈춤 스위치가 화면에 없다»가 있었다. **아무도 안 읽었다.** ⇒ ①찍는 모양을 고치고 ②△ 를 판정으로 올렸다. 다만 «화면이 안 부른다»(37종 · 대부분 정상)가 아니라 **«아무도 안 부른다»**(22종)로 올렸다 — 늘 빨간 자는 아무도 안 본다(AC-95).",
  "_규율": [
    "🔴 **새 orphan 이 생기면 그날 빨강이다.** 여기 적기 전에는 안 넘어간다 — 킬 스위치가 그날 잡혔을 자리다.",
    "🔴 **적어 뒀다고 없어지지 않는다.** 자는 갈래별 개수를 매번 찍고, «화면이 없다»의 수를 따로 센다. 그 수가 줄지 않으면 그것도 신호다.",
    "🔴 **까닭은 «내가 본 것»으로 적는다** — 이름으로 짐작하지 않는다. 아래 까닭은 전부 그 함수의 머리말이나 화면 전수 대조에서 나왔다.",
    "🔴 **모르면 «조사필요»에 둔다.** «유물»과 «아직 못 만든 것»을 짐작으로 가르면 다음 사람이 그 짐작을 사실로 읽는다.",
    "이제 부르는 데가 생기면 그 줄을 지운다 — 자가 «낡은 등록»으로 운다."
  ],
  "_언제": "2026-09-20 · C (첫 등록 22종) · 2026-09-21 · B (남는 8종을 네 갈래로 · 근거는 전수 대조) · 2026-09-22 · 메인 (A 가 내리기·프록시 콘솔을 만들어 5종을 지웠다 — 자가 «낡은 등록»으로 짚어 준 것만)",
  "바깥이 부른다(코드로는 증명할 수 없다)": {
    "/api/cs-inbound": "머리말: «메일 공급사의 인바운드 훅(Resend/SendGrid/Mailgun 계열)과 카카오 채널 훅이 여기로 밀어 넣는다» — 우리 코드가 부를 일이 없다",
    "/api/sso/enter": "머리말: «싸이렌MIS 허브 SSO 진입점» — 허브가 사용자를 이리로 보낸다",
    "/api/health": "바깥 감시가 찌르는 문. 우리 화면이 부를 이유가 없다"
  },
  "유물(옛 경로 · 호환으로 살려 둔다)": {
    "/api/support-ticket-create": "머리말이 스스로 적어 뒀다: «옛 경로 /api/support-ticket-create … 는 그대로 받는다(R1 화면 호환)». 지금 화면은 `/api/support-ticket` 을 쓴다",
    "/api/ops-audit": "`ops-center.ts` 가 «옛 경로는 그대로 살린다»로 남긴 묶음 경로. 지금 운영 화면(audit.html)은 `/api/ops-audit-search` 를 쓴다",
    "/api/ops-ticket-update": "한 번에 `status`·`tags`·`priority`·`assigneeId` 를 고치는 문. 🔴 **같은 일을 하는 문이 이미 셋이고 화면은 그 셋을 쓴다** — `-assign`·`-resolve`(status 네 가지 다 받는다)·`-priority`(태그까지 저장). 넷 다 `ops-cs.ts:277` 의 **같은 UPDATE 조립**으로 떨어지고, `ticket.html` 이 네 칸을 다 덮는다. 남는 차이는 «우선순위 없이 태그만 고치기» 하나뿐. 엄밀히는 «옛 경로»가 아니라 **«계약에 없이 같이 난 중복문»**이다(R4 계약 문 목록에 `-update` 없음 · 셋과 같은 커밋에서 났다). ⚠️ 감사 이름 `ops_ticket_update` 는 **네 문이 공유**하므로 «이 문이 쓰인다»는 증거가 아니다."
  },
  "화면이 없다(아직 못 만든 기능)": {
    "/api/ops-ticket-create": "운영자가 고객 대신 문의를 연다(`source:\"ops\"` · 만든 사람에게 자동 배정). 대신하는 문은 0개다(나머지 `createTicket` 호출 셋은 고객·메일훅·크론이라 운영자 문이 아니다). 🔴 **근거가 약하다** — 뜻은 `ops-cs.ts:11` **머리말에만** 있고 DESIGN §11.4 에도 R4 계약 문 목록에도 **없다**. R4 커밋에 계약 밖 덤으로 같이 난 문이다. «유물»로 볼 수도 없어(대신하는 문 0) «아직»에 두지만, 쓸 자리를 못 찾으면 다음 사람이 **«유물»로 내려도 된다**.",
    "/api/revenue-oauth-start": "고객이 애드센스·유튜브 수익 연결을 시작하는 문. 🔴 이번 판에 키가 없을 때 **브라우저 이동이면 화면으로 되돌려 보내게** 고쳤다(날 JSON 막다른 길 제거) + `revenue-sources.oauthReady` 로 화면이 **미리** 안다. 🔴 [2026-09-21 B] 서버 쪽은 이번 판에 다 됐다(이름·제목·기한까지 나간다). **화면은 A 가 만드는 중** — 그 브랜치가 머지되면 이 줄을 지운다. 아직 지우지 않는 건 `public/ops/**` 에 그 파일이 **없어서**다(있다고 적으면 그게 거짓이 된다).",
    "/api/pieces-reuse": "🔴 [R18 · 2026-09-26 B] «한 영상 여러 곳»의 **처음 한 번 묻기** 답하는 문(`netlify/functions/pieces.ts` · 정본 `lib/video/reuse.ts answerReuse`). 서버는 이 판에 다 됐다. **화면(원본 영상의 바텀시트 · `piece.html?reuse=ask`)은 A(`autocreate-a-f7`)가 같은 판에 만드는 중** — 머지 순서가 B → B2 → A 라 B 가 먼저 들어가면 이 문은 잠깐 «아무도 안 부른다». A 가 머지되면 이 줄을 지운다(안 지우면 이 자가 «적혔는데 이제는 부르는 줄»로 빨강을 낸다 — 그게 맞는 순서다).",
    "/api/pieces-remake": "🔴 [R18 · 트리거 §6-6 · 2026-09-26 B] «30초로 다시 만들 길» — 길이가 안 맞아 빠진 채널용으로 같은 소재 영상을 새로 만드는 문(`netlify/functions/pieces.ts` · 정본 `lib/director.ts remakeVideoFor`). 서버는 이 판에 다 됐다. **화면(원본 영상의 «빠진 채널» 줄 옆 단추 · 값은 `reuse.fit.skip[].remake.coins`)은 A(`autocreate-a-f7`)에게 v1.4 로 보냈다** — A 가 부르게 되면 이 줄을 지운다."
  },
  "조사필요(유물인지 아직인지 내가 못 가른다)": {
    "/api/coins-balance": "🔴 coins.html·home.html 전수에 `/api/coins*` 호출이 **0곳**이다. 화면이 잔액을 다른 문에서 받는 것 같은데 어디인지 못 찾았다 — 유물인지 «아직»인지 모른다. **모르는 채로 «유물»이라 적지 않는다**",
    "/api/topics-pick": "create.html 은 list·add·skip·refresh·reference·templates 를 부른다. `topics-pick`(candidate→picked)만 안 부른다 — 다른 문이 대신하는지, 빠진 건지 못 가렸다",
    "/api/support-ticket-message": "support.html 은 `/api/support-ticket`(단수)을 부른다. 그 문이 이어쓰기까지 겸하는지 확인 못 했다"
  },
  "_갈래": [
    "🔴 **아직** = `화면이 없다(아직 못 만든 기능)`. 설계·계약에 있는데 화면만 없다. **자가 이 칸의 수를 따로 센다** — 그래서 이 칸 이름은 바꾸지 않는다(바꾸면 그 수가 조용히 사라진다).",
    "🔴 **일부러** = `일부러 화면을 안 만든다(근거 있음)`. 화면이 없는 게 지금은 맞다. **근거(주석·설계·사장님 지시)를 까닭에 같이 적는다** — 근거 없이 이 칸에 넣으면 «아직»을 숨기는 자리가 된다.",
    "🔴 **유물** = `유물(옛 경로 · 호환으로 살려 둔다)`. 다른 문이 이미 그 일을 한다.",
    "🔴 **거짓양성** = 실은 화면이 부르는데 자가 못 본 것(보간·조립). **2026-09-21 전수에서 0건**이라 칸을 만들지 않았다 — 나오면 그날 만든다(빈 칸은 아무 말도 안 한다)."
  ],
  "일부러 화면을 안 만든다(근거 있음)": {
    "/api/ops-features": "플랫폼 한 벌짜리 기능 스위치(`ops_settings.features` · 지금 손님은 `byoAiKey` 하나). **기본 꺼짐** — 사장님 판단 2026-09-16 «수익 관점에서 BYO 안 하는 게 좋겠다»(`ops-features.ts` 머리말). «안 파는 기능»을 «키 꽂으면 즉시 가동»으로 재워 두는 자리라 지금 운영 화면이 없는 게 맞다. 끄는 일은 **이미 다 되어 있다** — `ai-key.ts:36,39,45` 가 꺼져 있으면 `enabled:false`·503 을 정직하게 준다. 🔴 **근거가 반쪽이다**: 머리말은 «기능을 재운 까닭»만 적었지 «화면을 안 만든다»고는 안 적었고, `scripts/_smoke/r8-byo-key-smoke.mts:98` 은 «운영센터에서 켤 수 있다»고 말하는데 실제로는 **API 로만** 켜진다. **다시 팔기로 하면 그날 화면이 필요하다** — 그때 이 줄을 «아직»으로 옮겨라."
  }
}
