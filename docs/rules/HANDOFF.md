# HANDOFF.md — AutoCreate 메인 세션 인수인계 (압축/새 세션용 단일 정본)

> 갱신 2026-09-15 · 작성자 = 메인(`autocreate-ca`) · **압축 후 새 메인 세션은 이 문서 + `docs/active/RESUME-TRIGGER.md` 를 먼저 읽는다.**
> 이 문서는 «지금까지의 서사»다. 규칙은 `CLAUDE.md`, 설계는 `docs/DESIGN.md`, 라운드 계약은 `docs/active/*-contract.md`, 함정은 `docs/rules/PITFALLS.md`(AC-1~64) 가 정본이고 여기서 요약·연결만 한다.

---

## 0. 한 장 요약

**AutoCreate(AC)** = 부수입을 원하는 개인이 여러 계정으로 글·영상을 AI로 만들어 자동 발행하고 광고·제휴 수익을 한 곳에서 보는 토스형 SaaS. AM(AutoMarketing)의 엔진을 이식하고 겉(제품·화면·계정 모델·수익 집계·결제·운영센터)은 새로 짰다.

- **개발 라운드 전부 종료**: Phase 0 · R1 · R2 · R3 · ui-v4 · R4 · R5 · R6 + KICC 이중 MID. 설계(`docs/DESIGN.md`) 대비 미개발 = 외부 선결조건(유튜브 심사 등)이 막은 것 + **§6 «사장님 실측 발견 5건»(진행 중)**.
- **라이브 = main `98e6a8d`**(배포 #10) · 🔴 **미배포 39커밋**(R8-A 전부 + 탈퇴 화면) → 배포 #11 대기 · 직전 #9 (`90364bc` · 라이브 확인 `ac.css?v=13` + «연결된 채널의 소재만») · 직전 #8 (`7bedd74` · 2026-09-15 · C «배포 가능» PASS 40·FAIL 0 · 라이브 스모크: `/api/push-key` publicKey 200 · `/api/account-slots` 401 · 설정 «알림 받기») · 이전 배포 #7 (`7985d37` · C «배포 가능» · 라이브 스모크 health 200 · create.html v9 «내 소재 넣기» · runner-download 401) · 로컬 main `012ce66`(B R6 §1 · 미배포 = 배포 #8 후보)(R6.5 전부 머지 · 러너 v1.1.4 ) · https://autocreate-endyd.netlify.app · GitHub `endyd116-dot/AutoCreate` · push = 배포(메인 단독)
- **결제(KICC) 실돈 실측 통과**(2026-09-15): 카드 등록(keyin MID) → ₩5,500 청구 → ₩2,000 부분 취소 → ₩3,500 환불 · 카드 최종 0원.
- 운영센터 `/ops` · `admin`/`admin1234`(**사장님 아직 안 바꿈**) · MIS 허브 SSO 카드 ⑥ 라이브.

---

## 1. 사장님(Swain) 지시 — 전역·항구 규칙

| # | 지시 | 반영 |
|---|---|---|
| 1 | **전본 개발** — 설계 대비 누락·왜곡·축소·요약 금지 · 트리거마다 «설계 대비 범위 지도» | `CLAUDE.md §8` · 메모리 `full-spec-no-reduction` |
| 2 | **UI = 깔끔·세련·심플 + 기능은 전적으로 토스형** · 잉크 블랙 | `DESIGN §13.0·13.0b·13.4` · 시안 정본 **`docs/screens-v4.html`** |
| 3 | **UI 세부는 메인이 판단**(시안 뿌리 + 더 심플·세련·고급·통일) · 사장님께 비교표 안 들고 간다 | 메모리 `ui-decisions-delegated` |
| 4 | **모든 화면 KST** | `DESIGN §13.5` · `CLAUDE §4.5b` |
| 5 | **파이프라이닝** — C 검증 중 다음 라운드 설계·발사 · C 결함은 C 가 수리 | `PARALLEL_GUIDE §2.6` |
| 6 | **배포는 라운드 끝에 묶어서**(라이브 P0/P1 은 핫픽스 즉시) | §2 |
| 7 | 러너는 **AM 러너 참조** | 이식 완료 |
| 8 | **부가세 별도(AM 방식)** — AM↔AC 코인 상호 이전 예정이라 같은 가격 구조 | `DESIGN §12.0` · 메모리 `pricing-vat-am-coin-exchange` |
| 9 | **KICC = 함께워크ON 사업자(광고대행업) 공용 · ON 과 구별** | `docs/active/KICC-GO-LIVE.md` · R4 계약 §1.6 |
| 10 | **러너 방향 = 관리형(우리 서버) 기본 + 비용 청구** — 결정 3개 대기(§6) | R7 후보 |
| 11 | 사장님이 실제로 써 보며 짚는 것 = 최고의 검증(2026-09-15 하루 5건 전부 진짜) | §6 |

---

## 2. 지금 상태 (2026-09-15)

### 배포 이력
| # | 커밋 | 내용 |
|---|---|---|
| 1 | `d2a4994` | Phase 0 |
| 2 | `e33807d`(+`ebbe372`) | R1 글 · R2 자동 편성·러너·승계 · R3 수익 |
| 3 | `0f3ec44` | R4 결제·체험·운영센터 12메뉴·CS·약관 |
| 4 | `e5458f8` | R5 영상 + KICC 이중 MID |
| 5 | `79dfe6a` | R6 내보내기·공유카드·추천인·영수증·관리형 신청·백업 |
| 6 | `4d06070` | R6 마감(공유 카드 금액 캐시) |
| **7** | **`7985d37`** | R6.5 사장님 실측 5건(직접 소재·캡션·자동편성 안내·건너뛰기·러너 배포 v1.1.4) + C 수리 4 + 화면 44px | 
| **8** | **`7bedd74`** | R7 전부(영상 축 개통 · 러너 배포·프록시·워밍업·관리형 계정당 · 탈퇴·파기·플랜 게이트·내부 테스트 제외·계정 더 쓰기·웹푸시 · 화면 전부 · C 검증 PASS 40/FAIL 0) |
| **9** | **`90364bc`** | 사장님 실측(만들기 «왜 이 채널만») · 러너 v1.1.7 · waitFor 준비 판정 |
| **10** | **`98e6a8d`** | B-1 통합 점검 P0 3건(심사 결과가 화면·승인에 안 닿던 것 · 영상 발행 실패 시 슬롯 · 옛 사유) |
| **11** | **`ba17be9`**(+`b229c09`) | R8-A 전부(글 계약 4축 · 영상 안전영역·배지 · 대가 3종·금칙 3층 · 검수 화면 «왜 이렇게 생겼나») + 🔴 보안 2(러너 지문 구속 · `.token` 권한) + 채널 «성질» 정본 한 곳 + 탈퇴 `reason:"closed"` · 라이브 확인 `ac.css?v=15`·«왜 이렇게 생겼나»·`GATE_HARD`·`ad_pointing` |
| **11** | **`ba17be9`**(+`b229c09`) | R8-A 전부 + 🔴 보안 2(러너 지문 구속 · `.token` 권한) + 채널 «성질» 정본 + 탈퇴 `reason:"closed"` |
| **12** | **`b040ad0`** | 🔴 **사장님 지시 3건이 코드가 된 판** — 하드 게이트 **0개** · 사진 조달(내 사진→스톡→AI · 편당 ₩458→₩134) · 직접 쓰기(`origin:"self"` · 코인 0) · 내려 주기 · 되먹임 원장 · 분 단위 발행 시각 · 채널 12행 · 대비 4.5:1 · 라이브 확인 `ac.css?v=17`·«꼭 보세요»·«왜 이렇게 생겼나» |
| 핫픽스 | `7ae8e78`→`2278ae7`→`f6c7eaa`→`f935034`→`7e0616e`→**`e7bd0c7`** | 티스토리 HTML · KICC 콜백 POST · 확인용 JSON · 빌키 keyin 고정 · 결과표 · 결제 실패 사유 시트 |

### 라이브에 있는 것
- **글 축**: 소재 → 디렉터(§5.3 6규칙·손보기) → 글·이미지(7채널 계약·AI티 8검사·고지) → 검수 → **편성표 자동 시계 7스텝** → 발행(네이버·티스토리 러너 / 블로거·WP API) → 승계 → 알림함.
- **수익**: 커넥터 5종(키 없으면 `not_configured` 정직) · 러너 스크랩 3종(**애드포스트 전 구간 라이브 실증**) · 홈 «오늘 번 돈»(확정+예상) · 신청 조건 게이지 · 학습 되먹임.
- **장사**: 체험 14일 → readonly · 코인(팩 3 + pack_trial) · 구독(월/연) · **KICC 이중 MID(auth/keyin · pg_mid · secretForMid)** · 플랜 게이트 · 운영센터 12메뉴 · 자동 티켓 · 원격접속 · 약관 4문서(«법률 검토 전») · 회사 정보.
- **영상**: provider 사다리(Omni·Veo·fal·스텁) · TTS(타입캐스트/Gemini) · SRT · **11축 심사 3등급(ffprobe 실측)** · 원가 관문(`checkAiCostCap` 재사용 · 소프트 통과/하드 ×3 환급) · 프레임 지문 · 러너 렌더(ffmpeg·BGM 12곡·배지) · 출구(유튜브 private·릴스·쓰레드·클립 스텁) · 화면 8.
- **R6**: 내보내기 ZIP(7일) · 공유 카드 PNG(핸들 숨김 기본) · 관리형 러너 신청 · 백업 상태(PITR 7일 · R2 버전관리 없음) · AC-36 await 전수 · 티스토리 HTML 모드. 🔴 **R6 §1(B 몫 · 추천인·영수증 페이지·세금계산서·회사 정보·AM↔AC 코인 이전)은 화면만 있고 서버가 없다**(2026-09-15 전수조사 발견 · AC-48 · `feature/p1r6-back` 으로 B 발주 중).

### 러너 실증 매트릭스
🔴 **정정(2026-09-15 전수조사 실측)**: 라이브 `posts` 는 **통틀어 2행이고 둘 다 `http://localhost:3997/index.html`**(C 하니스 스텁) — **실채널에 URL 이 뜬 적이 0회**다. 아래 «✓»는 러너가 그 단계까지 갔다는 뜻이지 발행이 아니다.
네이버 ✓(사진·서식·카나리) · 티스토리 ✓(HTML 모드 · **세션이 짧아 사람 2FA 필요** · `--reuse-tid=109` 는 티스토리 전용 회피책) · 블로거/WP 자격 없음 정직 정지 · **실제 «발행» 0건**(전부 임시저장). 🔴 **러너 배포 패키지 없음**(다운로드 404 · `npx ac-runner` 는 존재하지 않는 패키지 · B2 수리 중).

### 라이브 데이터·env(오늘)
- `channel_registry` 4 active(naver_blog·tistory·blogger·wordpress)/6 planned(유튜브 OAuth 앱 없어 못 켬).
- env: `FX_USD_KRW=1400`(원가 기준값) · `BGM_LICENSE_VERIFIED=1` · **`VAPID_PUBLIC_KEY`·`VAPID_PRIVATE_KEY`(is_secret)·`VAPID_SUBJECT`**(2026-09-15 메인이 생성·등록 · 웹푸시) · **`KICC_MODE=live`·`KICC_MALL_ID`·`KICC_MALL_ID_KEYIN`·`KICC_API_DOMAIN`(ON 이식)·`KICC_SECRET_KEY`(사장님 제공·is_secret)** · deps `@resvg/resvg-js` + `assets/fonts/Pretendard-*.otf`.
- Neon PITR **1일→7일**(메인 API).
- **테스트 계정 198 `test@autocreate.kr`/`autocreate12`** = 사장님 결제 테스트 계정 · 빌키 토스뱅크 ****0542 **보존** · **정리 완료 → trial · 자동청구 해제됨**.
- 보존 4집: 3·13·109·116(pro · MRR 49,000 은 이 집). 🔴 **t189(B2실증 · 티스토리 2FA 세션 보유)은 2026-09-15 정리에서 삭제됨** → 다음 티스토리 실측 때 **사장님 재로그인 1회 필요**(109 세션이 살아 있으면 `--reuse-tid=109`).
- **테스트 테넌트 대청소(2026-09-15 · 사장님 지시)**: B `scripts/ops-cleanup-tenants.mjs`(information_schema 자동 열거 · FK 위상 정렬 · 보호 5곳 id 하드코딩 3·13·109·116·198 · `--dry-run` 기본 · `--keep=` · `--revenue-test-rows` · R2 `r2DeletePrefix` 연동 · 감사 `ops_live_cleanup`) — 드라이런 91곳/1,853행/R2 887 · apply 는 B 창 사장님 Allow. B2 하니스 teardown(`finally` · 보존 id 우선 거부) 추가 · t200·t189 삭제. **규칙: 하니스는 끝에 자기 테넌트를 지운다(정리문 0 인 하니스 5개 = R7 항목)**.
- 운영 대시보드 «AI 원가 ₩15,820($11.30)」 = 전부 개발 검증(이미지 78% · C·B 하니스 9/13~14 · 198 $0.32 · B-1 $0.34) — 고객 0. «수익 회수 195,710」 = 전부 테스트 입력(R3 스모크·C·전수조사 수동 입력 + 애드포스트 스크랩 3,710) — 고객 0. R7 후보: 운영 숫자에서 내부 테스트 제외 표시.

### 🔴 돈 — 2026-09-15 밤 메인이 라이브 `ai_usage` 를 직접 읽은 값(추정 아님)

**글 1편 원가 = ₩458**(C 실호출 3편 $0.9819 ÷ 3 · FX 1,400). 쪼개면 —

| 무엇 | 3편 합 | 비중 | 한 편 |
|---|---|---|---|
| **사진**(`purpose='image'` · 18장) | **$0.8337 ≈ ₩1,167** | **85%** | ₩389(6장) |
| 글(`purpose='content'` · 6호출) | $0.1482 ≈ ₩207 | 15% | ₩69(2호출) |

- 🔴 **«글이 비싸다»가 아니라 «사진이 비싸다»** — 사진 1장(₩65) ≈ 글 1호출(₩35)의 두 배. 원가 분해 화면은 `purpose` 축을 **제일 먼저** 보여야 한다.
- **편당 글 호출이 2번인 이유** = 3편 **모두** 재작성에 걸렸다(유사도 또는 AI 티 → `content-gen.ts write()` 2회). 설계대로지만 값이 두 배가 된다.
- **받는 값**: 글 1코인 + 사진 6코인 = **7코인 = ₩3,500** (`lib/coin-table.ts` · 사진은 장당 1코인 별도 차감 — `piece:N:imgK` 원장 실측) ⇒ **마진 약 7.6배**.
- 🔴 **실호출 실험은 사진을 끄고 돌린다** — 글만 보는 실험에 값의 85%를 버릴 이유가 없다.
- 🔴 **운영 대시보드 «AI 원가»는 지금 거짓이다**(AC-71): 고아 103행 **$9.98** 이 «고객 비용»으로 세어진다(테넌트를 지우면 `NOT EXISTS` 거르개가 통과시킨다). 그중 **$9.00 은 하니스가 쓴 가짜 행**(`gemini-smoke` · 토큰 0 · $1.5000 × 6 · 전부 삭제된 테넌트). **진짜 고객 AI 비용 = 0원.** → B R8 발주 · 소급 채우기는 **합동 세션에서 사장님 Allow**.

---

## 3. 세션 지도 · 워크트리

| 세션(`ListAgents`) | 역할 | 폴더 | 브랜치 | 압축 시점 상태 |
|---|---|---|---|---|
| `autocreate-ca` | 메인 | `AutoCreate` | `main` | — |
| `autocreate-b-ae` | **B**(결제·운영센터·감사) | `AutoCreate-B` | `ops/cleanup-tenants`→`feature/p1r6-back` | 대청소 apply(사장님 Allow 대기 · 88곳) · 🔴 **R6 §1 서버 구현 발주**(추천인·invoice·tax·ops-company·coin-transfer) |
| `autocreate-b-53` | **B-1**(영상·내보내기·소재) | 창은 `AutoCreate-B` · **`cd ../AutoCreate-B1`** 로 일함 | `feature/p1r6-back1` | 전수조사 §4·§5·§5C·§16B·§6·§10(영상 축 고객 경로) → `audit/2026-09-15-B1.md` |
| `autocreate-b2-e4` | **B2**(러너·발행) | `AutoCreate-B2` | `feature/runner-dist` | ✅ 머지됨(`99a4a50` 배포 zip·다운로드·자동 업데이트·지문·rotate · `5638765` alt · `17a7f5d`) · `bbec8ce` runner-list 신호) · DDL 0012 적용 · 한 줄(Content-Disposition·«열쇠») 뒤 전수조사 §2·§8·§9 → `audit/2026-09-15-B2.md` |
| `autocreate-a-cc` | **A**(화면) | `AutoCreate-A` | `feature/p1r6-front` | ✅ 전부 머지됨(`…f982d0d` 건너뛰어요 정정 · `4a7c45e` 러너 화면) · 조사 1건: 시안 v4 ↔ 실제 화면 표(`audit/2026-09-15-A.md`) |
| `autocreate-c-41 [3d0736]` | **C**(검증·새 세션) | `AutoCreate-C` | `verify/p1r6-5` | R6.5 1차 검증 발부(topics-add · 캡션 §5C · A 화면 · 회귀) · 4·5절은 2차 |
| **`autocreate-b-e9`** | **B(새 세션 · 설계 대비 전수조사)** | **`AutoCreate-B3`**(새 워크트리) | `audit/design-2026-09-15` | 🔴 사장님 지시 — 트리거 `docs/active/2026-09-15-DESIGN-AUDIT-trigger.md` · 자기 몫 §0·1·3·5B·13·17·19 + 특별 항목 a·b·c + **통합**(`docs/active/2026-09-15-DESIGN-AUDIT.md` · 상위 20 은 직접 되짚기) |
| ~~`autocreate-c-41 [b3f2d6]`~~ ~~`autocreate-b-8a`~~ ~~`autocreate-a-a9`~~ | 옛 C·옛 B-1·옛 A | — | — | 빈 창 |

- 🔴 B·B-1 이 `AutoCreate-B` 폴더를 공유해 창 표시가 같다 → **모든 보고 첫 줄 = `■ 역할 · 폴더 · 브랜치 · 지금:`**(전 세션 적용). 커밋 전 `git branch --show-current`.
- 🔴 **라이브 데이터 변경은 그 세션 창에서 사장님 Allow** — 메인 승인은 다른 창을 열지 못한다. B 창은 auto 모드라 상자 자체가 안 뜬다(분류기 차단) → 사장님이 모드를 바꾸거나 메인이 직접(사장님 «청소해» 승인 있음 · 메인 창은 node 스크립트로 라이브 DB 쓰기가 됐다: 레지스트리 UPDATE·BGM 시드).

---

## 4. 인프라·키 (전부 등록)

- **Neon** `old-tree-90235056` · DDL `node scripts/neon-migrate.mjs`(0001~0011) · 키 `~/.neon-am-key` · PITR 7일.
- **Netlify** site `a14524de-ebfa-46e8-a116-dbc87343f276` · PAT `nfp_bB2BR1…`(AM 메모리 `neon-netlify-remote`) · **AC-8** 다른 사이트 secret 은 `****`.
- **R2** 버킷 **`siren-uploads`**(MIS·AM 과 공유 · 우리 키는 전부 `autocreate/` 접두 아래 · 러너 zip **`autocreate/runner/v1.1.8.zip`**+`latest.json`(sha256 0ec6b1e7… · 128,685B · v1.1.8 = R8-A 영상 고침: **제휴 고지 배지가 플랫폼 UI 에 가려지던 것** · 자막 좌우 채널값 · 폴백 220/450)) · 버전 관리 없음(AC-37) · 삭제 헬퍼 `r2DeletePrefix` 는 `autocreate/{tid}/` 꼴만.
- **KICC**: ON 계약 공용 · MID 2 · live · **빌키 = keyin 고정**(AC-45) · 복귀 주소 등록 불필요·KEYIN 시크릿 불필요(실측 확정) · 개통 정본 `docs/active/KICC-GO-LIVE.md`.
- 로컬 `.env`(워크트리 6곳 동기): `TEST_TISTORY_*`·`TEST_NAVER_*`·`FX_USD_KRW`·`BGM_LICENSE_VERIFIED`.

---

## 5. 결정 로그

| 주제 | 결정 |
|---|---|
| 슬롯 게이트 · reassigned · 전이표 정본 · finalize 실패 | R2 결정 그대로(fail-closed · 행 유지 · `account-health.ts` · 재시도 금지) |
| 크론 수동 | `/api/cron-run?every=&tid=&secret=` · 로컬 `CRON_BUDGET_MS=24000` |
| 홈 큰 숫자 | 확정·예상 둘 다 오늘 값 |
| 부가세 | 별도(AM 방식) |
| 구독 | tenants 정본 · subscriptions 장부 · readonly · included 월말 · 연납 월×10·포함분 매달 · 정지 3회째 |
| 영상 | Omni+Lite · video_15 Lite 강제 · 타입캐스트 기본 · 코인 구간제 · `uploaded_private` posts 만 · 3등급 심사 |
| 영상 원가 | **새 캡 없음** — `checkAiCostCap` 재사용 · 소프트 통과+운영 알림 · 하드 ×3 환급 · **하드는 `used` 만** · FX 없으면 안 막음 · FX 기본값 코드 금지 |
| 유튜브 업로드 | 서버 API 스트리밍(배경 함수 · 토큰 러너 미전달) |
| 공유 카드 | PNG(resvg+Pretendard · 함수 1개 included_files) · 핸들 숨김 기본 · 0원 미생성 · 키에 금액 |
| 영수증 공급자 | 운영센터 «회사 정보» 한 출처 |
| 관리형 가격 | `lib/plans.ts` 상수(요금제 칸 X) · Starter «못 쓴다≠공짜」 정가 |
| R2 덮어쓰기 | 결정론 키 0(TTS·자막 세대 해시 · AC-39) |
| KICC 콜백 | POST form 읽기(AC-44) · 맨 앞 감사 · 확인용 주문 JSON |
| 빌키 | keyin MID 고정(AC-45 · 인증 MID 는 8373 거절) |
| 결제 실측 | 고객 경로 그대로(AC-46) |
| 러너 복제 방어 | 토큰 없인 무용 + 인증 다운로드 + 기기 지문 + 토큰 재발급 + (R7) 셀렉터 서버 배포 |
| 캡션 | 생성 프롬프트와 분리 · 글쓴이 말투 ≤25자 · 채널별 `captionRate`(naver 0.3) · 묘사문 금칙 |
| 소재 입구 | AI 추천 + **직접 입력(`topics-add`)** · 코인 0 · 일 20 |

---

## 5B. R7 (2026-09-15 · **종료** · 배포 #8·#9·#10)

**결과**: §1 영상 축 · §2 러너·발행·프록시 · §3 결제·법·내부구분·계정 상품·푸시 · §4 화면 · B3 조사 잔여 — **전부 완료** · C 검증 **PASS 40 · FAIL 0 «배포 가능»** · 하루 203 커밋 · 배포 3회(#8 R7 전부 · #9 만들기 채널 안내+러너 v1.1.7 · #10 B-1 P0 3건).
**오늘 남긴 함정 AC-48~62**(15개) — 관통하는 한 줄: **«돌려 보지 않으면 잡히지 않는다»**(AC-51 배치 · AC-56 사슬 · AC-62 화면 한 바퀴) · **«대용물로 판정하지 마라»**(AC-57) · **«검사 코드의 catch 가 검사자의 눈을 가린다»**(AC-58).
**남은 것 = 사장님 합동 세션**(`docs/active/2026-09-15-OWNER-CHECKLIST.md` 12항목) + **R8**(다음 라운드 8 + settle 79곳 + recipe 카나리).
**오늘 숫자 2개**


**계약 = `docs/active/2026-09-15-P1R7-contract.md` v7.0**(유일한 입력 = 전수조사). 사장님 결정 4: ①영상 채널 켠다 ②실발행 GO(네이버 1건부터) ③요금제 채널 게이트 설계대로 ④관리형 기본·계정당 월요금·프록시 포함.
담당: B-1 §1 영상 개통 · B2 §2 실발행·러너·관리형 1대 · B §3 탈퇴/게이트/내부 구분 · A §4 화면 · C §5(AC-48 전수 diff 포함) · 메인 §7 DESIGN 갱신.

## 5C. R8-A — 「한국 매체 말투·구성 실측 → 우리 에디터 검증 → 디벨롭」 (2026-09-15 · **종료** · 미배포)

> 사장님 지시: «한국 시장에서 각 매체별 말투와 모습들을 **모조리 서칭·학습**한 다음, 지금 우리 글·영상 에디터 구성이 **제대로 되어 있나부터 검증**하고 디벨롭해라.»
> 계약 `docs/active/2026-09-15-R8A-channel-voice-contract.md` · 산출 `…-R8A-voice-text.md`(B-1) · `…-voice-video.md`(B2) · `…-voice-policy.md`(B3)

### 이 라운드의 한 문장
**«우리 계약은 실물이 아니라 SEO 블로그가 말하는 이상적인 글을 베끼고 있었다.»**

### 성적표
- 글(B-1): **➖ 11**(우리가 넣었는데 실물 근거 없음 — 네이버 4개는 **근거 미상**) · **❌ 8**(실물엔 있는데 우리에게 없음) · ⚪측정불가 4(➖ 로 안 셈 · AC-9)
- 🔴 **골격 가짓수 3~5 → 11~32**(블로거·워드프레스는 **4편째부터 반드시 겹치던** 상태) · **골격 반복 축 신설**(소프트 · `pieces.meta.structurePrint` · 음성 대조 통과) — 지금 `similarity` 는 **글자만** 보므로 «단어만 바꾸면 통과»였다
- 계약 성격 변경: 고정값 → **범위 + 주제군 갈래 + 수익 목적(3축) + 필수/선택/억제 3단**
- 🔴 **`briefs.goal` 이 `content-gen` 에 한 번도 안 갔다**(사장님 질문 «같은 네이버라도 수익 목적에 따라 달라야 하나?» 의 답) → 이제 프롬프트에 실린다: adpost «다시 오게 · 광고 자리 안 만든다(네이버가 붙인다)» / adsense «오래 읽게 · 광고 자리 앞뒤 문단 안 끊는다» / affiliate «고르게 돕는다 · 고지 첫머리»
- 영상(B2): 🔴 **안전영역이 전 채널 공용 220/300 이고 축이 «그 숫자인가»를 재고 있었다**(쇼츠는 390 필요 → 자막 마지막 줄이 먹힘) → 채널별 `SAFE_ZONE_OF` + 축이 **이름대로** 재게 · 🔴 **제휴 고지 배지가 플랫폼 UI 에 통째로 가려지고 있었다**(품질 아니라 정책) · 자막 2줄 규칙 신설 · **채널 분기 0개**·`contractFor` 결과 버림 정리 · 러너 **v1.1.8**
- 정책(B3 · 공식 문서 근거): 🔴 **협찬·체험단·무상 제공 글에 고지가 안 붙었다**(제휴일 때만 켜짐 · 켜는 화면 0) → **대가 3종**(`affiliate`·`sponsored`·`gift` · **켜기만 받는다**) · 금칙 **3층**(HARD/NEEDS_PROOF/TONE · `superlative` 하드→소프트 — «판매량 1위(2026년 9월 네이버 쇼핑 기준)» 같은 **실증 붙은 문장도 막던** 과차단) · **의료법 §56 환자 치료경험담** → 건강·의료 소재면 **경험담 구성 금지**(사람·자동 두 경로) · `ad_pointing` 축 · 해시태그 4건(쓰레드 토픽 태그 0~1 · 유튜브 설명 4 · `snippet.tags` 500자 컷)

### 🔴 사장님이 바로잡으신 것 — 유도 3층
«애드센스는 **무분별한** 유도가 문제지, **우리는 소구점 잡아서 유도시켜야** 한다.» ⇒ **광고를 향한 유도 = 직접·간접 모두 금지**(공식 원문 «어떤 식으로든 … 고의로 유도해서는 안 됩니다») / **우리 링크(쿠팡) = 허용** / **독자 행동 = 더 해야 한다**. 그리고 실측이 사장님 편이었다 — **실물은 «행동 유도 한 줄»로 끝나는데 우리는 `faq` 로 끝났다**(유도가 과한 게 아니라 **부족**했다).

### 실호출 2편($0.2147)로 배운 것 = AC-63
«프롬프트에 넣었다»는 증거가 아니다. **부딪치는 자리에서만 구조가 이긴다**(끝맺음은 ②칸 «순서 그대로 채워라»와 부딪쳐 안 따라왔고 → **구조로 고치니** 나타남 / 종결어미는 안 부딪쳐 문장만으로 따라옴 «~세요» 0→5). 🔴 **분량은 세 번째 종류** — 블록을 12→17 로 늘렸더니 글이 **줄었다**(1,849→1,503자) = 모델이 총 분량 감각을 갖고 **나눠 담는다**. R8 로.

### 못 한 것(정직)
🔴 **우리 도구가 `naver.com`·`threads.net` 을 아예 못 읽고 인스타는 로그인 벽** — 6채널 중 3채널 실물 미확인 · 영상은 **아예 볼 수 없어** 첫 3초·컷 길이·자막 모양이 ⬜미측정. 네이버 값에는 «실물 미확인 · 업계 통설» 주석. ⇒ **체크리스트 13·14번**(사장님 화면).
🔴 조사 중 확인: **네이버는 «본문 몇 자·이미지 몇 장» 같은 수치 기준을 공식적으로 낸 적이 없다** — 업계 «1,500자»는 통설이다.

## 5D. R8 — 「마지막 개발 라운드」 (2026-09-15 · **진행 중** · 배포 #12)

> 계약 `docs/active/2026-09-15-P1R8-contract.md` v8.0(+§10 사장님 사진 지시) · 목표 = 설계도 남은 **64행**(③54 + ④10)을 닫아 «개발은 끝났고 열쇠만 남았습니다»를 **사실로** 만드는 것.

### 🔴 이 라운드의 성격이 도중에 바뀌었다 — 사장님이 다섯 가지를 새로 넣으셨다

**전부 설계도에 아예 없던 것**이다. 즉 «100%»의 뜻이 하루 사이에 바뀌었다.

| # | 사장님 지시 | 무엇이었나 | 설계 |
|---|---|---|---|
| 1 | **사진 값이 제품을 막는다** | 글 1편이 **7코인(₩3,500)** — Pro 를 사도 월 21편인데 설계는 90편. 원가의 **85~92%가 사진** | §10(계약) |
| 2 | **직접 쓰기가 «주요 골자»** | ②맡기기·③디벨롭은 있는데 **①내가 다 쓰는 길이 통째로 없었다**(`INSERT INTO pieces` 가 한 곳뿐이고 반드시 AI 를 부른다) | **§5D 신설** |
| 3 | **내리는 워크플로우** | 🔴 **삭제 잡이 0개** — 우리는 **올릴 줄만** 알았다 | **§5E 신설** |
| 4 | **ML — 데이터는 우리가 가져온다** | 성과 회수는 있는데 배우는 게 «소재·시각» 둘뿐. **«어떻게 쓴 글이 잘 되나»가 한 번도 이어진 적 없다** | **§5F 신설** |
| 5 | **게이트 최소화 → 하드 0개** | 하루에 **세 번** 좁아졌다: ①법·플랫폼 → ②법·제3자·비가역 → ③**전부 소프트** | **CLAUDE §9 신설** |

그리고 **말투**: «앞으로는 우리가 **알려만 드리고** 그대로 올라가요» → 🔴 **«협박성 글 쓰지 마. «주요 안내는 사전에 알려드려요» 이렇게 하면 되지.»**(CLAUDE §3) — A 가 **하니스에 박았다**(위협 낱말이 화면에 있으면 빨강).
그리고 **코인**(사장님 승인): **글 1편 = 1코인**(AI 사진 1장 포함) · **AI 사진 추가 = 1장당 1코인** · **고객 사진·스톡·직접 쓴 글 = 0**. ⇒ Pro 가 월 21편 → **150편**. B 가 표를 짜는 중.

### 🔴 오늘의 관통선 — «다 만들어 놓고 아무도 안 부른다»

같은 병이 **여덟 번** 나왔다: `verify.post_alive` · `learn.ts` · `ads.*`(WP 위젯) · `coinCostOverlay` · `blocksCharCount` · `gapMinFor`·`floorMin` · `caps`(화면이 네 곳에서 읽는데 서버가 SELECT 를 안 함) · `review_blocked`(우리가 게이트를 0개로 만든 **그 변경**이 `IN ()` 를 만들어 SQL 에러로 행이 통째로 죽음).
⇒ **`scripts/verify-r8-deadends.mjs`**(C) — 「정의가 있나」가 아니라 **「제품이 부르나」**로 센다. 🔴 **하니스·문서는 호출로 안 센다** · **바깥 N · 자기 파일 M · 임포트만 K** 를 늘 같이 찍는다(한 숫자로 뭉치면 어느 쪽으로든 거짓말한다). **머지마다 돌린다.**

### 오늘 잡은 것 중 사장님께 의미 있는 것

- 🔴 **수익 목적이 라이브 글의 56% 에 한 줄도 안 닿았다** — 사슬은 안 끊겼고 **값**이 끊겼다(`goalOf` 가 `mixed` 를 쓰는데 `goalRules` 에 `mixed` 키가 0개). `grep` 으로 사슬만 따라가면 **초록**으로 나오는 자리.
- 🔴 **네이버 글 10편 중 8편이 재작성을 돌고 있었다**(C 가 제품 게이트로 40편씩 실측 · 수리 전엔 **100%**). **계약이 스스로와 싸웠다** — `visualMin` 이 요구하는 블록이 `tiers.optional` 에 있어 seed 로 떨어진다. **미달 = 재작성 = 그 편 값 두 배.** 수리 뒤 **전부 0%**.
- 🔴 **검색량을 잰 키워드를 버리고 그 숫자를 «소재 제목»에 붙여 모델에게 준다** — 모델이 **틀린 문구를 노리고 쓴다**(AC-57).
- 🔴 **인스타 주소가 열린 적이 없다** — 숫자 id 로 permalink 를 만들어 **발행은 «성공»인데 «글 보기»만 조용히 죽는다**(릴스에 이미 있던 코드).
- 🔴 **러너 열쇠 파일이 같은 PC 의 다른 사용자에게 읽혔다**(`0600` 을 Windows 가 무시해 644) · **지문 구속이 헤더만 빼면 통째로 건너뛰어졌다**.
- 🔴 **«다시 만들기»가 고객 사진을 날렸다**(오늘 사진 업로드가 생기며 **처음 닿을 수 있게 된** 경로).
- 🔴 **접근성**: 수익 화면 **한 장에서만 21건** 대비 미달(회색 글씨 전체가 2.0~3.0:1). 글자 전용 토큰을 따로 만들어 고침 · 감사가 **80여 장을 매번 다시 잰다**.
- **공식 문서가 뒤집은 것**: FAQ 리치 결과 **2025-05-08 지원 중단**(HowTo 2023-08 폐지) ⇒ 우리 `faq` 의 SEO 값 **0** · **AEO 전용 마크업은 없다** · 구글이 «**길이 자체는 순위와 무관**»이라고 직접 말한다(분량 손잡이의 근거를 «SEO»로 적으면 틀린 근거).
- **분량**: 736자 → **1,566자**($0.17 · 사진 끄고). «문단 최소 340자»는 **안 먹혔다** — 분량은 문장·블록 수·블록당 최소치 **셋 다로 직접 안 잡힌다**.

### 세션 지도(변동)

| 창 | 역할 | 폴더 |
|---|---|---|
| `autocreate-a-cc` | A 화면 | `AutoCreate-A` |
| `autocreate-b-ae` | B 결제·운영센터 | `AutoCreate-B` |
| `autocreate-b-53` | B-1(구) 글·영상 | `AutoCreate-B1` |
| **`autocreate-b-a6`** | **B-1(신)** | **`AutoCreate-B1B`** |
| `autocreate-b2-e4` | B2(구) · 대기 | `AutoCreate-B2` |
| **`autocreate-b2-b7`** | **B2(신)** | `AutoCreate-B2` |
| `autocreate-b-e9` | B3 정책 · 대기 | `AutoCreate-B3` |
| `autocreate-c-41 [3d0736]` | C 검증 | `AutoCreate-C` |

### 오늘 굳은 운영 규칙(새로 생긴 것)
- **CLAUDE §9** 게이트 최소화(하드 0개 · 말해 주기 넷) · **§3** 겁주지 않는다 · **§4.5** DDL 번호 **세션별 칸**(하루 **여덟 번** 겹쳤고 `ls` 로는 못 막는다) · **`.gitattributes`**(소스 LF · 전체 정규화는 브랜치가 빌 때).
- **PITFALLS AC-65~74**(10개) — 호환으로 열어 둔 문 · 플랫폼이 무시하는 안전장치 · **검사 코드를 셸 한 줄로 쓰지 마라**(오늘 5회) · 조이는 변경은 «통과해야 하는 것»도 재라 · **정의가 아니라 호출** · 잣대를 같게 해도 **대상**이 다르면 대용물 · 부모를 지우면 자식 분류가 넘어간다 · 값이 표에 없으면 사슬은 이어져도 아무 일 없다 · 계약은 생성에서도 발행에서도 안 따라온다 · **화면의 «숫자»도 서버가 정본**.

## 5E. 🔴 2026-09-16 — R8 마감·배포 · 일하는 방식을 바꾼 날

> 하루 숫자: **커밋 92 · 머지 31 · 창 6개**(→ 4개로 정리) · R8 **51% → 82%** · **배포 2회**(본판 `1a4eeb3` 143커밋 + 핫픽스 `fa1e446`)
> 🔴 **오늘의 한 줄**: «이어졌나»를 나중에 찾지 말고 **만드는 순간에 박는다.** 그리고 **검사는 일부러 망가뜨려 봐야 믿는다.**

### R8 최종 — **65칸 중 닫힘 53 · 일부 5 · 열림 7 (82%)**
`node scripts/verify-r8-audit64.mjs` 가 그 한 줄을 **직접 찍는다**. 분모 = ③54 + ④13 = 67 중 **안 만들기로 결정한 2칸(트레이)을 뺀 것**.
🔴 남은 12칸: **사장님 손 5**(브런치·클립 계정 · 클립 앱 화면 · 편성 다중 규칙 UI 확인) · **미룸 2**(릴스 90초=Phase 5 · 에디터 요소=R9) · **우리 몫 5**(계정 간 유사도 · piece `edited` · 쇼핑 태그 · X 영상 · 출처 헤더 25).

### 🔴 시간이 어디서 샜나 — 이 숫자가 오늘의 결론이다
| 무엇 | 몇 건 |
|---|---|
| **«다 만들어 놓고 아무도 안 부른다»** | **15건** |
| **검사가 틀려서 «미개발»로 셈**(제품은 멀쩡) | **6건** |
| **창을 잘못 짚음**(메인 4회) | **4건** |
⇒ **21건이 «만든 게 안 닿아서»** 생겼고 **찾는 시간이 만드는 시간보다 길었다.**
🔴 그중 **허용 목록이 세 번** 값을 조용히 먹었다(`ALLOWED_SETTINGS` · `sanitizeProfile` · `pieces-get meta`) — 저장 200 인데 새로고침하면 사라진다.

### 🔴 사장님이 바꾸신 것 — 일하는 방식(`PARALLEL_GUIDE §2.7`)
> «메시지 소통으로만 하니 **사소한 개발 하나가 너무 오래 걸린다.**»

**메인이 틀린 지점**: 어제 트리거 문서 12개 → 오늘 **1개**. 대신 **긴 메시지 20통 넘음.** 메시지마다 규칙·함정·상태를 처음부터 다시 적었고, **«누가 무엇을 맡았나»가 메인 머릿속에만** 있어서 **같은 일(B5)을 두 창에 줬다**(한 창이 만든 걸 통째로 버렸다).
⇒ **AC 다섯 + AM 넷**을 합쳐 양쪽 공통 정본으로 박았다. 핵심:
- **발부는 문서(트리거+담당 표) · 보고는 메시지 10줄** · 🔴 **단독·30분·계약 없는 일은 «메시지 5줄»이 더 빠르다**(AM 이 손본 자리 — 메인 판이 너무 절대적이었다)
- 🔴 **메인은 직렬 병목이 되지 않는다** — 기술 질문은 **창끼리 직접** · 메인은 **범위·머지·사장님 결재**만. **«메인이 글 쓰는 시간이 곧 사장님 대기 시간»**
- **C 는 «다른 모양의 자»로 잰다**(같은 하니스 재실행은 검증이 아니다) · **검증은 층을 나눈다**(무거운 검사 ≠ 정확한 검사) · **머지를 미루지 않는다**(미루는 건 push 하나뿐)
- **창 4개 고정**(A·B·B2·C+메인) — 창 4개면 붙일 짝 6개인데 **6개면 15개**. 오늘 사고 4건이 **전부 창을 늘린 뒤** 났다
- 🔴 메시지가 **진짜로 버는 둘은 남긴다**: **세션↔세션 직접 넘기기**(되돌릴 수 없는 사고를 막았다) · **«멈춰라»가 즉시 가는 것**
🔴 AM 에도 전달했고 AM 이 **같은 방식으로 전환**했다(AM `PARALLEL_GUIDE §2.6·2.6-1`). AC 함정 **AC-69·75·79·87·92** → AM PITFALLS **#1740~1744**.

### 사장님이 오늘 새로 정하신 것
| | 결정 |
|---|---|
| **코인 등급제** | **간단히 1 · 보통 2 · 프리미엄 3**(상한 3) · 내 사진·스톡 **0** · 덜 구우면 **덜 받고 돌려준다** · 사진 0장도 **1코인** · 기본값은 **계정마다** · 🔴 «최소»라 부르지 마라 |
| **BYO(고객 AI 키)** | **화면에서 끔** · 코드는 «키 꽂으면 즉시»로 **재워 둠**(Agency 용). 대신 **우리 AI 계정 2~3개**를 키 로테이션에 꽂으면 분산된다(이미 완성) |
| **레퍼런스 학습** | **캡처 + AI 비전** · 🔴 **우리 러너가 찍는다**(고객은 주소만) · **폰 폭 · 2~6장 · 10~15% 겹치게** · 코인 **0** |
| **계정** | 🔴 글/영상으로 **안 나눈다**(인스타 하나가 피드도 릴스도 · 네이버 ID 하나가 블로그도 클립도) |
| 🔴 **메인이 만든 게이트** | «남의 블로그를 러너로 긁지 마라»는 **코드에도 설계에도 없었다.** 메인이 혼자 지키던 것 → **없앴다** |
| **라운드** | **R9·R10 동시 · 그다음 R11** — R8 닫고 배포한 뒤 |

### 🔴 오늘의 관통선 — 어제 «여덟 번»이 오늘 **«열다섯 번»**이 됐다
`heroNeeded`(적기만 하고 읽는 곳 0) · `creditLineOf`(스톡 크레딧이 본문에 안 실림) · `/api/pieces-self`(**직접 쓰기** — 사장님 «주요 골자»인데 화면 0) · `/api/takedowns`(내리기) · `numberClaims` · 팀 시트(**요금표가 자리 5개를 파는데 늘릴 길 0**) · `team-accept.html`(**초대 메일이 없는 파일을 가리킴**) · `/app/index.html`(**탈퇴 되돌린 고객이 404**) · CS 바깥 유입(**값은 있는데 들어올 문이 없었다**) · E1 원가 분해(**서버가 여섯을 내는데 화면이 0곳**) · 영상 레퍼런스(**절반을 배워 오고도 `structure` 하나만 읽었다**) · `purgeProfileKeys` · `caps` · `refUnused`(한 칸 더 앞이 막혀 있었다) · 서식 «못 냈어요»(미리 막았다).

### 🔴 돈이 새던 자리 — B 가 AC-92 를 들고 훑어 찾았다(넷)
「다시 만들기」 재차감만 옛 식(**사진 9장에 10코인**) · 편성표 영상 견적이 «없으면 60초»로 답해 **15초 고객에게 28코인**(실제 12 · **2.3배**) · 환급이 실패해도 «**0개 돌려드렸어요**» · 영상 재차감이 «모르면 **제일 비싼 값**».
⇒ **AC-93** 이 나왔다: **그 값이 그대로 실행되면 기본값, 실행과 다른 것을 말하면 날조.** 그리고 **모르는 것으로 돈을 물릴 때는 모자라게 받는 쪽으로만 틀린다.**

### 함정 — 오늘 **AC-75~95 · 21개**가 늘었다(총 95)
특히: **AC-75**(설계가 «흡수»로 옮겨 적은 항목을 옛 이름으로 세지 마라) · **AC-79**(창은 «지금 무엇을 건드리나»로 가른다 · **«가라»보다 «멈춰라»가 늘 싸다**) · **AC-86**(화면 파일은 생성물이라 직접 고치면 **조용히 덮인다**) · **AC-87**(낱말로 세는 검사는 «그렸다»를 증명 못 한다 · **빨강을 낼 수 있는 검사라는 것까지가 증거**) · **AC-92**(폴백이 «모른다»를 «특정 값»으로 바꾸면 **일어나지 않은 일을 사실처럼 말한다**) · **AC-94**(«검사를 전부 돌린다»가 안전한 동작이 아니다).

### 배포 — **2회**
1. 본판 `1a4eeb3`(143커밋) · 2. 🔴 핫픽스 `fa1e446` — **배포 직후 라이브에서 잡았다**: `home.html` 이 혼자 옛 CSS·JS(`v=19`·`v=24`)를 부르고 있었다. 손수 관리 화면 **14장**이 `build-pages.mjs` 가 판을 올려도 **안 따라왔다**. **다시 오는 고객만** 옛 화면을 보고 **우리 쪽엔 오류가 한 줄도 안 났다.** ⇒ `scripts/verify-asset-versions.mjs` 신설(정본을 손으로 안 적고 `build-pages.mjs` 에서 **읽는다**).

### R9+R10 발부 완료 (2026-09-16)
계약서 `docs/active/2026-09-16-R9R10-contract.md` + 창 넷 트리거(`-A-` · `-B-` · `-B2-` · `-C-`).
브랜치는 네 폴더에 **미리 깔아 뒀다**: `feature/r9-front` · `feature/r9-back` · `feature/r9-runner` · `verify/r9`(전부 `fa1e446` 위).
🔴 **한 곳만 직렬**: «배운 서식을 글에 반영»은 **B2 의 서식 이식이 끝난 뒤**다.
🔴 **이 라운드의 본체는 «번짐»** — AM 이 **네 번** 얻어맞은 병. 코드가 아니라 **세 겹**(문단 경계 끊기 · 칠한 직후 «더럽다» · 발행 직전 자가검사)과 **변이 하니스**가 이식의 본체다.

---

## 6. 열린 항목

### 진행 중(압축 시점 · 세션별 발주)
1. ✅ **테스트 정리 완료**(2026-09-15 · 사장님이 B 창에서 직접 Allow · 감사 `ops_live_cleanup`): 160·196·197 삭제 · t189 trial+queued 잡 14 삭제 · **198 trial + `next_billing_at NULL`(10/15 자동청구 해제)** · 빌키 ****0542 보존 · 인보이스 #10 refunded 보존 · 포함코인 33 회수. **MRR 73,000 → 49,000**(116 pro 만) · 활성 유료 1 · 보존 4집 생존.
2. ✅ **B-1 머지됨(main `04a08c7`)**: `c0fbc2b` `POST /api/topics-add`(400 title·banned_category·duplicate(+topic 동봉)·channel · 429 rate · Topic.source ai|manual · manual 맨 위) · `84a2372` 캡션 §5C(image{prompt,caption?} 분리 · 묘사문 7패턴 금칙 · captionRate naver 0.3/tistory 0.2/blogger·WP 0.5 · alt 는 prompt 파생). 완료: 글 실호출 1건(t207 piece 330 · $0.34 · 캡션 1/6 «자리마다 쪽지랑 같이 올려두니 뿌듯했어요» · 묘사문 0) · 단독어 5개(`1a290df`). 남은 것: R2 `autocreate/207/` 잔재 + `r2Delete` 헬퍼 · B2 몫 `lib/publish/wordpress.ts:71` alt_text 를 `piece_assets.meta.alt` 로.
3. ✅ **A 머지됨(main `d0b186e`)** · 🔴 정정 진행 중 — «이번엔 건너뛰어요» 화면 규칙(자리 날짜−오늘 < lead)은 **틀렸다**(제작 스텝 창은 «오늘~오늘+lead» = lead 안 자리도 다음 produceHour 틱에 만든다). 서버 `skipReason:"too_soon"`(B) 만으로 판정하게 A 수리 중: `8dfa5a1` «내 소재 넣기» 시트(→ director 직행 · duplicate→기존으로 · «직접» 필 · «검색량 모름») · 자동 편성 꺼짐 배너(홈·편성표 · 켜기 동기화 · 규칙 0 숨김) · «이번엔 건너뛰어요»(skipReason too_soon 우선). 스샷 `_shots/r6f-*.png`.
4. ✅ **B2 머지됨**: `scripts/build-runner.mts`(재현 가능 zip · 의존성 0 zip 코덱) → R2 `autocreate/runner/v1.1.3.zip`+`latest.json` · `/api/runner-download`(로그인·«플랜 한도>0»·감사 · 10분 presign) · 하트비트 자동 업데이트(sha256 · 옛 판 보존) · 기기 지문(`x-runner-fp` 전 요청 · 인증 자리) · `/api/runner-rotate`(지문도 초기화) · 실측 23/23 · 하니스 trial. **셀렉터 서버 배포 = 1단계(표만 · 버전+카나리+폴백) R7 · 2단계(절차 DSL) 보류** · 복제 방어선 = «봐도 못 굴린다»(토큰·테넌트·묶기). 🔴 화면은 A 진행 중(내려받기 버튼이 JSON 새 탭 → API→url).
5. ✅ R6.5 코드 전부 main(`5582363`) · **C 1차 진행 중 · 2차(skipReason·러너 배포 절) 예고됨** → «배포 가능» → 배포 #7. ✅ B2 `8c627dd` 파일명 서명(`autocreate-runner-v1.1.4.zip`)·«열쇠» 통일 머지.
6. ✅ **설계 대비 전수조사 완료**(main `0ec19ed` · `docs/active/2026-09-15-DESIGN-AUDIT.md` 562행 · ✅373 🟡29 🟠81 🔒10 ❌33 ➖32 · §0.2 상위 20 · §0.3 영상 축 = **미개통 + 뒤 두 칸(mp4 받기·주소 적기) 없음** · §0.4 R7 묶음 3 · §0.5 설계 문장 갱신 후보 · §0.6 사장님 결정 7) → **R7 계약의 유일한 입력**. (분담: 새 B 통합+§0·1·3·5B·13·17·19+특별 항목 / B2 §2·8·9 / B-1 §4·5·5C·16B·6·10 / B §7·11·12·14·15·16 / A 시안↔화면 · 각자 `docs/active/audit/2026-09-15-<역할>.md`) → 결과 `DESIGN-AUDIT.md` 의 «R7 제안 묶음 3» 이 다음 라운드의 유일한 입력. 사장님이 짚은 것: 영상 축이 고객 화면에 안 보임(레지스트리 planned) · «있지만 안 되는 것» 많음 · 탈퇴 시 R2 파기 없음(B-1 발견).

### 사장님 결정 대기
- **실제 발행 GO** — 198 의 12:30 예약 글(@endyd116 실블로그)을 진짜 올릴지(러너를 이 PC 에 붙임 + 네이버 로그인 1회 필요). 러너 패키지가 없어 «집PC」 등록만 된 상태.
- **러너 방향 3개**: ①관리형 기본 vs 옵션 ②요금 단위(계정당/대당) ③프록시 비용 포함/별도 → **R7**(러너 팜 · 계정별 주거용 프록시(`proxyUrl` 자리 있음) · 셀렉터 서버 배포).
- 116(pro · MRR 49,000)을 0 으로 할지.

### 사장님 액션 — 🔴 **전부 `docs/active/2026-09-15-OWNER-CHECKLIST.md` 로 모았다**(사장님 지시 2026-09-15: «내가 해야 될 것들은 모든 개발 끝나면 마지막으로 메인이랑 진행하면서 조율하자»)
- **어떤 세션도 사장님을 기다리며 멈추지 않는다** — 준비(리허설·명령 한 줄·되돌리기)까지 하고 다음 일로 간다. 실행은 개발 끝난 뒤 합동 세션에서 한 번에.
- 합동 세션 9항목(실발행·티스토리 재로그인·대청소 Allow·회사 정보·admin 비번·채널 개통 확인·관리형 가격·AM 코인 키·라이브 한 바퀴) · 바깥 절차 4(유튜브 심사·법률·키·KICC 코드)는 언제든 먼저 시작 가능.

### (옛) 사장님 액션(바깥)
1 **유튜브 OAuth 앱 + 심사**(영상 채널 열쇠 · 수 주) · 2 약관 법률 검토·통신판매업 · 3 사업자 정보(회사 정보)·admin 비번 · 4 TYPECAST·쿠팡·Meta·Threads 키 · KICC 결제수단 코드(간편결제) · ~~복귀 주소~~ ~~KEYIN 시크릿~~.

### R7 후보
러너 팜(관리형 기본) · 프록시 배정 화면 · 셀렉터 서버 배포 · 관리형 가격 요금제 편집 · 인증 메일 배경화 · 릴스 90초 · 틱톡·페북 · AM↔AC 코인 이전 AM 쪽 · 유튜브 쇼핑 태그.

---

## 7. 문서 지도
`CLAUDE.md` · `docs/DESIGN.md` · `docs/rules/PITFALLS.md`(**AC-1~64**) · `PARALLEL_GUIDE.md` · 계약 R1 v1.3/R2 v2.11/R3 v3.5/R4 v4.5(§1.6 KICC)/R5 v5.6/R6 v6.0 · **`docs/active/KICC-GO-LIVE.md`** · `C-HANDOFF.md`·`R5-B-HANDOFF.md` · `SESSION-TRIGGERS.md` · `R6-screen-list.md` · `2026-09-14-R5-presurvey-video.md` · `docs/history/*-C-report.md`(R1~R6) · `docs/screens-v4.html` · `PROJECT_STATE.md`.

## 5F. 🔴 2026-09-16 밤 — R9+R10 · **«초록인데 고장»을 하루에 여덟 번 본 날**

### 숫자
| | |
|---|---|
| R8 감사 | **82% → 91%** (닫힘 53 → **59** · 열림 7 → 4) |
| 배포 묶음 | **81 커밋 · 156 파일 · +10,792 / −858** |
| 창 | 메인 + A · B · B2 · C + **b-49**(자를 재는 일) · b-4a(음성 대조) |
| 다른 창이 잡아 수리된 것 | **12건**(C 집계) |

### 🔴 이 날의 주제 하나 — **«검사가 초록인데 제품이 고장나 있었다»**
여덟 번 나왔다. 형태가 매번 달랐다:

| # | 어디 | 어떻게 속았나 |
|---|---|---|
| 1 | 감사 ④7 `avatar` | **부정형** — «나쁜 모양이 없으면 닫힘» ⇒ **파일을 통째로 가려도 초록** |
| 2 | 감사 E6 | **이름만 맞는 파일**이 있어서 초록(`ops-center.ts` 는 `/api/ops-audit` 를 연다) |
| 3 | 감사 B7 | **한 번도 만든 적 없는 이름**(`editorElements`)을 찾고 있었다 |
| 4 | 감사 A12 | **타입 별칭 글자**를 봐서 «90초를 진짜로 켜도 빨강이 안 풀린다» |
| 5 | 감사 D 접근성 | «**다음 라운드에 만든다**»는 **주석 한 줄**로 닫힘 6/6 |
| 6 | `verify-label-surface` | **주석에 먼저 나온 이름**에 걸려 엉뚱한 블록을 읽어 «0등급» |
| 7 | `verify-r8-deadends` | 심볼 `place` 가 흔해 **진짜 소비처를 다 가려도 초록**(AI 프롬프트 영어 산문·CSS `place-items`) |
| 8 | B2 러너 | 🔴 **55/0 초록 · 변이 6/6 빨강인데 «번짐 끊기»가 통째로 무력** |

🔴 **8번이 제일 비쌌다.** 그대로 갔으면 «꾸밈이 들어간 글은 **전부 발행 중단**»이 됐다.
**C 가 진짜 Chromium 으로 안 쟀으면 못 봤다.** B2 의 변이 여섯이 전부 **자기가 상상한 고장**만 찔렀기 때문이다.

### 🔴 그래서 굳은 규칙 (AC-96 ~ AC-101)
- **AC-96** «못 쟀다»를 «실패»로 세면 폴더마다 빨강 개수가 달라지고 **곧 아무도 빨강을 안 본다**
- **AC-97** 낱말로 세는 검사는 «없다»만이 아니라 «**있는데 못 본다**»도 만들고, **그때 사람이 없는 것을 지어낸다**
- **AC-98** 🔴 **메인이 부르는 좌표(해시·파일 목록)를 창들은 그대로 믿는다** — 확인 안 한 값을 말하면 창 수만큼 곱해진다
- **AC-99** 🔴 **자기가 만든 변이는 «자기가 생각한 고장»만 찌른다** · «있나»는 «도나»가 아니다 · **변이는 심은 자리 수를 센다** · 대조군을 짝으로
- **AC-100** 이 셸이 역슬래시를 한 겹 먹는다 · 🔴 **변이표는 원판 줄을 먼저 찍어라**(모든 칸이 빨강인 표는 «다 잡는다»가 아니다) · 글자 축은 **접두사에서 샌다**
- **AC-101** 🔴 «**한 파일 한 주인**»으로도 «**서로를 향해 고치는 것**»은 못 막는다 — 메인과 B 가 같은 시각에 반대 방향으로 라벨을 맞췄다

### 사장님이 정하신 것 / 사장님 손으로 넘어간 것
- **코인 등급제**(간단히 1 · 보통 2 · 프리미엄 3)가 서버·화면·견적 여섯 자리에 다 섰다
- **합동 세션 17** 🔴 **네이버 카나리 1회**(임시저장 · 발행 0 · 5분) — 지금 «번짐 0»은 **가짜 에디터 범위**다
- **합동 세션 18** 🔴 «스타일 배우기» **월 한도 숫자 네 개** — 지금 화면이 **B 임시값**(5/10/30/100)을 고객에게 말하고 있다
- **§5 배포 묶음** — `RECIPE_PUBLIC_KEY` 를 **먼저** 넣고 **그다음** 러너 빌드(거꾸로 하면 서명 표를 전부 거절)

### 메인이 이 날 한 실수 (그대로 적는다)
- 🔴 **없는 해시(`a70f0d0`)를 두 창에 말했다** — 기억으로 적었다. b-49 가 잡았다
- 🔴 나눠 줄 **파일 목록을 감사와 다른 규칙으로 뽑아** 20개 중 9개가 헛일이었다
- 🔴 `AC_STRIP` 추론이 틀렸다(b-49 가 **숫자로** 반박: 0→93 이면 «몇 칸»이 아니라 «재는 물건 자체»)
- 🔴 **내 변이가 부실해서** 잠깐 B2 를 의심했다(같은 낱말이 파일에 **두 번** 있었다)
- 🔴 AC-100 을 **적어 놓고 바로 다음 작업에서 두 번 더 밟았다**
⇒ **네 번 다 «받는 쪽의 의심»이 막았다.** 그게 마지막 방어선이다.

---

## 8. 메인 운영 습관
판단 필요한 것만 답한다 · 계약은 파일에 먼저 · 세션이 내 오류를 잡으면 그대로 인정(오늘 6번) · 머지 B→B2→A→C · 머지마다 tsc+build+2함수 grep · push=배포 → API ready → 라이브 스모크(증거) → 문서 갱신 · 증거 없는 실증 금지 · 초록도 빨강도 의심 · 사장님께 사람말·결과만 · 창 구분은 보고 첫 줄 · 라이브 변경은 그 창에서 Allow · **사장님이 짚은 건 즉시 발주**.
