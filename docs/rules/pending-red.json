{
  "_설명": "🔴 «아직 수리가 안 들어와서 빨간 자» 목록. 고장이 아니라 «할 일이 남았다»는 뜻이다. verify-safe-list 가 이 목록의 빨강을 «기다리는 빨강»으로 따로 세서, 그 사이에 난 진짜 회귀가 묻히지 않게 한다.",
  "_규율": "여기 넣는 것은 «왜 빨간지»와 «누가 고치면 초록이 되는지»를 반드시 적는다. 초록이 되면 그 줄을 지운다. 🔴 여기 넣어 두고 잊는 것이 제일 나쁜 쓰임이다 — 그러면 그냥 «검사를 끈 것»이다.",
  "_언제 생겼나": "2026-09-19 수리 라운드. C 의 자 다섯이 수리 전이라 전부 빨갰고, 그 사이에 난 진짜 회귀(verify-label-surface — A 의 말투 수리가 화면 사본을 안 고침)가 하마터면 묻힐 뻔했다. — 🔴 2026-09-19 수리 라운드 끝: 대기 0개. 목록을 비우는 것이 이 파일의 목표였다. — 🔴 2026-09-20 다시 1개(A · «B 가 고친 뒤에 넣자»가 잊히는 길이라 지금 넣고 빨간 채로 둔다 · 메인 지시). — 🔴 2026-09-20 밤(C): 그 한 줄은 B 가 고쳐 초록이 됐다(744abd2 `fix/script-gate-words`) ⇒ 규율대로 **지웠다**. 대신 B 가 스스로 «메인이 나눌 몫»이라 적어 둔 빨강 하나를 여기로 옮긴다 — 안 옮기면 그 빨강이 배치에 **늘 떠 있어** 그 사이의 진짜 회귀가 묻힌다(AC-95). — 🔴 2026-09-21(B): `verify-upstream-discarded` 의 regenNote 빨강은 **서버 쪽이 고쳐져 초록**이 됐다(생성기가 그 칸을 읽는다) ⇒ 규율대로 지웠다. 다만 **세 겹 중 한 겹(화면이 보내기)은 아직 A 몫**이라, 그 한 겹만 재는 새 자를 대신 넣는다 — 초록 자를 대기에 두면 그 사이의 진짜 회귀가 묻힌다(AC-95). — 🔴 2026-09-21 늦게(B): `verify-write-path-missing` 도 초록이 됐다(openedAt 서버 몫). 대기는 `verify-regen-note-reaches` 하나만 남는다.",
  "_지운 줄": [
    {
      "자": "verify-people-words.mjs",
      "언제": "2026-09-20 · B 가 지웠다",
      "왜 지웠나": "A 가 기다리던 수리가 들어왔다 — `lib/video/script.ts` 의 «문장 수 3(계약 4~8)» 을 «문장이 3개예요 — 4~8개가 알맞아요» 로 고쳤고(커밋 42b4314) 화면 사본(`public/js/mock.js`)도 같은 커밋에서 같이 고쳤다. 그 자는 지금 초록이다(실측 0곳)."
    },
    {
      "자": "verify-upstream-discarded.mjs",
      "언제": "2026-09-21 · B 가 지웠다",
      "왜 지웠나": "②축(«저장만 하고 아무도 안 읽는 칸»)이 초록이 됐다 — `meta.regenNote` 를 글 생성기(lib/content-gen.ts)와 영상 생성기(lib/video/gen.ts → script.ts)가 **제 이름으로 읽어 프롬프트에 싣는다**. 실측: scripts/_smoke/regen-note-smoke.mts(provider 무접촉 · 프롬프트 문자열에 그 문장이 그대로 실린다).",
      "🔴 남은 겹": "**화면이 그 한 마디를 보내는 것**(A 몫). 그 한 겹만 재는 자를 새로 냈다 — `verify-regen-note-reaches.mjs`(아래 대기). 서버만 고치고 목록을 비우면 «초록인데 고장»이 된다."
    },
    {
      "자": "verify-write-path-missing.mjs",
      "언제": "2026-09-21 · B 가 지웠다",
      "왜 지웠나": "`accounts.opened_at` 에 **쓰는 길을 냈다** — `POST /api/accounts-update { openedAt }`(YYYY-MM-DD · null=모름) + 응답에 `openedAt` 을 같이 싣는다(안 실으면 «저장은 200 인데 다시 열면 비어 있다»). 실측(761): 저장·재조회·되돌리기·앞날 거절, 그리고 🔴 **워밍업이 실제로 그 날을 본다**(2023-04-01 로 적으니 워밍업이 안 걸리고, null 로 되돌리니 1주차로 돌아왔다).",
      "🔴 남은 겹": "**화면 입력칸은 A 몫**이다. C 보고(2026-09-21)로는 A 가 `accounts.html` 에 칸을 넣어 이미 보내고 있다 — 내 워크트리의 main 에는 아직 안 들어와 있어서 **내가 눈으로 확인하지는 못했다**(AC-9). 머지 때 메인이 두 쪽이 맞물리는지 본다."
    }
  ],
  "대기": [
    {
      "자": "verify-regen-note-reaches.mjs",
      "왜": "🔴 새 자다(2026-09-21 · B). **세 겹을 따로 잰다** — ①서버 저장 ②생성기 읽기 ③앵글 오염 금지 ④**화면 보내기**. ①②③은 같은 날 B 가 고쳐 초록이고, **④ 하나만 빨갛다.**",
      "누가": "**A** — 검수 화면의 «다시 만들기»가 `{ id }` 만 보낸다(public/app/_tpl.txt:2550 · 3167). 손님이 «이번엔 이렇게 해 주세요»를 적을 자리가 없다.",
      "어디": "public/app/_tpl.txt — `UI.api(\"/api/pieces-regenerate\", { body: { id } })` 두 곳",
      "어떻게 고치면 초록": "«다시 만들기» 시트에 한 줄 입력칸을 두고 `{ id, note }` 로 보낸다(안 적으면 안 보내도 된다 · 서버는 빈 값을 무시한다). 이웃 «버리기»(`pieces-reject { reason? }`)가 이미 그 모양이다. 🔴 변이 M5 로 **A 가 고치면 ④가 초록이 되는 것**까지 확인해 뒀다.",
      "일부러 안 만든 것인가": "**아니다.** 계약(`POST /api/pieces-regenerate { id, note? }`)에 처음부터 적혀 있던 칸이다.",
      "등록": "2026-09-21 · B"
    }
  ]
}
