{
  "_설명": "🔴 «아직 수리가 안 들어와서 빨간 자» 목록. 고장이 아니라 «할 일이 남았다»는 뜻이다. verify-safe-list 가 이 목록의 빨강을 «기다리는 빨강»으로 따로 세서, 그 사이에 난 진짜 회귀가 묻히지 않게 한다.",
  "_규율": "여기 넣는 것은 «왜 빨간지»와 «누가 고치면 초록이 되는지»를 반드시 적는다. 초록이 되면 그 줄을 지운다. 🔴 여기 넣어 두고 잊는 것이 제일 나쁜 쓰임이다 — 그러면 그냥 «검사를 끈 것»이다.",
  "_언제 생겼나": "2026-09-19 수리 라운드. C 의 자 다섯이 수리 전이라 전부 빨갰고, 그 사이에 난 진짜 회귀(verify-label-surface — A 의 말투 수리가 화면 사본을 안 고침)가 하마터면 묻힐 뻔했다. — 🔴 2026-09-19 수리 라운드 끝: 대기 0개. 목록을 비우는 것이 이 파일의 목표였다. — 🔴 2026-09-20 다시 1개(A · «B 가 고친 뒤에 넣자»가 잊히는 길이라 지금 넣고 빨간 채로 둔다 · 메인 지시). — 🔴 2026-09-20 그 줄을 **지웠다**(B 가 고쳤다 · 아래 «_지운 줄» 참조) 그리고 새 자 하나를 넣었다.",
  "_지운 줄": [
    {
      "자": "verify-people-words.mjs",
      "언제": "2026-09-20 · B 가 지웠다",
      "왜 지웠나": "A 가 기다리던 수리가 들어왔다 — `lib/video/script.ts` 의 «문장 수 3(계약 4~8)» 을 «문장이 3개예요 — 4~8개가 알맞아요» 로 고쳤고(커밋 42b4314) 화면 사본(`public/js/mock.js`)도 같은 커밋에서 같이 고쳤다. 그 자는 지금 초록이다(실측 0곳)."
    }
  ],
  "대기": [
    {
      "자": "verify-write-path-missing.mjs",
      "왜": "🔴 새 자다(2026-09-20 · B). 세우자마자 **진짜 고장 셋**을 물고 나왔다 — 자가 틀린 게 아니라 제품에 길이 없다. 🔴 그 중 **둘(`feature_flags.key`·`tenant_id` = 영상 킬 스위치)은 같은 날 고쳐서 초록이 됐다.** 남은 하나만 기다린다.",
      "누가": "`accounts.opened_at` = **A(화면) + B(API)** 가 한 판에 — 메인이 묶어서 낸다",
      "어디": "lib/warmup.ts:57 이 읽고 6곳이 따라 읽는데 쓰는 길 0 · 라이브 92계정 전부 NULL",
      "어떻게 고치면 초록": "계정 연결/설정에 «이 블로그 언제 시작하셨어요?»(모르면 비워 둔다) + `accounts-update` 가 `openedAt` 을 받는다(허용 목록까지). 🔴 반쪽(API만)으로 하면 «초록인데 고장»이다 — 화면이 안 보내면 영원히 NULL.",
      "일부러 안 만든 것인가": "**아니다.** 주석이 스스로 «고객이 알면 입력»이라 적어 뒀다 — 입력할 자리를 안 만든 것이다. 일부러 뺀 칸이면 자의 `NO_WRITE_ON_PURPOSE` 에 **이유와 함께** 넣는다 — 지금 그 목록은 비어 있다.",
      "이미 고친 것": "`feature_flags`(영상 킬 스위치) — 2026-09-20 B. `POST /api/ops-ai-video-kill`(super_admin · 감사 high) + 운영센터 AI 메뉴 스위치. 실측: `checkVideoBudget(761) → allowed=false reason=killed` · 변이 M12 가 그 길을 들어내면 다시 운다.",
      "등록": "2026-09-20 · B"
    }
  ]
}
