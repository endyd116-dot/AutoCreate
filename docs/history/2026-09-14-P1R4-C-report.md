# P1R4 C 보고서 — «돈을 받고 · 운영한다» 검증·수리 (배포 #3 게이트)

> 브랜치 `verify/p1r4`(베이스 main `f95a084` → `597bcf1` 머지본) · 계약 v4.4 · 하니스 `scripts/verify-p1r4.mjs`(API · 94항목) · `scripts/shot-p1r1.mjs`(고객 22화면 + 운영 16화면 · `OPS=1`) · 실행 = 로컬 8901(`CRON_BUDGET_MS=24000` · `FX_USD_KRW=1400` 로 원가 상한 실측) · DB = 라이브 Neon · **실행마다 새 테스트 테넌트**(c+r4-<stamp>) · 끝에 돈·CS·운영진 행 정리.
> KICC·RESEND 미설정 → 카드 등록·실결제·청구 사다리는 «키 꽂으면»(정직 `not_configured` 경로만 검증).

## 1. 결함 — 전부 C 가 수리 (9건 + DDL 1)

| # | 심각도 | 결함 | 실측 | 수리 | 되짚기 |
|---|---|---|---|---|---|
| 1 | **P0** | `invoices.period varchar(20)` 인데 코인 영수증 period = 주문번호 `AC-COIN-{tid}-{pack}-{base36}` = **22자+** → 첫 실결제에서 INSERT 22001 → «돈은 받았는데 영수증·인보이스 없음»(AC-21·AC-30) | 주문번호 길이 23자 실측 | `drizzle/0007-r4-invoice-period-width.sql`(→ varchar 60 · order_no 와 동일) **라이브 적용**(information_schema 60 확인) · schema.ts 는 메인 동기(597bcf1) | period 23자 인보이스 INSERT 성공(invoice 7) |
| 2 | **P1** | 운영자 토큰 `oid` 가 **문자열**(원시 SELECT bigint → postgres-js "1") → `id === g.ops.oid` 자기 보호 가드가 항상 거짓 → **super_admin 이 스스로를 강등·비활성화 가능**(AC-28) | 하니스가 실제로 admin 을 operator 로 강등 → DB 즉시 복구 | `lib/auth-service.ts issueOpsSession` `Number(op.id)` · 가드 `Number()` | self 강등 400 `self` |
| 3 | **P1** | `requireFeature` **호출처 0**(AC-29) — directorEdit(손보기)·failover(승계)·runnerRevenue(러너 수익) 기능 게이트 전부 미작동 → Starter 가 Pro 기능 사용 | grep 호출 0 · starter 손보기 통과 | director-confirm 패치 시 402 `plan_feature`(코인보다 먼저) · 정지 승계는 failover 없으면 옮기지 않고 «Pro 로 바꾸면» 알림 · runnerRevenue 없으면 스크랩 잡 미적재 | starter 손보기 402 · starter 정지 → 승계 0 + 알림 · starter 스크랩 잡 0 |
| 4 | **P1** | `runner-register` 가 기기 한도(`runnerDevices`)를 안 잼 | starter 2대 등록 200 | `checkLimit(tid,"runnerDevices")` | 2대째 402 `plan_limit runnerDevices limit 1` |
| 5 | **P1** | **러너 수익 스크랩 잡(revenue.adpost/adfit/clip) 적재 경로 없음** — revenue-sync 는 API 소스만 · 러너는 잡이 있어야 움직임 → 애드포스트 수익 자동 수집 0(R3 §1.3·§2.1 미달) | 잡 적재 코드 grep 0 | `lib/cron/revenue-sync.ts` dailyHour 에 connected 러너 소스별 1건(enqueueJob 중복 0 · 플랜 게이트 · 함수 안 import AC-17) | pro → `revenue.adpost` queued 1 · 재실행 1 · starter → 0 + scrapeGated |
| 6 | **P1** | `ops-canary` 500 — `day >= date - ${days}`(바인딩 타입 없음 · AC-23 류) → 러너팜 카나리 화면 죽음 | 500 `operator does not exist` | `::int` + 괄호 | 200 |
| 7 | **P1** | 체험 종료(readonly)·정지(suspended)가 홈 «해야 할 일»에 없음(계약 §1.3 — 알림함에만) | todo `setup,verify` 만 | `home-summary` 첫 행 «요금제를 골라 주세요»/«결제 확인» | readonly 홈 todo `plan` 행 |
| 8 | P2 | AI 원가 상한이 director-confirm 에만 — 소재 뽑기·다시 만들기는 무제한(§1.5) | topics-refresh 202 | `requireAiBudget` 두 경로 추가 | cap 1원 + FX → 400 `ai_cost_cap` + 알림 |
| 9 | P2 | 금칙 카테고리가 `slots-assign-topic` 에만 — 디렉터 제안은 열림(«소재 단계에서 거부») | propose 200 | `lib/director.ts propose` 에 `findBannedCategory` + 감사 | 400 `banned_category` |

## 2. 통과 (API 94항목 중 93 PASS · 1 WARN=KICC 갭 · 증거 `_verify/p1r4-*.json`)
- **가입·동의**: privacy 빠지면 400 · 4건 consents 기록(버전) · `/api/plans` 공개 플랜만 · plans.trial 시드 = Pro 기능 전부·계정 5·비공개.
- **코인**: coin-packs(vatNote · pack_trial 5,000/500/5,500 once · 50k 50,000/5,000/55,000 · available) · pack_trial 이력 있으면 **KICC 전에** 400 `once` · 없는 팩 400 `pack` · KICC 없음 `{ok:false, step:not_configured}` 정직 · coin-history rows·balance{included,purchased,total} · 잔액 분리 합 = balance · **환불 5규칙(견적)**: 미사용 100코인 → 55,000(단가 550 = 결제액(부가세 포함)÷코인) · 30 사용 → 38,500 · 7일 지남 → `window`(우선) · 전부 사용 → `used` · 실행은 KICC 없음 정직 · 없는 주문 400 `quote/not_paid`.
- **구독**: subscription(plan 3금액·billingKey.has·trialEndsAt·vatNote) · 견적 pro/month **49,000+4,900=53,900** · pro/year **490,000(×10)/539,000** · starter 20,900 · 같은/불가 플랜 400 · 변경(빌키 없음) `not_configured`(200 ok:false) · 빌키 시작 not_configured · invoices rows · 체험 중 해지 400 · `billing.charge`(KICC 없음) errors 0·상태 무접촉.
- **체험·readonly**: D-3/D-1/D-0 알림 각 1건(2회 실행 멱등 · `trialNoticeHour`) · 종료 → `readonly` + `trial_ended` 1건 · 열람 200(auth-me·home·slots) + 홈 todo(#7) · 생성 3경로 403 `{step:writable, reason:readonly}` · 크론 우산이 readonly 를 건너뜀 · `ops-trial-extend 7` → trial 복귀 · 생성 다시 열림.
- **게이트**: 계정 6번째 402 `{step:plan_limit, resource:accounts, used 5, limit 5, planKey trial}` · rules-list maxRules null · starter 규칙 4 → 402 rules(3) · starter horizon 30 → 402(7) · starter 손보기 402 plan_feature · 금칙 소재 400 + 감사(자리·제안 둘 다) · `ai_cost_cap` 400 + 알림(판정 한 벌 `lib/billing/ai-cost-cap.ts`) · starter 러너 2대 402 · starter 정지 승계 0 + «Pro» 알림 · 스크랩 잡 pro 1/starter 0.
- **운영센터**: super_admin 12메뉴 200 · 대시보드 모양·매출 = 인보이스 합(KST 월)·AI 원가 = ai_usage 합(fxMissing false → krw/margin 실림) · 고객 상세 `setup{accounts,adMedia,rules,firstPublish}` · 코인 지급 30 → 30 · 회수 −50 → 0(포함분 잔량까지) · 이벤트 trial_days 21 → 새 가입 21일 · 쿠폰 생성·내역 · **가격 개정 게이트**(plan-update 가격 400 `price_gate` · 예약 → 적용일 전 49,000 · 적용일 지나면 59,000 `price_event` · 취소 → 복귀) · ops-invoices/receivables/billing-keys 모양 · ops-refund 인보이스 없는 주문 400 정직 · 채널 status planned→active → 고객 accounts-list **즉시** · disclosure 정본 문구 · 장애 공지 → 고객 `/api/notices` incident · 운영진 초대 · self 400 · **operator 역할표**: 요금제/결제/운영진/이벤트 403 + `ops_forbidden` 감사 · 고객/CS/대시보드 200 · AI/채널 변경 403 · **원격접속**: auth-me `impersonation` · 결제/충전/비밀번호 403 `impersonation` · 종료 감사+고객 알림 · runners/canary/ai-models 모양 · 카나리 ok null 허용 · AI 미실측 모델 400 사람말 · 롤백 정직 · 감사 검색(actorType·q·page·total).
- **CS**: 문의 201 → ops-ticket(context 플랜·러너·recentErrors·macros 자동 첨부) → 매크로 답변 → 고객 알림 → support-tickets 반영 → 해결 → 만족도 → 통계 4키 · 만족도 2회 정직 · **자동 티켓** 러너 실패 3회 → system 1건(2회 실행 멱등).
- **타 테넌트 0**: 티켓 404 · 환불 견적 정보 0 · 주문/인보이스 0 · ad-eligibility 404.
- **정적(AC-17/20/21)**: 새 lib/billing·subscription·plans·guards·cron 5스텝 최상단 import 고리 0(부를 때 import) · 배열 바인딩 전부 `sql.join` · 어휘 칸 폭: tenants.status 16 ⊇ readonly/suspended · tickets.status 12/priority 8/source 12 · plan_price_events.status 12 · notices.kind 12 · consents.kind 32 — **invoices.period 20 만 부족(#1 수리)**.

## 3. 화면 (`_shots/p1r4/` 고객 22화면 · `_shots/p1r4-ops/` 운영 16화면 · 폰 390 + 데스크톱 1280)
- 고객 **398 PASS / 2 FAIL(terms «러너» = 약관 정의문 «내 PC 프로그램»(러너) · 법률 문서라 유지) / 10 WARN**(시트 셀렉터·터치 1개). 결제 시트 «5,000 + 부가세 500 = 5,500 카드 청구» 3숫자 · 요금제 헤드라인 «체험 쓰고 있어요 · 월납 0원 · 부가세 별도» · 유료 약관 체크 · 약관 4문서 페이지 200.
- 운영 **258 PASS / 0 FAIL / 42 WARN**(«테넌트»·«러너» 낱말 = 운영 화면 허용 밀도 · 터치 1개씩). ⚠️ admin 계정이 `mustChangePassword` 라 모든 메뉴가 비밀번호 화면으로 갔다 — 임시 super_admin(`c-shot-op` · 촬영 후 삭제)으로 촬영. 대시보드는 아래 정리 후 MRR 0·전환 null 로 정직.
- 게이트 시트 5종(readonly/suspended/plan_limit/plan_feature/ai_cost_cap)은 API 응답 모양(step·reason·resource·limit)까지 확인 · 시트 자체는 mock 없이 실서버 402/403 을 받아야 떠서 스샷은 결제 시트·요금제·코인만.

## 4. 데이터 정리(라이브 대시보드 오염 0)
- 내 테스트 테넌트(c+r4-*·cc6) + B 의 `r4smoke*` 10집 + B2 `b2ver*` 1집: subscriptions 7·coin_ledger 9·coin_orders 1·runner_jobs 3·ai_usage 9 행 삭제 · plan_key/status → trial. **대시보드 MRR 272,000 → 0 · 전환 100% → null**(가짜 유료 8집이 만든 값이었다). 임시 운영자 2계정 삭제.

## 5. 갭(결함 아님 · 다음 라운드)
- KICC: 카드 등록·실결제·청구 사다리 D+0/+3/+7·연납 매달 포함분·쿠폰 2회 거절(결제 경로) — 정적으로 `lib/cron/billing-charge.ts`·`lib/billing-math.ts dunningSchedule`·`SUSPEND_AFTER_FAILS=3` 확인.
- 추천 코드 입력 칸 없음 · bonus_coin · 영수증 페이지(시트) · 세금계산서 실발급 — 계약 §8 그대로.
- 금칙 사전에 «도박 사이트·카지노(단독)» 없음(«카지노사이트»만) — 정책 사전 보강 제안.

## 6. 판정 — **배포 가능**(C)
결함 9건 수리·되짚기 · DDL 0007 라이브 적용 · tsc 0 · 하니스 93/94 · 화면 656 PASS. 배포 후 C6: 라이브 가입→체험→`pack_trial` not_configured→플랜 화면·운영센터 12메뉴(비밀번호 변경된 admin 으로)·자동 티켓·원격접속 회귀.

---

## 라이브 절 — C6-R4 (배포 #3 · main `0f3ec44` · 2026-09-14 17:40 KST · `scripts/verify-live-c6r4.mjs` · `_verify/live-c6r4-*.json`)
- 새 테스트 테넌트 115(consents 4 · trial) · ① 라이브 `cron-run hourly tid=115` → `trial.expire changed 1` · `trial_d3` 알림 **1건**(2회 실행 멱등) · ② 임시 **operator**(super_admin 아님 · 끝에 삭제) 로그인 → `ops-plans` **403** + `ops_forbidden` 감사(id 1596) · 대시보드/CS 200 · ③ 원격접속 시작 → `auth-me.impersonation` → `coin-purchase-start` **403 impersonation** → 종료 → 고객 알림 179 «운영자가 설정을 도와드렸어요» · ④ 채널 threads planned→active → 고객 `accounts-list` **즉시 active** → planned 복구 · ⑤ 라이브 스샷 `_shots/live-c6r4/`(coins·plan·home 54 PASS · 운영 index/cs/tenants 46 PASS · operator 역할이라 메뉴 6개만 보임 = 역할별 숨김 확인) · ⑥ 정리: 테넌트 115 돈·CS·알림·감사 행 · 임시 operator 2계정 삭제 · 메인 스모크 티켓 2건(«live smoke»·cp949 깨진 제목) 삭제.
- **결함 1건(P2 · 화면) 수리**: 운영 CS 통계가 데이터 없을 때 «**null분** · **null%**» 로 그려짐(`public/ops/cs.html` · `_tpl.txt`) → «—». A 에게 통보.
- 결함 0건(API) · 판정 유지 «배포 가능» · admin `mustChangePassword` 는 사장님 몫(운영 촬영은 operator 로).
