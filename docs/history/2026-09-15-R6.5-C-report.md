# R6.5 «사장님 실측 5건» — C 검증 보고서

> **판정: 배포 가능.** 기준 main **`516d671`** · 내 브랜치 `verify/p1r6-5`(수리 커밋 아래) · 로컬 하니스 **PASS 62 · FAIL 0 · WARN 2**(둘 다 «라이브에서 닿을 수 없는 분기» 정직 표기) · 화면 하니스 **54/54**.
> 하니스 `scripts/verify-r6-5.mjs`(절 10) · 프로브 `verify-r6-5-caption-probe.mts`(실호출 0) · `verify-r6-5-latest-probe.mts` · 화면 `scripts/shot-p1r1.mjs`(`_shots/r6-5/`).
> 규율: 증거 동반 · 빨강은 먼저 내 검사를 의심(C-HANDOFF 머리) · 실행 중 편집 금지(AC-34) · 발행 0건 · 정리까지가 검증.

## 0. 수리 커밋(P0/P1/P2)

| 급 | 결함 | 어디 | 커밋 |
|---|---|---|---|
| **P1** | `assign_topics` 가 «내 소재»를 먼저 집지 않았다 — `loadCandidates` 의 manual-first 정렬이 `pickFor` 의 «힌트+점수 max» 재선택에서 **사라졌다**(실측: 점수 95 AI 가 점수 12 manual 보다 먼저 · 슬롯 278:475·279:476) | `lib/cron/assign-topics.ts pickFor` — 힌트 일치 > **manual** > 점수 | `3908cbc` |
| **P1** | 만들기 화면 «레퍼런스로 구조 배우기» 행에 **`${UI.chev}` 가 글자 그대로** 찍혔다(정적 HTML 안의 템플릿 식 · R5 A 잔재 · 사장님이 쓰는 화면) | `public/app/_tpl.txt`(5줄) → 인라인 SVG | `e04cf3e` |
| P2 | 홈 토글 설명이 자동 편성 **꺼져 있는데도** «규칙 1개로 돌아가요 · 다음 글은 오늘 12:00» — 바로 위 배너 «꺼져 있어요»와 서로 다른 말 | `public/app/home.html` → «규칙 N개 · 지금은 꺼져 있어요» | `e04cf3e` |
| P2 | 묘사문 사전에서 «선물이 **가지런히 놓인 테이블**»이 빠져나감(음성 대조) | `lib/ai-tell-gate.ts` 7패턴 → «놓인·걸린·펼쳐진·담긴·쌓인 + 장소» | `7872104` |
> 설계 대비 못 만든 것: **없음**. 계약 §6 5건 중 ⑤ 러너 배포는 B2 `cc45885` 기준으로 잰다.

## 1. topics-add (0→25%) — 전건 초록
| 검사 | 증거 |
|---|---|
| 200 `topic{id, source:"manual", status candidate}` · `volumeKnown` boolean | id 416 · `volumeKnown true · factors.volume 1270`(키워드툴 키 있는 로컬) |
| 검색량 못 재면 **키 없음**(0 금지 · AC-9) | 없는 키워드 → `volumeKnown false · factors.volume 키 없음`(화면 «검색량 모름» 실측) |
| 감사 `topic_added`(횟수의 근거) | audit 2792 `{title, volume, volumeKnown}` |
| 400 title — 빈값 · 81자 / 80자 통과 | «소재는 80자까지 적을 수 있어요.» |
| banned_category «온라인 카지노 배팅 노하우» | 400 «도박·사행성 주제는 만들 수 없어요.» + 감사 `topic_banned_category{source:"manual"}` |
| 도박 단독어 5개 각각 + 조합어 1개 | 카지노·바카라·토토·배팅·도박 **각각 400** · «스포츠 토토 분석» 400 |
| duplicate — 30일 안 같은 norm_key → 400 + 기존 topic 동봉 · 상태 무관 · 공백 무관 | topic 416 vs 416 · picked 여도 · «가을  이불 세탁   …» 도 |
| channel — 없는 채널 400 / 없으면 연결 계정 첫 채널 | «foobar» 400 «고를 수 없는 채널이에요.» · 기본 naver_blog. ⚠️ `tiktok` 은 레지스트리의 **planned 채널**이라 통과한다(결함 아님 · 기록) |
| topics-list manual 맨 위(점수 부풀림 0) | 상위 `manual:7 manual:6 …` |
| 21번째 429 `step:"rate"` · 횟수 = 감사 `topic_added` COUNT(**KST 오늘**) | 20개 뒤 429 · 감사 한 행을 어제로 밀면 한 개 더 들어감 |
| readonly 테넌트도 넣어진다 · 디렉터 확정은 막힘 | 200 → propose 200 · **confirm 403 writable** |
| 타 테넌트 13 목록 누출 0 | 200 · 0 |
| 🔴 assign_topics 가 manual 을 먼저 | **수리 뒤** 슬롯 292:**509(manual·12)** · 293:508(ai·95). ⚠️ 이 절은 `autoSchedule` 을 켜야 잰다(신규 기본 false = 사장님 3번 결함 그 자체 · `needsAutoSchedule` 스텝은 아예 안 돈다) |

## 2. 캡션 §5C (25→50%) — 12/12 · 실호출 0
- 계약 표 `captionRate` naver 0.3 · tistory 0.2 · blogger/WP 0.5 · 영상 0.
- `fixBlocks` 단위: 사장님 실측 문장(«원목 테이블 위에 정갈하게 포장된 명절 선물 상자가 놓여 있는 모습») → **캡션 0** · 25자 초과 버림(최장 22자) · 옛 응답(caption 만·묘사문) → **prompt 로 옮기고 캡션 빔** · prompt 6/6 · **round(6×0.3)=2장** · 같은 글이면 같은 자리(결정론) · 다른 글이면 자리 달라짐.
- `ai-tell-gate` 음성/양성 대조: 손으로 넣은 묘사문 → `cliche` **실패** «사진 캡션이 묘사문(~하는 모습)» / 사람 캡션 → 통과 · 7패턴 사전 전부 걸림(수리 후) · 사람 캡션 4개 안 걸림.
- B-1 실호출 piece 330 보고(6장 중 캡션 1장 «자리마다 쪽지랑 같이 올려두니 뿌듯했어요» 19자 · 묘사문 0 · prompt 영문)와 **일치**. 발행 HTML 은 행이 정리돼 못 봤다(B-1 보고 + 내 단위 대조로 판정).
- `wordpress.ts` alt — B2 `5638765` 로 `alt_text: altText`(캡션과 분리) 확인. (내 검사가 **블록 주석 속 «종전엔 alt_text: caption»** 을 코드로 세어 빨강을 냈다 — 세 번째 밟은 함정 · 하니스 수리.)

## 3. A 화면 (50→75%) — 헌장 54/54 · 스샷 `_shots/r6-5/`
- 만들기: «내 소재 넣기» 행 → 시트(제목·접힌 검색 키워드·채널 칩·**Primary 1**) → `topics-add` → 디렉터 직행 · duplicate 배너 «그걸로 보낼까요» → 기존 topic 으로(`#dupGo`) · «직접» 필 · «검색량 모름»(volume 키 없음).
- 홈·편성표 배너 «자동 편성이 꺼져 있어요 — 켜면 3일 전에 글을 만들어 드려요 [켜기]»(같은 `tenant-settings` API · 켜면 두 화면 내려감 + 홈 토글 on) · 규칙 0 이면 숨김.
- 폰 390/데스크톱 1280: Primary ≤1 · 큰 숫자 ≤1 · 이모지 0 · 시스템 용어 0 · 가로 넘침 0 · 콘솔 0 · 실패 요청 0.
- 하니스 수리: API 뒤에 그려지는 셀렉터·시트(«내 소재 넣기»는 `accounts-list` 를 먼저 부른다 · 로컬 콜드스타트 3~4초)를 기다린다.

## 4. «이번엔 건너뛰어요» = `skipReason:"too_soon"` (2차 · main `516d671`)
규칙: T = 다음 제작 틱(`nextProduceTickUtc` · produceHour · 지금 시각 ≥ H 면 **내일**) · P = publish_at(없으면 그날 KST 23:59) · `P <= T` 면 too_soon · 글 있거나 만들어질 차례 아닌 자리엔 키 없음.
| 경우 | 결과 |
|---|---|
| 🔴 produceHour = **지금 시**(11시) → 다음 틱은 내일 → 오늘 23:30 자리 | too_soon ✅ |
| 내일 H:00(틱과 같은 시각 · P ≤ T) | too_soon ✅ |
| 내일 H+1 | 키 없음 ✅ |
| publish_at 없는 오늘 자리(23:59 기준) | too_soon ✅ |
| 모레 · 시각 없음 | 키 없음 ✅ |
| 글이 이미 있는 자리(in_review + piece) | 키 없음 ✅ |
| skipped 상태 | 키 없음 ✅ |
| produceHour = 미래(14시): 13:30 / 14:30 | too_soon / 없음 ✅ |
| 타 테넌트(13) slots-list 누출 | 0 ✅ |
| 화면은 서버 키만(«날짜 − 오늘 < lead» 계산 0) | ✅ · 스샷: 오늘 12:00 «이번엔 건너뛰어요 · 6시에 만…» · 19일 07:30 «예정» |

## 5. 러너 배포 (2차 · B2 `cc45885` · v1.1.4) — 발행 0건 · 러너 실행 0
| 검사 | 증거 |
|---|---|
| 비로그인 401 | ✅ |
| 200 `version·bytes·sha256·filename·url·expiresInSec 600` | v1.1.4 · 110,914B · `autocreate-runner-v1.1.4.zip` · sha `53ac918cc449…` |
| 감사 `runner_downloaded{version, bytes, planKey}` | audit 2954 |
| presigned 10분 | URL `X-Amz-Expires=600` |
| 🔴 **응답 헤더** `content-disposition` = 응답 filename | `attachment; filename="autocreate-runner-v1.1.4.zip"`(헤더를 직접 읽음) |
| 🔴 받은 zip **sha256 실대조** = 응답 = R2 `latest.json` | 110,914B · 셋 다 `53ac918cc4496473…` · PK 시그니처 |
| readonly → 403 writable | ✅ |
| 지문 — 첫 하트비트(fp A) 200 + 묶임 / 다른 지문(B) **401 «이 열쇠는 다른 PC에 연결돼 있어요»** + 고객 알림 + mismatch 1 | ✅ |
| runner-list 노출 범위 — `bound·otherDeviceAt·otherDeviceCount·version` 만 · **지문 값 0** | ✅ |
| rotate → 옛 열쇠 **즉사 401** · 새 열쇠 200 · 지문 초기화(새 PC 로 다시 묶임 · count 0) | ✅ |
| 틀린 열쇠 → 401 «러너 열쇠가 올바르지 않아요» | ✅ |
| A `runner.html` — 내려받기 `location.href=url` · «열쇠 다시 받기» · «다른 PC 가 이 열쇠로 켜려 했어요» 배너 · 지문 값 없음 | 정적 + 화면 하니스 초록 |
| ⚠️ 403 plan(runnerDevices 0) · 503 no_release | **라이브에서 닿을 수 없는 분기**(표준 4플랜 전부 ≥1 · latest.json 이 있다) — 정적 확인만(WARN 정직 표기) |

## 6. 회귀
`topics.ts` 5경로 500 없음 · 홈·편성표 200 · 글 크론 hourly errors 0 · `verify-p1r6.mjs` 재실행은 main 머지분(내보내기·공유 카드 수리 뒤)에서 R6 판정 그대로.

## 7. 내 하니스가 틀렸던 것(R6.5 · 5건)
`tiktok` 을 «없는 채널» 샘플로 씀(planned 채널) · 상한 20 을 다 쓴 뒤 readonly 절이 429 로 헛돎(절 순서) · autoSchedule 을 안 켜서 assign 스텝이 아예 안 돎 · `latest.json` 을 마지막 줄만 파싱(pretty JSON) · **블록 주석 속 «alt_text: caption» 을 코드로 셈**(세 번째).
> 초록보다 빨강을 의심하는 시간이 이 일의 본체 — 이번에도 빨강 8건 중 5건이 내 쪽이었다.

## 부록 — 정리
**남긴 테넌트 0.** 내 R6.5 집 **26개**(210·211·213·214·215 + 하니스·스샷 집 전부 · users 까지) 삭제 · 보존 4집 **4/4** · 임시 operator 0 · 발행 0건.
