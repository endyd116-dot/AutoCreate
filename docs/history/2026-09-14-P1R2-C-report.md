# P1R2·P1R3 C 보고서 — 검증·수리 (배포 게이트)

> 브랜치 `verify/p1r2`(베이스 main `111dc22` → `d6628cd` 머지본) · 하니스 `scripts/verify-p1r2.mjs`(API) · `scripts/shot-p1r1.mjs`(화면 13종) · `scripts/verify-failover.mts`(승계 · B2 작 + C 케이스) · `scripts/verify-b2-runner.mts`(러너 드라이런 · B2 작)
> 실행 = 내 전용 로컬 `netlify dev` 8901(`CRON_BUDGET_MS=24000` · `SITE_URL=BASE`) · DB = 라이브 Neon · **테스트 테넌트만**(c+p1 id 3 · c+p1b id 13 · 스크립트가 만든 임시 테넌트는 자동 정리).
> «실증»은 row id·스샷 경로 옆에서만 쓴다(PITFALLS #5·#9).

# R2 절 — «편성표가 스스로 돌아 실제로 발행된다» (계약 v2.11)

## R2-1. 결함 (전부 C 가 수리 · verify/p1r2)

| # | 심각도 | 결함 | 재현 | 수리 | 되짚기 증거 |
|---|---|---|---|---|---|
| 1 | **P1** | `slots-reschedule`(시각 바꾸기)가 **항상 500** — `publish_at BETWEEN x AT TIME ZONE 'UTC' - interval …` 는 Postgres 문법 오류(`syntax error at or near "AT"`). B 가 못 친 4경로 중 하나. | `POST /api/slots-reschedule {slotId, at}` | `netlify/functions/slots.ts` — 괄호 감싼 비교 2개로 | 수리 후 200(`slot.publishAt=at`·date 갱신) · `too_soon` 400 · 같은 채널 30분 안 `cadence` 409 |
| 2 | **P1** | publisher — **사람 개입 실패(gate·no_creds·auth_failed·account_blocked·provider_not_configured)가 전부 `failed`** 로 떨어짐. `publish()` 가 이들을 `retriable:false` 로 돌려주는데 publisher 가 `retriable:false = terminal` 로 읽어 `NEEDS_HUMAN` 이 죽은 코드였다. 계약 §3 «게이트 실패면 awaiting_manual + 알림(발행 금지)» 위반 — 발행함이 «직접 올려야 해요»를 못 가른다. | 자격 없는 블로거 계정의 scheduled piece → `cron-run 5m` | `lib/cron/publisher.ts` — `terminal = TERMINAL || (retriable:false && !human)` · finalize 실패(`config`)는 여전히 failed(계약 v2.5) | piece 96 `awaiting_manual` «계정 로그인 정보가 없어요 (no_creds)» · 알림 60 `publish_manual` · 재실행 attempts 1→1(재시도 0) |
| 3 | **P1** | 계정 정지 승계(`reassignSlots`)가 **«오늘 남은 칸(daily_cap−posts_today)»을 앞으로의 슬롯 전체 예산**으로 써서 한 주치 9자리 중 **2건만 옮기고 7건을 정지 계정에 남김**(남은 자리는 발행 때 `account_blocked`). 알림은 «2건 옮겼어요»만 말해 7건은 조용히 사라짐. B2 `verify-failover` 17/17 이 못 잡은 경우. | naver 계정 A(정지) 9자리 · 받는 B daily_cap 2 | `lib/account-health.ts` — 날짜별 여유(`cap − (오늘이면 posts_today) − 그 날 이미 잡은 자리`) · 못 옮긴 건수를 알림에 명시 | 감사 864 `reassigned.moved 5`(수리 전 815: moved 2·unmoved 7) · `verify-failover.mts` (다) 케이스 2개 추가 → **19/19** |
| 4 | P2 | 알림 문구에 시스템 용어 «슬롯»(assign_topics) · `no_topic` 알림 링크가 없는 화면 `/app/topics.html` | 알림함 화면 하니스 «시스템 용어 0» FAIL | `lib/cron/assign-topics.ts` — «그 날 자리» · `/app/create.html` | 새 알림부터 적용(기존 행은 테스트 데이터) |
| 5 | P2 | 검수 알림 링크 `pieces.html?id=`(목록 · id 무시) | review_deadline 알림 | `piece.html?id=` 로 | — |

## R2-2. 통과 (증거 = `_verify/p1r2-*.json` · row id)
- **크론**: `cron-run` 시크릿 없이 401 · hourly 5스텝 순서(roll→assign→produce→review→learn) errors 0 · 5m 2스텝(publisher·reap) · roll 재실행 changed 0 · `cron_tick` 감사 전건(id 522).
- **변수 8**: `autoSchedule=false` → roll/assign/produce/review 4스텝 `autoScheduleOff`(learn·publisher·reap 은 계속) · `horizonDays` 7→14 → roll checked 3→6 · `quietDays` 지정 → 그 날 빈 자리 0·재롤에도 0(v2.11) · `produceHour≠지금` → `skippedByHour "13시 ≠ 6시"` 명시 · `produceLeadDays`·`topicLeadDays`(창 안 자리만) · `reviewPolicy` 두 값 · `weeklyCoinCap=1` → `coin_short` 2건 + 알림 37 + 원장 무접촉(consume 0) · `bestTimeMode`(기존 자리 불변 · 설계대로 — 시각 검사는 reschedule 로).
- **슬롯 4경로 + IDOR**: assign-topic(이전 소재 후보 복귀·`topic_taken` 409·글 붙은 자리 `state` 400) · reschedule(#1 수리 후) · produce-now 202 `pieceId`+코인 차감 → 멱등(재차감 0) · 슬롯 producing · skip → skipped · **타 테넌트 4경로 전부 404·무변경**.
- **produce 크론**: `piece_auto_produced` 감사(id 613 · pieceIds [79]) · 코인 차감 · 슬롯 producing. ⚠️ 실제로 만든 틱은 **로컬에서 30초 500**(AC-12② 배경 함수 동기 실행) — 감사 행으로 판정 · 로컬 한정.
- **review_deadline**: 생성 완료 후 슬롯 `in_review` 동기화(slot 130) · `silence_approves` 마감 지남 → piece·슬롯 `scheduled` + 감사 `piece_approve by cron`(id 563) · `require_confirm` + 발행 시각 지남 → `awaiting_manual` + 알림 36 `review_missed`.
- **러너**: register(token `acr_` 1회 평문·install cmd · runner-list 에 평문 0) · heartbeat `sleepSec 60` · 잘못된 토큰 401 · online 칩 · `accounts-relogin` POST → session.login 잡 queued(중복 0 · GET 왕복) · claim → 계정 login 자격 실림 + 감사 `runner_creds_issued`(632) · `runner-session-upload` → 계정 `active` · 잡 done · IDOR runner-remove 404.
- **발행**: publisher due → `publish.naver_blog` 잡 적재(41) · piece `publishing` · 슬롯 `publishing` · payload 7키 · 재실행 재적재 0 · claim(SKIP LOCKED · 쿠키 실림) · 같은 잡 재claim 0 · **report ok → finalize: posts 1행(id 7) · piece published + external_url · 슬롯 published · posts_today+1 · 알림 post_published** · report 2회 → posts 1행(멱등 · `not_claimed(done)`) · external_url 있는 piece 재due → `already 1`·잡 1 · **러너 오프라인 30분+** → 잡 적재 유지 · 슬롯 `awaiting_runner` · 알림 `runner_offline`(취소 아님) · API 채널 자격 없음 → #2 수리 후 awaiting_manual.
- **reap**: claim 후 16분 → `queued` 회수(job 44 · detail released 1) · 미 claim 잡 release `ok:false`.
- **계정 전이(§7.2)**: `rate_limited` → cooldown · daily_cap 2→1 · 알림 · `login_fail` → pending_login · session.login 잡 · 알림 · `suspended` → 정지 · 미래 슬롯 행 유지+계정 교체(#3 수리 후) · 알림 · 감사 · `selector_changed` → 계정 전이 없음 · 감사 risk high `ourBug:true`(AC-10) · health_score 0~100 · last_error_kind.
- **알림함**: 모양 · `unread = home-summary.unread`(21=21) · read(id) −1 · read(전부) 0(홈도 0) · IDOR 타 테넌트 읽음 → 무변경.
- **발행함**: pieces 기준 · status 3어휘 · alive boolean · published 행 externalUrl/publishedVia/publishedAt · 실패 행 failReason · `?status=` 필터 · IDOR 0.
- **소재 배경화(v2.9)**: rate_limit 정직 응답(하루 3회 소진 — 202→폴링 흐름은 tid2 로 재검 예정) · **고아 방지** startedAt 11분 전 → `running:false`.
- **화면 13종**(폰 390·데스크톱 1280 · `_shots/p1r2/`): 로그인 리다이렉트 0 · Primary ≤1(A 수리 후 create 1) · 큰 숫자 ≤1 · 이모지 0 · 가로 넘침 0 · 콘솔 에러 0 · ≥400 요청 0 · **터치 44**(A 의 `::before` 오버레이 — `elementFromPoint` 자로 재서 통과 · notifications 1개만 WARN) · `toLocale*` 0건(KST 규칙).
- **러너 드라이런(네이버 · 1회 · 임시저장까지)**: `verify-b2-runner.mts naver_blog` **종료코드 0** · «임시저장까지 성공 · 서식 스윕 문단 10 · 어긋남 0» · 인용구·구분선·체크리스트·해시태그가 실제 에디터 요소로 — 스샷 `runner/_shots/job-78-mu0scjki/90-임시저장.png`(저장 13). B2 의 검증 프로필(`verify-naver_blog`)을 복사해 세션 로그인(비밀번호 로그인 0 · AC-19). 임시 테넌트 73 정리됨.

## R2-3. 결함 아님·보류
- 로컬 전용: 실제 produce 틱 30초 500(AC-12②) · `netlify dev` 정적 폴백 405(AC-7).
- 슬롯 게이트(«슬롯 없는 자동 생성 거부»): HTTP 로 재현 불가(크론·produce-now 가 항상 slotId 를 넘긴다) — `lib/slot-gate.ts` 정적 확인만. 단위 시험은 R3 이후 제안.
- `learn`: API 채널 통계는 이제 `fetchStats` 로 묻고 null=«못 물어봤다»(main a123c7c) — 로컬엔 6h 지난 post 가 없어 회수 경로 미실행(응답 errors 0 · skipped 구분 확인).
- 소재 배경화 202→폴링→added: 테스트 테넌트 rate_limit 소진 — R3 절에서 tid2 로 되짚는다.

## R2-4. 만진 파일
`netlify/functions/slots.ts` · `lib/cron/publisher.ts` · `lib/account-health.ts` · `lib/cron/assign-topics.ts` · `lib/cron/review-deadline.ts` · `scripts/verify-p1r2.mjs`(신규) · `scripts/shot-p1r1.mjs` · `scripts/verify-failover.mts`(케이스 추가).

---

# R3 절 — «수익이 한 곳에 모이고 소재로 되먹인다» (계약 v3.5 · 하니스 `scripts/verify-p1r3.mjs`)

실행: 로컬 8901(main `904e69c`+ 머지본) · 테스트 테넌트 3·13 · `settings.revenueSyncHour` 로 시각 이동 · `_verify/p1r3-2026-09-14T05-45-18.json` **48 PASS / 0 FAIL / 0 WARN**.

## R3-1. 결함
**0건.** (R3 코드에서 새 결함을 찾지 못했다. 아래 «통과» 항목이 지도 §-1 의 R3 열과 §6 시나리오 전부를 덮는다.)

## R3-2. 통과 (§6 시나리오 순서 · 증거 = row id · JSON)
- **수동 입력 → 홈·수익 탭 일치**: `revenue-manual`(meta · 오늘 12,340 → 20,000 덮어쓰기) · `revenue-summary.todayConfirmedKrw 20000 = home-summary.revenue.todayConfirmedKrw` · `monthKrw` 일치 · 옛 키(today/month/lastMonthSameDay) 호환 유지 · `bySource meta freshness:manual`.
- **KST 경계**: 저장 `day` = 보낸 KST 날짜(UTC 05:24 시점에도 2026-09-14 그대로) · 어제(KST) 0원 행 → `home.yesterdayKrw 0` · 내일 날짜 400 `day`.
- **같은 날 2회 수집 → 1행**: `(tenant,source,COALESCE(account),COALESCE(piece),day)` 업서트 · rows 1 · 값 덮어쓰기.
- **0원 행 vs 미수집(§1.4c)**: `revenue-daily.days[]` — 오늘 20,000 · 어제 `krw:0` 행 있음 · 3일 전 키 없음.
- **확정/예상 분리(§1.4 v3.2)**: 수동 4종=확정 20,000 · 예상 0 → 러너 adpost 오늘 3,210 이 `todayEstimatedKrw` 로만(확정 불변).
- **`not_configured` 는 에러가 아니다**: coupang connect(키 없음) → `not_configured` · `revenue.sync` 실행 후에도 상태 유지 · `revenue_error` 알림 0 · 스텝 errors 0.
- **키 필드(§1.4d)**: 일부만 → 400 «키를 모두 넣어 주세요(secretKey)» · 객체 저장 → `connected` · `cred_enc` 암호화(평문 0) · 감사엔 필드명만.
- **연속 실패 → error + 알림**: 잘못된 키로 sync 3회 → `status error · fail_count 3 · last_error_kind auth · lastError 노출` · 알림 `revenue_error` **1건**(중복 0) · **행 0**(실패를 0원으로 안 쓴다) · disconnect → `disconnected`.
- **구글 동의 진입점**: adsense connect(앱 키 없음) → 503 `provider_not_configured`(안내 톤).
- **러너 스크랩 report**: `revenue.adpost` 잡 claim → `revenueRows` 3행(그중 1행 `"1,234원"` 문자열) → **written 2 · rejected 1**(0 으로 고쳐 넣지 않음) · `freshness runner` · `bySource adpost runner`.
- **parse 실패(AC-9·AC-10)**: `errorKind:"parse"` report → 행 추가 0(2행/3,710 불변) · `revenue_sources.status error / last_error_kind parse` · 잡 `failed/parse` · 감사 `runner_job_failed risk high ourBug:true` · **계정 상태 전이 0**.
- **TOP 5 귀속**: pieceId 로 수동 입력 38,200 → `topPieces[{pieceId 68, title, channel, krw}]` · `byAccount @c_p1_naver 41,910`(=38,200+adpost 3,710) · ≤5·내림차순 · 쿠팡 `subId` 파서 `piece_{n}`·`p{n}`(정적).
- **ad-eligibility**: GET `thresholds {adpost:{posts 50, visitors 300}, ypp:{subs 1000,…}}` · `links` 5키 · 계정 `adpost{state,posts,visitors,ready}` · 글 채널 `adsense{state}`(v3.5) · POST `applied`→pending → `approved`→approved · 응답에 갱신 목록 · 감사 `ad_media_state_claimed`(972) · `accounts.monetize` jsonb object.
- **타 테넌트 0**: summary 0원 · days 0 · 남의 piece 귀속 400 · 남의 accountId 키 등록 400 · ad-eligibility 404.
- **learn 표본<5 중립**: topic 88 `performanceSamples 0` → 수익 항 미적용(`performanceOf: samples >= minSamples(5)` 정적) · learn 스텝 errors 0.
- **`.ad-slot` 실글**: 새로 만든 티스토리 piece 132 `bodyHtml` 에 `.ad-slot` **2곳(mid·end)** + R1 `.adsense` 2곳(발행 게이트가 둘 다 실체화 · 정규식 확인) · 네이버 piece 68 은 0.
- **계정 간 유사도 게이트**: `lib/similarity.ts` 한 벌을 assign/director-auto/content-gen 이 재사용(참조 27회 · 정적). 실제 소재 교체는 같은 테넌트 두 계정의 유사 글이 필요해 로컬 재현 보류.
- **정지 승계 실증(§2.3)**: `verify-failover.mts` **19/19**(R2 절 참조 · (다) 케이스 포함).
- **소재 배경화(v2.9 · R2 이월)**: tid 13 에서 `topics-refresh` 202 `started:true` → 실행 중 재호출 `started:false running:true` → 폴링 43s 후 `running:false · added 15`. 구간 로그 **`[topics] refresh tid=13 22196ms (ctx 1446 · llm 8186 · 검색량 875 · 트렌드 68 · 저장 11621)`** — 저장 11.6s 는 로컬→Neon 왕복(15행)이고 프로덕션에선 ~1s · LLM 8s 가 고정비 → 배경화가 맞았다.
- **화면(ui-v4 · `_shots/p1r2/`)**: 13화면 220 PASS · 계정 연결 그리드 **글 4채널만**(네이버·티스토리·블로거·워드프레스 · 영상 6채널 숨김) · 수익·수익 매체 화면 «러너» 0·시스템 용어 0 · 터치 44(revenue 1개 WARN).

## R3-3. 보류·메인 판단
- 계정 간 유사도 게이트의 **실제 소재 교체**와 클립 모집 D-7 알림은 데이터 조건(두 계정 유사 글·`channel_registry.monetize.clipOpen`)이 없어 정적 확인만 — 라이브 운영 데이터가 쌓이면 C6 로.
- 온보딩에서 영상 채널 선택(«곧 열려요» 칩)은 A 작업 중이라 스샷만(`_shots/p1r2/`에 없음 · 별도 확인 필요).
- 알림함에 남은 «슬롯» 문구는 수리 전 테스트 데이터 — 테스트 테넌트에서 삭제했다.

## 합산 — 배포 게이트 판정
- R2 결함 5건(P1 3·P2 2) **수리·되짚기 완료** · R3 결함 0 · 하니스 R2 API 84 PASS / R3 48 PASS / 화면 220 PASS / 승계 19/19 / 네이버 드라이런 종료코드 0.
- **배포 가능**(C 판정). 배포 후 C6: 라이브에서 `topics-refresh` 구간 로그 숫자 · 시나리오 1~3 재현 · 회귀(로그인·로그아웃·운영센터).
- PITFALLS 추가: AC-23(BETWEEN+AT TIME ZONE) · AC-24(여유는 날짜별) · AC-25(retriable:false ≠ 사람 개입 불필요).
- 브랜치 `verify/p1r2` · 최종 커밋은 아래.

---

# 라이브 절 — C6 (배포 `6aa78b2d` · main `e33807d` · https://autocreate-endyd.netlify.app · 2026-09-14 14:55 KST)

하니스 `scripts/verify-live-c6.mjs`(새 테스트 테넌트 **c+c6-mu0twb59@autocreate.test · tenant 83** · 코인 0 · 발행·생성 없음) · 화면 `scripts/shot-p1r1.mjs` → `_shots/live-c6/`. 증거 = `_verify/live-c6-2026-09-14T05-55-40.json` + 아래 row id.

## L-1. 라이브 결함 1건 → 수리(verify/p1r2 `45063b7` · **재배포 필요**)
| 심각도 | 결함 | 라이브 재현 | 수리 | 되짚기 |
|---|---|---|---|---|
| **P1** | `slots.assign_topics` 가 **`no_topic` 이 된 자리를 다시 보지 않는다**(SELECT 가 `status='planned'` 만). 소재가 없을 때 리필을 시작하고 자리를 `no_topic` 으로 두는데, 리필이 끝나 소재가 생겨도 그 자리는 **영영 비어 있다**(계약 §1 «다음 주기 재시도» 위반 · 사용자는 «소재 정하는 중»을 계속 본다). | tenant 83: 규칙 저장 → cron(hourly) → 3자리 `no_topic` + 리필 `started:true` → 리필 완료(added 15) → cron 재실행 → **여전히 no_topic 0/3 배정** | `lib/cron/assign-topics.ts` — 대상 `status IN ('planned','no_topic')`(CAS 도 같이) | 수리 코드로 로컬 8901 에서 같은 tenant 83 에 `cron-run` → `assign_topics changed 3` · slots 189/190/191 `topic_assigned`(topic 187·192·181) |

## L-2. 통과 (21/22 · 라이브)
- **회귀**: 가입 201(쿠키) · auth-me(trial 14일 · 코인 0) · 로그아웃 → auth-me 401 · 재로그인 · 틀린 비밀번호 401 · **비밀번호 변경 → 새 비밀번호 로그인** · 운영센터 admin 로그인(super_admin · mustChangePassword true) · **SSO 카드**(`/ops/login.html` «싸이렌 MIS 허브에서 들어가기» → tbfa.co.kr/admin-hub.html · 콘솔 에러 0 · `_shots/live-c6/ops-login-phone.png`).
- **시나리오**: 온보딩 글+영상 저장(영상 골라도 200) · `accounts-list` channels 10 = **active 4(naver_blog·tistory·blogger·wordpress) / planned 6** · 계정 연결(id 122 · pending_login · 응답 평문 0) · 규칙 저장 slotsCreated 3 · coinsPerWeek 21 · `cron-run hourly tid=83` 200(807ms · 5스텝 errors 0 · roll 멱등) · 시크릿 없이 401 · 슬롯 3자리(09-15·17·19) · assign_topics `noTopic 3 + refill started:true` 정직 · **`topics-refresh` 배경 완료 `added 15`**(startedAt→finishedAt 실측 **≈12s** · 20초 기준 통과 · 구간 상세는 Netlify 함수 로그 — 메인) · 소재 검색량 숫자 **14/15**(키워드툴 라이브 키 정상) · home-summary revenue 4키·todo·unread · 알림함 unread = 홈 · 러너 목록/수익 매체(thresholds)/수익 요약 200 · **IDOR 타 테넌트 슬롯 skip 404**.
- **화면 v4 라이브**(`_shots/live-c6/` · 폰 390·데스크톱 1280 × home·accounts·schedule·revenue·ad-media·posts·onboarding·login = 16장): 헌장 검사 134 PASS · 계정 연결 그리드 **4채널만** · 온보딩 «영상» 타일에 **«곧 열려요» 칩**(`onboarding-phone.png`) · «러너»·«슬롯»·이모지 0 · 콘솔 에러 0 · ≥400 요청 0.

## L-3. 메모
- 메인 스모크 테넌트 82(name 깨짐)는 서버 결함 아님 — 운영센터에서 정리 권장(FK 행이 있어 DB 직접 삭제는 안 했다).
- 다음: 수리 `45063b7` 머지·재배포 후 tenant 83 에서 `cron-run` 1회로 `topic_assigned` 라이브 되짚기(30초).
