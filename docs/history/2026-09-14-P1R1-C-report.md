# P1R1-C 보고서 — 검증·fix (C)

> 발부 2026-09-14 · 워크트리 `../AutoCreate-C` · 브랜치 `verify/p1` · 베이스 main `e6b3def`
> 1단계(정합 감사 + 하니스) → 2단계(라이브 시나리오 + fix)는 메인 «B·A 머지됨» 호출 후.

## C1. 계약·설계 정합 감사 (정적 · 2026-09-14)

대조 = 계약서 §1~§7 키 ↔ A 트리거 화면 항목 ↔ B 트리거 함수 항목 ↔ DESIGN §5.2·§5B.3/5B.4·§5C.1/5C.2·§7.1·§16B.3 ↔ 현 코드(main e6b3def).
판정: **P0 = 흐름이 끊기거나 게이트가 빠진다 · P1 = 머지에서 무음 파손 가능 · P2 = 설계 어휘·표기**.

### C1-A. 트리거/계약 정정이 필요한 것 (메인 몫)

| # | 심각도 | 위치 | 내용 | 제안 |
|---|---|---|---|---|
| 1 | **P0** | 계약 §5 `settings` ↔ 범위 지도 «변수 8 ✅ B8/A5» | 계약 settings 는 4개(autoSchedule·produceLeadDays·reviewPolicy·horizonDays). DESIGN §5B.4 변수 8 중 **topicLeadDays·produceHour·bestTimeMode·weeklyCoinCap·quietDays[] 5개가 계약·B8·A5 어디에도 없다**. B8 `rollSlots` 는 «quietDays 제외»를 하는데 quietDays 를 넣는 API 가 없다. | 계약 §5 `settings` 에 5개 추가 + `rules-settings` body 동일 + A5 규칙 시트/설정에 «쉬는 날(quietDays)» 최소 노출(나머지 4개는 설정 탭 «자동 편성 변수» — DESIGN §13.0b). 또는 지도 R2 로 명시 이동(현재는 «✅»라 누락). |
| 2 | **P1** | 계약 §5 `rules-settings` ↔ 기존 `tenant-settings`(`ALLOWED_SETTINGS`) | 같은 `tenants.settings` jsonb 를 두 경로가 쓴다(홈 토글은 `/api/tenant-settings {autoSchedule}` · 편성표는 `/api/rules-settings`). 키 이름은 같지만 B8 이 별도 merge 를 짜면 PITFALLS #10 «한 경로 수리 → 다른 경로 유입». | B8 에 «`tenant-settings.ts` 의 `mergeSettings` 재사용(top-level 키 · read→merge→full write)» 한 줄 명시. |
| 3 | **P1** | 계약 §4 `pieces-list?status=` | 허용값이 `in_review|scheduled|published|all` 뿐. A4 세그먼트 «만드는 중»은 `generating` 을, ⋯ 시트 후엔 `failed/rejected` 를 봐야 한다. B7 이 화이트리스트로 짜면 A4 폴링이 400. | `status=generating|in_review|scheduled|published|failed|all` 로 정정(A4 는 `all` 로 받아 클라 필터해도 됨 — 어느 쪽인지 글자로). |
| 4 | **P1** | 계약 §4 `PieceRow` ↔ A4 «스텝 바(글 쓰는 중→사진 만드는 중→검사 중 · 상태 문자열로 추정)» | 생성 중 상태는 `generating` 하나라 A 가 스텝을 추정할 값이 없다(DESIGN §13.0b «만드는 중 = 스텝 텍스트»). | `PieceRow.stage?:"writing"\|"images"\|"checking"`(B6 가 `meta.stage` 갱신 · generating 일 때만 실림) 추가. |
| 5 | **P1** | 계약 §4 `pieces-approve` ✗ 케이스 | B7 은 approve 실패를 `step:"gate"` 로 답하는데 계약 §4 엔 ✗ 모양이 없고 A4 도 처리 문장이 없다(토스트? 시트?). | `✗ 게이트 → { ok:false, step:"gate", error, checks:[GateCheck] }` 명시 + A4 «시트: 실패 항목 목록 + 수정하러 가기». |
| 6 | **P1** | 계약 §4 `Block.type` ↔ B5/B6 · 시드 `emotion_profiles.structure` | Block 유니온에 **`toc`·`summary`·`affiliate`(제휴 링크 블록)** 이 없다. B5 «본문 슬롯에 링크 블록 삽입», 티스토리 계약 «목차·요약 박스»(DESIGN §5C.1 · 시드 structure 에 `toc`·`summary` 있음). B 가 임의 타입을 만들면 A 미리보기가 못 그린다. | Block 에 `"toc"\|"summary"\|"affiliate"`(affiliate = `{ type:"affiliate", url, text, imageUrl? }`) 추가. |
| 7 | **P1** | 계약 §3 `director-confirm` 코인 롤백 | B5 «하나라도 insufficient 면 전부 되돌리고(piece 삭제)» — 이미 성공한 `consume` 원장 행 처리가 없다(piece 는 지웠는데 코인은 빠진 채). | «consume 전에 총액 ≥ 잔액 선검사(부족이면 원장 무접촉·coin_short)» 또는 «실패 시 성공분 `refund:piece:{id}` grant». 전자 추천. |
| 8 | **P1** | 계약 §7 «home-summary 변경 없음» ↔ A6 «todo review 링크 → /app/pieces.html» | 링크는 **백엔드**(`home-summary.ts:35` → `/app/schedule.html`)가 만든다. A 는 `netlify/` 금지 영역. | B 트리거에 1줄 추가: `home-summary.ts` review todo link → `/app/pieces.html`. (A 클라 치환은 PITFALLS #13 부류.) |
| 9 | **P1** | 계약 §1 `accounts-add` 응답 status | 계약은 «pending_login 고정», B3 은 wordpress 를 REST 인증 후 `active`. A1 알약 매핑엔 둘 다 있으나 계약 문장이 틀렸다. | 계약: `status:"pending_login"`(session·oauth) `\| "active"`(wordpress 인증 성공). |
| 10 | **P1** | 계약 §1 `accounts-list` 행 ↔ «AccountRow 에 추가: minGapMin·goldenHours·proxyUrl·monetize» | 추가분이 목록 응답 줄엔 없고 각주로만 있다. A1 상세 시트는 목록 행에서 이 값을 읽는다(별도 GET 없음). | `accounts-list.accounts[]` 에 minGapMin·goldenHours?·proxyUrl?·monetize 포함을 목록 줄에 직접 적기. |
| 11 | **P1** | DESIGN §16B.3 게이트 ↔ 범위 지도 «✅ B6/B7» | 트리거에 미배정: ① 계정 핸들에 «쿠팡» 검사(accounts-add · B3 없음) ② 글당 제휴 링크 ≤2(approve · B7 없음) ③ 애드센스 자리 라벨 «광고»(render · B6 없음) ④ 상품 이미지 = API 제공분만(B5 쿠팡 검색 시). | B3/B6/B7 에 각 1줄. |
| 12 | **P2** | 계약 §4 `GateReport.checks[].key` | key 어휘 미정 — 하니스·A 렌더가 «AI티 검사 존재»를 이름으로 못 센다. | 어휘 고정 제안: `cliche·para_start·bullet_ratio·sentence_var·translationese·superlative·persona·visual_min·disclosure·banned·similarity`. |
| 13 | **P2** | 계약 §3 `PieceSpec.format` ↔ DESIGN §5.2 | DESIGN: `longform\|listicle\|qna\|compare\|story\|cardnews\|shorts_*` · 계약: `story\|info\|listicle\|compare\|qna\|guide\|cardnews`. CLAUDE §8 «설계를 바꾸면 DESIGN 먼저». | DESIGN §5.2 format 어휘를 계약대로 갱신(shorts_* 는 Phase 2 유지). |
| 14 | **P2** | 계약 §3 `Brief.goal` | goal 결정 규칙이 계약·B5 에 없다(테넌트 settings? 계정 monetize?). DESIGN §5.3-1 은 goal 로 채널 우선순위를 정한다. | B5 에 «goal = 배정 계정들의 monetize 키 존재 → adpost/adsense/affiliate, 둘 이상이면 mixed» 같은 결정론 1줄. |
| 15 | **P2** | 계약 §5 `Rule.every` | `week\|day` — DESIGN §5B.3 «주 N회/월 N회/매일», DDL 주석 `day\|week\|month`. | `month` 추가 또는 지도에 R2 명시. |
| 16 | **P2** | 계약 §4 `pieces-update` 응답 | 서버가 고지 블록을 «다시 넣는다»면 정본 bodyHtml 이 바뀌는데 응답은 `{ ok, gate }` 뿐 → A 화면이 서버 정본과 어긋남. | 응답에 `bodyHtml`(서버 정본) 추가 또는 A4 «저장 후 pieces-get 재조회» 명시. |
| 17 | **P2** | DESIGN §5B.8 슬롯 시트(미리보기·소재 바꾸기·시각 바꾸기·지금 만들기·건너뛰기) ↔ A5 «건너뛰기 + 정보» | 지도엔 R2 로 안 적혀 있다(«화면 3스텝 ✅»만). | 지도에 «슬롯 시트 4동작 R2» 명시. DESIGN §5B.8 «주/월 세그먼트» ↔ §13.0b «소비 캘린더(월)» 도 설계 내부 불일치 — 13.0b 우선으로 확정 문장 필요. |
| 18 | **P2** | DESIGN §7.1 필드 ↔ 계약 §1 | `avatar`·`auth_method`·`last_error_kind`·`account_groups` 가 계약 투영에 없다(지도 «필드 전부 ✅»). monetize 는 DESIGN `coupang_subid_prefix` ↔ 계약 access/secret 키(링크 생성엔 후자가 맞다). | 지도에 «avatar·group·last_error_kind = R2(정지 감지)» 명시 · DESIGN §7.1 monetize 를 계약대로 갱신. |
| 19 | **P2** | DESIGN §5B.9 플랜 한도(Starter 규칙 3·horizon 7) ↔ 계약 §5 | `rules-save` 한도 초과 응답이 없다(`plans.ts limits.maxRules/horizonDays` 는 이미 있음). | `✗ 한도 → step:"limit"` 1줄(체험은 무제한이라 시나리오엔 안 걸림). |
| 20 | **P2** | B4 스코어식 | «DESIGN §5 팩터식 그대로»라 적었으나 DESIGN 엔 수식이 없다(AM content-topics 가중치). | DESIGN §5 또는 §3.3 에 수식 1줄 기록(전본 규칙). |

### C1-B. 정합 OK 로 확인한 것
- 계약 §1~§6 의 경로 접두(`accounts-·topics-·director-·pieces-·rules-·slots-·coins-·personas-`)는 현 코드에 0건(충돌 없음 · `netlify/functions` grep).
- 채널 키 10개 = `UI.CH` = `channel_registry` 시드 = 계약 §0 일치.
- DDL: `accounts`(min_gap_min·golden_hours·proxy_url·monetize·browser_profile_key)·`account_creds(kind·enc·purged_at)`·`topics(norm_key·factors·expires_at)`·`briefs(pieces·reasons·coin_cost)`·`pieces(blocks·meta·gate_report)`·`piece_assets`·`slots(rule_id·slot_date·origin)`·`cadence_rules` 8필드·`coin_ledger_idem`(tenant_id,kind,ref,bucket)·`affiliate_links(sub_id)`·`ai_usage`·`notifications` — 계약이 요구하는 컬럼 전부 존재. **B 의 «DDL 추가 불필요» 실측 맞음.**
- 코인 지급 API `POST /api/ops-coins-grant { id, coins, reason }`(admin) 존재 — 시나리오 준비에 쓸 수 있음.
- `tenants.settings` 화이트리스트에 `produceLeadDays·reviewPolicy·bestTimeMode·weeklyCoinCap·quietDays·horizonDays` 이미 있음(→ #1·#2 의 근거: 키 이름은 이걸 따르면 된다 · `topicLeadDays`·`produceHour` 만 추가).
- 계약 §1 Persona.profile 9필드 = DESIGN §5C.2 «넣는 것» + §7.1 personas 항목 전부 포함.
- 계약 §4 status 어휘 = DESIGN §4.1 piece 상태 10개 일치. 계약 §1 status 어휘 = §7.1 6개 일치.
- DESIGN §5C.2 «세는 것» 8검사 = B6 게이트 항목 8개 1:1.
- DESIGN §5C.1 7행 = B5 writing-contracts «7행 전부».
- DESIGN §5.2 지시서 필드 12개 = 계약 PieceSpec+Brief(goal·mode·reasons·coinCost·pieces[…]) 전부 있음(format 어휘만 #13).

### C1-C. 하니스가 관대하게 받는 것(계약 정정 전까지)
- `director-confirm` 200/202 둘 다 통과(B5 «202 즉시 반환» ↔ 계약 미명시).
- `accounts-add` status `pending_login|active`.
- `pieces-list?status=generating` 400 이면 `all` 로 재시도하고 «#3 미정정» 으로 기록.
- `GateReport.checks[].key` 는 이름 대신 **개수 ≥ 8 + disclosure 관련 key 1개** 로 «AI티 검사 존재» 판정(#12 정정되면 이름 대조로 승격).

> **v1.1 반영(메인 e33fd56)**: C1 20건 전부 계약서 v1.1 로 정정됨(`[v1.1]` grep). 하니스는 v1.1 을 정본으로 엄격 검사한다.

## C2. API 하니스 — `scripts/verify-p1r1.mjs` (커밋 fdd173c)
- 실행: `node scripts/verify-p1r1.mjs`(로컬 8899) · `BASE_URL=https://autocreate-endyd.netlify.app OPS_USER=… OPS_PASS=… node scripts/verify-p1r1.mjs`(라이브).
- 흐름 = 계약 §10 시나리오 1 순서 그대로: 가입/로그인 → 코인 30(운영센터 `ops-coins-grant` · 로컬은 DB grant 폴백) → 계정 2(naver_blog·tistory · 핸들 «쿠팡» 거부 2케이스) → accounts-update(§7.1 필드) → personas → oauth-start(준비 중 정직) → topics-refresh(검색량 숫자·금칙 앵글) → director-propose(계정 배정·코인 합·reasons 3·일정 미래) → confirm(차감 = coinsCharged) → **재확정(잔액·consume 행 수 불변)** → 생성 대기(stage 전이 관측 · in_review) → pieces-get(GateKey 12 순서·AI티 8·블록 어휘·이미지 URL 200·캡션·고지 첫 블록) → 홈 todo review 링크 → pieces-update(bodyHtml 반환·**고지 삭제 → 재삽입**) → approve(scheduled · ✗ 면 step gate 모양) → rules-save 주3회(coinsPerWeek 21·멱등 slotsCreated 0) → slots-list 7일치(중복 0·manual 슬롯) → rules-settings 8키 왕복 + tenant-settings 와 한 jsonb → slots-skip → **IDOR**(타 테넌트 pieces-get/accounts-remove/approve) → **자격 평문 0 · password 키 0**(전 응답 본문).
- 판정: 키 누락·타입 = FAIL · 계약 외 키 · null 실림 = WARN(«없으면 키 생략»). 결과 표 stdout + `_verify/p1r1-<ts>.json`(gitignore).
- 라이브 스모크(2026-09-14 05:47 KST · P1R1 API 배포 전): 로그인/가입 201·쿠키·판정 기제 동작 확인 · P1R1 경로 17건 404(예상) · 테스트 테넌트 `c+p1@autocreate.test` 라이브 생성됨.

## C3. 화면 하니스 — `scripts/shot-p1r1.mjs` (커밋 c775ba2)
- 실행: `node scripts/shot-p1r1.mjs`(playwright = `../AutoMarketing/node_modules` · 실행 위치 무관) · `MOCK=1` 이면 A 의 `?mock=1` 층.
- 폰 390×844(모바일 에뮬) / 데스크톱 1280×800 × 8화면(accounts·create·director·pieces·piece·schedule·home·account) 전체 스샷 + 시트 열기 스샷(채널 탭·디렉터에게·손보기·⋯·자동 편성 켜기) → `_shots/p1r1/`.
- 자동 검사: 로그인 리다이렉트 없음 · 보이는 `.btn.primary` ≤1(시트 밖/안 따로) · `.hl .num` ≤1 · 이모지(Emoji_Presentation) 0(채널 마크 `.mk` 제외 · 딩뱃은 WARN) · 시스템 용어(테넌트·piece·슬롯·러너 잡) 0 · `scrollWidth` 가로 넘침 0(PITFALLS #24) · 콘솔 에러 0 · ≥400 요청 0 · 44px 미만 터치 타깃 WARN(달력 셀 제외).
- 라이브 스모크(Phase 0 화면): 122 PASS · FAIL 12 = 전부 미배포 화면 404(director·pieces·piece) · WARN = 시트 셀렉터(A 구현 후 확정)·터치 타깃.

---

# 2단계 — C4 로컬 실행·수리 (2026-09-14 · main `6110f17`→`bf41124` 기준)

실행 환경: 내 전용 로컬 `netlify dev` **포트 8901**(다른 세션의 8899 무접촉 · 정적 서버는 `_verify/_static.mjs` 로 3997) · DB = 라이브 Neon(**테스트 테넌트 `c+p1@autocreate.test` id 3 한정** · PITFALLS #8) · 하니스 2종.

## C4-1. 결함 목록 (전부 내가 수리 · 브랜치 `fix/p1-c4` → main 머지 `b23efa2`·`bf41124`)

| # | 심각도 | 결함 | 재현 | 기대/실제 | 수리 | 되짚기 증거 |
|---|---|---|---|---|---|---|
| 1 | **P0** | 배경 생성 호출이 실패해도 piece 가 `generating` 에 영원히 남는다 — 코인은 차감된 채, 알림 0, 홈 «해야 할 일»은 `in_review` 만 세므로 **어느 화면에도 신호가 없다**. `confirm` 이 `void triggerGenerate(...)` 로 결과를 버렸다. | SITE_URL 이 닿지 않는 주소인 상태로 `director-confirm`(또는 `pieces-regenerate`) | 기대 = 실패를 사용자에게 알리고 코인 환급 / 실제 = 영원한 «만드는 중» | `lib/director.ts` — 실패 시 `failed`+환급+알림+슬롯 `failed`(한 왕복 CTE) · 호출은 `Promise.all` 동시 · 응답 대기 6초 상한 | piece 11 `failed` «생성을 시작하지 못했어요(서버에 연결하지 못했어요)» · 원장 `grant 4 ref=refund:piece:11:c4` · 홈 unread 0→1 |
| 2 | **P0** | 이미 갇힌 글은 «다시 만들기»도 거부 — `pieces-regenerate` 가 `status==='generating'` 이면 즉시 되돌려 보내 **탈출구가 없다**(코인 차감 고정). | 갇힌 piece 에 `pieces-regenerate` | 기대 = 다시 걸어 준다 / 실제 = 200 무동작 | `netlify/functions/pieces.ts` — 20분 초과면 재호출(코인 재차감 0 · 감사 `piece_retrigger`) | piece 13(21분 갇힘) → 202 + 배경 함수 큐잉 · 잔액 24→24 · **piece 12·13·14·37·38 전부 `in_review` 로 회수**(라이브 DB row) |
| 3 | **P1** | `rules-settings` 에 `quietDays` 를 넣으면 500(`22P02 malformed array literal`) — 쉬는 날을 저장할 수 없다(계약 v1.1 ScheduleSettings 8키 중 하나). | `POST /api/rules-settings {quietDays:["2026-09-15"]}` | 기대 200 / 실제 500 | `netlify/functions/rules.ts` — JS 배열 `= ANY(${arr}::date[])` 바인딩을 날짜별 캐스팅 `IN (…)` 으로 | 같은 호출 200 · `settings.quietDays` 왕복 확인 |
| 4 | **P1** | 건너뛴(`skipped`) 슬롯이 다음 `rollSlots` 에서 되살아난다 — 사용자가 «건너뛰기» 한 날이 다시 편성된다(AC-2 계열). | `slots-skip` 후 `rules-save`(roll 재실행) | 기대 = 그 날은 비어 있다 / 실제 = 같은 규칙·같은 날 슬롯 재생성 | `lib/slots.ts` — `have` 집합에 skipped 포함(시각 점유에서는 제외) | 재롤 후 `slotsCreated 0` · 건너뛴 날 재생성 0 |
| 5 | **P1** | 꺼진 규칙의 미래 슬롯이 남아 **중복 편성**이 된다 — 정리가 `status='planned'` 만 지워서 `topic_assigned`·`no_topic`·`coin_short` 슬롯이 살아남는다. | 규칙 A 로 편성 → 규칙 A 끄고 규칙 B 저장 | 기대 = 하루 1건 / 실제 = 09-17·09-19 각 2건(rule 8 잔존 + rule 10 신규) | `netlify/functions/rules.ts` — 정리 범위 = 글 안 붙은 미래 슬롯 전부(`piece_id IS NULL` · `slot_date >= CURRENT_DATE` · planned·topic_assigned·no_topic·coin_short) | 재실행에서 날짜 중복 0 |
| 6 | **P1** | `topics-refresh` 가 30초 함수 타임아웃 500 — 소재는 DB 에 들어가는데 화면은 실패를 본다(그리고 하루 3회 제한만 깎인다). | 로컬 `POST /api/topics-refresh` | 기대 = 한도 안 / 실제 = 30s 타임아웃 500 | `lib/naver-volume.ts` 검색량 조회 3콜 동시+예산 12s · `lib/naver-datalab.ts` 401 단락(키 문제면 남은 묶음 생략) · `lib/topics.ts` 구간 타이밍 로그 | 로그 `[topics] refresh tid=… Nms (ctx·llm·검색량·트렌드·저장)` — **라이브 숫자는 C6 에서** |

> 🔴 6번의 남은 위험: 지연의 절반은 **후보 생성 LLM 1콜(pro·6000토큰)** 자체다. 메인 판정 기준(HANDOFF): **C6 라이브 측정 total > 20초 → 배경화(P1R3)**, 이하면 유지.

## C4-2. 결함이 아닌 것(분류)
- **로컬 전용 증상**: `netlify dev` 가 `-background` 를 동기 실행해 배경 호출 스텝이 로컬에서만 500(AC-12②). ⇒ 호출 대기에 6초 상한을 둬 부모가 먼저 죽지 않게 했다(AC-12 에 한 줄 보강).
- **키 없음(선결조건)**: 데이터랩 `growthPct`(네이버 앱에 데이터랩 API 미추가 · 401) · 쿠팡 키 · OAuth 앱 키 · WP 테스트 사이트. **정직 경로는 검사했고 전부 통과** — `accounts-oauth-start` → `step:"provider_not_configured"` · 검색량/트렌드 없으면 **키 자체를 싣지 않는다**(`factors` 에 `volume`/`growthPct` 부재 · 0 으로 채우지 않음) · 제휴 키 없으면 고지 블록만.
- **하니스 과민(내 쪽 정정)**: 건너뛴 슬롯을 «편성»으로 셈 · 비활성 규칙을 규칙 수에 포함 · 앞 실행이 남긴 `quietDays` 로 슬롯 수가 줄어듦 → 하니스 수정(`48ef5fa` 등).

## C4-3. 마지막 하니스 결과 (로컬 8901 · 계약 v1.2 엄격)
- **C2 API**: **PASS 84 · FAIL 1 · WARN 2**(399s). 유일한 FAIL = 수리 **이전** 코드가 남긴 갇힌 piece 37·38 — 수리된 구제 경로로 회수해 `in_review` 확인(위 표 2번 증거). WARN 2 = 제휴 없는 piece 라 고지 검사 비대상 · quietDays 재롤 시 기존 슬롯 처리(설계 미정 · 아래 열린 질문).
- 통과 항목(발췌): 코인 차감 = `coinsCharged` · **재확정 재차감 0**(잔액·consume 행 수 불변) · `GateKey 12` 순서 고정 · AI티 8검사 · 블록 어휘 · **이미지 URL 200 3/3** · 고지 삭제 → 재삽입 · `pieces-update` 가 저장본 `bodyHtml` 반환 · approve → `scheduled` · `coinsPerWeek 21`(3×(1+6)) · rules-save 멱등(`slotsCreated 0`) · **IDOR 3종 404** · **응답 본문 자격 평문 0 / `password` 키 0**(56개 응답).
- **C3 화면**(폰 390 / 데스크톱 1280 · 8화면 + 시트): **PASS 132 · FAIL 6 · WARN 10**. 스샷 `_shots/p1r1/`.

## C4-4. 화면에서 올라온 것 (A·설계 판단 몫 — 내가 고치지 않음)
1. **`create.html` 에 Primary 버튼 28개**(소재 행마다 «디렉터에게» `.btn primary sm`). A 트리거가 그렇게 지시했지만 DESIGN §13.0 헌장은 «화면당 Primary 1개»다 — **트리거와 헌장의 충돌**이라 설계 판단이 필요하다(추천: 행 액션은 `secondary`/텍스트 버튼, Primary 는 상단 «새로 뽑기» 하나). 스샷 `_shots/p1r1/create-phone.png`.
2. **터치 타깃 44px 미만**: create 29개 · pieces/schedule 각 4개 · home 1개(달력 셀 제외). DESIGN §13.0 접근성 하한 위반(P2).
3. **`account.html` 이 `/api/posts-list?status=published` 404** — P1R2-B(발행 이력 API) 머지 전이라 «미착수»로 분류. B 머지 후 재확인.

## C4-5. 열린 질문 1개 (메인)
- `quietDays` 를 **나중에** 지정했을 때 그 날 **이미 만들어진** 슬롯을 어떻게 하나? 지금 코드는 «글 안 붙은 `planned` 은 지우고, 이미 소재·글이 붙은 슬롯은 남긴다». 설계(§5B.4)는 «슬롯을 만들지 않는다»만 말한다. 지금 동작이 맞으면 계약에 한 줄 박자.
