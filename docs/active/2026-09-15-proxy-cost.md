# 프록시(계정별 IP) 원가 측정 — 1차 · 2026-09-15

> 사장님 질문: **«프록시 계정 1개당 비용 정확히 얼마야?»** + **«계정 1개도 코인으로 살 수 있게 · 프록시는 우리가 사고 고객에겐 마진 붙여서»**
> 이 문서 = ①시장 가격 실조사(오늘) ②계정 1개당 월 원가 ③권장 판매가·마진 ④**«정확히»가 되려면 남은 실측 2개**.
> 환율 = 우리 기준값 `FX_USD_KRW=1400`. 표시가는 **부가세 별도**(AM 방식 · DESIGN §12.0).

---

## 0. 한 줄 답

**계정 1개당 월 원가 = 약 ₩3,400 ~ ₩7,000**(한국 정적 주거용 IP 1개 기준 · 대량 구매하면 아래쪽, 소량이면 위쪽).
모바일 IP 로 가면 **₩16,000 ~ ₩21,000**(차단 내성은 제일 높다). 권장 기준값 = **계정당 월 ₩5,000**.

---

## 1. 시장 가격 실조사(2026-09-15 · 공급사 공개가)

| 유형 | 한국 IP | 공개가 | 원화(FX 1400) | 우리 용도 적합성 |
|---|---|---|---|---|
| **정적 주거용(ISP) · IP 고정** | 제공(공급사별 재고 편차) | **$2.44~2.87 / IP / 월**(Proxy-Cheap) · **$5 → $2.50**(PrivateProxy · 1,000개 이상) | **₩3,400 ~ ₩7,000** | 🟢 **가장 맞다** — 계정 1개 = IP 1개 고정(sticky 걱정 없음) |
| 회전 주거용 · GB 과금 | 제공 | $0.78~2.60/GB(Proxy-Cheap 할인~정가) · 시장 일반 $2~8/GB · 한국 «$1/GB 부터» 광고 | 계정당 0.6~1.2GB 가정 시 **₩700 ~ ₩13,400** | 🟡 sticky 세션으로 IP 고정은 되지만 **세션이 끊기면 IP 가 바뀐다**(계정-IP 1:1 유지가 불안) |
| **모바일(전용 포트)** | 제공 | **$11.61~15 / 포트 / 월** · 일부 $117/월 | **₩16,300 ~ ₩21,000** | 🟢 차단 내성 최고(통신사 IP) · 🔴 비싸다 — 중요 계정만 |
| 데이터센터 | 제공 | 월 $1 미만~ | ₩1,000 내외 | 🔴 **쓰지 마라** — 네이버·메타가 가장 먼저 거른다 |

> 🔴 **주의 2개**: ①한국 정적 주거용 IP 는 미국·유럽보다 **재고가 적다** — 계정 20개를 한 공급사에서 한 번에 못 받을 수 있다(2~3곳 분산 전제). ②«$0.23/IP» 같은 초저가는 대부분 **미국 데이터센터/정적** 이고 한국·주거용이 아니다.

## 2. 계정 1개당 월 원가(우리 기준)

| 항목 | 금액/월 | 근거 |
|---|---|---|
| 한국 정적 주거용 IP 1개 | **₩5,000** | 위 표 중앙값(소량 구매 시작가 ₩7,000 · 규모 커지면 ₩3,400) |
| 러너 실행 분담(관리형일 때 · 노트북 1대에 계정 N개) | **₩500 ~ 1,500** | 전기·감가·대역 — 노트북 1대가 계정 10~20개를 돌린다고 보면 계정당 ₩1,000 안팎 |
| 운영(모니터링·교체·장애) | **₩500** | IP 가 죽으면 갈아 끼우는 사람 시간 |
| **합계(내 PC 러너 + 전용 IP)** | **≈ ₩5,500** | 프록시 + 운영 |
| **합계(관리형 = 우리 서버 + 전용 IP)** | **≈ ₩6,500 ~ 7,000** | 위 + 실행 분담 |

> AI 생성 원가(글·이미지·영상)는 **여기 없다** — 그건 이미 코인으로 따로 받는다(이중 과금 금지).

## 3. 권장 상품·판매가(사장님 결정용 · 부가세 별도)

| 상품 | 무엇 | 원가 | **권장가(월)** | 코인 환산 | 마진 |
|---|---|---|---|---|---|
| **계정 1개 + 전용 IP**(내 PC 러너) | 계정 슬롯 1 + 그 계정 전용 한국 IP | ₩5,500 | **₩12,000** | **24코인/월** | 약 2.2배 |
| **관리형 계정 1개**(우리 서버 + 전용 IP) | 위 + 우리가 대신 돌림(PC 필요 없음) | ₩7,000 | **₩25,000** | 50코인/월 | 약 3.6배 |
| 모바일 IP 로 올림(선택) | 중요 계정만 | +₩18,000 | **+₩25,000** | +50코인/월 | 약 1.4배 |

- **코인 1개 = ₩500**(AM 동일 · 변경 없음).
- 🔴 **«한 번 사면 끝»이 아니라 30일마다 돌아오는 비용이다**(IP 가 월 과금이라) → 상품도 **30일권 + 자동 갱신**(다음 30일에 코인 자동 차감 · 잔액 부족하면 3일 전 알림 → 만료 시 그 계정만 «쉼»). 영구 구매로 팔면 우리가 매달 손해를 본다.
- 플랜 포함분: Starter 1 · Pro 3 · Agency 10 같은 식으로 **플랜에 포함된 계정 수**를 두고, 그 이상은 이 상품으로 산다(요금제 표와 한 출처).

## 4. 🔴 «정확히»가 되려면 남은 실측 2개

이 문서의 숫자는 **공급사 공개가 + 사용량 가정**이다. 다음 두 가지가 실측되면 «정확히»가 된다.

1. ~~계정 1개가 한 달에 쓰는 트래픽(GB)~~ → **✅ 실측했다(B2 · 2026-09-15). 아래 §4.1 로 대체.**
2. **한국 IP 실구매 견적 3곳** — 공개가는 «부터»다. 계정 10개·20개·50개로 **실제 견적**(한국 재고 · sticky 보장 · 대역 상한 · 결제 조건)을 받아 표를 채운다. 실제 구매는 사장님 합동 세션 11번.

### 4.1 ✅ 트래픽 실측(B2 · 2026-09-15 · 러너 계측기 `meterContext`)

측정 방법: Playwright 컨텍스트에 CDP 를 붙여 **`Network.dataReceived`(encodedDataLength)** 를 합산했다 —
전선 위 실제 바이트다(`response.body().length` 로 세면 **압축 풀린 크기**라 3~5배 과대계상된다).
업링크는 정확한 신호가 없어 **요청 본문 길이 합**으로 근사했다(헤더·TLS 제외 — 이미지가 업링크의 거의 전부라 «몇 MB냐»는 맞는다).

| 잰 것 | 실측 | 비고 |
|---|---|---|
| 네이버 블로그 **글 1장 열기** | **↓1.24MB** (요청 133건) | `verify.post_alive` 가 하는 일 = 이 값 |
| 네이버 **글쓰기 진입**(로그인으로 튕김) | **↓3.21MB** (요청 21건) | 🔴 로그인 화면 기준 **하한** — 진짜 에디터는 이보다 무겁다 |
| 티스토리 첫 화면 | ↓2.73MB ↑117KB (요청 82건) | 참고값 |
| **생성 이미지 1장**(실제 R2 자산 24장) | **평균 0.76MB** (중앙값 0.77 · 0.65~0.87) | 업링크의 거의 전부 |
| **글 1건 이미지 6장 업로드** | **↑4.59MB** | 10장이면 ↑7.65MB |

**글 1건 합계(추정 · 실측 부품 조립)**: 에디터 진입 ↓3.2MB + 이미지 ↑4.6MB + 저장·발행 왕복 ~0.5MB ≈ **9~10MB/건**.

🔴 **가정(20~40MB)의 2~4배 과대였다.** 월 트래픽을 다시 계산하면:

| 하루 발행 | 월 건수 | 월 트래픽/계정 | GB 과금($2/GB 기준) |
|---|---|---|---|
| 1건 | 30 | **≈0.29GB** | ≈₩800 |
| 2건 | 60 | **≈0.58GB** | ≈₩1,600 |
| 3건 | 90 | **≈0.87GB** | ≈₩2,400 |

**결론(가격 결정 입력)**: 글 채널만 쓰는 계정은 트래픽이 **1GB 미만**이라 **GB 과금이 IP 과금(₩3,400~7,000)보다 싸다.**
다만 §2 표의 🟡 대로 **회전 주거용은 sticky 가 끊기면 IP 가 바뀌어** 계정-IP 1:1 이 깨진다 —
**싼 쪽이 아니라 «IP 가 안 바뀌는 쪽»을 골라야 한다**는 판단은 그대로다. GB 과금을 쓰려면 **sticky 보장**이 계약 조건이다.
⚠️ 영상(쇼츠) 계정은 이 표 밖이다 — mp4 업로드가 한 건에 수십 MB라 따로 재야 한다.

⚠️ **아직 안 잰 것**: 로그인된 **진짜 에디터**의 무게(위 3.21MB 는 로그인 화면이다). 계측기는 러너에 **상시로** 들어갔으니,
합동 세션의 첫 실발행이 돌면 그 값이 `report.bytes` 로 **자동으로** 남는다 — 그때 이 표를 확정한다.

## 5. 정직하게 말할 것(화면·안내 문구)

- IP 를 나눠도 **유튜브·인스타·쓰레드는 우리 서버에서 API 로 올라간다** — 그쪽은 IP 보다 **우리 앱·계정 신호**로 묶인다. «IP 를 나눴으니 정지가 안 된다»고 말하면 안 된다.
- 프록시가 죽으면 **그 계정은 멈춘다**(우리 IP 로 대신 나가지 않는다 · fail-closed). 이게 정지 연쇄를 막는 핵심이라 «가끔 멈출 수 있다»를 미리 알린다.
- IP 는 **빌리는 것**이다 — 공급사 사정으로 바뀔 수 있고, 바뀌면 그 계정은 새 IP 로 다시 묶인다(기록·알림).
