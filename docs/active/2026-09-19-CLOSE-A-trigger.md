# A 트리거 — 라운드 마감 · 마지막 빨강 한 칸 (2026-09-19)

## 0. 지금 상태 (내가 재서 적는다 · 짐작 아님)
- 배선검사 `node scripts/verify-r8-deadends.mjs` = **PASS 134 · FAIL 1**.
- 🔴 **그 빨강 1개가 너의 몫이다.** 이거 초록되면 R11·R12 화면 축은 끝이다.
- 베이스 = 로컬 `main` HEAD `9313548`. 워크트리 `../AutoCreate-A` · 브랜치 `feature/r11-front`.

## 1. 이번 몫 = **R12-6 화면** 하나
> 계약 원문(§4 표): `R12-6 | **당근 채널** — 세션 쿠키 · 사진 10장 · 🔴 서식 0 · 🔴 수익 없음을 말해 준다 | §7 | B2·B·**A**`

### 🔴 서버는 **이미 다 실어 보내고 있다** — 너는 화면만 붙인다
| 값 | 어디서 오나 | 모양 |
|---|---|---|
| `noRevenueChannel: true` | `lib/revenue/aggregate.ts:176` — 계정 한 줄마다 | 그 줄이 돈 안 붙는 채널일 때만 붙는다 |
| `noRevenueChannels: string[]` | `lib/revenue/aggregate.ts:188` — 응답 맨 위 | 이번 집계에 섞인 «돈 안 붙는 채널» 목록 |
| `monetizable: false` | `lib/accounts.ts:204` — 채널 목록 | 계정 고르는 자리에서 쓴다 |

⇒ **API 새로 만들 것 없다. 새 필드 요청할 것 없다.** 안 쓰고 있을 뿐이다.

### 왜 이게 빨강으로 남아 있나 (자의 사유 원문)
> «당근 계정의 **0원이 고장으로 보인다** — 설계가 «정직하게 말해야 하는 것 셋»의 첫째로 박아 둔 자리다(AC-10 «설정 안 됨은 오류가 아니다»)»

고객이 당근 계정을 켜 놓고 수익 화면을 본다 → **0원**. 지금 화면은 그게
«아직 안 벌린 건지» «고장인지» «원래 안 붙는 건지» **한 글자도 말해 주지 않는다.**

### 붙일 곳 둘
1. **수익 화면** — `noRevenueChannel` 인 계정 줄에 표시. 0원 옆에 **«왜 0원인지»**가 있어야 한다.
2. **계정/채널 고르는 자리** — `monetizable:false` 인 채널은 **고르기 전에** 알려 준다(고른 뒤에 실망 금지).

## 2. 🔴 말투 — 여기가 이 칸의 진짜 어려운 데다
CLAUDE §3 «겁주지 않는다» + §9-① «또렷하게 ≠ 겁주기».

- ❌ «당근은 수익이 발생하지 않습니다» (사실만 던지고 끝 — 고객은 «그럼 왜 넣었지» 가 된다)
- ❌ «수익을 원하시면 다른 채널을 이용하세요» (책임 떠넘기기)
- ✅ **①사실 한 줄 ②그래서 어떻게 쓰면 되는지** 두 줄이 붙어야 한다.
  예: «당근은 광고가 안 붙는 채널이에요. **0원이 정상이에요.**» + «여기선 **손님을 데려오는 용**으로 써요 — 번 돈은 블로그·유튜브 쪽에 쌓여요.»
  (문구는 네가 다듬어라. 조건은 **«0원이 정상»이 또렷할 것** · **다음에 뭘 하면 되는지가 붙을 것**.)
- 회색 작은 글씨로 흘리지 마라(§9-①). 🔴 **막지 않기로 했으면 말은 또렷해야 한다.**

## 3. 지켜야 할 선
- 🔴 **막지 마라.** 당근 계정을 **고를 수 없게** 하거나 예약을 **거부**하면 §9 위반이다. 말해 주는 것까지다.
- 시안 정본 = `docs/screens-v3.html` · 토큰 = DESIGN §13.4. 새 색·새 컴포넌트 만들지 마라.
- 🔴 **`public/app/_tpl.txt` 가 정본이다**(AC-105). 생성물(`public/app/*.html`)만 고치면 `build-pages.mjs` 가 지운다.
  고친 뒤 **build-pages 를 돌려서 생성물까지 맞춰라.** 정본만 고치고 끝내도 낡은 초록이 된다.
- 이모지 아이콘 금지 · 모달 금지 · 시스템 용어 금지.

## 4. 끝났다는 증거 (이것만 보고한다)
```
node scripts/verify-r8-deadends.mjs      # FAIL 0 이어야 한다
node scripts/verify-pages.mjs            # 정본↔생성물 어긋남 0
npx tsc --noEmit
```
🔴 **`| tail` 금지 · `&&` 사슬 금지**(AC-67) — 종료코드가 씻긴다. 한 줄씩 따로 돌려라.

## 5. 보고 양식
`[브랜치 · 커밋해시 · 한 줄 요약 · 위 세 자의 종료코드]` — **commit 만. push 금지.**
막히면 30분 안에 한 줄. 문구가 애매하면 **네가 정하고 «이렇게 썼다»고 적어라**(사장님께 비교표 안 들고 간다).
