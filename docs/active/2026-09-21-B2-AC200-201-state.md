# [B2 · 2026-09-21] 중복 게시(AC-200) · 교차 발행(AC-201) — **끝났고 커밋됐다**

> 재부팅 대기 쪽지. 이 창은 사라진다. 🔴 **WIP 가 아니다 — 몫 둘 다 완료다.**
> 브랜치 `feature/r12-paragraph-guard` · 해시 **`47b3abb`** · **push 0**

## 1. 무엇이 들어갔나

| | 무엇 | 자리 |
|---|---|---|
| ⑴ | **발행 전 «채널에 이미 있나»를 묻는다** | `lib/publish/already.ts`(신규 · 순수+IO) · `lib/publish/reconcile.ts`(신규) |
| | 부르는 두 자리 | `lib/publish/index.ts` 앞머리 · 🔴 `lib/runner-jobs.ts reapStaleJobs` **되돌리기 직전** |
| ⑵ | **쓰기 전에 «지금 어느 블로그인가»를 읽어 댄다** | `runner/lib/auth-naver.mjs`(`readMyBlogId`·`judgeBlogIdentity`) · `runner/channels/naver-blog.mjs` |
| | 기대값↔실물 왕복 | `expectBlogId`(claim) ↔ `identity`(report) · `lib/runner-jobs.ts` |
| | 답할 길 | `accounts-update { confirmBlogId }` · 계정 조회 `identity{seen,confirmed,matchesHandle}` |
| DDL | `accounts.identity jsonb` | `drizzle/0091-ac201-account-identity.sql` — 🔴 **Neon 에 적용 완료** |
| 자 | 88검사 · 변이 12종 전부 죽음 | `scripts/verify-publish-already.mts`(46/0) · `scripts/verify-blog-identity.mts`(42/0) |
| 탐침 | 읽기 전용 | `runner/probe-identity.mjs` · 사진 `_shots/probe-identity/`(3장 · gitignore) |

🔴 **핵심 한 줄**: 멱등의 열쇠가 «우리 기록»이라 «기록이 없는 사고»는 원리적으로 안 보였다.
러너가 올리고 보고 전에 죽으면 15분 뒤 `reapStaleJobs` 가 잡을 되돌리고 **다른 러너가 같은 글을 또 올린다.**
그 길은 `publish()` 를 안 거쳐 멱등 검사 ①을 아예 안 탄다. ⇒ **되돌리기 직전에 채널에 묻는다.**

## 2. 🔴 다음 사람이 꼭 알아야 할 것

1. 🔴 **«못 쟀음» 하나 — ⑵ 는 아직 스모크다.** 로그인 상태의 `MyBlog.naver` 리다이렉트를 **못 쟀다**
   (이 PC 에 로그인된 네이버 프로필이 없다 — 발행한 러너는 «걷기시험 PC»다). 로그아웃 쪽(→ `nidlogin`)만 실물로 봤다.
   ⇒ **로그인 프로필 있는 기기에서 `cd runner; node probe-identity.mjs` 한 번**이면 실증이 된다(`via:"myblog"` + 실제 아이디).
2. 🔴 **AM `detectBlogIdOnPage`(BlogHome 링크 최빈값)를 폴백으로 넣었다가 재 보고 뺐다.**
   로그아웃으로 **세 번** 열었더니 최빈값이 매번 **다른 남의 블로그**였다(`aronmovie`→`nuk1905`→`vicoy`).
   위험한 곳은 발행이 아니라 **장부**다: 그 값이 `identity.observed` 로 적히면 화면이 남의 블로그를 «맞나요?»로 내밀고,
   누르면 `confirmed`→`expectBlogId` 가 되어 **가드가 자기 폴백에 무장해제된다.** 자에 못을 박아 뒀다 — 되살리면 빨강.
3. **카나리 0건** — t778 큐가 비었고(#324~327 cancelled · #321~323 done) `RUNNER_TOKEN` 도 없다. 돌릴 잡이 없었다.
4. ⚠️ **B 영역을 만졌다**(머지 때 겹친다): `lib/runner-jobs.ts` · `lib/publish/*` · `db/schema.ts`(표 정의 안 고치고 컬럼 맵만 append) ·
   `lib/accounts.ts` · `netlify/functions/accounts.ts`. 🔴 `lib/format-marks.ts` 는 **안 건드렸다** —
   `identity` 는 «글의 꾸밈»이 아니라 «계정의 신원»이라 `accounts.identity` 가 사는 곳이 맞다고 봤다(사유는 타입 주석).
5. **A 몫으로 남은 것 하나**: 계정 화면의 «이 주소가 맞아요» 단추. 서버·API·조회는 다 됐다.
6. 🗑 `scripts/_tmp/` 는 **버려도 되는 임시 조회 스크립트 셋**이다(삭제 권한이 없어 못 지웠다). 커밋에는 안 들어갔다.

## 3. 선 — **셈으로** 확인했다(말이 아니라)

내 창 = **06:40~07:22 KST**. 커밋 `47b3abb` = 07:22:11.

- **공개 발행 0** — 최근 8시간 `posts` 새 행 0건.
- **사장님 계정 466 무접촉** — 새 글 0 · 새 잡 0 · #1978·#1985·#1986 셋 다 `published` 그대로.
- **라이브 행 쓰기 0** — 시험 테넌트에도 **한 행도 안 썼다**. DB 에 보낸 쓰기 문장은 **DDL 0091 하나뿐**.
  `accounts.identity` 에 값 있는 행 **0건** · 이번 판이 만드는 감사 3종 **전 기간 0건**.
- **provider 0 · 코인 0 · push 0.**
- 🔴 **빨강 둘이 떴는데 내 것이 아니었다**(셈으로 갈랐다):
  · `ai_usage` 새 행 6건 = **t761 · 09:51~09:58 · director** → 내 커밋(07:22) **뒤**다. 09:38 `user_login` 이 앞에 있다.
  · 보존 테넌트 글 1건 변경 = **piece #2001(t198) · 12:00:49** → 같은 초에 `cron_publisher`+`publish_failed` 가 있다.
    **라이브 발행 크론**이 손댄 것이다. 내 커밋은 **push 를 안 했으므로 운영에서 돌 수가 없다.**
  ⇒ 둘 다 **라이브 크론/다른 창**. 다만 메인이 알아 둘 것: **보존 테넌트 198 에 대고 발행 크론이 계속 돌고 있다.**

## 4. 재개할 때

머지 순서는 **B → DDL → A**(CLAUDE §4.5). DDL 0091 은 **이미 적용돼 있다** —
안 걸어도 아무것도 안 죽지만(선택 컬럼으로 읽는다), 안 걸면 «고객이 확인해 준 주소»를 저장할 데가 없어
`mismatch` 가 **매번** 뜬다(한 번 묻기가 영원히 묻기가 된다).

재개 즉시 할 일은 위 §2-1 **한 줄**이다 — 로그인 프로필 있는 기기에서 탐침 1회.
