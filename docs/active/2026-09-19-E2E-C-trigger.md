# C 트리거 — 🔴 전 구간 한 줄로 통과(첫 리허설) · 2026-09-19

## 0. 왜 이 일이 지금 제일 값진가
라이브 실측: **테넌트 109곳 전부 내부 · 바깥 고객 0곳 · 발행된 글·영상 라이브 전체 0건 · 코인 산 사람 0명.**
🔴 **조각은 다 검사했는데 «한 줄로 이어서» 돌려 본 적이 없다.** R8 감사 91%의 «닫힘» 중 **22칸은 «서버에 있다»까지만 보고 센 것**이다.
⇒ 이 리허설이 **그 22칸의 진짜 값**을 처음으로 잰다. «되는 척하는 초록»이 어디 있는지 여기서 나온다.

## 1. 사장님 승인 (2026-09-19 · 메인 창에서 직접)
- 🔴 **제미나이 실호출 허용** — **글 1건 + 쇼츠 1건까지.** 그 이상 필요하면 **멈추고 물어라**(돈이다).
- 🔴 **실발행은 금지.** 바깥 채널에 올라가면 안 된다. **발행 직전까지**만.
- 누가: **C**(전 영역을 가로지르는 일이라 한 창이 끝까지 본다).

## 2. 걸어야 할 길 (한 줄로)
소재 → 디렉터(채널·감성·구성) → 편성 슬롯 → **글 생성(실 AI 1건)** → 검수/게이트 → 예약 →
잡 적재 → 러너 기기 등록 → **러너 claim(canary = 받아 놓고 안 올린다)** → 🔴 **발행 직전에서 멈춘다.**
그리고 같은 길을 **쇼츠 1건**으로 한 번 더(영상 축 · ffmpeg 포함).

## 3. 지켜야 할 선
- 🔴 **시드 테넌트를 새로 만든다.** 보존 테넌트 **3·13·109·116·198(+451)** 은 읽지도 쓰지도 마라.
- 🔴 **`ai_usage` 는 어떤 경우에도 안 지운다**(돈 쓴 기록).
- 🔴 **실발행 0건.** `canary`(`lib/runner-jobs.ts:716 dryRunClaim`)로 claim 까지만. 커넥터에 토큰 꽂지 마라.
- 코인은 시드 테넌트에 **네가 넣어서** 쓴다. 🔴 **piece 당 1회만 빠지는지 확인**(재생성·수리는 무료여야 한다).
- DDL 칸 = **`0101`~`0109`**. 🔴 이번 일엔 DDL 이 필요 없을 것이다 — 필요하면 왜 필요한지 먼저 적어라.
- 커밋만. **push 금지**(메인 단독).

## 4. 🔴 무엇을 보고해야 하나 — «됐다»가 아니라 «어디서 어떻게»
① **어디까지 갔나** — 각 단계별 ✅/❌ 와 **끊긴 지점의 정확한 이유**(step 라벨·에러 원문)
② 🔴 **«초록인데 고장»** — 기존 자가 초록인데 실제로는 안 되는 칸. **이게 이 일의 본체다.**
③ **돈** — 코인이 몇 개 빠졌나 · 멱등이 지켜졌나 · `ai_usage` 에 원가가 남았나
④ **시간** — 각 단계 몇 초. 🔴 배경 함수 15분 한도에 닿는 데가 있나
⑤ **KST** — 예약 시각·마감 판정이 화면과 서버에서 같은가
⑥ 🔴 **«못 쟀어요»** — 재료가 없어 못 잰 축은 **통과로 적지 말고 그대로 적어라**(AC-9)
⑦ **다시 돌릴 수 있게** — 한 번 쓰고 버리지 마라. `scripts/` 에 **다시 돌아가는 자**로 남겨라(시드 만들기 포함).

## 5. 검증 규율
```
npx tsc --noEmit
node scripts/verify-safe-list.mjs --run
node scripts/verify-r8-deadends.mjs
```
🔴 `| tail` 금지 · `&&` 사슬 금지 · **`; echo "$?"` 도 금지**(AC-67 · AC-110 — 셸 코드를 먹는다). 한 줄씩 따로.
🔴 `verify-safe-list` 는 **종료코드 2 = «못 쟀음» = 통과 아님**.
🔴 `.mts` 는 `npx --yes tsx`.

## 6. 보고 양식
`[브랜치 · 커밋해시 · 어디까지 갔나 한 줄 · 위 자들의 종료코드 · «초록인데 고장» 몇 건]`
막히면 30분 안에 한 줄. **실호출을 더 해야 하면 멈추고 물어라.**
