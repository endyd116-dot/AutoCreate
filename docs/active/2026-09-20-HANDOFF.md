# 2026-09-20 인수인계 — «초록을 믿지 않는 법»을 쌓은 날

> 압축 뒤 새 세션이 **제일 먼저** 읽는다. 재개 명령은 `docs/active/RESUME-TRIGGER.md`.
> 🔴 CLAUDE §5 대로 **PARALLEL_GUIDE · CLAUDE §4·§5·§6 · PITFALLS 상단**을 같이 읽고 시작한다.

---

## 0. 한 줄

**라이브 발행 0건이던 제품이 3건이 됐고, 그 과정에서 «검사가 초록인데 고장인 것» 약 30개를 찾아 고쳤다.**
🔴 **하나도 검사로 안 잡혔다. 전부 눌러 보고 나왔다.**

---

## 1. 지금 상태 (2026-09-20 07:2x KST)

```
main = 15d3d2c · 미배포 0 · 배포 완료
검사: verify-safe-list --run = 실제로 잰 104개 전부 통과
      verify-r8-deadends = PASS 135 · FAIL 0
      verify-repair-round = 15/19칸 초록 · 빨강 0
      변이 잔재 0건
⏳ 기다리는 빨강 2 (고장 아님 · docs/rules/pending-red.json)
⊘ 못 쟀음 3 (재료 없음 — key-contract 화면 펼치기 5 · runner-live-inside · hand-on-button)
```

### 🔴 실제로 올라간 글 (사장님 블로그 · 지우지 않았다)
| | 주소 | 무엇 |
|---|---|---|
| #1978 | `blog.naver.com/endy1116/224417377648` | **첫 실발행** · 민글 |
| #1985 | `.../224417433862` | 서식 시험 1 — 🔴 **러너가 옛 코드라 꾸밈 전부 0** |
| #1986 | `.../224417455406` | 서식 시험 2 — 🔴 **꾸밈·사진3·인용·표·목록 다 실림 · 번짐 0%** · ❌ 링크 줄 사라짐 |

---

## 2. 🔴 오늘 서사 — 사장님이 세 번 방향을 잡으셨다

### ① «눌러 봐»
검사 90개가 전부 초록인 상태에서 **첫 손님이 가입 버튼도 못 눌렀다**(체크박스가 CSS 중복 정의로 안 보임).
그리고 **운영자가 손님을 «0곳»으로 못 찾았다**(`smoke`·`demo`·`test` 로 시작하는 **진짜 상호**를 내부로 분류).

### ② «서식은 확인 못 했네»
B2 가 올리기 **전에** 오프라인으로 돌려 보고 🔴 **«우리 파서가 꾸밈을 먼저 다 지우고 있었다»**를 찾았다.
그 뒤 더 파서 🔴 **«손님이 검수창에서 고친 글이 네이버·티스토리에 반영되지 않고 AI 원문이 나간다»**까지 나왔다.

### ③ «AM 러너를 분석해»
AM `scripts/naver-blog-runner.mjs`(2,715줄 · 함수 **49개**) 를 **39/49(80%)** 읽고 **버그 6개**를 찾았다.
🔴 **전부 «조용히 사라지는» 종류**였다: 사진 주소 `&amp;` · 문장 안 링크 · 소제목 굵게 해제 · 링크 카드 대기 · 빈 종이 · 제목 셈.

---

## 3. 🔴 오늘 고친 것 — 갈래별

### «윗물이 만든 것을 아랫물이 버린다» (4 · 전부 닫음)
서버가 만든 정확한 문장을 발행 쪽이 버림 · 고친 글을 AI 원문이 이김 · 서식이 조용히 지워짐 · 편성표가 고친 제목을 모름

### «읽는 쪽은 보는데 쓰는 길이 없다» (4 · 2 닫음)
`scriptIssues`(닫음) · `slots.title`(닫음) · 🔴`regenNote`(**세 창 몫 · 남음**) · 🔴`opened_at`(**A+B · 남음**)

### «같은 규칙인데 문이 한쪽에만» (4 · 전부 닫음)
발행엔 override 있는데 글쓰기엔 없음 · 워밍업이 쓰기/발행에서 갈림 · `awaiting_manual` 에서 문 셋이 막힘 · 캡은 넘기는데 간격은 안 넘김

### 돈 · 안전
🔴 **영상 킬 스위치에 손잡이가 없었다** — 영상이 돈을 태워도 DB 손질 말고는 못 껐다. 달았다(감사 high · 끄기 전 «무슨 일이 나는지» 세 줄).

---

## 4. 🔴 다음 판 목록 — **전제까지 적었다. 전제를 빼면 새 고장이 난다**

### 최우선
| 무엇 | 🔴 전제 |
|---|---|
| **`bleed.total` ↔ 계획 문단 수 대조** | 🔴 **자를 만들 필요도 없다 — 빼기 한 번.** 러너가 이미 세고 있다. #1986 때 맞춰 봤으면 그날 알았다 |
| **링크 줄 사라짐 수리** | ①앵커까지 **기다린다**(`waitLinkCards` 가 «카드»만 기다려 이 경우를 안 덮는다) ②관찰→고침 ③⚠️ **주소를 마지막으로 미는 건 «피함»이지 «고침»이 아니다** |

### 제품 위험
| 무엇 | 🔴 전제 |
|---|---|
| **중복 게시** — 쓰기 전에 «이미 올라갔나»를 채널에 안 묻는다 | «멱등의 열쇠가 «우리 기록»이면 «우리 기록이 없는 사고»는 구조적으로 못 막는다» · 재료는 AM 에 다 있다 · 🔴 **우리 `verifyPublishedUrl` 은 AM 보다 깨끗하다 — 그건 지킨다** |
| **stealth 없음** | 🔴🔴 **터지면 우리 코드가 «해외 IP 차단»으로 잘못 적는다** — 사유가 틀리면 다음 사람이 엉뚱한 데를 판다 · «탐지 회피»가 아니라 **정상 발행을 사람처럼 보이게** |
| **교차 발행** — 남의 블로그에 갈 수 있다(§4.6) | ①에디터 **화면에서** 블로그 주소 읽는 법을 새로 찾아야 한다 ②🔴 **handle ≠ 블로그 주소인 정상 계정을 막으면 안 된다** |
| 🔴 **«취소가 취소가 아니다»** | `cadence-check.ts:96` 이 `draft` 도 센다 ⇒ **고객이 글을 취소해도 그날 몫을 계속 먹는다** |
| 🔴 **설정 시트가 고객 값을 워밍업 값으로 덮어쓴다** | 메인이 실제로 밟아 `min_gap` 180→360 을 부쉈다(되돌림). 열었다 저장만 해도 난다 |
| **워밍업이 «우리가 안 지 며칠»로 잰다** | `opened_at` 은 읽는 곳 6 · **쓰는 곳 0** · 라이브 92계정 전부 NULL. **설계는 옳고 입력 화면을 안 만들었다** |

### 폰트 · 구조 (사장님 물음)
🔴 **설계에 이미 답이 있다**: `DESIGN §Q3` «**관리형 러너가 기본**» · `§731` «렌더 = **서버리스 불가** · 관리형 러너 팜» · `§Q5` «**계정 하나 = IP 하나**».
| | |
|---|---|
| **관리형 러너**(기본) | 폰트를 **한 번 깔면 끝** |
| **고객 PC 러너**(선택지) | 🔴 **동봉이 답** — `src: local()` 이라 **동봉이 아니었다**(주석은 «동봉»이라 적혀 있다) |
🔴 **지금까지 구운 모든 자막이 «맑은 고딕»이었다**(이 PC 에 Pretendard 0개). **안 깨지니까 아무도 안 봤다.**
🔴 **동봉하든 안 하든 «어느 폰트로 그렸나»를 재는 축이 먼저다**(`document.fonts.check()` — 브라우저가 답을 갖고 있다).
· 🔴 **영상 굽기는 IP 무관**(계산일 뿐) ↔ **발행은 계정마다 IP 필요**. 갈라서 판단한다.

### 자 · 파이프
🔴 **러너가 말한 것이 사람에게 닿는 길 셋이 다 조건부다**: `notes` 는 **서버가 버린다**(`runner-jobs.ts:854`) · 콘솔은 **죽여야** 보인다(파일로 안 남긴다) · 카나리 `canary_runs.detail` 은 **임시저장까지만**.
⇒ ①러너 콘솔을 **파일로도** ②사실은 `formatMarks`(meta)로 **화면까지** ③**러너 사진(`RUNNER_SHOTS`)을 기본으로 켤 것인가**
· `verify-hand-on-button` 이 **늘 exit 2** 다(모의가 안 닿는 화면 7개) — **주인 A** · 늘 ⊘ 인 자는 아무도 안 본다(AC-95)
· 🔴 **`verify-api-surface` 의 △ 가 44종을 한 줄에 붙여 찍어서 13개가 묻혔다** — 그래서 킬 스위치를 놓쳤다. «△ 를 언제 판정으로 올리나»
· 운영 라우트 13개가 화면 0 — **내리기 콘솔 · 프록시 콘솔**이 통째로 없다. «아직 / 일부러 / **유물** / 거짓양성» 네 칸으로 가른다

---

## 5. 🔴 라이브 시험 재료 — **지우지 마라**

```
테넌트 778  wain.walk.mu8ig21x@gmail.com / WalkTest2026!   (걷기시험 가게)
계정   466  @endy1116 · status active · 🔴 daily_cap 4 · min_gap_min 60
            ← 둘 다 메인이 사장님 지시로 바꾼 값(기본 2 / 180)
기기   #161 «걷기시험 PC» · 쿠키 저장됨(사장님이 직접 로그인)
글     #1977 rejected(«버릴 수 없던» 고장의 증거) · #1978·#1985·#1986 published
       #1987~#1990 draft + 예약 걷음(A·B 대조군)
사진   runner/_shots/job-325-* · job-327-*  ← 🔴 링크 원인의 증거. 지우지 마라
그 밖  테넌트 761·764 · 계정 467·468 · 운영자 20 · 공지 3 · 프로모션 10·11 · 쿠폰 7·8 (전부 비활성)
```
🔴 **보존 테넌트 3·13·109·116·198(+451) 무접촉** · 🔴 **`ai_usage` 는 어떤 정리에서도 안 지운다**

---

## 6. 창 배치 (이름은 재시작마다 바뀐다 · **폴더가 정체**)

| 폴더 | 몫 | 오늘 이름 |
|---|---|---|
| `../AutoCreate-A` | 화면 | `autocreate-a-ad` |
| `../AutoCreate-B` | 서버·운영 | `autocreate-b-30` |
| `../AutoCreate-B2` | 러너·영상 | `autocreate-b2-42` |
| `../AutoCreate-C` | 검증 | `autocreate-c-f0` |
| 메인 나무 | 메인(단독 push) | `autocreate-a1` |

🔴 **PARALLEL_GUIDE §2.8**: 트리거는 **사장님께 복붙 블록으로** 드린다(사장님이 2026-09-19 에 지시).
⚠️ 다만 **2026-09-20 에 사장님이 «메인이 직접 넘겨줘»라고 하셔서** 그날은 메인이 직접 쐈다. **다음 판에 다시 물어라.**

### 🔴 AC 번호 칸 (오늘 두 번 겹쳤다 — 다음 판부터)
`A 130~139 · B 140~149 · B2 150~159 · C 160~169 · 메인 170~179`
지금 쓴 마지막 번호 = **AC-123**. 그때까지는 **메인이 main 을 읽고 준다**.

---

## 7. 🔴 오늘 배운 규율 — PITFALLS AC-112~123

| | |
|---|---|
| **112** | 변이가 안 울면 **변이를 먼저 의심해라** (일곱 중 넷이 그랬다) |
| **113** | «초록»도 두 뜻 — 고쳐졌거나 **자가 그 자리를 안 보거나** |
| **114** | 수를 말할 거면 **무엇을 세었나**도 (14 vs 10 · 43 vs 49) |
| **115** | 생성물 · **한 줄 함수의 `//` 주석** — tsc 가 구조적으로 못 본다 |
| **116** | **한 점 재고 «절대»라고 쓰지 마라** |
| **117** | 자가 안 보는 자리 위에 **배치가 그 자를 안 보는 자리** |
| **118** | (B2) 검수창 수정이 버려지던 자리 |
| **119** | 축이 «어느 함수의» 문인지 말 안 하면 **옆에 같은 글자가 생기는 순간 무력** |
| **120** | 없음을 증명하는 자는 **«내가 쓰기를 볼 줄 아나»부터** |
| **121** | 🔴 **«울었다»도 의심해라** — 부정형 단언은 대상을 못 찾아도 통과 |
| **122** | «그려졌나»·«손이 붙었나»·«갈 길이 있나»는 **셋** |
| **123** | «아무 일도 안 났다»는 **양쪽 뜻** |

### 🔴 글로 안 남은 것 중 제일 값진 셋
1. **«실패를 안 찍은 것 ≠ 실패가 없던 것»** — 로그 부재는 반증이 아니다. B2 가 **셈으로**(12자 < 67자) 자기 가설을 죽였다.
2. **사전 등록의 진짜 값** — «결과를 정직하게 읽는 것»이 아니라 🔴 **«실험이 성립하는지 먼저 되짚는 것»**에서 났다(대조군 오염을 돌리기 전에 잡았다).
3. **«근거 없이 손대지 않는다»** — 제목을 «재기만» 하고 안 지웠기에 **멀쩡한 제목을 안 지웠다**. 하루 만에 값을 했다.

### 🔴 메인이 오늘 틀린 것 (다음 사람이 같은 걸 밟지 않게)
· **«띄웠다 ≠ 떠 있다»를 세 번** — 서버 두 번, 러너 한 번. **머지했다고 돌고 있는 프로세스가 새 코드가 아니다**
· **UTC 를 KST 로 읽었다**(`TZ=` 가 이 셸에서 안 먹는다) · **캐던스로 세 번 틀렸다**(일일 ↔ 주간 ↔ 간격을 섞었다)
· **DOM 에 있는 것을 «보인다»로 읽었다**(`#cta` 가 `display:none` 인데 «버튼이 죽었다»고 사장님께 보고)
· **함정 번호를 확인 안 하고 줬다**(두 번 충돌) · **`AC_SERVER` 를 빠뜨려 라이브에 붙었다**
🔴 **네 창이 다 되받아 줬다. 되받는 창이 없으면 이 중 하나도 안 걸렸다.**
