# 칸(번호) 배정 — **정본**

> 🔴 **여기가 정본이다.** 쪽지로만 오간 배정은 창이 압축되면 사라진다(B 가 짚었다 · 2026-09-23).
> 바꾸는 사람은 **메인 한 명**이다. 판마다 다시 나누고, 나눈 즉시 이 파일을 고친다.
> 🔴 **남의 칸은 비어 보여도 쓰지 마라** — 2026-09-23 에 겹친 번호 셋(`AC-94`·`AC-95`·`AC-219`)이 **전부** 그렇게 났다.

**마지막 배정: 2026-09-26 (메인)** — 메인 칸이 차서 `280~289` 를 새로 열었다

## AC 번호 (PITFALLS 항목)

| 창 | 칸 | 쓴 것 |
|---|---|---|
| 메인 | `280~289` | — (🔴 `170~179` 는 **다 찼다** · 2026-09-26 새 칸) |
| A | `270~279` | — |
| B | `250~259` | — |
| B2 | `260~269` | 260 · 261 · 262 (`263~269` 남음) |
| C | `240~249` | 240 (옮겨 온 것) |

· 앞 판의 칸(메인 `170~179` · A `180~189` · B `190~199`/`220~229` · B2 `200~209` · C `210~219`/`230~239`)은 **다 찼다.**
· `AC-176`·`AC-177` 은 **새 교훈이 아니라 겹친 번호를 옮긴 자리**다(옛 `AC-95` 둘째 · 옛 `AC-94` 첫째).

## DDL 번호 (`drizzle/`)

| 창 | 칸 |
|---|---|
| B | `0084~0089` (`0087` 썼고 `0088~0089` 남음) |
| B2 | `0091~0099` |
| C·메인 | `0101~0109` |

> ⚠️ 🔴 **`CLAUDE.md` §4.5 의 DDL 표(B = `0031~0039` …)는 그 시점의 기록이라 지금 칸이 아니다.**
> 표를 고칠지는 **사장님 판단**이라 손대지 않았다. 그때까지는 **이 파일이 정본**이고,
> 새 트리거가 칸을 말하면 **트리거가 우선**이다.

## 칸이 찰 때

메인에게 한 줄 보낸다. 메인이 새 칸을 나누고 **이 파일을 먼저 고친 뒤** 알린다.
