# R9+R10-B2 트리거 — 러너가 서식을 실제로 낸다 + 레퍼런스 캡처 (B2)

> 발부 2026-09-16 · 베이스 = `main` **`fa1e446`** · 폴더 **`C:\Users\Administrator\Desktop\작업\dev\AutoCreate-B2`** · 브랜치 **`feature/r9-runner`**(이미 만들어 뒀다)
> 계약서: `docs/active/2026-09-16-R9R10-contract.md` — **먼저 읽어라.** 🔴 전본 개발(축소·왜곡·요약 금지).

## 읽을 것
`docs/active/2026-09-16-R9R10-contract.md` · `docs/active/2026-09-15-R9-reference-and-split-design.md` **§2.1b·§2.1c·§2.1d·§2.1e·§3.1·§3.2·§3.3·§4.1·§4.2** · `docs/active/2026-09-16-B2-video-capability.md`(네가 쓴 것) · `CLAUDE.md` **§3·§4.5·§4.7·§8·§9** · `docs/rules/PITFALLS.md` **AC-9·51·58·63·66·67·68·69·73·77·79·86·87·90·92·93**

[자율주행 — 완전 자율] 파일 R/W·git(커밋까지)·npm/npx·bash 자율. 🔴 **push 금지** · 🔴 **실호출·R2 업로드 금지**(대외는 사장님 Allow · AC-50).
[진행률] `📊 진행률 X%/100 — 방금: … / 다음: …` 최소 8회. 보고는 **10줄 이내**.

## 🔴 순서 — 이것만 직렬이다
**R9(서식)을 먼저 끝내고 R10(캡처)으로 간다.** A·B 가 «배운 서식을 글에 반영»하는 자리가 **네 R9 위에 선다.** 네가 늦으면 그 칸만 멈춘다(나머지는 안 멈춘다).

---

# R9 — 러너가 서식을 실제로 낸다 (0→55%)

## B2-1 (0→10%) 🔴 먼저 AM 을 **읽고 정리**해라 (코드 복사 아님)
AM 리포 **읽기만** 허가한다(쓰기·커밋·복사 금지): `C:\Users\Administrator\Desktop\작업\dev\AutoMarketing`
- `public/runner/naver-blog-runner.mjs`(202KB) — `planEditorOps` · 실행부 · `breakFormatBeforePara` · `markFormatDirty` · `measureFormatBleed`
- `scripts/verify-runner-format.mjs`(변이 6종 `--mutate=m1..m6`)
- `scripts/runner-format-repro.mjs`

[내는 것] **우리 구조에 맞춰 무엇을 어떻게 옮길지** 한 장(짧게). 🔴 **AM 은 AM 구조(잡 큐·상태기계)에 묶여 있다 — 통째로 복사하지 마라.**

## B2-2 (10→35%) 🔴 **«번짐» 세 겹 — 이게 이 라운드의 본체다**
AM 하니스 머리말: 「이 병은 **네 번째 판**이다 — #799 색 · #800·#801 굵게 · 08-20 밑줄 · **09-15 빨강/가운데/기울임**」
**병 이름 = «번짐».** 한 문단에 색을 칠하면 **다음 문단까지 따라간다**(굵게·밑줄·가운데도 같다).

[만들 것 — 셋 다]
1. **문단 경계마다 서식을 끊는다**(AM `breakFormatBeforePara`) — 🔴 AM 은 이걸 **URL 전용 방어였던 것을 «한 곳의 규칙»으로 올렸다.** 우리도 한 곳이어야 한다.
2. **칠한 직후 «더럽다»고 적는다**(`markFormatDirty`) — 🔴 **칠한 직후**가 번짐의 출발점이다. 나중에 적으면 늦다.
3. 🔴 **발행 직전에 자기가 쓴 글을 다시 재고**(`measureFormatBleed`), 번진 문단이 **상한(%)을 넘으면 발행을 멈춘다.**
   ⚠️ **이건 CLAUDE §9 의 게이트가 아니다** — «고객 글에 대한 우리 판단»이 아니라 **«우리 러너가 방금 망쳤다»는 작업 품질 검사**다. 계약서 §4-1 에 적어 뒀다.
   🔴 상한은 **상수 한 곳**(env 로만 흔든다 · 코드 여러 곳에 숫자를 흩지 마라).

[금지된 고침 — AM 이 적어 뒀다] 🔴 «**캐럿을 눈감고 토글하지 마라**»(상태를 안 보고 버튼만 다시 누르는 수리).

[검증] 🔴 **변이 하니스**를 우리 것으로 만든다(AM 6종에 대응). «이 방어를 빼면 빨개지나»를 **하나씩** 확인해라 — **빨강이 안 나면 그 검사는 아무것도 못 잡는다**(AC-87 · 네가 B5 에서 4종 한 그대로).

## B2-3 (35→50%) 우리 블록 **18종** → AM op **7종** 내리기
AM op: `title · heading · para · quote · image · divider · tagline`
우리 `BlockType` **18종**: `hook · para · h2 · h3 · quote · list · checklist · table · image · divider · tip · faq · hashtags · disclosure · adsense · toc · summary · affiliate` + **`place`**(B-1 이 R8 에서 더했다)
- 🔴 **내릴 수 없는 것은 «못 냈어요»로 적는다**(AC-9 · AM 은 그 자리를 `marksDemoted` 라 부른다)
- ⚠️ 이게 **R8 잔여 «에디터 실제 요소»(B7)** 다 — 그 칸도 같이 닫힌다

## B2-4 (50→55%) 서식 마크를 받는다
B 가 서버에서 «여기는 밑줄·형광펜·이모지»를 적어 보낸다(R9-1). **계약(키 이름)은 B 와 직접 맞춰라** — 메인 거치지 마라(PARALLEL_GUIDE §2.7).
🔴 AM 의 **뜻 마크**(`value`·`line`·`row`)를 참고해라 — **뜻을 적고 보이는 모양은 러너가 정한다**. 그래야 채널이 늘어도 서버가 안 바뀐다.

---

# R10 — 레퍼런스 캡처 + 영상 칸 (55→95%)

## B2-5 (55→75%) 🔴 **글 레퍼런스 — 러너가 찍는다**
[흐름] 고객이 주소 붙여넣기 → **러너가 연다**(계정 프로필·프록시 그대로) → 캡처 → B 에게 넘긴다

[사장님이 못 박은 것 — 그대로]
- 🔴 **폰 폭(430px)으로** 찍는다 — 「데스크톱 폭이면 한 문단이 2줄로 보이는데 **실제 독자(모바일)에겐 5줄**이다. 독자가 보는 폭이 아니면 «문단 리듬»이 **틀린 값**이 된다」
- 🔴 **한 장은 안 된다** — 전체 캡처는 폭 1,000에 높이 12,000 꼴이고 모델이 **줄여서** 보므로 **밑줄과 굵게를 구분 못 한다**
- 🔴 **2분할·4분할을 못 박지 않는다** — 찍기 전에 **페이지 높이를 알 수 있다.** 한 조각이 «폰 화면 1.5개» 정도가 되게(보통 3~4장 · 긴 글 6장)
- **6장을 넘으면 더 안 늘린다** — 처음·중간·끝 위주로
- 🔴 **겹쳐 자른다(10~15%)** — 딱 자르면 경계에 걸린 문단·사진이 **양쪽에서 반씩 잘려 둘 다 못 읽는다**

[안전선 — 막는 게 아니라 «긁기로 번지지 않게»]
1. **고객이 손으로 넣은 주소만**(자동 수집 0)
2. **한 번에 하나** · 요금제 한도 안에서
3. 🔴 **캡처는 즉시 버린다** — 남의 글은 **한 장도 안 남는다**
4. **공개된 글만** · 로그인해야 보이는 건 **고객 자기 계정**으로만

[못 열면] 🔴 **«화면을 찍어서 올려 주세요»** 로 떨어진다(A 가 화면을 만든다). **막지 마라** — 그건 «없는 길»이지 우리 판단이 아니다.
⚠️ **네이버·쓰레드·인스타가 이 길로 풀린다**(R8-A 에서 «우리 도구가 못 읽는다»로 막혔던 셋).

## B2-6 (75→90%) 🔴 **영상 레퍼런스 칸 늘리기 — 네가 잰 표대로**
네 실측(`2026-09-16-B2-video-capability.md`)이 정본이다.
- ✅ **낸다**: 타이포그래피(자막이 CSS) · 자막 위치·줄 수·강조색 · 속도(컷당 초·전체 길이) · 디자인(색·여백·안전영역)
- 🔴 ❌ **못 낸다 → 배울 칸에서 뺀다**: **자막 모션**(자막이 PNG 한 장) · **컷 전환**(씬을 `concat` 으로만 잇는다)
- 🟠 `audio_tempo` — 레퍼런스가 **저장을 안 한다.** 🔴 «읽게만»으로는 안 되고 **새 규칙**이다(나레이션 길이가 바뀌면 자막 시각·컷 창·전체 길이가 전부 움직인다). **이번엔 저장까지만** 하고 «반영»은 다음 라운드로 적어라.
🔴 **표를 고쳤으면 그 문서의 머리말(«렌더가 바뀌면 이 표도 바뀐다» + 잰 날 + 다시 재는 법)도 갱신해라.**

## B2-7 (90→95%) R8 잔여 — 네 몫 둘
- **인스타 쇼핑 태그**(`product_tag`) — 고지 축 4행 중 마지막
- **X 영상** — 지금 `x.ts:157` 이 «X 에는 아직 영상을 올려 드릴 수 없어요»로 **정직하게 막는다.** 🔴 **X 가 실제로 영상을 받는지 먼저 재라** — 못 받으면 «없는 길»이니 그대로 두고 «못 만든다»고 적어라.

---

## 검증 (95→100%)
```
npx tsc --noEmit > /dev/null 2>&1; echo $?
node scripts/verify-scripts-parse.mjs > /dev/null 2>&1; echo $?
node scripts/verify-r8-deadends.mjs  > /dev/null 2>&1; echo $?
node scripts/verify-asset-versions.mjs > /dev/null 2>&1; echo $?
node scripts/verify-safe-list.mjs --run          # 🔴 safe 만 돈다(라이브에 붙는 검사는 빼고)
npx --yes tsx scripts/verify-thread-chain.mts · verify-recipe.mts · verify-reference-apply.mts
+ 네가 새로 만들 것: verify-runner-format(변이) · verify-reference-capture
```
🔴 **`| tail` 로 파이프하지 마라**(AC-67). 🔴 **`.mts` 는 `npx --yes tsx` 로 돌린다**(`node --experimental-strip-types` 는 확장자 없는 import 를 못 푼다 — 네가 잡은 것).
🔴 **`runner/node_modules` 가 있는 폴더에서 러너 검사를 돌려라**(메인 폴더엔 없다).

## 보고
`[폴더 · 브랜치 · 커밋해시 · 변경요약 · 검증 증거(종료코드) · 밟은 함정]` **10줄 이내**.
🔴 **B 와 직접 맞춰라**(서식 마크 키 이름 · 캡처 넘기는 모양). 메인은 범위·머지만 한다.

## 네 대기 몫 — 라운드 끝
러너 새 판 R2 업로드(🔴 **그 창에서 사장님 Allow 로만**) · `RECIPE_*` env · `scripts/**` 타입오류 10건 + `.gitattributes` 전면 재정규화 + tsconfig 에 `scripts/**`.
