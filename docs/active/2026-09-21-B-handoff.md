# B 상태 쪽지 — 재부팅 전 (2026-09-21)

> 이 창은 사라진다. 다음 B 가 **이것부터** 읽고 이어받는다.

## 어디까지 했나

브랜치 `feature/b-fail-cost-counts` · 해시 **`2bde29a`** · **미커밋 0** · **push 안 했다**(메인이 머지한다).
로컬 `main` 에 이미 들어간 앞선 판: `2aaea31`(원장 새는 자리 여섯) · `c2fa1b9` · `dbf0623` · `b62a5e4`.

### 🔴 사장님 결재 둘 — **둘 다 끝냈다**

| 결재 | 상태 |
|---|---|
| `video_90 = 42` 확정 | ✅ **끝.** 값은 원래 42라 안 바뀌었고 **주석만** «임시값·결재 대기» → «2026-09-21 결재»로 고쳤다(`lib/coin-table.ts:39`) |
| «실패했을 때도 글처럼 맞춰줘» | ✅ **끝.** 단 **«아는 것만»** — 아래 참조 |

**실패분 상한 먹이기 — 어떻게 갈랐나**
- 아는 금액 → `cost_usd` 에 넣는다(상한이 먹는다): `download_failed` · `store_failed`
  (= 제공사가 **다 만들어 준 뒤** 우리가 못 받아 오거나 못 넣은 자리 — 확실히 청구된다)
- 🔴 모르는 것 → **안 넣는다**: `provider_failed` · `http` · `timeout` · `empty`
- ⇒ **글과 완전히 같아지지 않는다.** 글은 토큰 수로 계산이 되고 이건 안 된다. 그 차이를 주석·DDL·자에 글자로 적어 뒀다.
- `cost_usd_maybe` 칸은 **안 지웠다**(메인 지시). 뜻을 «아직 옮기지 못한 돈»으로 바꿨고 **지금 비어 있는 게 정상**이다.

## 다음 한 걸음

1. 🔴 **메인이 `2bde29a` 를 머지하는지 확인**하고, 안 됐으면 알려라.
2. 🔴 **실호출 승인이 나면 «기록이 실제로 들어가는지» 1건으로 실증**한다. 지금은 **코드로만 확인했다**(실패 행 라이브 0건).
   제일 싼 길: **타입캐스트 키를 잘못 준 채로 합성 한 번** → `tts:fail` + `fail_kind='http'` 행이 앉고 금액이 NULL 로 들어가는지 본다.
   **provider 과금 0** 이고 배선은 전부 같은 `recordAiUsage` 를 탄다.
3. `verify-label-surface` 가 **빨갛다 — 이건 A 몫이다**: «90초는 고를 수 있는데 `UI.VIDEO_COIN` 에 칸이 없다».
   화면이 `accounts-list.coins.table` 을 읽게 되면 저절로 풀린다(값을 손으로 적을 필요 **없다** — 서버 표에 `video_90:42` 가 이미 있다).

## 🔴 아직 안 올린 발견 하나 — 다음 창이 메인에게 전해라

`audit_logs` 에 **`ai_cost_unrecorded`(2026-09-15)** 한 건이 있고, 거기에 **실호출 1건의 실측**이 들어 있다:

```
piece 440 · tenant 352 · 총 $3.6251
video_clip/gemini-omni-1.1-flash  n=9  $3.60   ← 컷당 $0.40
tts   n=9 $0.0101 · video_script n=1 $0.0105 · director n=1 $0.0045
```

🔴 **우리 추정은 같은 조건(60초 그래픽)에서 $6.421 이다 — 실측의 1.77배다.**
까닭: 우리 식은 omni 를 **초당 $0.10** 으로 보는데(7초 컷 → $0.70), 실측은 **컷당 $0.40** 이다.
AM 실측(«omni 클립당 $0.49»)과도 실측 쪽이 맞는다.

⇒ 🔴 **메인이 «우리 추정이 30~40% 낮을 수 있다»고 걱정한 것과 방향이 반대다.** 적어도 이 1건에서는 **추정이 높다.**
⇒ 사장님 가격 결재가 «90초 원가 ₩12,923 · ×1.63» 위에 있는데, 그 원가가 **과대**일 수 있다.
⚠️ **표본 1건이다. 단정하지 마라.** 그리고 그 감사는 «스모크 정리가 `ai_usage` 를 지워서» 남긴 복구 기록이다(AC-54).

## 막힌 것 · 못 쟀음

- **실패 기록이 실제로 앉는지 못 쟀다**(라이브 실패 행 0건 · provider 실호출 0 선).
- **과거에 이 변경이 있었다면 합이 얼마였나는 재구성 불가** — 과거 실패는 기록이 아예 없다.
- **`%:fail` 행이 라이브에 0건**이다. 글 경로가 실패를 기록하는 것은 **코드상 사실**이지 실측된 적이 없다.
- 실패 호출을 제공사가 청구하는지 **여전히 못 쟀다**(청구서를 본 적이 없다). 이제 **세기 시작했으니** 실호출이 돌면 쌓인다.

## 손대던 파일

`lib/coin-table.ts` · `lib/video/providers/index.ts` · `lib/video/tts-typecast.ts` · `lib/billing/ai-cost-cap.ts` ·
`db/schema.ts` · `scripts/verify-ai-usage-leaks.mjs` · `drizzle/0085-ai-usage-fail-comment.sql`

## 🔴 라이브에 남긴 것

| 무엇 | 어디 | 되돌릴 일 |
|---|---|---|
| DDL `0084` — `ai_usage.fail_kind` · `cost_usd_maybe` + 부분 인덱스 | 라이브 | **없다**(추가형 · 적용·확인 완료) |
| DDL `0085` — 두 칸의 `COMMENT` 갱신 | 라이브 | **없다** |
| 시험 테넌트 **761** `settings.kinds = ["text","video"]` · `kindsSet=true` | 라이브 | A 가 영상 화면을 만드는 동안 **켜 둔 채로 두라** |
| 시험 테넌트 761 지시서 **184**(proposed) | 라이브 | A 가 쓰라고 남겼다 |

**그 밖 라이브 쓰기 0 · 실결제 0 · 실발행 0 · provider 실호출 0 · 보존 테넌트 무접촉.**
내 DDL 칸은 `0084~0089` 이고 **`0086` 부터 남았다.**
