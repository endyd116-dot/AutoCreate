# P1R2-A 트리거 — 러너 연결 · 발행함 · 슬롯 시트 4동작 · 재로그인 흐름 (A)

> 발부 2026-09-14 · 베이스 = main `bbc4d6e`(P1R1 머지본 + R2 계약서) · 워크트리 `../AutoCreate-A` · 브랜치 `feature/p1r2-front`
> 🔴 이 라운드의 한 줄: **«자동으로 돌아가는 것을 사람이 믿을 수 있게 보여 준다.»** 크론이 만든 글, 러너가 올린 결과, 정지된 계정의 승계 — 전부 화면에서 한눈에.
> 🔴 B·B2 와 동시에 돈다. API 가 아직 없으면 `?mock=1` 로 끝까지 만든다(R1 의 `public/js/mock.js` 확장).

```
P1R2-A다. 베이스 = main bbc4d6e. 워크트리 ../AutoCreate-A · 브랜치 feature/p1r2-front(이미 체크아웃됨 · npm ci·.env 완료).
읽을 것(순서대로): docs/rules/PARALLEL_GUIDE.md(§2.6) · CLAUDE.md §3(UX)·§5·§6·§8 · docs/rules/PITFALLS.md §0 ·
        docs/active/2026-09-14-P1R2-contract.md(🔴 정본 — §2 러너 API·§5 화면·§6 추가 API) ·
        docs/active/2026-09-14-P1R1-contract.md(v1.3 · §4B bodyHtml class) ·
        docs/DESIGN.md §13.0(헌장)·§13.0b(토스 패턴 사전 — «연결 기기»·«내 소비»)·§13.3b(반응형)·§5B.8(편성표)·§8(러너)·§7.2(계정 전이) ·
        코드(R1 산출 · 네가 쓴 것): public/js/ui.js·mock.js · public/css/ac.css · public/app/_tpl.txt · scripts/build-pages.mjs · public/app/schedule.html·accounts.html·pieces.html·piece.html·home.html
[자율주행 — 완전 자율] 파일 R/W/Edit·git·bash/PowerShell·npm/npx·curl 자율. 커밋 자유·push 차단(메인이 머지). ①애매한 화면 해석 ②계약 이탈 불가피 ③비가역 작업일 때만 질문. 30분 내 보고.
[🔴 전본 규칙 — CLAUDE §8] 계약 §-1 «이번(R2)» 열 중 화면 항목 전부가 범위. DESIGN §5B.8·§8 화면 항목을 하나도 빼지 않는다. 못 만들면 사유 보고.
[진행률 — 의무] "📊 진행률 X%/100 — 방금: … / 다음: …" 최소 8회.
[보고 대상] 메인 세션(autocreate-ca) — SendMessage.
```

■ 규칙(R1 과 동일 · 어기면 반려)
  화면당 색 3 · 그룹 섹션 · 큰 숫자 1 · Primary 1 · 옵션은 시트 · 시스템 용어 0(«러너»는 예외 — 고객에게 «내 PC 프로그램»으로 부르되 기기 이름은 그대로) · 12 컴포넌트 + R1 프리미티브(.chip/.stepper/.fr) 안에서 · 새 컴포넌트 금지.
  용어: «내 PC에서 켜기» «지금 켜져 있어요/꺼져 있어요» «올라간 글» «직접 올리기» «다시 로그인» «건너뛰기» «지금 만들기».

■ A1 (0→8%) mock 확장
  R2 API 를 mock 에 추가: `runner-list/register/remove` · `posts-list` · `slots-assign-topic/reschedule/produce-now` · 슬롯 상태 `no_topic`·`coin_short`·`awaiting_runner`·`awaiting_manual` 시나리오 · 러너 online/offline 두 상태.

■ A2 (8→30%) 러너 연결 — `public/app/runner.html`(신규) + 홈·내 계정 연결
  토스 «연결 기기» 패턴: 상단 상태 문장(«내 PC 프로그램이 켜져 있어요 · 방금 전 응답» / «꺼져 있어요 · 마지막 응답 2시간 전») · 기기 목록(이름·버전·대기 중인 일 N건·해제) · 하단 «내 PC에서 켜기».
  등록 흐름(한 화면 한 질문): 이름 입력 → 등록 → **토큰 1회 표시**(복사 버튼 · «다시 볼 수 없어요» 경고) → 설치 3단계(① 프로그램 내려받기 ② 토큰 붙여넣기 ③ 켜두기) → 완료 체크.
  러너가 필요한 이유 한 문장: «네이버·티스토리는 프로그램이 대신 올려요».
  홈: 러너 오프라인인데 러너 채널 예약이 있으면 todo «내 PC 프로그램이 꺼져 있어요 → 러너». 내 계정 화면에 «내 PC 프로그램» 행 추가.

■ A3 (30→52%) 발행함 — `public/app/posts.html`(신규) + 탭 연결
  날짜별 그룹 섹션: 마크 · 제목 · @계정 · 시각 · 우측 상태(«올라감» + 외부 링크 아이콘 / «직접 올려야 해요» warn). 행 탭 → 시트: 외부 링크 열기 · 조회수(있으면 «조회 1,240 · 어제 기준») · 본문 복사(직접 올리기용) · 실패 사유 사람말.
  빈 상태: «아직 올라간 글이 없어요 · 편성표를 확인해 보세요».
  ⚠️ «발행함» 탭은 R1 에서 «편성표»로 교체됐다(DESIGN §5B.1) — 발행함은 **편성표 안의 링크**와 내 계정에서 들어간다(탭 5개 유지).

■ A4 (52→74%) 편성표 완성 — `schedule.html`
  슬롯 시트 **4동작 전부**(DESIGN §5B.8): ① 소재 바꾸기(후보 목록 시트 → `slots-assign-topic`) ② 시각 바꾸기(시간 칩 + 내일/모레 → `slots-reschedule`) ③ 지금 만들기(코인 안내 후 `slots-produce-now` → 완료 체크 → 검수로) ④ 건너뛰기.
  상태 점·알약 추가: `no_topic`(«소재 없음» 회색) · `coin_short`(«코인 부족» 주황 + «충전하기») · `awaiting_runner`(«PC 대기» 주황 + «러너») · `awaiting_manual`(«확인 필요» 빨강) · `published`(초록).
  달력 상단 요약 문장: «이번 주 12건 · 오늘 2건 나가요».

■ A5 (74→88%) 계정 재로그인·승계 — `accounts.html` + `home.html`
  `pending_login` 계정 행 → «다시 로그인» → 시트: «내 PC 프로그램이 로그인 창을 열어요» + 3단계 안내 + 진행 상태 폴링(잡 상태: 대기 중 → 창 열림 → 완료) · 러너 꺼져 있으면 «먼저 프로그램을 켜 주세요 → 러너».
  승계 알림: 홈 todo 와 알림함에 «@a 계정이 정지됐어요 · 글 3건을 @b 로 옮겼어요»(링크 → 계정).
  계정 상세 시트에 «최근 오류» 사람말 + «건강 점수» 표시(있으면).

■ A6 (88→94%) 홈 마감
  오늘 편성 행의 새 상태 반영 · 러너/승계/발행 실패 todo · «자동 편성» 토글 설명을 «규칙 N개로 돌아가요 · 다음 글은 내일 07:30» 으로(있으면).

■ 검증 (94→98%)
  `?mock=1` 전 화면 왕복(폰 400·데스크톱 1280 스샷) · 콘솔 0 · 헌장 자동 체크(Primary 1·큰 숫자 1·이모지 0·시스템 용어 0) · B/B2 가 로컬에 오르면 mock 없이 재주행(메인이 알림).

■ 보고 (98→100%)
  [브랜치·해시·변경 파일 · 스샷 경로 · 계약 이탈(원칙 0) · 새 컴포넌트(원칙 0) · 🔴 설계 대비 못 만든 것과 사유(없으면 «0»)]
