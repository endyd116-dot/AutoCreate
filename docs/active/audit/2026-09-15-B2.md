# 설계 대비 전수조사 — B2 영역(§2 채널 매트릭스 · §8 러너 · §9 수익 통합)

> 조사자 B2 · 2026-09-15 · 브랜치 `feature/runner-dist` · 방식 = `docs/active/2026-09-15-DESIGN-AUDIT-trigger.md` §1 어휘 · §2 행 형식 · §4 규율.
> 코드는 고치지 않았다(조사만). 실호출·실발행 0 — 러너는 `--peek`/드라이런, DB 는 읽기와 테스트 테넌트만.
> 마지막 열 «설계?» = ➖ 는 **코드가 아니라 설계 문장을 고쳐야 할 것 같다**는 제안(결정은 메인·사장님).

---

## 0. 요약 5줄

1. **상태별 개수(총 80 판정 단위)**: ✅ 33 · 🟡 19 · ❌ 12 · 🟠 11 · ➖ 3 · 🔒 1 · ⬜ 1. 별도로 **«설계 문장을 고치자» 제안 6건**(마지막 열 ➖).
   *(숫자는 표에서 기계로 센 값이다 — 처음엔 손으로 «총 62» 라고 적었다가 실제로 세어 보고 고쳤다.)*
2. **가장 큰 것 — «코드는 넓고, 실제로 지나간 길은 좁다».** 라이브 실측: `posts` 에 발행 성공 행이 **`naver_blog` 2건뿐**(티스토리·블로거·워드프레스·영상 전 채널 **0건**), `revenue_daily` 는 **수동 12행 + 애드포스트(러너) 2행**뿐 — **API 수익 소스 5종(애드센스·유튜브·쿠팡·알리·링크프라이스)은 한 행도 쓴 적이 없다.**
3. **🟠/❌ 상위 5**(고객이 먼저 부딪히는 순): ①영상 채널 4종이 레지스트리 `planned` 라 **계정 연결 화면에서 아예 안 보인다**(코드·커넥터는 다 있다) ②DESIGN §2 가 지목한 «코드 정본» `lib/channel-registry.ts` **파일이 없다** ③API 수익 5종 **실회수 0회**(키를 꽂아 본 적이 없다) ④`publish.brunch` 잡 종류 **없음**(설계 §8.2 에 있다) ⑤설계 §2 의 페이스북·X·브런치·미디엄 **어디에도 없다**.
4. **크기 합**: S 15 · M 22 · L 8. L 8개가 무게의 대부분이고 그중 5개가 **영상 축**(유튜브 열기 · 네이버 클립 구현 · 틱톡 · 페북 릴스 · 러너 v2 트레이), 나머지가 브런치·관리형 팜·(글 채널 잔여).
5. **한 줄 결론**: 이 영역의 문제는 «안 만들었다»가 아니라 **«만들어 놓고 고객 앞에 안 열었다 · 한 번도 안 통과시켜 봤다»** 다. R7 은 새 기능보다 **열기와 첫 통과**가 값이 크다.

---

## 1. DESIGN §2 채널 매트릭스

> 설계 문장: «코드 정본 = `lib/channel-registry.ts`».
> **실측: 그 파일은 없다.** 실제 채널 정의는 두 군데로 흩어져 있다 — `lib/accounts.ts:17 ALL_CHANNELS`(10개 · 타입) + DB `channel_registry` 표(10행 · 상태·정렬 · 운영 화면 `ops/channels.html` 이 바꾼다 · `netlify/functions/ops-channels.ts:41`).

| § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 | 설계? |
|---|---|---|---|---|---|---|---|
| 2 | 코드 정본 `lib/channel-registry.ts` | ❌ | `ls lib/channel-registry.ts` = 없음 · `grep -r channel-registry lib/ netlify/ public/` **0건** | 설계가 가리키는 파일이 없어, 새 사람이 «채널이 어디 정의돼 있나»를 못 찾는다 | 한 파일로 합치거나(권장) 설계 문장을 실제 두 곳으로 고친다 | S | ➖ |
| 2 | 상태 어휘 `live\|beta\|planned\|paused` | 🟠 | DDL 주석 `drizzle/0001-init.sql:205` 은 4값 · **실데이터·코드는 `active\|planned\|down`**(`ops/channels.html:48` · 라이브 10행 전부 active/planned) | DDL 주석과 실제가 다르다 — 주석을 믿고 `live` 를 넣으면 고객 그리드에서 사라진다 | 주석을 실제 어휘로 정정 | S | ➖ |
| 2.1 | 네이버 블로그 = 러너 발행 | ✅ | `runner/channels/naver-blog.mjs`(43KB) · registry `active` · 화면 `public/app/accounts.html:38` 통과 · **실측 `posts` 2행(published_via=runner)** · 카나리 `canary_runs` 2026-09-14 ok=true | — | — | — | |
| 2.1 | 티스토리 = 러너 발행 | 🟠 | `runner/channels/tistory.mjs` · registry `active` · 카나리 2026-09-15 **ok=true(임시저장까지)** · HTML 모드 실측 통과 | **끝까지 발행한 적이 없다** — `posts` 에 tistory 행 0. 카나리는 «임시저장까지»라 «완료» 버튼 뒤(공개 발행·예약)는 한 번도 안 지나갔다 | 테스트 계정으로 공개 발행 1건 끝까지 | M | |
| 2.1 | 구글 블로거 = Blogger API v3 | 🟡 | `lib/publish/blogger.ts` · `lib/publish/index.ts:180` 배선 · registry `active` · OAuth 시트 `accounts.html:70` | 커넥터·화면은 있는데 **실제로 올라간 적 0**(`posts` 0행) — OAuth 앱 키로 한 번도 안 통과시켰다 | 테스트 계정 OAuth → 발행 1건 | M | |
| 2.1 | 워드프레스 = REST + 앱 비밀번호 | 🟡 | `lib/publish/wordpress.ts` · `index.ts:181` · registry `active` · 시트 `accounts.html:62-68` | 같음 — `posts` 0행. 앱 비밀번호를 꽂아 본 적이 없다 | 테스트 사이트로 발행 1건 | M | |
| 2.1 | 네이버 클립 «게시물형»(텍스트+이미지) P3 | ❌ | registry 의 `naver_clip` 은 `category=video` 한 행뿐 · 텍스트 게시물형 경로 없음 | 설계는 **글 채널로도** 한 행을 뒀는데 코드엔 영상 한 갈래뿐 | 게시물형을 별도 채널로 둘지 결정 | M | ➖ |
| 2.1 | 쓰레드(보조 배포) | 🟡 | `lib/publish/threads.ts` · `index.ts:182`(영상) · registry **`planned`** | `planned` 라 **계정 연결 그리드에서 안 보인다**(`accounts.html:38` 필터) — 고객이 연결할 수 없다 | 텍스트 발행 경로 확인 후 `active` 전환 | M | |
| 2.1 | 인스타 피드·카드뉴스 | ❌ | `lib/publish/instagram.ts` 는 **REELS 전용**(`instagram.ts:112 media_type:"REELS"`) · 피드·카드뉴스 코드 0 | 설계의 «피드·카드뉴스»는 없다(영상 릴스만 있다) | 피드 이미지 발행 추가 여부 결정 | M | ➖ |
| 2.1 | 페이스북 페이지 P3 | ❌ | `ALL_CHANNELS`·registry·`public/js/ui.js:97` **모두 없음**(grep 0) | 없음 | P3 — 다음 라운드 | M | |
| 2.1 | X(트위터) P4 | ❌ | 발행 채널로 없음. **수익 소스로만** 존재(`lib/revenue/types.ts:8` `"x"` · 수동 입력 칩) | 발행은 없다 | P4 — 보류 | M | |
| 2.1 | 브런치스토리 P4 | ❌ | grep `brunch\|브런치` = **0건**(코드·화면 전부) · 설계 §8.2 의 `publish.brunch` 잡도 없음 | 없음 | P4 — 보류 | L | |
| 2.1 | 미디엄·서브스택 | ➖ | 설계 자체가 «보류» | — | — | — | |
| 2.2 | 유튜브 쇼츠 = Data API v3 | 🟡 | `lib/publish/youtube.ts` · `index.ts:182` 배선 · **registry `planned`** | 커넥터는 있는데 **채널이 `planned` 이라 계정 연결에서 안 보인다** → 고객은 유튜브를 연결할 수 없다 → 영상이 나갈 길이 없다 | registry `active` + OAuth 앱 키 + 발행 1건 | L | |
| 2.2 | 유튜브 심사 전 **비공개 고정** | ✅ | `lib/publish/youtube.ts` 의 privacy 고정 · `posts.status` 어휘에 `uploaded_private`(`db/schema.ts postsR5`) | — | — | — | |
| 2.2 | 유튜브 쿼터(업로드 1,600u ≈ 6건/일) | ✅ | `lib/publish/youtube.ts:92-98` — 업로드 **전에** 오늘 개수를 세어 `DAILY_CAP` 이면 «내일 이어서»(retriable) · 403 `quotaExceeded`·429 도 따로 처리(`:71`) · 주석 «맞고 나서 배우지 않는다» | (미실측 — 올려 본 적이 없다) | §2.2 열릴 때 함께 실측 | — | |
| 2.2 | 네이버 클립 = 러너(API 없음) | 🟠 | 잡 종류 `publish.naver_clip` 있음(`runner-jobs.ts:61`) · `runner/channels/naver-clip.mjs` **1.3KB 스텁** · registry `planned` | **스텁이다** — 잡을 집으면 «못 한다»고 정직하게 실패한다. 실제 업로드 0 | PC 업로드 경로 실측 → 구현 | L | |
| 2.2 | 인스타 릴스 = REELS 컨테이너·폴링 | 🟡 | `lib/publish/instagram.ts`(컨테이너→폴링 3단계 · `video_processing` retriable) · registry `planned` | 화면에서 연결 불가(`planned`) | `active` + 실발행 1건 | M | |
| 2.2 | 쓰레드 영상 | 🟡 | `lib/publish/threads.ts` VIDEO · registry `planned` | 같음 | 같음 | S | |
| 2.2 | 틱톡(심사 전 «본인만 보기») | ❌ | registry 에 `tiktok` 행은 있으나 **커넥터 파일 없음**(`lib/publish/tiktok.ts` 0) | 계정만 개념상 있고 올릴 코드가 없다 | P3 | L | |
| 2.2 | 페이스북 릴스 · 유튜브 롱폼 · X 영상 | ❌ | 전부 코드 0 | 없음 | P3~P4 | L | |
| 2.3 | 애드센스 = API 회수 | 🟡 | `lib/revenue/adsense.ts` · `google-oauth.ts` · 화면 `ad-media.html:80-85` | **실회수 0행**(`revenue_daily` 에 adsense 없음) — 연결해 본 적이 없다 | 테스트 계정 OAuth → 1일치 회수 | M | |
| 2.3 | 애드포스트 = 러너 스크랩 | ✅ | `runner/channels/revenue-adpost.mjs` · **실측 `revenue_daily` 2행(freshness=runner)** | — | — | — | |
| 2.3 | 카카오 애드핏 = 러너 스크랩 | 🟡 | `runner/channels/revenue-adfit.mjs` · 잡 `revenue.adfit` | 실회수 0행 | 실측 1회 | S | |
| 2.3 | 유튜브 파트너 = Analytics API(영상 단위) | 🟡 | `lib/revenue/youtube.ts` | 실회수 0행 · 채널 OAuth 자체가 막혀 있다(위 2.2) | 2.2 가 열린 뒤 | M | |
| 2.3 | 네이버 클립 인센티브 = 러너 스크랩 | 🟡 | `runner/channels/revenue-clip.mjs` · 잡 `revenue.clip` | 실회수 0행 | 실측 1회 | S | |
| 2.3 | 쿠팡 파트너스 = API · **subId=piece** | 🟡 | `lib/affiliate-coupang.ts:51`(subId 인자) · `lib/revenue/coupang.ts` · 키 입력 UI `ad-media.html:36 KEYED.coupang` | 실회수 0행 — **글 단위 귀속이 실제로 맞는지 한 번도 대조 못 했다**(이 설계의 핵심 주장) | 키 등록 → 1일치 회수 → piece 귀속 대조 | M | |
| 2.3 | 알리익스프레스 어필리에이트 | 🟡 | `lib/revenue/aliexpress.ts` · 키 UI `KEYED.aliexpress` | 실회수 0행 | 실측 | S | |
| 2.3 | 링크프라이스 | 🟡 | `lib/revenue/linkprice.ts` · 키 UI `KEYED.linkprice` | 실회수 0행 | 실측 | S | |
| 2.3 | 텐핑·애드픽 | ❌ | grep 0 | 없음 | 보류 | M | |
| 2.3 | 네이버 쇼핑커넥트 | ❌ | grep 0 | 없음 | 보류 | M | |
| 2.3 | 메타·틱톡·X 프리미엄 = 수동 입력 | ✅ | `lib/revenue/types.ts:8` · 수동 입력 시트 `revenue.html:161-189` · **실측 meta 4행·sponsor 8행(manual)** | — | — | — | |
| 2.3 | 3등급(자동/반자동/수동)을 화면이 숨기지 않는다 | ✅ | `ui.js:247 UI.FRESH = {api:"자동", runner:"내 PC", manual:"직접 입력"}` · 배지 `revenue.html:138` | — | — | — | |
| 2.3 | 신선도 «어제 12:30 기준» 표기 | 🟠 | `revenue.html:137` 은 **상대시각**(`UI.ago` → «3시간 전 가져옴») | 설계가 든 예시는 **절대시각**인데 상대시각뿐이라, 하루 지난 값인지 아침 값인지 구분이 안 된다 | 절대 시각(KST) 병기 | S | |
| 2.3 | 제휴 링크에 **전부 piece id 를 subId 로** | 🟠 | 쿠팡만 subId 지원(`affiliate-coupang.ts`) · 알리·링크프라이스는 링크 생성 코드 없음 | «전부»가 아니다 — 쿠팡 한 곳뿐 | 알리·링크프라이스 링크 생성 + subId | M | |

---

## 2. DESIGN §8 러너

| § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 | 설계? |
|---|---|---|---|---|---|---|---|
| 8.1 | 내 PC 러너 = 전 플랜 | 🟠 | 배포 일체 `scripts/build-runner.mts` · `/api/runner-download` · 실측 28/28(`scripts/verify-runner-dist.mts`) · R2 `autocreate/runner/v1.1.4.zip` | 플랜 게이트가 **`runnerDevices > 0`** 이라 한도 0 인 플랜이 생기면 못 받는다. 지금은 전 플랜 ≥1 이라 사실상 전 플랜 | 설계대로 «전 플랜»을 유지할지 확정 | S | |
| 8.1 | Windows 런처 `runner-start.bat` | ➖ | 실제 이름은 `runner/run.bat`(+ `run.sh` 신규) | 이름만 다르다 | 설계 문장의 파일명 정정 | S | ➖ |
| 8.1 | v2 트레이 앱 | ❌ | grep `tray\|트레이` 0 | 콘솔 창을 켜 둬야 한다 | v2 과제 | L | |
| 8.1 | 관리형 러너 = 우리 서버 러너 팜 | 🟠 | 신청 `netlify/functions/managed-runner.ts` · 화면 `runner.html:19,61-76` · 배정 `ops-runners.ts` | **팜이 실제로 돌지 않는다** — 기기는 운영자가 손으로 배정하고, 우리 서버에서 러너를 띄우는 코드·인프라는 없다 | 팜 인프라(격리 프로필·기동) | L | |
| 8.2 | `publish.naver_blog` | ✅ | `runner-jobs.ts:58` · 실측 `posts` 2행 | — | — | — | |
| 8.2 | `publish.tistory` | 🟠 | 같은 줄 · 카나리 ok | 임시저장까지만 실측(위 §2.1) | 공개 발행 1건 | M | |
| 8.2 | `publish.naver_clip` | 🟠 | `runner-jobs.ts:61` · 채널 파일은 스텁 | 잡만 있고 못 올린다(정직하게 비재시도 실패) | 구현 | L | |
| 8.2 | `publish.brunch` | ❌ | `RUNNER_JOB_KINDS`(`:57-62`) 에 없음 · `grep -r "publish.brunch"` **0건**. 다만 **명시적 연기**다 — `runner-jobs.ts:46` 주석 «DESIGN §8.2 의 나머지(naver_clip·brunch·render.video)는 Phase 3» | 설계에 있는데 종류가 없다(조용한 누락은 아니고 «Phase 3» 로 적혀 있다) | P4 — 브런치는 작가 승인이 선결 | L | |
| 8.2 | `render.video` | 🟡 | `runner-jobs.ts:61` · `runner/channels/render-video.mjs`(23KB) · 실측 `scripts/verify-r5-render.mts` | 고객 화면에서 «영상 만들기»를 고를 길이 영상 채널 `planned` 때문에 닫혀 있다 | §2.2 열기 | L | |
| 8.2 | `session.login`(헤드풀 재로그인) | ✅ | `runner/channels/session-login.mjs` · 실측 job #207 done(사장님 2FA) | — | — | — | |
| 8.2 | `session.verify` | ⬜ | 종류는 있음(`:58`) | 실행 흔적을 못 찾았다 | 실측 1회 | S | |
| 8.2 | `revenue.adpost` | ✅ | 실측 `revenue_daily` 2행 | — | — | — | |
| 8.2 | `revenue.adfit` · `revenue.clip` | 🟡 | 종류·채널 파일 있음 | 실행 0 | 실측 | S | |
| 8.2 | `verify.post_alive` | ✅ | `runner/channels/post-alive.mjs` · 3값(alive/false/unknown) 규율 `:6` | — | — | — | |
| 8.2 | (설계에 없는 것 5종) `revenue.stats`·`ads.*` 4종 | ➖ | `runner-jobs.ts:58-60` | 설계 §8.2 목록에 없다(나중에 는 것) | 설계 목록 갱신 | S | ➖ |
| 8.3 | 큐·상태기계 = 서버 단일 출처 | ✅ | `lib/runner-jobs.ts` claim/report/release · 경로 `/api/runner-queue` | 설계는 `/api/runner/queue`(슬래시) — 실제는 `-` | 설계 경로 표기 정정 | S | ➖ |
| 8.3 | 토큰은 테넌트 스코프 | ✅ | `authRunner` → `runner_devices.tenant_id` · claim 이 그 테넌트 잡만 | — | — | — | |
| 8.3 | claim 이 세션·프로필키·본문 패키지를 내려준다 | ✅ | `claimJobs` 가 자격 평문 + `browser_profile_key` + `RunnerPublishPayload` · 감사 `runner_creds_issued` | — | — | — | |
| 8.3 | report 를 믿지 않는다 — URL 확인 후 published | ✅ | `runner-jobs.ts:799 verifyPublishedUrl(externalUrl, title)` · 실패 시 `verify_not_found` | — | — | — | |
| 8.3 | 우선순위 발행>세션>수익>렌더 | ✅ | `JOB_PRIORITY`(`:68-79`) 발행 10 · 세션 20 · 수익 60 · 렌더 70 | — | — | — | |
| 8.3 | 하트비트 60초 → 온라인/오프라인 칩 | ✅ | `heartbeat` sleepSec 60/5 · 칩 `runner.html:44` · 창 5분(`ONLINE_WINDOW_MIN`) | — | — | — | |
| 8.3 | 오프라인 30분이면 due 발행을 `awaiting_runner` 로 표기 | 🟠 | `fleetState`·`awaiting_runner` 경로는 있음(`publish/index.ts` runner 분기 · `accounts.html:113` 배너) | **화면 어디에도 «30분»이 없다**(grep 0). 고객은 얼마나 꺼져 있어야 경고가 뜨는지 모른다 | 홈 «해야 할 일»에 임계와 함께 표기 | S | |

---

## 3. DESIGN §9 수익 통합

| § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 | 설계? |
|---|---|---|---|---|---|---|---|
| 9.0 | 애드포스트: 상태 표시 + 조건 충족 알림 | ✅ | `lib/ad-eligibility.ts:27`(글 50·방문 300) · 게이지 `ad-media.html:76` · 상태 칩 `:55` | — | — | — | |
| 9.0 | 애드센스 — 티스토리: 러너가 «수익 설정» 연동 | 🟡 | `runner/channels/ads-setup-tistory.mjs` · 잡 `ads.setup_tistory` | 실행 0 — 실측한 적 없다 | 실측 1회 | M | |
| 9.0 | 애드센스 — 블로거: 글 단위 `.ad-slot` + 수익 탭 상태 읽기 | 🟡 | `runner/channels/ads-setup-blogger.mjs`·`ads-status-blogger.mjs` · `lib/publish/gate.ts` ad-slot | 실행 0 | 실측 | M | |
| 9.0 | 애드센스 — WP: `wp/v2/widgets` 사이드바 위젯 + 되돌리기 | 🟡 | `lib/publish/ads.ts:5,46`(위젯 백업·DELETE 되돌리기 · 남의 위젯 안 지운다) | 실행 0 — 실제 WP 사이트에 안 걸어 봤다 | 실측 | M | |
| 9.0 | YPP: 진행률 게이지 + «신청하러 가기» | ✅ | `ad-eligibility.ts:28`(구독 1,000·조회 1,000만) · 게이지 `ad-media.html:92` · 링크 `:34` | 값은 채널 OAuth 로 채워야 하는데 유튜브 연결이 막혀 있다(§2.2) → 게이지가 늘 0 | §2.2 열기 | M | |
| 9.0 | 클립: 모집 일정 알림 + 조건 게이지 | 🟡 | 모집창 `ad-eligibility.ts:53-56`(`channel_registry.monetize.clipOpen` · 운영자 입력) | 운영자가 넣어야만 뜨고, 넣은 적 없다 | 운영 입력 + 알림 실측 | S | |
| 9.0 | 쿠팡: 키 등록 → 링크 자동 생성(subId=piece) → 고지 삽입 | ✅ | 키 UI `ad-media.html:36` · `affiliate-coupang.ts` · 고지 `lib/disclosure.ts`(§4.7 게이트) | 수익 회수까지는 미실측(§2.3) | — | — | |
| 9.0 | 알리·링크프라이스: 키 등록 → 링크 생성·회수 | 🟠 | 키 UI 있음(`KEYED`) · 회수 코드 있음 | **링크 «생성»이 없다** — 회수만 있고 제휴 링크를 만드는 코드가 없다 | 링크 생성 + subId | M | |
| 9.0 | 매체별 «한 화면 한 단계» 가이드 + «가입 완료했어요» | ✅ | `ad-media.html:122-131 howSheet()` · 3단계 문구 `:28-34` · 체크 `claimSheet() :133-148` | — | — | — | |
| 9.0 | Agency 원격접속으로 셋업 대행 | 🔒 | 임퍼소네이션 자체는 있음(`lib/auth.ts signImpersonationToken`) | 수익 매체 셋업을 대행하는 **전용 흐름은 없다**(일반 원격접속뿐) | 대행 체크리스트 화면 | M | |
| 9.1 | 애드센스 일 1회 | ✅ | `lib/cron/revenue-sync.ts:30 revenueSyncStep` · 우산 `cron/runner.ts:59` | 실회수 0(§2.3) | — | — | |
| 9.1 | 유튜브 애널리틱스 일 1회 | ✅ | 같음 · `lib/revenue/youtube.ts` | 실회수 0 | — | — | |
| 9.1 | 쿠팡 일 1회 **13:00** | ✅ | `revenue-sync.ts` 주석 «hourly(06:00 KST · 쿠팡 13:00)» | — | — | — | |
| 9.1 | 알리·링크프라이스 일 1회 | ✅ | `API_SOURCES`(`revenue/index.ts:25`) 에 포함 | 실회수 0 | — | — | |
| 9.1 | 애드포스트·애드핏·클립 = 러너 일 1회 | ✅ | 잡 3종 · 실측 adpost 2행 | adfit·clip 미실측 | — | — | |
| 9.1 | 기타 = 수동 입력 | ✅ | 수동 시트 `revenue.html:161-189` · 실측 12행 | — | — | — | |
| 9.2 | `revenue_daily` 필드 전부 | ✅ | `drizzle/0001-init.sql:442-456` — tenant·source·account·piece·day·amount_krw·currency·fx_rate·freshness·raw 전부 있음 | — | — | — | |
| 9.2 | 유니크 `(tenant, source, account, piece, date)` | ✅ | `:455 UNIQUE (tenant_id, source, account_id, piece_id, day)` | — | — | — | |
| 9.3 | 홈 큰 숫자 = 오늘 번 돈 · **확정/예상 분리** | ✅ | `home.html:58`(확정 진하게 · 예상 회색) · `:59` 카운트업은 확정값만 · `types.ts:24 CONFIRMED_SOURCES` | — | — | — | |
| 9.3 | «어제보다 ±N원» | ✅ | `public/app/home.html:63` — `이번 달 … · 어제보다 <b>+N원</b>`(증가만 초록) · 어제 전체 합과 비교 | — | — | — | |
| 9.3 | 수익 탭: 이번달 총합 → 소스별 → 계정별 → 글/영상별 TOP 5 | ✅ | `revenue.html:132-153`(어디서/어느 계정이/잘 번 글) · 5개 제한은 서버(`mock.js:605 slice(0,5)`) | — | — | — | |
| 9.3 | 신선도 배지 | ✅ | `revenue.html:138 UI.freshPill` | 절대시각 없음(§2.3 참조) | — | — | |
| 9.3 | «수익 나는 소재 학습» — piece 수익을 topic factor 로 | ✅ | `lib/cron/learn.ts:177 revenueByTopic()` | 되먹일 수익 데이터가 수동 12행뿐이라 사실상 안 돈다 | 수익 회수가 돌면 자동 | — | |

---

## 4. 이 조사에서 실측한 것(증거)

| 무엇 | 결과 |
|---|---|
| `channel_registry` 라이브 10행 | active 4(naver_blog·tistory·blogger·wordpress) · planned 6(threads·instagram·youtube_shorts·naver_clip·reels·tiktok) |
| `posts` 발행 성공 행 | **`naver_blog` / `published_via=runner` 2건뿐** — 그 외 전 채널 0 |
| `revenue_daily` | `sponsor`(manual) 8 · `meta`(manual) 4 · `adpost`(runner) 2 — **API 소스 0행** |
| `canary_runs` | tistory ok=true(09-14·09-15) · naver_blog ok=true(09-14) |
| 러너 배포 | `scripts/verify-runner-dist.mts` **28/28 통과** · R2 `autocreate/runner/v1.1.4.zip` sha256 `53ac918c…` |

- 사용한 테넌트: `러너배포실증`(key `runner-dist-verify` · trial) · `B2실증` 189(trial). **보존 4집(3·13·109·116) 무접촉.**
- 조사가 만든 행: 없음(읽기만). 이 조사로 인한 발행 0건.

## 5. R7 제안 묶음(B2 영역 몫)

| 묶음 | 내용 | 크기 합 |
|---|---|---|
| **A. 영상 축을 고객 앞에 연다** | registry `youtube_shorts`→active · OAuth 앱 키 · 발행 1건 실측 · `render.video` 를 만들기 화면에서 고르게 · 쿼터 게이트 | L+L+S |
| **B. «한 번도 안 지나간 길» 첫 통과** | 티스토리 공개 발행 · 블로거·워드프레스 발행 각 1건 · API 수익 5종 중 최소 2종 첫 회수(쿠팡 subId 귀속 대조 포함) | M×5 |
| **C. 정직 표기 보수** | 신선도 절대시각 · «30분 오프라인» 임계 표기 · 홈 «어제보다» · DDL 상태 어휘 주석 · 설계 §2/§8.1/§8.2/§8.3 문장 정정(➖ 5건) | S×6 |
