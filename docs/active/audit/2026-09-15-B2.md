# 설계 대비 전수조사 — B2 영역(§2 채널 매트릭스 · §8 러너 · §9 수익 통합)

> 조사자 B2 · 2026-09-15 · 브랜치 `feature/runner-dist` · 방식 = `docs/active/2026-09-15-DESIGN-AUDIT-trigger.md` §1 어휘 · §2 행 형식 · §4 규율.
> 코드는 고치지 않았다(조사만). 실호출·실발행 0 — 러너는 `--peek`/드라이런, DB 는 읽기와 테스트 테넌트만.
> 마지막 열 «설계?» = ➖ 는 **코드가 아니라 설계 문장을 고쳐야 할 것 같다**는 제안(결정은 메인·사장님). 🔴 **[2026-09-23 · R16]** 같은 표를 **다시 쟀다**(브랜치 `audit/2026-09-23-B2` · 511 커밋 뒤) — 행은 **하나도 안 지웠고** 상태 칸만 고치고 증거 칸에 «**2026-09-23 재측정**: …» 을 덧붙였다. **결과·세는 법·남은 일 = §6**(맨 아래). 아래 §0 과 §5 는 **09-15 당시의 글**이니 지금 수는 §6 을 봐라. 🔴 **줄 번호는 이 파일의 현재 줄 그대로다**(§6 이 가리키는 «줄 30» 은 지금 30줄이다).

---

## 0. 요약 5줄 *(2026-09-15 당시 · 지금 수는 §6)*

1. **상태별 개수(총 80 판정 단위)**: ✅ 33 · 🟡 19 · ❌ 12 · 🟠 11 · ➖ 3 · 🔒 1 · ⬜ 1. 별도로 **«설계 문장을 고치자» 제안 6건**(마지막 열 ➖).
   *(숫자는 표에서 기계로 센 값이다 — 처음엔 손으로 «총 62» 라고 적었다가 실제로 세어 보고 고쳤다.)*
2. **가장 큰 것 — «코드는 넓고, 실제로 지나간 길은 좁다».** 라이브 실측: `posts` 에 발행 성공 행이 **`naver_blog` 2건뿐**(티스토리·블로거·워드프레스·영상 전 채널 **0건**), `revenue_daily` 는 **수동 12행 + 애드포스트(러너) 2행**뿐 — **API 수익 소스 5종(애드센스·유튜브·쿠팡·알리·링크프라이스)은 한 행도 쓴 적이 없다.**
3. **🟠/❌ 상위 5**(고객이 먼저 부딪히는 순): ①영상 채널 4종이 레지스트리 `planned` 라 **계정 연결 화면에서 아예 안 보인다**(코드·커넥터는 다 있다) ②DESIGN §2 가 지목한 «코드 정본» `lib/channel-registry.ts` **파일이 없다** ③API 수익 5종 **실회수 0회**(키를 꽂아 본 적이 없다) ④`publish.brunch` 잡 종류 **없음**(설계 §8.2 에 있다) ⑤설계 §2 의 페이스북·X·브런치·미디엄 **어디에도 없다**.
4. **크기 합**: S 15 · M 22 · L 8. L 8개가 무게의 대부분이고 그중 5개가 **영상 축**(유튜브 열기 · 네이버 클립 구현 · 틱톡 · 페북 릴스 · 러너 v2 트레이), 나머지가 브런치·관리형 팜·(글 채널 잔여).
5. **한 줄 결론**: 이 영역의 문제는 «안 만들었다»가 아니라 **«만들어 놓고 고객 앞에 안 열었다 · 한 번도 안 통과시켜 봤다»** 다. R7 은 새 기능보다 **열기와 첫 통과**가 값이 크다.

---

## 1. DESIGN §2 채널 매트릭스

> 설계 문장: «코드 정본 = `lib/channel-registry.ts`».
> **실측: 그 파일은 없다.** 실제 채널 정의는 두 군데로 흩어져 있다 — `lib/accounts.ts:17 ALL_CHANNELS`(10개 · 타입) + DB `channel_registry` 표(10행 · 상태·정렬 · 운영 화면 `ops/channels.html` 이 바꾼다 · `netlify/functions/ops-channels.ts:41`).

| § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 | 설계? |
|---|---|---|---|---|---|---|---|
| 2 | 코드 정본 `lib/channel-registry.ts` | ✅ | `ls lib/channel-registry.ts` = 없음 · `grep -r channel-registry lib/ netlify/ public/` **0건** · **2026-09-23 재측정**(파일·grep): 🔴 **그 파일이 생겼다** — `lib/channel-registry.ts:131 CHANNELS` **17행**(성질: 연결방식·publishVia·jobKind·축·서식). `lib/accounts.ts:12` 가 그 표를 읽는 얇은 래퍼다(`ALL_CHANNELS`=`CHANNEL_KEYS`). 두 표 어긋남은 `scripts/verify-channel-tables.mjs` 가 매번 잰다(실행 ✅ «두 표가 어긋나지 않는다»). | 설계가 가리키는 파일이 없어, 새 사람이 «채널이 어디 정의돼 있나»를 못 찾는다 | 한 파일로 합치거나(권장) 설계 문장을 실제 두 곳으로 고친다 | S | ➖ |
| 2 | 상태 어휘 `live\|beta\|planned\|paused` | ✅ | DDL 주석 `drizzle/0001-init.sql:205` 은 4값 · **실데이터·코드는 `active\|planned\|down`**(`ops/channels.html:48` · 라이브 10행 전부 active/planned) · **2026-09-23 재측정**(grep): 주석이 고쳐졌다 — `drizzle/0001-init.sql:206` 이 «어휘는 `active`·`planned`·`down`»으로 바뀌었고 `drizzle/0014-r7-channel-status.sql:15` 가 **CHECK 제약**으로 강제한다(+`:18` COMMENT 로 뜻까지). 라이브 17행 전부 그 셋 안. | DDL 주석과 실제가 다르다 — 주석을 믿고 `live` 를 넣으면 고객 그리드에서 사라진다 | 주석을 실제 어휘로 정정 | S | ➖ |
| 2.1 | 네이버 블로그 = 러너 발행 | ✅ | `runner/channels/naver-blog.mjs`(43KB) · registry `active` · 화면 `public/app/accounts.html:38` 통과 · **실측 `posts` 2행(published_via=runner)** · 카나리 `canary_runs` 2026-09-14 ok=true | — | — | — | |
| 2.1 | 티스토리 = 러너 발행 | 🟠 | `runner/channels/tistory.mjs` · registry `active` · 카나리 2026-09-15 **ok=true(임시저장까지)** · HTML 모드 실측 통과 · **2026-09-23 재측정**(라이브 SELECT): **그대로다** — `posts` 에 tistory **0행** · `runner_jobs publish.tistory` 는 `failed` 1건(09-14)뿐 · `canary_runs` tistory ok=true 2건(마지막 09-14). 코드는 그 뒤 4커밋 고쳤다(`e7505a7` «완료» 뒤 비공개 경로 · `5b42b8e` HTML 모드 confirm · `6eac37e` 서식 폴백 · `39ad694` 셀렉터 표). 🔒 **남은 것은 코드가 아니라 카카오 2FA 로그인 1회**(`PROJECT_STATE.md` §4 «사장님 손» 목록 — **메인 기록 · 내가 잰 게 아니다**). | **끝까지 발행한 적이 없다** — `posts` 에 tistory 행 0. 카나리는 «임시저장까지»라 «완료» 버튼 뒤(공개 발행·예약)는 한 번도 안 지나갔다 | 테스트 계정으로 공개 발행 1건 끝까지 | M | |
| 2.1 | 구글 블로거 = Blogger API v3 | 🟡 | `lib/publish/blogger.ts` · `lib/publish/index.ts:180` 배선 · registry `active` · OAuth 시트 `accounts.html:70` · **2026-09-23 재측정**: `posts` blogger **0행** 그대로 · 계정은 `pending_login` 1개(id=70)뿐. 🔒 registry 는 `active` 인데 `lib/accounts.ts:246` 이 `connectable=false` 로 말한다(`providerMissing`=`no_provider_key` · `GOOGLE_OAUTH_CLIENT_ID`). «자격 없음» 정직 경로는 재실증했다(`scripts/verify-api-channels.mts` 재실행 ✓ 발행 0건). | 커넥터·화면은 있는데 **실제로 올라간 적 0**(`posts` 0행) — OAuth 앱 키로 한 번도 안 통과시켰다 | 테스트 계정 OAuth → 발행 1건 | M | |
| 2.1 | 워드프레스 = REST + 앱 비밀번호 | 🟡 | `lib/publish/wordpress.ts` · `index.ts:181` · registry `active` · 시트 `accounts.html:62-68` · **2026-09-23 재측정**: `posts` 0행 · `accounts` 에 wordpress **0행**. 🔴 **이건 키가 아니라 우리 몫이다** — 앱 비밀번호 채널이라 `providerMissing=null` 이고 registry `active` ⇒ **`connectable=true`. 지금 당장 붙일 수 있는데 한 번도 안 붙였다.** 정직 경로만 실증(`verify-api-channels.mts` ✓). | 같음 — `posts` 0행. 앱 비밀번호를 꽂아 본 적이 없다 | 테스트 사이트로 발행 1건 | M | |
| 2.1 | 네이버 클립 «게시물형»(텍스트+이미지) P3 | 🟠 | registry 의 `naver_clip` 은 `category=video` 한 행뿐 · 텍스트 게시물형 경로 없음 · **2026-09-23 재측정**: **별도 채널이 생겼다** — `lib/channel-registry.ts:158 naver_clip_post`(axis `text`) · 라이브 `channel_registry` 에 행 있음(`planned` · sort 23) · `accounts.html:56` 이 «곧 열려요»로 보여 준다. 다만 `publishVia:null` 이라 못 올린다(**일부러** · 같은 파일 «못 채운 칸» 주석). 남은 것은 코드가 아니라 **PC 업로드 경로 1회 실측**. | 설계는 **글 채널로도** 한 행을 뒀는데 코드엔 영상 한 갈래뿐 | 게시물형을 별도 채널로 둘지 결정 | M | ➖ |
| 2.1 | 쓰레드(보조 배포) | 🟡 | `lib/publish/threads.ts` · `index.ts:182`(영상) · registry **`planned`** · **2026-09-23 재측정**: 글 갈래가 생겼다 — `publishThreadsText` 가 배선됐고 `lib/publish/index.ts:68` 이 `piece.kind` 로 글·영상을 가른다(종전엔 무조건 영상이라 글은 영원히 막혔다). 🔒 registry 여전히 `planned` · `THREADS_APP_ID` 없음 ⇒ `connectable=false`. | `planned` 라 **계정 연결 그리드에서 안 보인다**(`accounts.html:38` 필터) — 고객이 연결할 수 없다 | 텍스트 발행 경로 확인 후 `active` 전환 | M | |
| 2.1 | 인스타 피드·카드뉴스 | 🟡 | `lib/publish/instagram.ts` 는 **REELS 전용**(`instagram.ts:112 media_type:"REELS"`) · 피드·카드뉴스 코드 0 · **2026-09-23 재측정**(grep): 🔵 **반만 왔다** — `publishInstagramFeed` 가 생겼다(`lib/publish/instagram.ts` 사진 1장/캐러셀 2~10장 · 배선 `lib/publish/index.ts:63`). «REELS 전용»이 아니다. 아직 못 쓰는 까닭 둘: ①🔒 registry `planned` + `META_APP_ID` 없음 ②`channel-registry.ts:139 textGen:false`(카드뉴스 계약이 보강돼야 켠다 — B-1 실측 «10편 중 9편 골격 겹침»). | 설계의 «피드·카드뉴스»는 없다(영상 릴스만 있다) | 피드 이미지 발행 추가 여부 결정 | M | ➖ |
| 2.1 | 페이스북 페이지 P3 | 🟡 | `ALL_CHANNELS`·registry·`public/js/ui.js:97` **모두 없음**(grep 0) · **2026-09-23 재측정**(grep): 🔵 **반만 왔다** — `lib/publish/facebook.ts publishFacebookPost` 생김 · 배선 `lib/publish/index.ts:64` · registry `facebook` 행(`planned` · `retractVia:api`) · 페이지 토큰으로 올린다. 🔒 `META_APP_ID` 없어 `connectable=false` · `textGen:false`(페북용 글 계약 미도착). | 없음 | P3 — 다음 라운드 | M | |
| 2.1 | X(트위터) P4 | 🟡 | 발행 채널로 없음. **수익 소스로만** 존재(`lib/revenue/types.ts:8` `"x"` · 수동 입력 칩) · **2026-09-23 재측정**(grep): 🔵 **반만 왔다** — 발행 채널이 생겼다(`lib/publish/x.ts publishToX` · 배선 `index.ts:66` · registry `x` 행 · 한글 2자 계산 `weightedLen` · 삭제는 된다). 🔒 `X_CLIENT_ID` 없음 + **X 는 쓰기가 유료 플랜**(설계 §2.1 «P4 선택»). | 발행은 없다 | P4 — 보류 | M | |
| 2.1 | 브런치스토리 P4 | 🟠 | grep `brunch\|브런치` = **0건**(코드·화면 전부) · 설계 §8.2 의 `publish.brunch` 잡도 없음 · **2026-09-23 재측정**: registry 에 **행이 생겼다**(`lib/channel-registry.ts:155 brunch`) — 다만 `publishVia:null`·`jobKind:null` 이 **일부러**다(같은 파일 «못 채운 칸» 주석: 작가 승인 계정이 없어 `brunch.co.kr/write` 를 한 번도 연 적이 없다 · 추측 셀렉터를 박지 않는다 — 티스토리에서 그게 이틀을 태웠다). 화면엔 «곧 열려요»로 뜬다. 🔒 **작가 승인 1건이 선결**. | 없음 | P4 — 보류 | L | |
| 2.1 | 미디엄·서브스택 | ➖ | 설계 자체가 «보류» | — | — | — | |
| 2.2 | 유튜브 쇼츠 = Data API v3 | 🟡 | `lib/publish/youtube.ts` · `index.ts:182` 배선 · **registry `planned`** · **2026-09-23 재측정**(파일·grep): 🔴 **«유튜브 커넥터가 없다»는 사실이 아니다** — `lib/publish/youtube.ts`(20,435바이트)가 있고 `lib/publish/index.ts:57` 에 배선돼 있다. 합성 콘텐츠 플래그도 있다(`youtube.ts:181 containsSyntheticMedia:true`). 막는 것은 둘: ①registry `planned`(**우리 손**) ②🔒 `GOOGLE_OAUTH_CLIENT_ID` 없음. 🔴 라이브 `accounts` 의 `youtube_shorts` **active 4행은 실연결이 아니다** — handle 이 `c_r5_yt_*` 인 **R5 시험 하니스 산물**(09-14 생성). | 커넥터는 있는데 **채널이 `planned` 이라 계정 연결에서 안 보인다** → 고객은 유튜브를 연결할 수 없다 → 영상이 나갈 길이 없다 | registry `active` + OAuth 앱 키 + 발행 1건 | L | |
| 2.2 | 유튜브 심사 전 **비공개 고정** | ✅ | `lib/publish/youtube.ts` 의 privacy 고정 · `posts.status` 어휘에 `uploaded_private`(`db/schema.ts postsR5`) | — | — | — | |
| 2.2 | 유튜브 쿼터(업로드 1,600u ≈ 6건/일) | ✅ | `lib/publish/youtube.ts:92-98` — 업로드 **전에** 오늘 개수를 세어 `DAILY_CAP` 이면 «내일 이어서»(retriable) · 403 `quotaExceeded`·429 도 따로 처리(`:71`) · 주석 «맞고 나서 배우지 않는다» | (미실측 — 올려 본 적이 없다) | §2.2 열릴 때 함께 실측 | — | |
| 2.2 | 네이버 클립 = 러너(API 없음) | 🟠 | 잡 종류 `publish.naver_clip` 있음(`runner-jobs.ts:61`) · `runner/channels/naver-clip.mjs` **1.3KB 스텁** · registry `planned` · **2026-09-23 재측정**: **안 움직였다** — `runner/channels/naver-clip.mjs` 여전히 **1,295바이트 스텁**(머리말 «정직하게 멈춘다») · 라이브 `runner_jobs publish.naver_clip` **0행**. | **스텁이다** — 잡을 집으면 «못 한다»고 정직하게 실패한다. 실제 업로드 0 | PC 업로드 경로 실측 → 구현 | L | |
| 2.2 | 인스타 릴스 = REELS 컨테이너·폴링 | 🟡 | `lib/publish/instagram.ts`(컨테이너→폴링 3단계 · `video_processing` retriable) · registry `planned` · **2026-09-23 재측정**: 커넥터 그대로(`publishReels` · 배선 `index.ts:62`) · registry `planned` · 🔒 `META_APP_ID` 없음 ⇒ `connectable=false`. | 화면에서 연결 불가(`planned`) | `active` + 실발행 1건 | M | |
| 2.2 | 쓰레드 영상 | 🟡 | `lib/publish/threads.ts` VIDEO · registry `planned` · **2026-09-23 재측정**: `publishThreadsVideo` 배선 `index.ts:68` · registry `planned` · 🔒 `THREADS_APP_ID` 없음. | 같음 | 같음 | S | |
| 2.2 | 틱톡(심사 전 «본인만 보기») | 🟡 | registry 에 `tiktok` 행은 있으나 **커넥터 파일 없음**(`lib/publish/tiktok.ts` 0) · **2026-09-23 재측정**(grep): 🔵 **반만 왔다** — `lib/publish/tiktok.ts` 가 생겼다(배선 `index.ts:67`) · registry `tiktok` 행에 «삭제 API 없음 ⇒ `retractVia:null`»까지 적혀 있다. 🔒 `TIKTOK_CLIENT_KEY` 없음. | 계정만 개념상 있고 올릴 코드가 없다 | P3 | L | |
| 2.2 | 페이스북 릴스 · 유튜브 롱폼 · X 영상 | 🟡 | 전부 코드 0 · **2026-09-23 재측정**(grep): 🔵 **셋 다 생겼다** — 페북 릴스 `publishFacebookReels`(`index.ts:65` · start→rupload→finish) · 유튜브 롱폼 `publishYoutubeLong`(`index.ts:58` · registry `youtube_long` · 쇼츠와 같은 `videos.insert`·같은 쿼터) · X 영상(`lib/publish/x.ts` · `channel-registry.ts:150` «사진과 같은 청크 업로드에 처리 대기 한 단계»). 🔒 앱 키 셋(메타·구글·X) 전부 없음. | 없음 | P3~P4 | L | |
| 2.3 | 애드센스 = API 회수 | 🟡 | `lib/revenue/adsense.ts` · `google-oauth.ts` · 화면 `ad-media.html:80-85` · **2026-09-23 재측정**(라이브 SELECT): **한 행도 안 늘었다** — `revenue_daily` 전체가 여전히 14행(`sponsor` manual 8 · `meta` manual 4 · `adpost` runner 2)이고 **adsense 0행**. 🔒 구글 OAuth 앱 선결(§2.1 blogger 와 같은 키). | **실회수 0행**(`revenue_daily` 에 adsense 없음) — 연결해 본 적이 없다 | 테스트 계정 OAuth → 1일치 회수 | M | |
| 2.3 | 애드포스트 = 러너 스크랩 | ✅ | `runner/channels/revenue-adpost.mjs` · **실측 `revenue_daily` 2행(freshness=runner)** | — | — | — | |
| 2.3 | 카카오 애드핏 = 러너 스크랩 | 🟡 | `runner/channels/revenue-adfit.mjs` · 잡 `revenue.adfit` · **2026-09-23 재측정**: `revenue_daily` adfit **0행** · `runner_jobs revenue.adfit` **0행**. 러너 채널은 스텁이 아니다(`runner/channels/revenue-adfit.mjs` 6,053바이트 · 돌아가는 adpost 와 같은 크기) · 배선 `runner/core.mjs:50`. 🔴 **키가 아니라 «실행 1회»가 남았다(우리 손)**. | 실회수 0행 | 실측 1회 | S | |
| 2.3 | 유튜브 파트너 = Analytics API(영상 단위) | 🟡 | `lib/revenue/youtube.ts` · **2026-09-23 재측정**: `revenue_daily` youtube **0행**. 🔴 «영상 단위»는 **코드에 있다** — `lib/revenue/youtube.ts:27` 이 `dimensions=day,video` 로 부르고 `:37-52` 가 video id → `posts.channel_ref` → piece 로 귀속하며 못 맞춘 것은 계정으로 남긴다(안 버린다). 🔒 §2.2 유튜브 OAuth 가 열려야 돈다. | 실회수 0행 · 채널 OAuth 자체가 막혀 있다(위 2.2) | 2.2 가 열린 뒤 | M | |
| 2.3 | 네이버 클립 인센티브 = 러너 스크랩 | 🟡 | `runner/channels/revenue-clip.mjs` · 잡 `revenue.clip` · **2026-09-23 재측정**: `revenue_daily` clip **0행** · `runner_jobs revenue.clip` **0행**. 러너 채널 실물(`revenue-clip.mjs` 6,443바이트 · 배선 `runner/core.mjs:51`). 실행 1회가 남았다. | 실회수 0행 | 실측 1회 | S | |
| 2.3 | 쿠팡 파트너스 = API · **subId=piece** | 🟡 | `lib/affiliate-coupang.ts:51`(subId 인자) · `lib/revenue/coupang.ts` · 키 입력 UI `ad-media.html:36 KEYED.coupang` · **2026-09-23 재측정**(라이브 SELECT): 🔴 **화면·경로는 지나갔는데 성공이 0이다** — `revenue_sources` 에 쿠팡 키가 **실제로 들어간 행이 있다**(id=1 · tenant 55 · `cred_enc` 있음). 그런데 `status=error` «쿠팡 인증 실패(401)» `fail_count=3` · `last_ok_at` 없음. 다른 3테넌트는 `not_configured`. ⇒ **맞는 키로 한 번도 성공 못 했고**, 그래서 이 설계의 핵심 주장인 **subId=piece 귀속 대조도 못 했다**(`affiliate_links` 라이브 **0행**). | 실회수 0행 — **글 단위 귀속이 실제로 맞는지 한 번도 대조 못 했다**(이 설계의 핵심 주장) | 키 등록 → 1일치 회수 → piece 귀속 대조 | M | |
| 2.3 | 알리익스프레스 어필리에이트 | 🟡 | `lib/revenue/aliexpress.ts` · 키 UI `KEYED.aliexpress` · **2026-09-23 재측정**: `revenue_daily` aliexpress 0행 · `revenue_sources` 의 aliexpress 2행 전부 `not_configured`(«자격이 등록되지 않았어요»). 키 UI 는 그대로(`ad-media.html:36 KEYED.aliexpress`). | 실회수 0행 | 실측 | S | |
| 2.3 | 링크프라이스 | 🟡 | `lib/revenue/linkprice.ts` · 키 UI `KEYED.linkprice` · **2026-09-23 재측정**: `revenue_daily` linkprice 0행 · `revenue_sources` linkprice 2행 전부 `not_configured`. 키 UI 그대로(`KEYED.linkprice`). | 실회수 0행 | 실측 | S | |
| 2.3 | 텐핑·애드픽 | ✅ | grep 0 · **2026-09-23 재측정**: 🔴 **설계가 먼저 바뀌었고 코드가 그대로다** — `docs/DESIGN.md:103-105` 가 2026-09-15 에 «공개 API 가 없어 **수동만** · 이름만»으로 정정됐다. 코드: `lib/revenue/types.ts:20`(목록) · `:31`(freshness `manual`) · `:43-44`(이름 «텐핑»·«애드픽») · `:63 CONFIRMED_SOURCES` · `:77`(하루 기준 KST). 화면: `netlify/functions/revenue.ts:105 manualSources` → `public/app/revenue.html:244-245` 수동 입력 시트가 **서버 목록을 그대로** 그린다(화면에 이름을 안 박는다). | 없음 | 보류 | M | |
| 2.3 | 네이버 쇼핑커넥트 | ✅ | grep 0 · **2026-09-23 재측정**: 위 행과 같은 정정·같은 배선 — `lib/revenue/types.ts:20`·`:31`·`:45`(«네이버 쇼핑커넥트») · `:77` · `revenue.ts:105` · `revenue.html:244`. 라이브 `channel_registry.monetize` 에도 실려 있다(naver_blog·naver_clip·naver_clip_post). | 없음 | 보류 | M | |
| 2.3 | 메타·틱톡·X 프리미엄 = 수동 입력 | ✅ | `lib/revenue/types.ts:8` · 수동 입력 시트 `revenue.html:161-189` · **실측 meta 4행·sponsor 8행(manual)** | — | — | — | |
| 2.3 | 3등급(자동/반자동/수동)을 화면이 숨기지 않는다 | ✅ | `ui.js:247 UI.FRESH = {api:"자동", runner:"내 PC", manual:"직접 입력"}` · 배지 `revenue.html:138` | — | — | — | |
| 2.3 | 신선도 «어제 12:30 기준» 표기 | 🟠 | `revenue.html:137` 은 **상대시각**(`UI.ago` → «3시간 전 가져옴») · **2026-09-23 재측정**(grep): **안 붙었다** — `public/app/revenue.html` 에 `UI.timeKST`·`UI.dateKST` **0건**이고 `:189` 는 여전히 `UI.ago`(«3시간 전 가져옴») · `ad-media.html:57` 도 같다. (그 사이 붙은 것: 매체마다 세는 «하루»가 다르다는 기준 줄 `revenue.html:193` · `DAY_BASIS_OF`.) | 설계가 든 예시는 **절대시각**인데 상대시각뿐이라, 하루 지난 값인지 아침 값인지 구분이 안 된다 | 절대 시각(KST) 병기 | S | |
| 2.3 | 제휴 링크에 **전부 piece id 를 subId 로** | 🟠 | 쿠팡만 subId 지원(`affiliate-coupang.ts`) · 알리·링크프라이스는 링크 생성 코드 없음 · **2026-09-23 재측정**(grep): **그대로다** — 링크를 «만드는» 코드는 여전히 쿠팡 한 곳뿐(`lib/affiliate-coupang.ts:65 deeplink` · `:73 subIdFor`). `lib/revenue/aliexpress.ts`·`linkprice.ts` 에는 회수(`sync*`)만 있고 링크 생성 함수 0. `affiliate_links` 를 INSERT 하는 자리도 쿠팡 둘뿐(`lib/content-gen.ts:434` · `lib/video/gen.ts:286`) · 라이브 `affiliate_links` **0행**. | «전부»가 아니다 — 쿠팡 한 곳뿐 | 알리·링크프라이스 링크 생성 + subId | M | |

---

## 2. DESIGN §8 러너

| § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 | 설계? |
|---|---|---|---|---|---|---|---|
| 8.1 | 내 PC 러너 = 전 플랜 | ✅ | 배포 일체 `scripts/build-runner.mts` · `/api/runner-download` · 실측 28/28(`scripts/verify-runner-dist.mts`) · R2 `autocreate/runner/v1.1.4.zip` · **2026-09-23 재측정**: `netlify/functions/runner.ts:136` 이 `plan.limits.runnerDevices > 0` 로 재고, `lib/plans.ts:42`·`:45`·`:48`·`:51` 네 플랜 전부 ≥1(trial 1 · starter 1 · pro 2 · agency 5) ⇒ **지금 전 플랜이 맞다**. 화면 `public/app/runner.html:34` «프로그램 내려받기» → 10분 링크 + 감사(`runner.ts:143`). 남은 위험은 «운영자가 플랜을 0 으로 고치면»뿐이라 설계 위반이 아니다. | 플랜 게이트가 **`runnerDevices > 0`** 이라 한도 0 인 플랜이 생기면 못 받는다. 지금은 전 플랜 ≥1 이라 사실상 전 플랜 | 설계대로 «전 플랜»을 유지할지 확정 | S | |
| 8.1 | Windows 런처 `runner-start.bat` | ➖ | 실제 이름은 `runner/run.bat`(+ `run.sh` 신규) | 이름만 다르다 | 설계 문장의 파일명 정정 | S | ➖ |
| 8.1 | v2 트레이 앱 | ❌ | grep `tray\|트레이` 0 · **2026-09-23 재측정**(grep): **안 움직였다** — 소스에 `tray`·`트레이` **0건**(`node_modules`·`runner/profiles` 캐시 제외). 여전히 콘솔 창을 켜 둬야 한다. | 콘솔 창을 켜 둬야 한다 | v2 과제 | L | |
| 8.1 | 관리형 러너 = 우리 서버 러너 팜 | 🟠 | 신청 `netlify/functions/managed-runner.ts` · 화면 `runner.html:19,61-76` · 배정 `ops-runners.ts` · **2026-09-23 재측정**: 주변은 늘었다 — 팜 집계(`netlify/functions/ops-runners.ts:82` farm 칸 managed·online·queued·claimed·oldestQueuedMin) · 배정 화면(`public/ops/runners.html:69`) · 오프라인 감시(`lib/cron/managed-runner-watch.ts:28 MANAGED_OFFLINE_MIN=30`). 🔴 **그런데 우리 서버에서 러너를 띄우는 코드는 여전히 0**(grep `spawn`·`docker`·`provision` 0건) · 라이브 `managed_runner_requests` **0행** · `runner_devices` 는 `kind=own` **17대뿐**(관리형 0대). | **팜이 실제로 돌지 않는다** — 기기는 운영자가 손으로 배정하고, 우리 서버에서 러너를 띄우는 코드·인프라는 없다 | 팜 인프라(격리 프로필·기동) | L | |
| 8.2 | `publish.naver_blog` | ✅ | `runner-jobs.ts:58` · 실측 `posts` 2행 | — | — | — | |
| 8.2 | `publish.tistory` | 🟠 | 같은 줄 · 카나리 ok · **2026-09-23 재측정**: §2.1 티스토리 행과 한 몸 — `posts` 0행 · `runner_jobs publish.tistory` failed 1건(09-14). 🔒 카카오 2FA 로그인 1회가 선결(메인 기록). | 임시저장까지만 실측(위 §2.1) | 공개 발행 1건 | M | |
| 8.2 | `publish.naver_clip` | 🟠 | `runner-jobs.ts:61` · 채널 파일은 스텁 · **2026-09-23 재측정**: 그대로 — 잡 종류는 있고(`lib/runner-jobs.ts:75`) 채널 파일은 1,295바이트 스텁 · 라이브 적재 **0건**. | 잡만 있고 못 올린다(정직하게 비재시도 실패) | 구현 | L | |
| 8.2 | `publish.brunch` | ❌ | `RUNNER_JOB_KINDS`(`:57-62`) 에 없음 · `grep -r "publish.brunch"` **0건**. 다만 **명시적 연기**다 — `runner-jobs.ts:46` 주석 «DESIGN §8.2 의 나머지(naver_clip·brunch·render.video)는 Phase 3» · **2026-09-23 재측정**(grep): `RUNNER_JOB_KINDS`(`lib/runner-jobs.ts:71-77`) 에 **여전히 없다**. 다만 «왜 안 채웠나»가 이제 코드에 적혀 있다(`lib/channel-registry.ts:155` + 같은 파일 «못 채운 칸» 주석). 🔒 작가 승인 선결이라 우리 손 밖이다. | 설계에 있는데 종류가 없다(조용한 누락은 아니고 «Phase 3» 로 적혀 있다) | P4 — 브런치는 작가 승인이 선결 | L | |
| 8.2 | `render.video` | 🟡 | `runner-jobs.ts:61` · `runner/channels/render-video.mjs`(23KB) · 실측 `scripts/verify-r5-render.mts` · **2026-09-23 재측정**: 🔴 **화면 쪽 막힘은 풀렸다** — 영상 켜기의 정본이 `tenant-settings.kinds` 가 되고 `public/app/create.html:86`·`:96-97` 이 그걸 읽어 «올릴 영상 채널이 없어도 만들어 두고 직접 올리시면 돼요»까지 말한다(영상 채널 `planned` 에 안 매인다). 라이브에 `pieces kind=video` **3행**(awaiting_manual 2 · failed 1)이 실제로 있다. 🔴 **그런데 `runner_jobs render.video` 0행** — 렌더 잡이 한 번도 적재·실행된 적이 없다(«러너 코드에 있다» ≠ «러너가 실제로 보냈다»). | 고객 화면에서 «영상 만들기»를 고를 길이 영상 채널 `planned` 때문에 닫혀 있다 | §2.2 열기 | L | |
| 8.2 | `session.login`(헤드풀 재로그인) | ✅ | `runner/channels/session-login.mjs` · 실측 job #207 done(사장님 2FA) | — | — | — | |
| 8.2 | `session.verify` | 🟠 | 종류는 있음(`:58`) · **2026-09-23 재측정**(grep): 🔴 **«못 쟀다»에서 «쟀다»로 — 결과는 «영원히 안 돈다»** 다. 종류(`lib/runner-jobs.ts:72`)·우선순위(`:90`)·러너 처리기(`runner/core.mjs:44`)·헤드풀 표시(`:100`)가 다 있는데 **그 이름으로 enqueue 하는 호출부가 0곳**이다(`lib/account-health.ts:37` 은 타입에만 있다). 라이브 `runner_jobs` 도 0행. AC-29 «만들어 두고 부르는 사람이 없는 것»과 같은 모양. | 실행 흔적을 못 찾았다 | 실측 1회 | S | |
| 8.2 | `revenue.adpost` | ✅ | 실측 `revenue_daily` 2행 | — | — | — | |
| 8.2 | `revenue.adfit` · `revenue.clip` | 🟡 | 종류·채널 파일 있음 · **2026-09-23 재측정**: 라이브 `runner_jobs` 에 둘 다 **0행** 그대로. 종류·배선·채널 파일은 실물이다(`runner/core.mjs:50-51` · 각 6KB). | 실행 0 | 실측 | S | |
| 8.2 | `verify.post_alive` | ✅ | `runner/channels/post-alive.mjs` · 3값(alive/false/unknown) 규율 `:6` | — | — | — | |
| 8.2 | (설계에 없는 것 5종) `revenue.stats`·`ads.*` 4종 | ➖ | `runner-jobs.ts:58-60` | 설계 §8.2 목록에 없다(나중에 는 것) | 설계 목록 갱신 | S | ➖ |
| 8.3 | 큐·상태기계 = 서버 단일 출처 | ✅ | `lib/runner-jobs.ts` claim/report/release · 경로 `/api/runner-queue` | 설계는 `/api/runner/queue`(슬래시) — 실제는 `-` | 설계 경로 표기 정정 | S | ➖ |
| 8.3 | 토큰은 테넌트 스코프 | ✅ | `authRunner` → `runner_devices.tenant_id` · claim 이 그 테넌트 잡만 | — | — | — | |
| 8.3 | claim 이 세션·프로필키·본문 패키지를 내려준다 | ✅ | `claimJobs` 가 자격 평문 + `browser_profile_key` + `RunnerPublishPayload` · 감사 `runner_creds_issued` | — | — | — | |
| 8.3 | report 를 믿지 않는다 — URL 확인 후 published | ✅ | `runner-jobs.ts:799 verifyPublishedUrl(externalUrl, title)` · 실패 시 `verify_not_found` | — | — | — | |
| 8.3 | 우선순위 발행>세션>수익>렌더 | ✅ | `JOB_PRIORITY`(`:68-79`) 발행 10 · 세션 20 · 수익 60 · 렌더 70 | — | — | — | |
| 8.3 | 하트비트 60초 → 온라인/오프라인 칩 | ✅ | `heartbeat` sleepSec 60/5 · 칩 `runner.html:44` · 창 5분(`ONLINE_WINDOW_MIN`) | — | — | — | |
| 8.3 | 오프라인 30분이면 due 발행을 `awaiting_runner` 로 표기 | ✅ | `fleetState`·`awaiting_runner` 경로는 있음(`publish/index.ts` runner 분기 · `accounts.html:113` 배너) · **2026-09-23 재측정**: 서버가 30분을 재고(`lib/publish/contract.ts:174` «30 이상이면 계약상 `awaiting_runner`» · `lib/video/render-queue.ts:181` · `lib/publish-one.ts:140` 이 슬롯을 `awaiting_runner` 로 옮긴다) **화면이 그 상태를 사람말로 말한다**(`public/app/schedule.html:197` «내 PC 프로그램이 꺼져 있어요 / 켜 두면 기다리던 이 글이 바로 올라가요 / 켜는 법 보기 → /app/runner.html»). 🔴 09-15 의 «고객이 임계를 모른다»는 **숫자를 박는 대신 «얼마나 꺼져 있었나»를 말하는 쪽**으로 풀렸다 — `netlify/functions/home-summary.ts:222` «40분째 응답이 없어요»(§13 «화면이 숫자를 갖지 않는다»). | **화면 어디에도 «30분»이 없다**(grep 0). 고객은 얼마나 꺼져 있어야 경고가 뜨는지 모른다 | 홈 «해야 할 일»에 임계와 함께 표기 | S | |

---

## 3. DESIGN §9 수익 통합

| § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 | 설계? |
|---|---|---|---|---|---|---|---|
| 9.0 | 애드포스트: 상태 표시 + 조건 충족 알림 | ✅ | `lib/ad-eligibility.ts:27`(글 50·방문 300) · 게이지 `ad-media.html:76` · 상태 칩 `:55` | — | — | — | |
| 9.0 | 애드센스 — 티스토리: 러너가 «수익 설정» 연동 | 🟡 | `runner/channels/ads-setup-tistory.mjs` · 잡 `ads.setup_tistory` · **2026-09-23 재측정**: 🔴 **부르는 문이 생겼다** — `public/app/ad-media.html:77` «연결» → `/api/ad-eligibility action=connect_ads` → `netlify/functions/ad-eligibility.ts:39` → `lib/ads-connect.ts:50`, 그리고 `:87` 이 `ads.setup_tistory` 를 적재한다. 09-15 의 «적재 0건»은 닫혔다. 🔴 **그런데 라이브 `runner_jobs` 에 `ads.*` 0행** — 아직 한 번도 안 돌았다. (되돌리기가 없는 길이라 «확인»으로만 말한다 — `ads-connect.ts:62-69`.) | 실행 0 — 실측한 적 없다 | 실측 1회 | M | |
| 9.0 | 애드센스 — 블로거: 글 단위 `.ad-slot` + 수익 탭 상태 읽기 | 🟡 | `runner/channels/ads-setup-blogger.mjs`·`ads-status-blogger.mjs` · `lib/publish/gate.ts` ad-slot · **2026-09-23 재측정**: 같은 문(`lib/ads-connect.ts:88` → `ads.setup_blogger` / `ads.revert_blogger`). 라이브 실행 **0**. 블로거 계정 자체가 `pending_login` 1개뿐이고 🔒 구글 앱 키가 없다(§2.1). | 실행 0 | 실측 | M | |
| 9.0 | 애드센스 — WP: `wp/v2/widgets` 사이드바 위젯 + 되돌리기 | 🟡 | `lib/publish/ads.ts:5,46`(위젯 백업·DELETE 되돌리기 · 남의 위젯 안 지운다) · **2026-09-23 재측정**: 🔴 **«export 만 되고 호출 0건»이 풀렸다** — `lib/ads-connect.ts:72-74` 가 `insertWordpressAdWidget`/`removeWordpressAdWidget` 를 실제로 부른다(`adsWayOf` 가 wordpress → `wp_widget` · 즉시 결과). 🔴 라이브에 **워드프레스 계정이 0개**라 실행 0(§2.1 과 같은 자리 — 붙일 수 있는데 안 붙였다). | 실행 0 — 실제 WP 사이트에 안 걸어 봤다 | 실측 | M | |
| 9.0 | YPP: 진행률 게이지 + «신청하러 가기» | ✅ | `ad-eligibility.ts:28`(구독 1,000·조회 1,000만) · 게이지 `ad-media.html:92` · 링크 `:34` | 값은 채널 OAuth 로 채워야 하는데 유튜브 연결이 막혀 있다(§2.2) → 게이지가 늘 0 | §2.2 열기 | M | |
| 9.0 | 클립: 모집 일정 알림 + 조건 게이지 | 🟠 | 모집창 `ad-eligibility.ts:53-56`(`channel_registry.monetize.clipOpen` · 운영자 입력) · **2026-09-23 재측정**(라이브 `jsonb_typeof`): 🔴 **09-15 가 후하게 쟀다 — «운영자가 안 넣었다»가 아니라 «넣을 수가 없다»다.** ①운영 화면 `public/ops/channels.html:51` 에는 수익 매체 **칩(배열)**만 있고 모집창(from·to) 입력칸이 **없다** ②`netlify/functions/ops-channels.ts:58` 은 `monetize` 를 **배열로 통째 덮어쓴다** ③라이브 `jsonb_typeof(monetize)=array` 라 `lib/ad-eligibility.ts:57` 의 `monetize->'clipOpen'` 은 **언제나 NULL** ⇒ `:152` D-7 «모집이 시작해요» 알림이 **구조적으로 안 뜬다**. 게이지는 그려지되 늘 «아직 모집 전이에요»(`ad-media.html:156`). ⚠️ 라이브가 깨진 건 아니다(오류 0 · 조용히 안 뜰 뿐). | 운영자가 넣어야만 뜨고, 넣은 적 없다 | 운영 입력 + 알림 실측 | S | |
| 9.0 | 쿠팡: 키 등록 → 링크 자동 생성(subId=piece) → 고지 삽입 | ✅ | 키 UI `ad-media.html:36` · `affiliate-coupang.ts` · 고지 `lib/disclosure.ts`(§4.7 게이트) | 수익 회수까지는 미실측(§2.3) | — | — | |
| 9.0 | 알리·링크프라이스: 키 등록 → 링크 생성·회수 | 🟠 | 키 UI 있음(`KEYED`) · 회수 코드 있음 · **2026-09-23 재측정**: §2.3 «제휴 링크 전부 subId» 행과 한 몸 — 회수(`syncAliexpress`·`syncLinkprice`)만 있고 **링크 생성 0**. 라이브 `affiliate_links` 0행. | **링크 «생성»이 없다** — 회수만 있고 제휴 링크를 만드는 코드가 없다 | 링크 생성 + subId | M | |
| 9.0 | 매체별 «한 화면 한 단계» 가이드 + «가입 완료했어요» | ✅ | `ad-media.html:122-131 howSheet()` · 3단계 문구 `:28-34` · 체크 `claimSheet() :133-148` | — | — | — | |
| 9.0 | Agency 원격접속으로 셋업 대행 | ✅ | 임퍼소네이션 자체는 있음(`lib/auth.ts signImpersonationToken`) · **2026-09-23 재측정**: 🔴 **이건 키가 막은 게 아니었다 — 잘못 붙은 🔒 다.** 대행 흐름이 화면에 다 있다: `public/ops/tenant.html:19` «셋업 체크리스트» + `:54` **네 칸이 설계 문장 그대로**(① 계정 연결 ② 수익 매체 ③ 편성 규칙 ④ 첫 발행 — `docs/DESIGN.md:1062`) · 판정은 서버가 한다(`netlify/functions/ops-tenants.ts:118-119 setup.adMedia`) · 원격접속 단추 `tenant.html:21` → `:67` 시트(«결제 경로는 막히고 전부 감사에 남아요 · 60분 상한 · 끝내면 고객에게 알림») → `/api/ops-impersonate`. | 수익 매체 셋업을 대행하는 **전용 흐름은 없다**(일반 원격접속뿐) | 대행 체크리스트 화면 | M | |
| 9.1 | 애드센스 일 1회 | ✅ | `lib/cron/revenue-sync.ts:30 revenueSyncStep` · 우산 `cron/runner.ts:59` | 실회수 0(§2.3) | — | — | |
| 9.1 | 유튜브 애널리틱스 일 1회 | ✅ | 같음 · `lib/revenue/youtube.ts` | 실회수 0 | — | — | |
| 9.1 | 쿠팡 일 1회 **13:00** | ✅ | `revenue-sync.ts` 주석 «hourly(06:00 KST · 쿠팡 13:00)» | — | — | — | |
| 9.1 | 알리·링크프라이스 일 1회 | ✅ | `API_SOURCES`(`revenue/index.ts:25`) 에 포함 | 실회수 0 | — | — | |
| 9.1 | 애드포스트·애드핏·클립 = 러너 일 1회 | ✅ | 잡 3종 · 실측 adpost 2행 | adfit·clip 미실측 | — | — | |
| 9.1 | 기타 = 수동 입력 | ✅ | 수동 시트 `revenue.html:161-189` · 실측 12행 | — | — | — | |
| 9.2 | `revenue_daily` 필드 전부 | ✅ | `drizzle/0001-init.sql:442-456` — tenant·source·account·piece·day·amount_krw·currency·fx_rate·freshness·raw 전부 있음 | — | — | — | |
| 9.2 | 유니크 `(tenant, source, account, piece, date)` | ✅ | `:455 UNIQUE (tenant_id, source, account_id, piece_id, day)` | — | — | — | |
| 9.3 | 홈 큰 숫자 = 오늘 번 돈 · **확정/예상 분리** | ✅ | `home.html:58`(확정 진하게 · 예상 회색) · `:59` 카운트업은 확정값만 · `types.ts:24 CONFIRMED_SOURCES` | — | — | — | |
| 9.3 | «어제보다 ±N원» | ✅ | `public/app/home.html:63` — `이번 달 … · 어제보다 <b>+N원</b>`(증가만 초록) · 어제 전체 합과 비교 | — | — | — | |
| 9.3 | 수익 탭: 이번달 총합 → 소스별 → 계정별 → 글/영상별 TOP 5 | ✅ | `revenue.html:132-153`(어디서/어느 계정이/잘 번 글) · 5개 제한은 서버(`mock.js:605 slice(0,5)`) | — | — | — | |
| 9.3 | 신선도 배지 | ✅ | `revenue.html:138 UI.freshPill` | 절대시각 없음(§2.3 참조) | — | — | |
| 9.3 | «수익 나는 소재 학습» — piece 수익을 topic factor 로 | ✅ | `lib/cron/learn.ts:177 revenueByTopic()` | 되먹일 수익 데이터가 수동 12행뿐이라 사실상 안 돈다 | 수익 회수가 돌면 자동 | — | |

---

## 4. 이 조사에서 실측한 것(증거)

| 무엇 | 결과 |
|---|---|
| `channel_registry` 라이브 10행 | active 4(naver_blog·tistory·blogger·wordpress) · planned 6(threads·instagram·youtube_shorts·naver_clip·reels·tiktok) |
| `posts` 발행 성공 행 | **`naver_blog` / `published_via=runner` 2건뿐** — 그 외 전 채널 0 |
| `revenue_daily` | `sponsor`(manual) 8 · `meta`(manual) 4 · `adpost`(runner) 2 — **API 소스 0행** |
| `canary_runs` | tistory ok=true(09-14·09-15) · naver_blog ok=true(09-14) |
| 러너 배포 | `scripts/verify-runner-dist.mts` **28/28 통과** · R2 `autocreate/runner/v1.1.4.zip` sha256 `53ac918c…` |

- 사용한 테넌트: `러너배포실증`(key `runner-dist-verify` · trial) · `B2실증` 189(trial). **보존 4집(3·13·109·116) 무접촉.**
- 조사가 만든 행: 없음(읽기만). 이 조사로 인한 발행 0건.

## 5. R7 제안 묶음(B2 영역 몫)

| 묶음 | 내용 | 크기 합 |
|---|---|---|
| **A. 영상 축을 고객 앞에 연다** | registry `youtube_shorts`→active · OAuth 앱 키 · 발행 1건 실측 · `render.video` 를 만들기 화면에서 고르게 · 쿼터 게이트 | L+L+S |
| **B. «한 번도 안 지나간 길» 첫 통과** | 티스토리 공개 발행 · 블로거·워드프레스 발행 각 1건 · API 수익 5종 중 최소 2종 첫 회수(쿠팡 subId 귀속 대조 포함) | M×5 |
| **C. 정직 표기 보수** | 신선도 절대시각 · «30분 오프라인» 임계 표기 · 홈 «어제보다» · DDL 상태 어휘 주석 · 설계 §2/§8.1/§8.2/§8.3 문장 정정(➖ 5건) | S×6 |

---

# 6. 🔴 2026-09-23 재측정(R16) — 511 커밋 뒤에 같은 표를 다시 쟀다

> 재측정자 B2 · 브랜치 `audit/2026-09-23-B2` · 방식 = `docs/active/2026-09-23-RE-AUDIT-trigger.md`.
> **코드 0줄 고쳤다**(트리거 규칙 ⑥ «재는 판»). 라이브는 **SELECT 만** — INSERT·UPDATE·DELETE 0. 보존 4집(3·13·109·116) 무접촉.

## 6.1 모수 — 🔴 **44** 다(메인의 43 이 아니다)

| | 09-15 | **09-23** |
|---|---|---|
| 총 판정 단위 | 80 | **80**(안 흘렸다) |
| ✅ | 33 | **40** |
| 🟡 코드만 · 라이브 0회 | 19 | 23 |
| 🟠 있지만 안 됨 | 11 | 12 |
| ❌ 없다 | 12 | **2** |
| 🔒 · ⬜ | 1 · 1 | 0 · 0 *(둘 다 다시 재서 갈랐다 — 6.4)* |
| ➖ 설계 변경 | 3 | 3 |
| **열린 행**(🟡🟠❌🔒⬜) | **44** | **37** |

**세는 법**: 상태는 «줄»이 아니라 **«칸»**에 있다 — 표 각 행의 **네 번째 칸**만 센다. 항목 이름에 이스케이프된 파이프가 있으면 칸이 밀리므로, 그것을 먼저 다른 글자로 바꿔 두고 자른다.

🔴 **메인의 43 에서 빠진 한 행을 찾았다**: 28줄 «채널 상태 어휘» 행 — 항목 이름 안에 이스케이프된 파이프가 **셋** 있어 칸이 밀리고, 네 번째 칸이 상태 기호가 아니라 `beta\` 로 읽힌다. B 가 B1 파일에서 겪은 것과 **같은 자리**다.

🔴 **합이 파일 제 요약과 맞는다**: 33+19+11+12+3+1+1 = **80** — §0-1 이 적어 둔 «총 80» 과 한 개도 안 틀렸다. ⇒ 행을 안 흘렸다는 증거다.

## 6.2 다섯 줄

```
■ 모수: 열린 행 44개 (칸으로 세어서 · 파일 제 요약 80 과 맞춘다)
■ ✅ 닫혔다: 7개   (전부 «파일:줄» 근거)
■ 아직 열림: 37개  (그중 🔒 18 · 우리 손 19)
■ 🔴 더 나빠졌다: 0개
■ ⊘ 못 쟀다: 0개 — 판정을 못 내린 행은 없다. 다만 «모르는 것» 3가지는 6.5 에 적었다
   7 + 37 + 0 + 0 = 44 ✓
   └ 🔵 반만 왔다: 5개 — 🔴 위 37 안에 들어 있다(합에 안 더했다)
```

### ✅ 닫힌 7행

| 줄 | 항목 | 09-15 | 근거 |
|---|---|---|---|
| 27 | 코드 정본 `lib/channel-registry.ts` | ❌ | `lib/channel-registry.ts:131` 17행 · `lib/accounts.ts:12` 가 읽는다 · `scripts/verify-channel-tables.mjs` 실행 ✅ |
| 28 | 채널 상태 어휘 | 🟠 | `drizzle/0001-init.sql:206` 주석 정정 + `drizzle/0014-r7-channel-status.sql:15` CHECK 강제 |
| 56 | 텐핑·애드픽 | ❌ | 설계가 «수동만»으로 정정(`docs/DESIGN.md:103-105`) · `lib/revenue/types.ts:20`·`:43-44` → `revenue.ts:105` → `revenue.html:244` |
| 57 | 네이버 쇼핑커넥트 | ❌ | 같음(`lib/revenue/types.ts:45`) |
| 69 | 내 PC 러너 = 전 플랜 | 🟠 | `netlify/functions/runner.ts:136` + `lib/plans.ts:42`·`:45`·`:48`·`:51` 네 플랜 전부 ≥1 · 화면 `public/app/runner.html:34` |
| 90 | 오프라인 30분 → `awaiting_runner` | 🟠 | 서버 `lib/publish/contract.ts:174`·`lib/publish-one.ts:140` · 화면 `public/app/schedule.html:197` · 홈이 «40분째 응답이 없어요»(`netlify/functions/home-summary.ts:222`) |
| 107 | Agency 원격접속 셋업 대행 | 🔒 | `public/ops/tenant.html:19`·`:54` 네 칸이 설계(`docs/DESIGN.md:1062`) 그대로 · `netlify/functions/ops-tenants.ts:118` · 원격접속 `tenant.html:21`→`:67` |

### 🔵 반만 왔다 5행 — ❌ 였는데 **커넥터가 생겼다**

`35` 인스타 피드·카드뉴스(`publishInstagramFeed`) · `36` 페이스북 페이지(`publishFacebookPost`) · `37` X(`publishToX`) · `46` 틱톡(`publishToTiktok`) · `47` 페북 릴스·유튜브 롱폼·X 영상(셋 다).
전부 `lib/publish/index.ts:60-68` 에 배선돼 있다. 🔴 **다만 다섯 다 «남은 것이 앱 키»라 다음 판에서 싼 목록이 아니다** — 우리가 지금 할 수 있는 싼 목록은 6.3 의 🟢 쪽이다.

## 6.3 🔴 열린 37행을 **두 무더기로 갈랐다**(이 판의 핵심)

### 🔒 우리 손 밖 — **18행** · 사장님/외부가 움직여야 다음 걸음이 있다

| 사장님·외부가 할 일 **한 가지** | 그게 푸는 행 |
|---|---|
| **구글 OAuth 앱 1개** | 31 블로거 · 40 유튜브 쇼츠 · 47 유튜브 롱폼 · 48 애드센스 · 51 유튜브 파트너 · 100 애드센스(블로거) — **6행** |
| **메타 앱 1개** | 35 인스타 피드 · 36 페이스북 페이지 · 44 인스타 릴스 · (47 페북 릴스) — **3행 +** |
| **Threads 앱 1개** | 34 쓰레드 글 · 45 쓰레드 영상 — **2행** |
| **틱톡 앱 1개** | 46 틱톡 — **1행** |
| **X 앱 1개(+유료 플랜)** | 37 X · (47 X 영상) — **1행 +** |
| **티스토리 로그인 1회(카카오 2FA)** | 30 티스토리 발행 · 74 `publish.tistory` · 99 애드센스(티스토리) — **3행** |
| **브런치 작가 승인 1건** | 38 브런치 채널 · 76 `publish.brunch` — **2행** |

🔴 **앱 키 다섯 개와 로그인 두 번이 18행을 푼다.** 그중 **구글 하나가 6행**으로 제일 크다.

### 🟢 우리 손 — **19행** · 키도 심사도 없이 지금 할 수 있다

| 줄 | 항목 | 왜 우리 손인가 |
|---|---|---|
| **32** | **워드프레스 발행** | 🔴 **앱 비밀번호 채널이라 우리 앱 키가 필요 없다** — registry `active` + `providerMissing=null` ⇒ **`connectable=true`. 지금 당장 붙일 수 있는데 계정이 0개다.** 이 영역에서 **제일 싼 «첫 통과»** |
| **101** | 애드센스 — WP 사이드바 위젯 | 위와 한 몸(WP 계정이 생기면 바로 실행) · 부르는 문은 이미 있다(`lib/ads-connect.ts:72-74`) |
| 33 | 네이버 클립 게시물형 | 업로드 경로 1회 실측 |
| 43 · 75 | 네이버 클립 러너 | 1,295바이트 스텁 구현 |
| 50 · 52 · 81 | 애드핏·클립 수익 스크랩 | 코드·배선 다 있다 — **실행 1회** |
| 53 · 54 · 55 | 쿠팡·알리·링크프라이스 회수 | **맞는 키 하나로 1일치 회수 1회**(고객 키라 시험 키로 된다) |
| 60 | 신선도 절대시각 | 화면 한 줄 |
| 61 · 105 | 알리·링크프라이스 **링크 생성** | 쿠팡(`lib/affiliate-coupang.ts:65`)을 본떠 두 곳 |
| 71 | 러너 v2 트레이 | 소스에 0건 |
| 72 | 관리형 러너 팜 | 기동 코드가 0(신청·배정·감시는 다 있다) |
| 77 | `render.video` | 🔴 **화면 막힘은 풀렸는데 잡이 한 번도 안 적재됐다** |
| 79 | `session.verify` | 🔴 **부르는 곳이 0곳** |
| 103 | 클립 모집 알림 | 🔴 **모양이 어긋나 있다**(아래 6.4-③) |

## 6.4 🔴 이번에 «다시 재서» 뒤집힌 것 넷

**① 「유튜브 커넥터 자체가 없다」는 사실이 아니다** *(메인이 B 실측이라며 전한 말 — 내가 직접 쟀다)*
`lib/publish/youtube.ts` 는 **20,435바이트로 존재**하고 `lib/publish/index.ts:57-58` 에 쇼츠·롱폼 둘 다 배선돼 있다.
«합성 콘텐츠 플래그»도 있다 — `lib/publish/youtube.ts:181` 이 `containsSyntheticMedia` 를 참으로 보낸다. 심사 전 비공개 고정(`:171`)과 일일 쿼터(`:52`·`:150`)도 그대로다.
⇒ 🔴 **«유튜브 커넥터 · OAuth · 합성 콘텐츠 플래그» 세 행은 한 덩이가 아니다.** 커넥터와 플래그는 **이미 있고**, 남은 것은 **구글 앱 키 하나뿐**이다.

**② 라이브 `youtube_shorts` 계정 4개는 실연결이 아니다** — handle 이 `c_r5_yt_mu11brby` 꼴인 **R5 시험 하니스 산물**(전부 09-14 생성). «라이브에 계정이 있다»로 세면 «유튜브가 연결돼 있다»는 거짓이 된다(트리거 ④ 대용물 — «라이브에 초안 4건»과 같은 자리).

**③ 클립 모집 알림은 «운영자가 안 넣은 것»이 아니라 «넣을 수 없는 것»이다** — 09-15 가 후하게 쟀다.
운영 화면(`public/ops/channels.html:51`)에는 모집창(from·to) 입력칸이 **없고**, `netlify/functions/ops-channels.ts:58` 은 `monetize` 를 **배열로 통째 덮어쓴다**. 라이브에서 `jsonb_typeof(monetize)` 가 `array` 라 `lib/ad-eligibility.ts:57` 이 읽는 `clipOpen` 은 **언제나 NULL** ⇒ `:152` D-7 «모집이 시작해요» 알림이 **구조적으로 안 뜬다**.
⚠️ **라이브가 깨진 것은 아니다** — 오류 0, 조용히 안 뜰 뿐이다(그래서 이 판에서 고치지 않았다).

**④ `session.verify` 는 «못 쟀다»가 아니라 «영원히 안 돈다»** — 종류(`lib/runner-jobs.ts:72`)·우선순위(`:90`)·러너 처리기(`runner/core.mjs:44`)·헤드풀 표시(`:100`)가 다 있는데 **그 이름으로 잡을 만드는 호출부가 0곳**이다(`lib/account-health.ts:37` 은 타입에만 있다). ⬜ → 🟠. AC-29 «만들어 두고 부르는 사람이 없는 것»과 같은 모양.

## 6.5 ⊘ 내가 **모르는 것** — 행 판정은 안 바뀌지만 적어 둔다

1. ~~**프로덕션 넷리파이 env 에 OAuth 앱 키가 실제로 꽂혀 있나**~~ → 🔴 **[2026-09-23 · 메인이 재서 닫았다 · 내가 잰 게 아니다]** 라이브 env **35개 중 채널 관련 이름은 5개뿐**이고 전부 네이버(`NAVER_OPENAPI_CLIENT_ID`·`_SECRET` · `NAVER_SEARCHAD_ACCESS_LICENSE`·`_CUSTOMER_ID`·`_SECRET_KEY`) — **키워드·트렌드 조회용이지 발행용이 아니다.** `GOOGLE_OAUTH_*`·`META_APP_*`·`THREADS_*`·`TIKTOK_*`·`X_CLIENT_*` 은 **이름조차 하나도 없다.** ⇒ **🔒 18행은 안 줄어든다.**
   > 🔴 **이 자리에서 배운 것 — «없음»은 결론이 되고 «있음»은 안 된다(비대칭).**
   > AC-172 가 말하는 마스킹은 **값**에 걸리지 **이름**에 안 걸린다. 그래서 **이름이 없으면 키도 없다**는 결론은 성립한다.
   > 반대는 성립하지 않는다 — **이름이 있어도 «쓸 수 있는 키»라고는 못 한다**(값이 빈 문자열일 수도, 만료됐을 수도, 남의 프로젝트 것일 수도 있다 · AC-170).
   > ⇒ 내가 «못 쟀다»고 접어 둔 것은 옳았지만, **재는 방법이 있었다**(이름만 보는 것). 다음에 마스킹된 자료를 만나면 **«값이 아니라 이름으로 답할 수 있는 질문인가»를 먼저 묻는다.**

   남은 원래 셈: 위 🔒 18행은 ①코드가 키를 요구한다(`lib/oauth-providers.ts:44-49`) ②라이브 `channel_registry` 가 그 채널들을 전부 `planned` 로 두고 있다(**내가 쟀다**) ③env 에 이름이 없다(**메인 실측**) — 이제 셋 다 같은 쪽을 가리킨다.
2. **티스토리 코드를 네 번 고친 것이 실제로 통하나** — 못 쟀다(카카오 2FA 로그인이 있어야 재 본다). «한 번도 성공 못 했다»는 쟀고(`posts` 0행), «이제는 될까»는 못 쟀다.
3. **쿠팡 subId=piece 귀속이 맞나** — 이 설계의 핵심 주장인데 못 쟀다. 맞는 키로 성공한 회수가 **0회**이고 `affiliate_links` 도 **0행**이라 대조할 자료가 없다.

## 6.6 이번 판에서 **고치지 않고 적어만 둔 것**(표 밖 · 다음 판 몫)

- 🔴 **`publish.daangn` 은 서버가 만들 수 없는 잡이다.** `lib/channel-registry.ts:153` 이 그 잡 이름을 말하고 `runner/core.mjs:61` 에 처리기까지 있는데, `lib/runner-jobs.ts:110 PUBLISH_JOB_KINDS` 가 **세 개뿐**(`naver_blog`·`tistory`·`naver_clip`)이라 `publishJobKindOf("daangn")` 이 **항상 `null`** 이다. ⇒ 당근은 «키 꽂으면 즉시 가동»이 **아니다**. (정직하게 막히므로 라이브 사고는 아니다. `scripts/verify-channel-tables.mjs` 는 이 칸을 안 본다.)
  *당근은 2026-09-17 에 늘어난 채널이라 위 80행 표에 **행이 없다** — 그래서 모수에 안 더했다.*
- `scripts/verify-api-channels.mts` 는 `.env` 를 셸로 먼저 주입하지 않으면 `read ECONNRESET` 으로 **가짜 빨강**이 난다(`scripts/_lib/load-env.mjs` 머리말이 설명하는 그것). 주입하고 돌리면 ✓ 통과한다(블로거·워드프레스 «자격 없음» 정직 경로 · 발행 0건).

## 6.7 이번 재측정에서 쓴 라이브 수(전부 SELECT)

| 무엇 | 09-15 | **09-23** |
|---|---|---|
| `channel_registry` | 10행(active 4 · planned 6) | **17행** — active 는 **여전히 같은 4개**(naver_blog·tistory·blogger·wordpress) · planned 13 |
| `posts` 발행 성공 | `naver_blog` 2 | **`naver_blog` 5**(그 외 전 채널 **여전히 0**) |
| `pieces kind=video` | (안 쟀다) | **3행**(awaiting_manual 2 · failed 1) — **발행 0** |
| `revenue_daily` | 14행(sponsor 8 · meta 4 · adpost 2) | **14행 — 한 행도 안 늘었다.** API 소스 **여전히 0행** |
| `affiliate_links` | (표 없음) | 표는 생겼고 **0행** |
| `revenue_sources` 실제 키가 들어간 행 | — | **쿠팡 1건**(tenant 55) — `status=error` 401 · `fail_count=3` · `last_ok_at` 없음 |
| `runner_jobs` 돈 적 있는 종류 | — | `publish.naver_blog` · `publish.tistory`(failed) · `revenue.adpost` · `revenue.stats` · `session.login` · `verify.post_alive` **6종뿐** |
| `runner_jobs` **0행인 종류** | — | `session.verify` · `revenue.adfit` · `revenue.clip` · `ads.*` 4종 · `render.video` · `publish.naver_clip` · `publish.retract` · `reference.capture` |
| `runner_devices` | — | `own` **17대** · 관리형 **0대** |
| `managed_runner_requests` | — | **0행** |
| `accounts` | — | naver_blog 78(active 32) · tistory 10(전부 pending_login) · blogger 1(pending_login) · youtube_shorts 4(**시험 산물**) · **wordpress 0** |

🔴 **한 줄 결론(09-15 과 같고, 더 날카로워졌다)**: 8일 동안 **코드는 크게 늘었는데**(채널 10→17 · API 커넥터 5→11 · ❌ 12→2) **«지나간 길»은 거의 그대로다** — 발행은 네이버 3건 늘었고 수익은 **한 행도 안 늘었다**.
그리고 이제 **왜 그런지가 갈린다**: 열린 37 중 **18은 사장님 손**(앱 키 5개 + 로그인 2번 + 브런치 승인)이고 **19는 우리 손**이다.
우리 손 19 중 **제일 싼 것은 워드프레스**다 — 키도 심사도 필요 없고 서버가 이미 «붙일 수 있다»고 말하는데, **여덟 달 동안 계정이 0개**다.
