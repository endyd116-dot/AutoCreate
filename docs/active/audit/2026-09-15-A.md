# A 조사 — 시안 v4 화면 목록 ↔ 실제 화면(`public/app/*.html`) · 2026-09-15

> 담당 A(화면) · 폴더 `AutoCreate-A` · 브랜치 `feature/p1r6-front`(main `5582363` 위) · **조사만 — 코드 수정 0**.
> 발주: 메인 «`docs/screens-v4.html` 시안의 화면 목록 ↔ `public/app/*.html` 실제 화면을 표로(시안에 있는데 없는 화면·요소 · 시안과 다른 곳 · 헌장 위반 남은 곳)». 설계 전수조사(`2026-09-15-DESIGN-AUDIT-trigger.md`)의 새 B 조사에 붙이는 A 몫 1건.
> 증거: 파일:줄 · 모의 화면 경로(`?mock=1`) · 스샷 `_shots/audit-a-*.png`(AutoCreate-A 워크트리 · git 밖). 상태 어휘는 트리거 §1 그대로(✅ 🟡 🟠 🔒 ❌ ➖ ⬜).
> 🔴 **라이브는 재지 않았다** — A 는 DB·라이브 계정이 없다. 아래 «모의» 는 `public/js/mock.js` 의 모의 응답이고, 라이브에서 값이 다를 수 있는 곳은 ⬜ 로 적었다.

> ## 🔴 2026-09-23 · R16 «다시 재기» 재측정 (A · 브랜치 `audit/2026-09-23-A`)
>
> **아래 09-15 본문은 그대로 둔다.** 상태 글자만 고쳤고 각 행 증거 칸에 «2026-09-23 재측정: …»을 덧붙였다. 세부는 **§7**.
>
> | | |
> |---|---|
> | **모수(열린 행)** | **8** — 표 7행(`23·35·43·44·45·52·68`줄) + 산문 1행(37줄 완료 체크 900ms) |
> | ✅ 닫혔다 | **5** (전부 «파일:줄» 근거) |
> | 아직 열림 | **3** (서로 다른 결함으로는 **2**) |
> | 🔴 더 나빠졌다 | **0** |
> | ⊘ 못 쟀다 | **0** |
> | 🔵 반만 왔다 | **0** |
>
> 🔴 **모수를 내가 다시 센 까닭**: 상태는 «줄»이 아니라 «칸»에 있다. 칸으로 셌고(`_shots/count-a.py` · `\|` 이스케이프를 지켜서 쪼갬) **네 표의 칸 수가 모두 고르다**(§1=7 · §2=6 · §3=4 · §4=4) — B1 처럼 **밀린 행은 없다.**
> 그런데 🔴 **이 파일 제 §0 요약(6)과 내 8 이 달랐다.** 흘린 게 아니라 **세는 자가 달랐다** — 맞춰 보면 딱 맞는다:
> **6** −1(§0 은 터치를 «2곳»으로 쪼개 세는데 표는 1행) +2(§1 #4·#16 은 §2 와 **같은 결함의 대응표 쪽 자리** — 행은 둘) +1(**완료 체크 900ms** · §0 이 «말·모양만 다른 곳»으로 분류해 🟠 로 안 셌다) = **8** ✓

## 0. 한 장 요약 — 🔴 **2026-09-15 당시의 값이다(그대로 둔다)**

> 이 절의 🟠 는 **09-15 의 상태**다. 지금 값은 바로 위 표와 §7 을 봐라.
> 이 절이 센 **6**(시안 요소 3 + 결함 1 + 헌장 2곳) 중 🔴 **다섯이 닫혔다** — 4타일 · 온보딩 칩 · `${UI.chev}` · 탭 44 · 달력 44.
> 남은 것은 **운영센터 마진 줄 하나**뿐이다. (§0 이 🟠 로 안 세던 **완료 체크 900ms** 도 아직 열려 있다 — 54줄.)

- 시안 v4 = **16장**(고객 폰 11 + R5 폰 4 + 운영센터 데스크 1) + 모션 규칙 6줄. 16장 모두 대응하는 실제 화면이 **있다**(❌ 화면 0). 대응 화면은 §1 표.
- 🟠 **시안 요소가 실제엔 없는 곳 3**: ① 계정 연결 격자의 준비 중 채널 4타일(쓰레드·인스타그램·네이버 클립·릴스 — `status ≠ active` 면 숨김 · c91b73b 결정) ② 온보딩 첫 단계 «영상» 타일의 «곧 열려요» 칩(영상 채널이 하나도 active 가 아니면 붙는다) ③ 운영센터 매출 fx 없음 때 «마진 줄 사라짐»(실제는 줄이 남고 문장). ①②는 **레지스트리 값 하나가 영상 축 입구를 통째로 좌우한다** — 라이브 `channel_registry` 의 영상 채널 status 는 ⬜ 미실측(새 B 특별 항목 a 와 직결).
- 🟠 **실제 화면의 눈에 보이는 결함 1**: 만들기 «레퍼런스로 구조 배우기» 행 오른쪽에 글자 **`${UI.chev}`** 가 그대로 찍힌다(`public/app/create.html:18` — HTML 부분에 JS 템플릿 식을 적었다 · R5 A 실수).
- 🟠 **헌장 위반 남은 곳 2(하니스가 제외해 초록이던 것)**: 하단 탭 항목 눌림 영역 **56×42**(44 미만 · `ac.css:128`) · 달력 날짜 칸 **38px 높이**(`ac.css:184` · 가로만 불가하고 세로는 44 가능). 그 밖 Primary 1 · 큰 숫자 1 · 이모지 0 · 시스템 용어 0 은 21화면 전부 통과(모의).
- **시안과 말·모양만 다른 곳 6**(§3): 손보기 Primary «이대로»→«적용 · N코인» · 만든 글 기본 보기(혼합 목록 → 세그먼트 하나) · 내 PC 행의 버전(➖ 메인 지시 09-15) · 완료 체크 900ms(시안 400ms) · 릴스 «처리 중» 그룹 위치 · 발행함 «어제 · N건» 개수 표기. **시안이 없는 실제 화면 8**(§5): 온보딩(v3 만) · 만들기 · 디렉터 전체(v3 만) · 요금제 · 코인 · 문의 · 내 계정 허브 · 가입/로그인/비밀번호 — 시안 없이 헌장으로만 만든 화면이라 새 B 여정 조사에서 우선 눌러 볼 곳.

## 1. 대응표 — 시안 v4 16장 ↔ 실제 화면

| # | 시안 v4 화면 | 실제 화면(경로) | 상태 | 시안 요소 중 있는 것 / 없는 것 | 시안과 다른 곳 | 증거 |
|---|---|---|---|---|---|---|
| 1 | 홈 | `/app/home.html` | ✅ | 문장형 헤드라인 «오늘 N원 벌었어요»+회색 «예상»+«이번 달·어제보다» ✓ · 할 일 = 제목 없이 행 ✓ · «오늘 편성 · 전체 ›» ✓(→schedule) · 자동 편성 토글 행 ✓ / 없는 것 0 | 실제가 더 있음: 장애 배너(R4 `?incident=1`) · 자동 편성 꺼짐 한 줄(09-15) | `home.html:85` · `_shots/audit-a-home.png` |
| 2 | 편성표 | `/app/schedule.html` | ✅ | 헤드라인 «9월 이번 주 N건 · 오늘 N건 나가요» ✓ · 세그 2주/월 ✓ · 달력 점 ✓ · 날짜 그룹 «15일 화요일» ✓ · 필 ✓ / 없는 것 0 | 실제가 더 있음: «영상» 필+«60초» · «이번엔 건너뛰어요»(서버 skipReason) · 꺼짐 배너 | `_tpl.txt` schedule 블록 · `audit-a-schedule.png` |
| 3 | 수익 | `/app/revenue.html` | ✅ | 월 선택 ✓ · 잉크 큰 숫자 ✓ · 날마다 막대+빗금 범례 «0원이 아니에요» ✓ · «어디서 · 관리» 행+필(직접 입력/자동/내 PC) ✓ / 없는 것 0 | 실제가 더 있음: 공유 카드(R6) · 어느 계정이 · 잘 번 글 · 직접 받은 수익 넣기 | `audit-a-revenue.png` |
| 4 | 계정 연결 | `/app/accounts.html` | ✅ | 흰 페이지+회색 타일 ✓ · «N개 연결됨» ✓ · 연결 목록 «오늘 0/2 · 건강 100 · 정상» ✓ · 풀린 계정 «다시 로그인» 버튼 ✓ / **없는 것: 준비 중 채널 4타일(쓰레드 유입 · 인스타그램 제휴 · 네이버 클립 클립 인센티브 · 릴스 제휴)** — 시안 9타일, 실제 모의 5타일 | `status !== "active"` 면 숨긴다(연결 계정 있으면 남김) — 결정 c91b73b(2026-09-14 ui-v4-trim) «채널이 열리면 자동으로 나타난다». 시안 v4 는 그 결정 뒤에도 9타일 그대로 → **시안과 코드가 어긋난 채** | `_tpl.txt` accounts 블록 18행 · `lib/accounts.ts:84`(레지스트리 비면 전부 planned) · 라이브 status ⬜ · `audit-a-accounts.png` · **2026-09-23 재측정(A 실측 · ✅): 숨기는 필터가 없어졌다.** `public/app/accounts.html:56` 은 이제 `.filter((c) => c.status !== "down" \|\| cnt[c.key])` — **내려간 채널만** 뺀다. 나머지는 `soon()`(`:59`)로 갈라 «곧 열리는 채널 N개» 줄(`:69`)에 **접어** 두고, 누르면 그대로 펴진다(`:71`). 🔴 **라이브 17행을 그대로 먹여서 그려 봤다**(`_shots/reaudit-a-live17.mjs` · route 로 `/api/accounts-list` 가로채기 · 제품 코드 무수정): 첫 화면 3타일(naver_blog·tistory·wordpress) + 접힘 14 = **17/17 이 화면에 닿는다 · 안 나오는 채널 0**. 시안의 그 4타일(threads·instagram·naver_clip·reels) **전부 있다** |
| 5 | 내 PC 프로그램 | `/app/runner.html` | ✅ | 헤드라인 문장 ✓ · 기기 행 ✓ · 왜 필요한가요 3줄(+영상 1줄) ✓ · 하단 «내 PC에서 켜기» 1개 ✓ / 없는 것: 행의 «버전 1.0.3» | 버전은 상세 시트 작은 글씨로(➖ 메인 지시 09-15 · 4a7c45e) · 실제가 더 있음: PC 없이 쓰기(R6) · 다른 PC 경고 필 | `audit-a-runner.png` |
| 6 | 올라간 글 | `/app/posts.html` | ✅ | «직접 올려야 해요»가 맨 위 그룹 ✓ · 세그 3 ✓ · 오늘/어제/날짜 그룹 ✓ · 우측 시각+외부 링크 자국 ✓ / 없는 것 0 | 그룹 제목에 «· N건» 개수가 붙음(시안엔 없음 · 작음) | `_tpl.txt` posts 블록 `EXT` · `audit-a-posts.png` |
| 7 | 수익 매체 | `/app/ad-media.html` | ✅ | 매체마다 같은 자리(설명·계정 행·게이지 «38 / 50편»·버튼 줄) ✓ · «키 필요» 회색 ✓ · gnote ✓ / 없는 것 0 | 실제가 더 있음: 유튜브 수익 창출(게이지+«구글로 연결») · 알리 · 링크프라이스 · 클립 인센티브(대기) · 그 밖에 받은 돈 | `audit-a-ad-media.png` |
| 8 | 알림 | `/app/notifications.html` | ✅ | «안 읽음 N · 모두 읽음» ✓ · 종류 아이콘+문장+상대시각 ✓ · 읽은 것 흐리게(opacity .55) ✓ · 행 탭→해당 화면 ✓ / 없는 것 0 | — | `_tpl.txt` notifications 블록 `readAt ? 'style="opacity:.55"'` · `audit-a-notifications.png` |
| 9 | 만든 글 | `/app/pieces.html` | ✅ | 세그 하나로 상태 가름(개수 포함) ✓ · «만드는 중» 행 아래 스텝 ✓(글: 글 쓰는 중→사진→검사 · 영상: 대본→목소리→장면→합성→검사→완료) / 없는 것 0 | 시안은 «봐주세요 행 + 만드는 중 그룹»을 한 화면에 섞어 그렸는데 실제는 세그먼트 하나만 보인다(기본 «만드는 중») — 구조 차이(작음) | `audit-a-pieces.png` |
| 10 | 검수(글) | `/app/piece.html?id=` | ✅ | 채널 렌더 미리보기(가운데 인용·요약 줄·점선) ✓ · 고지 첫 줄 잠금 상자+설명 ✓ · 하단 «이대로 발행 예약» 1개 ✓ · ⋯ 더보기 = 다시 만들기(1회 · 무료)·버리기 ✓ · 본문 인라인 수정(contenteditable 3곳) ✓ / 없는 것 0 | 실제가 더 있음: «발행 전 검사» 축 목록(✓ 글리프 · 이모지 아님) | `audit-a-piece-post.png` · 더보기 시트 실측 |
| 11 | 계정 · 보안 | `/app/settings.html` | ✅ | 이메일·인증 필 ✓ · 비밀번호 바꾸기 / 모든 기기에서 로그아웃 / 로그아웃(빨간 글자 · 맨 아래) ✓ / 없는 것 0 | 실제가 더 있음: «내 자료 · 전부 내보내기»(R6) · 인증 메일 배너 | `audit-a-settings.png` |
| 12 | 디렉터 · 영상 행 (R5) | `/app/director.html?topicId=` | ✅ | «이 소재로 … 3곳에 만들까요?» ✓ · 글 행+영상 행(«영상» 필 · 그래픽 스토리 60초 · 훅 «반전으로 시작» · 28코인) ✓ · 쓰는/남는 코인 ✓ · 이유 줄에 «@shorts_d 는 훅 «반전»…» ✓ · [손보기][만들기] ✓ / 없는 것 0 | 행 아래 «왜 이 시각·계정»(골든타임·오늘 0/2) 한 줄이 더 있음 | `audit-a-director.png` |
| 13 | 손보기 시트 · 영상 섹션 (R5) | 위 화면의 «손보기» 시트 | ✅ | 포맷 세그 3 ✓ · 길이 칩 15/30/60 ✓ · 목소리 행(타입캐스트 · 미리듣기는 다음에) ✓ · «계정마다 다르게 (자동)» 읽기 전용 ✓ · 코인은 길이 바꾸면 한 줄로 재계산(«60초 28코인 → 30초 12코인») ✓ · Primary 1 ✓ | Primary 글자 «이대로»(시안) vs «적용 · 41코인»(실제) · 재계산 힌트가 시안은 바꾸기 전, 실제는 바꾼 뒤에 뜸 | `_tpl.txt` director 블록 66·79행 · `audit-a-director-tune.png` |
| 14 | 검수 · 영상 (R5) | `/app/piece.html?id=(영상)` | ✅ | `video controls playsinline`+포스터 ✓ · «광고 · 제휴» 배지+시작 자막 ✓ · 자막 SRT 행 ✓ · 설명란(제목·설명·태그 · 첫 줄 고지 잠금) ✓ · 심사 축 칩(✓ · P1 «재생성 통과» 주황) ✓ · [다시 만들기(1회)][이대로 예약] ✓ / 없는 것 0 | — | `audit-a-piece-video.png` |
| 15 | 발행함 · 비공개 업로드됨 (R5) | `/app/posts.html?uploaded=private` | ✅ | «공개로 바꿔 주세요 N건» 맨 위 · 필 «비공개 업로드됨 · 공개 전환 필요» · «스튜디오 ›» · «심사 전엔 비공개로 올라가요» 한 줄 ✓ · 릴스 «처리 중» 회색 ✓ / 없는 것 0 | 릴스 «처리 중»이 시안은 «오늘» 그룹 안, 실제는 «처리 중 N건» 그룹으로 따로(작음) | `audit-a-posts-private.png` |
| 16 | 운영센터 · 우리 매출 (데스크) | `/ops/index.html` | 🟠 | 왼쪽 레일 12메뉴+역할 숨김 ✓ · 헤드라인 «9월에 N원 벌었어요 · MRR·ARR» ✓ · 구독·코인 단색 막대(1등만 잉크) ✓ · AI 원가·마진 ✓ · 고객 4지표 ✓ · 발행·수익 회수 막대+회수 총액+코인 판매 ✓ · 환율 없으면 «원가 미환산» 회색 필 ✓ / **없는 것: 환율 없을 때 «마진 줄이 사라진다»** — 실제는 줄이 남고 «원가가 환산돼야 계산해요» | 0 으로 접지 않는 뜻은 같음(계약 §0.1) · 시안 문장과만 다름 | `ops/_tpl.txt` index 블록 17–18행 · `?fx=0` 실측 · `audit-a-ops-dash.png` · **2026-09-23 재측정(A 실측 · 🟠 그대로): 안 바뀌었다.** `public/ops/index.html:56` 이 `r.marginKrw == null` 이면 줄을 지우지 않고 «원가가 환산돼야 계산해요»를 넣는다. 1280px 에서 `?mock=1&fx=0` 로 그려 실측 → `#margin` = «원가가 환산돼야 계산해요»(보임) · `#ai` = «원가 미환산 $212.40 · 환율(FX_USD_KRW)을 넣어 주세요» · 콘솔오류 0. 뜻은 계약 §0.1 대로이고 **시안 문장과만 다르다** — 고칠 곳은 코드 1줄이거나 시안 캡션이다(판단 요) |

모션 규칙(시안 v4 맨 아래 6줄): 숫자 카운트업 ✅(`ui.js:106 UI.countUp`) · 스프링 바텀시트+≥1100px 우측 300px 패널 ✅(`ac.css:191·197`) · 눌림 0.985 ✅(`ac.css:83`) · 완료 체크 풀스크린 **900ms**(시안 «400ms 후 자동 닫힘» · `ui.js:142`) 🟠 작음 · 스켈레톤+폴링 ✅ · «없음 ≠ 0»(빗금·키 필요 회색) ✅.

> **2026-09-23 재측정(A 실측) — 이 산문 줄에도 «열린 행»이 하나 있다.** 🔴 **완료 체크 900ms 는 §0 요약이 «🟠»로 세지 않았다**(§3 «말·모양만 다른 곳 6» 으로 분류해서). 트리거가 경고한 «아무도 안 센 행»이 내 파일에도 하나 있었던 셈이라 **모수에 넣어 센다.** 잰 값: `public/js/ui.js:439` `setTimeout(… , 900)` — **900ms 그대로 · 09-15 에서 안 바뀌었다.** 나머지 모션 5줄은 ✅ 그대로(`ui.js:106` countUp · `ac.css:191·197` 시트/패널 · `ac.css:83` 눌림 0.985).

## 2. 시안에 있는데 실제엔 없는 것

| 항목 | 상태 | 어디 | 안 되는 것(한 줄) | 남은 일 | 크기 |
|---|---|---|---|---|---|
| 계정 연결 격자 준비 중 채널 4타일(쓰레드·인스타그램·네이버 클립·릴스) | ✅ | `_tpl.txt` accounts 블록 18행 `.filter((c) => !c.status \|\| c.status === "active" \|\| cnt[c.key])` · **2026-09-23 재측정(A 실측 · ✅): (a) 로 갔다 — 그리고 한 걸음 더 갔다.** 필터는 `public/app/accounts.html:56` 에서 `c.status !== "down" \|\| cnt[c.key]` 로 바뀌어 **내려간 채널만** 뺀다. «곧 열려요/준비 중이에요» 칩은 `:59`(`soon()`)·`:60`(`soonWord()`) · 접는 줄은 `:69` · 펴는 것은 `:71`. 🔴 **숨기는 게 아니라 접는 것**이다(AC-191 주석 `:61–63`). 🔴 **라이브 17행으로 실측**(`_shots/reaudit-a-live17.mjs` · 제품 코드 무수정): 첫 화면 3 + 접힘 14 = **17/17 · 안 나오는 채널 0** · 그 4타일 전부 있다 · 펼친 판에서도 헌장 통과(Primary 0 · 44미만 0 · 가로스크롤 없음 · 콘솔 0) | 레지스트리 status 가 active 가 아니면 타일 자체가 없다 — 고객은 그 채널이 «있다는 것»도 못 본다. 모의 = 5/9 타일 | 결정을 정하라: (a) 시안대로 9타일 + «곧 열려요» 칩(온보딩과 같은 방식) 또는 (b) 시안을 코드에 맞춰 고친다. **라이브 레지스트리의 영상 채널 status 확인(⬜ · 운영센터 채널 화면)** — planned 면 «영상 축이 화면에서 안 보인다»의 첫 원인 · **2026-09-23: 그 ⬜ 를 쟀다 → 영상 채널 6개 전부 `planned` · active 0.** 즉 «영상 축이 화면에서 안 보인다»의 첫 원인은 **화면이 아니라 레지스트리**다(A 몫 아님 · 아래 §7) | S |
| 온보딩 «영상» 타일 «곧 열려요» 칩 | ✅ | `onboarding.html:76` `#videoSoon.hidden = CH.some(video && active)` · **2026-09-23 재측정(A 실측 · ✅ · 🔴 두 문장으로 갈라 적는다):** ①**잰 것** — 칩 로직은 `public/onboarding.html:104–105` 로 그대로이되 바로 아래 `:106` 에 **`#videoNote` 가 새로 붙었다**(`:20` «지금도 만들어 둘 수 있어요 · 올리는 건 채널이 열리면»). 모의·라이브17 두 판 모두 그려서 확인 — 칩 «채널은 곧 열려요» + 설명 한 줄이 **같이** 뜬다. ⇒ §9-4 «우리가 대신 해 줄 수 있는 것»·§3 «겁주지 않는다»에 맞다. ②**뜻하는 것이 아닌 것** — «영상 축이 고객에게 열려 있다»가 **아니다**. 라이브 영상 채널 6개(youtube_shorts·naver_clip·reels·tiktok·youtube_long·facebook_reels) **전부 `planned`** | 영상 채널이 하나도 active 가 아니면 첫 화면부터 «영상 곧 열려요» — 모의에서 그렇다. 라이브 ⬜ | 위와 같은 뿌리(레지스트리). 라이브 값 확인 뒤 판단 · **2026-09-23: 라이브 값을 쟀다**(`SELECT * FROM channel_registry` · `_shots/reaudit-a-live.mjs`) **→ 칩이 뜨는 게 맞다.** 화면은 사실대로 말하고 있다. 남은 것은 **화면 일이 아니라 채널을 여는 일** | — |
| 운영센터 매출 · 환율 없을 때 «마진 줄 없음» | 🟠 작음 | `ops/_tpl.txt` index 18행 · **2026-09-23 재측정(A 실측 · 🟠 그대로): 안 바뀌었다.** 지금 자리는 `public/ops/index.html:56` — `r.marginKrw == null` 일 때 줄을 지우지 않고 «원가가 환산돼야 계산해요»를 넣는다. 1280px `?mock=1&fx=0` 로 그려 실측(`#margin` 보임 · 콘솔 0) | 줄이 남고 «원가가 환산돼야 계산해요» — 뜻은 같고 시안 문장과만 다르다 | 시안 캡션을 코드에 맞추거나(문서) 줄을 숨기거나(코드 1줄) · **2026-09-23: 아직 아무것도 안 정했다 — 판단이 필요한 한 줄이라 9일째 그대로다** | S |
| 내 PC 행 «버전 1.0.3» | ➖ | `_tpl.txt` runner 블록(4a7c45e) | 메인 지시(09-15) «version 은 상세 시트 작은 글씨» — 시안 v4 «내 PC 프로그램» 장이 옛 모양 | 시안 갱신(문서) | S |

## 3. 시안과 다른 곳(있긴 한데 모양·말이 다름)

| 항목 | 시안 | 실제 | 판단 |
|---|---|---|---|
| 만들기 «레퍼런스로 구조 배우기» 행 오른쪽 | 셰브런 | 셰브런 (2026-09-23 실측) | ✅ **고쳐졌다** — 옛 자리 `public/app/create.html:18` 은 이제 진짜 `<svg class="chev">` 다(`_tpl.txt` create 블록 5행도 같다). **2026-09-23 재측정(A 실측):** ①`create.html` 의 `${UI.chev}` 는 `:123`·`:162` 둘뿐이고 **둘 다 인라인 `<script>`(32–192행) 안**이라 제대로 치환된다. ②전수 훑기 — HTML 51개의 **첫 `<script` 앞 정적 구역**에 남은 `${…}` 는 **0개**(유일한 적중 `public/app/team-accept.html:14` 는 HTML 주석 안이라 안 그려진다). ③브라우저로 6화면(`create·home·director·pieces·accounts·revenue`) 렌더 텍스트에서 `${` **0건**(`_shots/reaudit-a.mjs` T4) |
| 손보기 시트 Primary | «이대로» | «적용 · 41코인» | 말만 다름 · 코인 합이 보이는 실제가 낫다고 봄(메인 판단) |
| 만든 글 기본 보기 | 봐주세요 행 + «만드는 중» 그룹 혼합 | 세그먼트 하나(기본 «만드는 중») | 구조 차이 · 시안 캡션 «세그먼트 하나로 상태를 가른다»와는 실제가 더 맞음 |
| 발행함 그룹 제목 | «어제» | «어제 · 1건» | 개수 표기 추가(작음) |
| 발행함 릴스 «처리 중» | «오늘» 그룹 안 | «처리 중 N건» 그룹 | 사람이 할 일(주황) / 기다리는 중(회색)을 그룹으로도 갈랐다 |
| 완료 체크 | 400ms 후 자동 닫힘 | 900ms | 모션 규칙 차이 · 900 이 읽히기엔 낫다(판단 요) |
| 디렉터 대상 행 | 두 줄 | 세 줄(«네이버 블로그 아침 골든타임 · @cook_a 오늘 0/2» 추가) | 정보 추가 · 헌장 «설명 한 문장» 안 |

## 4. 헌장(§13.0) 위반 남은 곳 — 모의 21화면 × 폰 400px 실측

| 규칙 | 결과 | 남은 곳 | 증거 |
|---|---|---|---|
| Primary 버튼 ≤1 | ✅ 21/21 | — | `dump-real` Primary(보임) 열 · 시트 열린 상태 포함(러너 등록·재발급·손보기 시트 1) |
| 큰 숫자 1 | ✅ | — | `.hl .num` 0~1 |
| 이모지 0 | ✅ | — | 검수 축의 ✓ 는 U+2713 글리프(하니스도 이모지 블록만 센다 · `shot.mjs:12`) |
| 시스템 용어 0(테넌트·슬롯·piece·러너 잡·러너) | ✅ 고객 21화면 | 운영센터 레일 «러너»는 운영자 화면(허용) | `dump-real` 금칙어 열 |
| 터치 ≥44(눌리는 영역) | ✅ **2곳 다 닫혔다** | ① 하단 탭 항목 `.tab` **56×42**(`ac.css:128` · min-height 없음 · 탭 바는 62px 인데 항목이 42) ② 달력 날짜 `.cal .dd` **45×38**(`ac.css:184 height:38px`) — CSS 주석(`ac.css:296`)은 «가로 44 불가»만 말하는데 세로는 44 가능 · **2026-09-23 재측정(A 실측 · ✅): 둘 다 44 가 됐다.** ①`public/css/ac.css:161` 에 `min-height:44px` 가 붙었다(주석도 «눌리는 영역 세로 44 이상(§13.0 · 조사 09-15)» — **이 조사를 보고 고친 자국**) ②`public/css/ac.css:224` 가 `height:38px` → **`height:44px`** · 안내 주석도 `:354` 로 고쳐졌다 | `shot.mjs:86` 이 `.tabs`·`.cal` 을 제외해 초록이었다 — **하니스 초록 ≠ 통과**(#9). 그 밖의 40px 미만(세그 30 · 칩 36 · .btn.sm 40 · 배너 × · «모두 읽음» 19)은 `::before` 오버레이로 44 확보(`ac.css:298–303`) ✓ · 🔴 **2026-09-23: CSS 원문을 믿지 않고 «그려서» 쟀다** — 400px 폰에서 `getBoundingClientRect()` 로 `.tab` = **56×44**(5개 전부) · `.cal .dd` = **45.1×44**(8개 전부). 그리고 🔴 **이번 하니스는 `.tabs`·`.cal` 을 빼지 않는다**(`_shots/reaudit-a.mjs` T1·T7) — 그 제외가 #9 의 거짓 초록이었으므로. 고객 화면 **17장 전부 «세로 44 미만» 0건** |
| 카드 대신 그룹 섹션 · 색 3 · 8px 그리드 | ✅(육안) | — | 스샷 21장 |
| 팝업 모달 0 · 툴팁 0 · 위젯 격자 0 | ✅ | — | 시트만 씀(`UI.sheet`) |
| 콘솔 오류 0 | ✅ 21/21 | — | `dump-real` 콘솔 열 |

## 5. 시안이 없는 실제 화면(v4 밖) — 새 B 여정 조사에서 먼저 눌러 볼 곳

| 실제 화면 | 시안 | 비고 |
|---|---|---|
| `/onboarding.html` | v3 «온보딩»만(v4 없음) | 한 화면 한 질문 3단계 · 영상 타일 «곧 열려요» 칩(§2) · 인증 메일 배너 |
| `/app/create.html` 만들기(소재 목록·내 소재 넣기·레퍼런스) | 없음 | `${UI.chev}` 결함(§3) |
| `/app/director.html` 전체(글 행·이유·손보기 글 섹션) | v3 «디렉터»만 · v4 는 영상 행 부분만 | — |
| `/app/plan.html` 요금제(플랜 3·카드·청구서·영수증) | 없음 | KICC 실측 통과(HANDOFF) |
| `/app/coins.html` 코인(충전·장부·환불·키인) | 없음 | — |
| `/app/support.html` 문의 | 없음 | — |
| `/app/account.html` 내 계정 허브(체험 D-day · 4그룹) | 없음 | — |
| `/register.html` `/login.html` `/forgot.html` `/receipt.html` | 없음 | forgot 은 계정 존재를 말하지 않는다(응답 모양 동일) |
| 운영센터 나머지 15장(고객·요금제·이벤트·결제·CS·러너·AI·채널·공지·운영진·감사·티켓·FAQ·비밀번호·고객 상세) | 데스크 시안은 «매출» 1장뿐 | `docs/screens-web-v1.html` 은 고객 홈·디렉터·편성표·수익 데스크 4장 |

## 6. 조사 방법 · 한계

- 방법: `docs/screens-v4.html` 16장을 요소 단위로 풀어(`dump-v4.mjs`) 같은 화면을 모의(`?mock=1`)로 열어 텍스트·Primary·필·터치 크기·금칙어·콘솔을 자동으로 적고(`dump-real.mjs`/`dump-real2.mjs` · 스크래치) 스샷 21장(`_shots/audit-a-*.png`)을 눈으로 대조했다. 코드 근거는 `public/app/_tpl.txt`·`public/js/ui.js`·`public/css/ac.css` 의 줄로 적었다.
- 한계: **모의 데이터만** — 라이브 레지스트리(`channel_registry.status`)·라이브 계정·실제 영상 산출물은 안 봤다(⬜). 시안 v3·web-v1 의 화면은 v4 와 겹치는 것만 봤다(v3 온보딩·디렉터 전체 장은 §5 에 적고 요소 대조는 안 했다).
- 이 조사가 고친 코드: **0**. 발견한 결함(§3 `${UI.chev}` · §4 탭·달력 44)은 메인이 라운드로 발주하면 A 가 고친다(각 S).

---

# 7. 🔴 2026-09-23 · R16 재측정 — 자세히

> 브랜치 `audit/2026-09-23-A` · **제품 나무 무접촉(코드 0줄)** · 하니스는 `_shots/`(git 밖).
> 🔴 **재는 판이다.** 5분이면 고칠 것을 봐도 적어만 뒀다(트리거 §2-6).

## 7.1 무엇으로 쟀나 (트리거 §2-2 «재서 말한다»)

| 자 | 무엇 | 파일 |
|---|---|---|
| 칸 세기 | `\|` 이스케이프를 지켜 표를 쪼개고 **상태 칸만** 본다 | `_shots/count-a.py` |
| 브라우저 | 정적 서버(**포트 8923**) + Playwright · 400px 폰 · 17화면 | `_shots/reaudit-a.mjs` |
| 브라우저(라이브값) | **라이브 17행을 route 로 먹여** 그린다 · **포트 8924** | `_shots/reaudit-a-live17.mjs` |
| DB 읽기 | `SELECT * FROM channel_registry` (읽기만) | `_shots/reaudit-a-live.mjs` |
| grep | CSS·JS 줄 확인 | 본문 각 행 |

🔴 **포트를 8923/8924 로 갈랐다** — 워크트리마다 갈라 쓰지 않으면 «옛 폴더가 200 을 준다»(`ac-stale-verify-server`).
🔴 **`_shots/` 는 git 밖이라 다음 창엔 없다** — 그래서 **잰 값은 전부 이 추적되는 파일에 적었다**(`ac-shots-gitignored`).

> 🔴 **세는 자로 두 번 틀릴 뻔했다 — 적어 둔다(다음 창에 그대로 온다).**
> ① `\|` 이스케이프를 안 지키고 쪼개면 칸이 밀린다(트리거가 B1 83줄로 경고한 그것).
> ② 🔴 **더 고약한 쪽**: 상태를 고친 **뒤에** «줄에 그 글자가 있나»로 세면 **13** 이 나왔다. 까닭은 —
> 트리거가 «행을 지우지 말고 증거 칸에 덧붙이라»고 했으므로 **닫은 행에도 옛 ⬜·🟠 가 증거 칸에 그대로 남는다.**
> ⇒ **«고친 뒤»에는 줄로 세는 게 더 위험하다.** 상태 칸(§1=c3 · §2=c1 · §3=c3 · §4=c1)만 보게 고쳐서 **2** 를 얻었다.
> ③ 그리고 내가 새로 쓴 §7.2 장부까지 «조사 행»으로 삼켰다 — **장부는 행이 아니다.** §7 부터는 안 센다.

## 7.2 모수 8 이 어디로 갔나

| # | 줄 | 행 | 09-15 | 2026-09-23 | 근거(파일:줄) |
|---|---|---|---|---|---|
| 1 | 23 | §1 #4 계정 연결 | 🟠 | **✅** | `public/app/accounts.html:56·59·69·71` + 라이브17 실측 17/17 |
| 2 | 43 | §2 준비 중 채널 4타일 | 🟠 | **✅** | 같은 자리 · threads·instagram·naver_clip·reels 전부 있다 |
| 3 | 44 | §2 온보딩 «곧 열려요» 칩 | 🟠 | **✅** | `public/onboarding.html:20·104–106`(`#videoNote` 새로 붙음) |
| 4 | 52 | §3 만들기 `${UI.chev}` | 🟠 | **✅** | `public/app/create.html:18` 진짜 `<svg class="chev">` · 전수 51 HTML 정적구역 누출 **0** |
| 5 | 68 | §4 터치 ≥44 (2곳) | 🟠 | **✅** | `public/css/ac.css:161`(`min-height:44px`) · `:224`(`height:44px`) · 그려서 56×44 / 45.1×44 |
| 6 | 35 | §1 #16 운영센터 매출 | 🟠 | 🟠 **그대로** | `public/ops/index.html:56` — 마진 줄이 남는다(`?fx=0` 실측) |
| 7 | 45 | §2 fx 없을 때 마진 줄 | 🟠 | 🟠 **그대로** | ↑ 같은 결함의 다른 자리 |
| 8 | 37 | 모션 · 완료 체크 900ms | 🟠(산문) | 🟠 **그대로** | `public/js/ui.js:439` `setTimeout(…, 900)` · 시안 400ms |

**✅ 5 + 열림 3 + 더 나빠짐 0 + ⊘ 0 = 8** ✓
🔴 **열린 3 은 서로 다른 결함으로는 2** 다(6·7 이 한 결함). **둘 다 «고칠 일»이 아니라 «정할 일»** 이다 — 코드 한 줄 대 시안 캡션 한 줄이라 9일째 아무도 안 정했다. **메인/사장님 판단 1분이면 닫힌다.**

## 7.3 ✅ 로 올린 것의 «두 문장» (트리거 §2-4 대용물 검사)

| 행 | 내가 **잰 것** | 그것이 **뜻하지 않는 것** |
|---|---|---|
| #4·4타일 | 라이브 17행을 먹이면 17칸이 **전부 화면에 닿는다**(3 앞 + 14 접힘, 눌러서 폄) | «17채널로 발행된다» — **붙일 수 있는 칸은 3개**(naver_blog·tistory·wordpress). `blogger` 는 status=active 인데 **우리 앱 키가 없어** 못 붙는다(`accounts.html:57–58`) |
| 온보딩 칩 | 화면이 레지스트리 값대로 칩을 띄우고 «지금도 만들어 둘 수 있어요»를 **같이** 말한다 | «영상 축이 열려 있다» — 라이브 영상 채널 **6개 전부 planned** |
| `${UI.chev}` | 정적 구역 누출 0(전수) + 렌더 텍스트 `${` 0건(6화면) | «만들기 화면이 다 된다» — 이 조사는 **그 글자 하나**만 봤다 |
| 터치 44 | 고객 17화면에서 세로 44 미만 **0건** · 🔴 이번엔 `.tabs`·`.cal` 을 **안 뺐다** | «헌장 전부 통과» — 육안 축(색 3·8px 그리드)은 여전히 눈으로 본 것이다 |

## 7.4 🔴 메인 특별 항목 — «모의 11 ↔ 라이브 17» 은 지금도 그대로다

**그대로다. 차이 6칸.** (`_shots/reaudit-a-live.mjs` · 오늘 `SELECT` 실측)

- **라이브 `channel_registry` = 17행** · status 집계 **`{active: 4, planned: 13}`**
- **모의(`public/js/mock.js`) = 11칸** — 라이브에만 있는 6칸: `facebook` · `x` · `brunch` · `naver_clip_post` · `youtube_long` · `facebook_reels` (**전부 planned**)
- 모의에만 있는 칸: **없음**

| 축 | 값 |
|---|---|
| active 4 | `naver_blog`(runner) · `tistory`(runner) · `blogger`(api) · `wordpress`(api) — **전부 글(text)** |
| 🔴 **실제로 붙일 수 있는 칸** | **3** — `blogger` 는 active 인데 앱 키가 없다 ⇒ 계정 연결 첫 화면 타일 **3개**(라이브17 실측) |
| 🔴 **영상 채널 6개 전부 `planned`** | `youtube_shorts` · `naver_clip` · `reels` · `tiktok` · `youtube_long` · `facebook_reels` |

> 🔴 **C 의 품질 등급이 이 수에 매달려 있다면**: 오늘 잰 값은 **영상 채널 active = 0** 이다.
> 화면은 다 있고 그 사실을 바르게 말한다(칩 + «지금도 만들어 둘 수 있어요»). **막힌 것은 화면이 아니라 레지스트리다.**
> 이건 **A 몫이 아니다** — 채널을 여는 일(B2·운영)이다. 여기엔 **잰 값으로만** 남긴다.

## 7.5 🟠 적어만 두고 안 고친 것 (트리거 §2-6)

1. 🔴 **모의가 라이브와 어긋나 있다 — 그런데 모의 주석은 «같다»고 말한다.**
   `public/js/mock.js:111` 주석: «라이브 channel_registry 와 같게». 실제는 **11 ↔ 17**.
   그래서 **모의로만 재면 첫 화면 타일이 5개**로 보이는데 **라이브는 3개**다. 09-15 조사도, 오늘 1차(T2)도 그 5를 봤다.
   오늘은 라이브 17행을 먹여서 확인했으므로 **결론은 안 바뀌었다.** 하지만 **다음 사람이 모의만 보면 틀린다.**
   ⇒ 할 일(다음 판 · S): `mock.js` 채널 목록에 라이브 6칸을 더한다. **A 몫이고 작다.**
2. 운영센터 fx 없을 때 마진 줄(§1 #16 · §2) — **코드 1줄 대 시안 캡션 1줄.** 정하면 끝난다.
3. 완료 체크 900ms vs 시안 400ms(§1 모션) — **정하면 끝난다.**

### 🔴 AC-270 (PITFALLS 에 붙일 것 · 메인께)

> **«모의에서 통과»는 «라이브에서 통과»의 대용물이다 — 모의가 «라이브와 같다»고 **적어 놓았을 때** 특히 그렇다.**
> 2026-09-23 · A. `mock.js:111` 은 «라이브 channel_registry 와 같게»라고 적혀 있지만 **11 ↔ 17** 로 벌어져 있었다.
> 모의로 재면 계정 연결 첫 화면이 **5타일**, 라이브 값을 먹이면 **3타일**이다.
> ⇒ **주석의 «같게»를 믿지 말고, 라이브 행을 화면에 먹여서 그려 봐라.** 제품 코드를 안 고치고도 된다 —
> Playwright `context.route("**/api/**")` 로 진짜 행을 돌려주면 된다(`_shots/reaudit-a-live17.mjs`).
> 🔴 **모의와 라이브의 수가 다르면, 그 수에 달린 모든 초록은 다시 재야 한다.**

## 7.6 ⊘ 못 쟀다 — **0**. 대신 이 판이 **안 본 것**은 이렇다

이번 판은 «열린 행 8개»만 다시 쟀다. 아래는 **모수 밖**이고, 낙제로도 통과로도 안 센다.

- §1 의 ✅ 14행을 **요소 단위로 다시 대조하지는 않았다.** 대신 **되돌아감 검사(smoke)** 는 돌렸다 — 고객 17화면에서 Primary ≤1 · 큰 숫자 ≤1 · 이모지 0 · 금칙어 0 · **콘솔 오류 0** · 세로 44 미만 0 (`_shots/reaudit-a.mjs` T7 · 걸린 화면 **0**). **더 나빠진 것은 안 보인다.**
- `/app/piece.html?id=`(§1 #10·#14 검수 글·영상)은 id 가 있어야 열려서 **이번 17화면에 안 넣었다.** 두 행 모두 09-15 에 ✅ 였고 이번에 건드리지 않았다.
- §5 의 «시안 없는 화면 9» 는 상태 칸이 없어 모수 밖이다(그중 `onboarding`·`create` 는 위에서 쟀다).
- 육안 축(색 3 · 그룹 섹션 · 8px 그리드)은 **여전히 눈으로 본 것**이고 이번엔 새로 안 봤다.
