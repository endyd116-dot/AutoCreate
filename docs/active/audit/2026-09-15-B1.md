# 설계 대비 전수조사 — B-1 영역 (§4 · §5 · §5C · §16B · §6 · §10) · 2026-09-15

> 🔴 **2026-09-23 재측정(R16 · B)** — 아래 09-15 본문은 **그대로 두고**, 열린 행에 «2026-09-23 재측정: …» 을 덧붙였다.
>
> | | 2026-09-15 | **2026-09-23** |
> |---|---|---|
> | 판정 단위 | 143 | **143**(같다 — 행을 하나도 안 흘렸다는 증거) |
> | ✅ | 103 | **116** |
> | 🟠 | 15\* | **12** |
> | 🟡 | 4 | **2** |
> | ❌ | 9 | **2** |
> | 🔒 | 2 | **2**(외부 선결 · 안 바뀐다) |
> | ➖ | 9 | 9 |
> | **열린 행** | **31\*** | **18** |
>
> \* 🔴 **09-15 요약은 «🟠 15 · 열린 31»이 아니라 «14 · 30»으로 적혀 있었다.** 83줄(`§5.2 mode auto \| reviewed`)이
> 항목 칸의 `\|` 때문에 **표 훑기에서 한 칸 밀려** 그 요약에서도, 메인의 셈에서도 빠져 있었다 — **09-15 부터 아무 데도 안 세어져 있었다.**
> 2026-09-23 에 처음 세어졌다. 🔴 **모수를 세는 자가 먼저 틀린다.**
>
> 🔴 **닫힌 13행은 전부 «파일:줄» 근거가 있다.** 증거 없는 ✅ 0.
> 🔴 **재는 법**: `grep`(코드가 있나) → **부르는 쪽까지**(정말 도나) → 라이브 SELECT(실물이 있나) → 화면(§4.8 «쓸 수 있나»).
>    «파일이 있다»를 ✅ 로 쓰지 않았다(AC-178) — 실제로 그렇게 갈린 자리가 셋이다:
>    ① `draft 4` 가 라이브에 있는데 **전부 시험 하니스 산물**이라 🟠 유지 ② 페르소나 적합도는 «점수를 잰다»가 아니라
>    **`healthScore + bonus` 로 배정을 바꾼다**까지 보고 ✅ ③ ⚠️ **장소 카드는 내가 틀렸다** — «만드는 곳이 0»이라 ❌ 로 적었는데
>    `content-gen.ts:117` 에 **프롬프트 어휘로 있다**(같은 날 정정 · 🟠). 🔴 내 과녁이 `'"place"'`(따옴표 포함)라 `place{place:{…}}` 를 못 봤다(AC-250 ③).
>
> 🔵 **반만 왔다(= 아직 열림 안에 든다 · 다음 판에서 제일 싼 것) 1행**: `§10.2 정본 동기화`(내려받기·복사 O · PR 생성 0). ⚠️ 장소 카드는 위 정정으로 빠졌다 —
>    `§10.2 정본 동기화`(내려받기·복사 O · PR 생성 0).
> ⚠️ `§5C.1 블로거·WP` 는 🔵 가 아니라 **행이 잘못 묶인 것**이다 — WP 는 ✅(`wordpress.ts:137 slugOf`), 블로거는 `slug` 0건.
>    다음 판에 **두 행으로 쪼갠다.**
>
> ---
>
> ## 🔴 [2026-09-23 · R17] «닫았다 7»을 **갈라서** 적는다 — B2 가 짚었다
>
> R17 에서 **열린 행 18 → 11** 이 됐다. 그런데 🔴 **«내가 닫은 것»과 «애초에 안 열려 있던 것»은 다른 수**이고,
> 사장님이 «너희가 오늘 뭘 했나»를 보는 수라 **뜻이 아예 다르다**(B2 지적 · 자기 표에서 같은 갈래를 먼저 잡았다).
> ⇒ 기계로 다시 세고(`git show <base>` 대비 상태 칸 비교) **행마다 «내 코드가 그 파일을 만졌나»를 붙였다**:
>
> | 갈래 | 수 | 행 |
> |---|---|---|
> | **㉮ 이 판에 만들어서 닫혔다** | **3** | §10.2 정본 동기화(AC-256) · §5.4 집필 계약 화면(AC-258) · §16B.2 유튜브 «유료 프로모션»(AC-253) |
> | **㉯ 이미 돼 있었고 내가 굳혔다** | **1** | §5.2 `mode auto` — 요구는 `cron/director-auto.ts:174` 가 **이미** 채우고 있었다. 내가 한 건 `propose()` 의 박힌 글자를 `origin` 파생으로(맞는 까닭을 «우연»에서 «구조»로) |
> | **㉰ 순수 정정 — 코드 0줄** | **3** | §5.2 `heroNeeded` · §16B.2 협찬 고지 문구 · §6.2 영상 내려받기 |
> | ㉱ 쪼개며 드러난 ✅ | 2 | §4.3 글·대본 · §5C.1 워드프레스 (둘 다 코드 0줄) |
>
> 🔴 **㉰·㉱ 다섯은 우리가 오늘 한 일이 아니다 — 내가 R16 에 잘못 적어 둔 것이다.**
> 근거는 «안 만졌다»를 **파일로 세서** 확인했다: `lib/stock/plan.ts`·`lib/content-gen.ts`·`netlify/functions/piece-video.ts`·`lib/r2.ts`
> 가 이 판 diff 에 **없다**(㉮ 셋의 파일 `ai-models-sync.ts`·`ops-emotion.ts`·`publish/youtube.ts` 는 **있다**).
>
> ⚠️ **이 갈래는 두 방향으로 읽힌다 — 둘 다 말한다**:
> · «완벽하게 다 개발됐나»에는 **좋은 소식**이다 — 내가 말한 것보다 **더 돼 있었다**.
> · «오늘 뭘 했나»에는 **나쁜 소식**이다 — 일곱 중 셋만 우리가 만든 것이다.
> 🔴 두 수를 한 칸에 담으면 어느 쪽으로든 **거짓말이 된다.**

> 담당 B-1(`autocreate-b1` · 워크트리 `AutoCreate-B1` · 브랜치 `feature/p1r6-back1`) · 트리거 `docs/active/2026-09-15-DESIGN-AUDIT-trigger.md` §1 어휘·§2 행 형식·§4 규율 그대로.
> 코드 수정 0 · 실호출 0(라이브 DB 는 **읽기 SELECT 4건**만 · 테넌트 행 생성 0) · 증거 없는 ✅ 없음.

## 0. 요약 5줄

1. **판정 단위 143건** — ✅ 103 · 🟡 4 · 🟠 15 · 🔒 2 · ❌ 9 · ➖ 9 · ⬜ 0(실측이 필요한 곳은 라이브 SELECT 로 대신 쟀다).
2. 🟠/❌ **상위 5**(고객이 먼저 부딪히는 순): ① `channel_registry` 영상 4채널 전부 `planned` → 계정 그리드에서 **숨김** → 영상 계정 연결 자체가 불가 → **라이브 영상 piece 0건**(사장님이 «영상이 전부 빠져 있다»고 보신 직접 원인 · 운영센터 토글 한 번이면 열린다) ② 기존 테넌트 87/99 는 `settings.kinds` 에 video 가 없고 온보딩 뒤엔 **켤 화면이 없다** ③ 러너가 **프레임 지문을 안 보내** 영상 계정 간 중복 판정(pHash)이 실제로 안 돈다(심사 축이 «지문 없음»으로 통과) ④ 인스타 **카드뉴스는 발행 경로 0**(REELS 만) · 쓰레드 «스레드 2~3개 연결» 0 ⑤ 영상 «직접 올리기» 경로의 뒤 두 칸 — **mp4 내려받기 버튼이 없고**(모바일 사파리 불가) **직접 올린 주소를 적을 곳도 없다**(API 0 · piece 가 영원히 awaiting_manual · 수익 매칭 불가 — B3 지적).
3. **영상 축 결론**: 코드는 R5 §1 100% · 화면 8장 존재 — 그러나 고객 경로의 **첫 문(계정 연결 그리드)이 닫혀 있어** 아무도 못 들어간다. 유튜브 OAuth 없는 경로(만들기→심사→내려받기→앱에서 직접 올리기)는 **naver_clip 으로 열려 있으나** 역시 그리드에서 숨겨져 있다. 【§A 절】
4. 설계와 코드가 갈라진 ➖ 9건은 전부 계약서·사장님 결정 근거가 있다 — 단 **DESIGN.md 본문이 안 고쳐진 것 2건**(§5C.3 «캡션 필수» · §6.2 «릴스 90»)은 CLAUDE §8 «설계를 바꿀 땐 DESIGN 먼저» 위반 상태다.
5. 남은 일 크기 합: **S 24 · M 6 · L 4**(L 행은 6이지만 프레임 지문 3행이 같은 일) ≈ 라운드 1.5 — L 4 = 프레임 지문(러너 보고+judge) · 인스타 카드뉴스 발행 · 관리형 러너 팜(Phase 4) · 유튜브 OAuth 앱 심사(외부).

---

## 1. §4 핵심 파이프라인 · 상태기계

### §4.1 객체 4개
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 4.1 | topic 상태 candidate→picked→used/expired(7일) | ✅ | `lib/topics.ts:256,260`(+7d) · `assign-topics.ts:122`(picked) · `director.ts:436`(used) · `topics.ts` skip→expired · 직접 넣은 소재만 +30일(`addManualTopic`) | — | — | — |
| 4.1 | brief 상태 proposed→confirmed→producing→done | ✅ | `director.ts:244`(proposed) · 확정 시 confirmed · 편성표 건너뜀 skipped. `producing`·`done` 을 만드는 코드 **0**(`grep "briefs SET status"` = confirmed·skipped 2건) · **2026-09-23 재측정: ✅** `lib/cron/brief-sweep.ts:39`(confirmed→producing)·`:47`(→done) · 🔴 **라이브 분포에 실물**: `producing 9 · done 15`(SELECT) | 지시서 진행 상태를 brief 로는 못 본다(piece 상태로만) | 전이 2개를 넣거나 설계에서 brief 상태를 3개로 줄여 ➖ 처리 | S |
| 4.1 | piece 상태 10(generating→draft→in_review→approved→scheduled→publishing→published / awaiting_manual / failed / rejected) | 🟠 | `pieces.ts:32` STATUSES 에 10개 전부 · 실제 전이: `pieces SET status=` = generating·in_review·scheduled·publishing·published·awaiting_manual·awaiting_runner·failed·rejected. **`draft`·`approved` 를 만드는 코드 0** — generating→in_review→scheduled 로 건너뛴다 · `awaiting_runner` 는 설계 밖 추가(R2 §7-2 승인 → ➖) · **2026-09-23 재측정: 그대로 🟠** — 라이브에 `draft 4 · approved 2` 가 있어 잠시 «닫혔나» 싶었지만, **누가 언제 만들었나**를 보니 draft 4는 전부 시험 테넌트 778 의 하니스 산물(`[링크 시험 A]` 따위)이고 approved 2는 09-15 것뿐이다. **제품 경로는 여전히 안 만든다.** 🔴 «라이브에 행이 있다» ≠ «제품이 만든다»(AC-178) | 어휘 2개가 죽어 있다 · 화면 스텝 바는 실제 전이만 그린다(문제는 없음) | 설계 문구를 실제 전이로 정리(draft·approved 삭제 또는 의미 부여) | S |
| 4.1 | post 불변 이력(URL·ref·시각·통계) | ✅ | `db/schema.ts:380` posts · `lib/publish/finalize.ts` 1행 · `posts.status` 어휘 +uploaded_private(R5 §0.1-6) | — | — | — |
| 4.1 | brief 1 → piece N(글+영상 혼합) | ✅ | `director.ts:366` specs 루프 · 영상 분기 `isVideo`(R5 §1.1) · 라이브에선 영상 piece 0(§A) | — | — | — |

### §4.2 흐름(전이 14)
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 4.2 | 소재후보 ← 크론 리필 / «소재 뽑기» | ✅ | `netlify/functions/topics.ts` topics-refresh(202·하루 3회) · `topics-refresh-background` origin cron · **+ 직접 넣기 `topics-add`(2026-09-15 · DESIGN §5.1 누락 수리)** | — | — | — |
| 4.2 | 소재후보 → 지시서제안(디렉터 자동) | ✅ | `director.ts:142 propose` · `lib/cron/produce.ts:109`(자동) | — | — | — |
| 4.2 | 지시서제안 → 확정(«이대로»/손보기/자동모드) | ✅ | `public/app/director.html:20` CTA 2(손보기·만들기) · `produce.ts:109 confirm(origin:"auto")` | — | — | — |
| 4.2 | 확정 → 생성중(코인 선차감 · 멱등 ref=piece) | ✅ | `director.ts confirm` → `coin-ledger.consume(tid, item, "piece:{id}")` · 유니크 `coin_ledger_idem` | — | — | — |
| 4.2 | 생성중 → 초안(글·이미지·영상 완성) | 🟠 | 글: `content-gen.ts` stage writing→images→checking → **in_review 직행** · 영상: `video/gen.ts` 6단계 → in_review · **2026-09-23 재측정: 그대로 🟠** — 위 §4.1 행과 같은 자리(draft 를 거치지 않는다) | `draft` 상태를 거치지 않는다(설계 어휘만 죽음 · 동작 문제 없음) | §4.1 행과 함께 설계 정리 | S |
| 4.2 | 초안 → 검수대기(코드 게이트) | ✅ | `content-gen.ts` runGate → 실패 시 재작성 1회 → in_review + gate_report · 영상 `finalizeRender → judgeVideo` | — | — | — |
| 4.2 | 검수대기 → 승인(사용자 / **자동승인(신뢰 계정)**) | ✅ | 사용자: `pieces-approve → content-approve.approvePiece` ✓ · 자동: `lib/cron/review-deadline.ts:39` `reviewPolicy==='silence_approves'`(**테넌트 단위**) · **2026-09-23 재측정: ✅** `lib/cron/review-deadline.ts:36` 가 `accountsTrust` 를 읽는다 — 다만 🔴 **«막는 축»이 아니라 «알려 주는 축»**이다(`:67` 사장님 지시 2026-09-15 · CLAUDE §9). 설계의 «신뢰 계정만 자동승인»과 **방향이 반대**로 닫혔다 — 근거 있음 | «신뢰 계정만 자동승인»이 아니라 테넌트 전체 정책 하나. 계정별 신뢰(건강도·이력) 조건 0 | 계정별 `trusted` 조건을 review_deadline 에 더하거나 설계를 ➖ 로 | S |
| 4.2 | 검수대기 → 반려(재생성 무료 1회 / 폐기) | ✅ | `pieces.ts` regenerate(regen_limit 1회 · 코인 0 · 실패 환급분만 재차감) · reject | — | — | — |
| 4.2 | 승인 → 예약(디렉터 슬롯 · 캐던스) | ✅ | `content-approve.ts approvePiece` → scheduled + `slots` publish_at · `best-time.pickPublishAt` | — | — | — |
| 4.2 | 예약 → 발행중(크론 due → API 즉시 / 러너 claim) | ✅ | `lib/cron/publisher.ts` · `runner-jobs.ts` claim · 영상은 `publish-video-background` | — | — | — |
| 4.2 | 발행중 → 발행완료(URL 회수) | ✅ | `lib/publish/finalize.ts finalizePublish` posts 1행 · external_url | — | — | — |
| 4.2 | 발행중 → 수동대기(러너 실패 + 알림) | ✅ | `pieces SET status='awaiting_manual'` 6곳 + notifications `publish_manual` · 홈 «해야 할 일» | — | — | — |
| 4.2 | 수동대기 → 발행완료(**직접 올린 주소 적기** → posts 행 · 수익 귀속) | ✅ | `posts.html:77` awaiting_manual 시트 = «본문 복사하기»뿐 · `netlify/functions/posts.ts` 는 externalUrl **읽기만**(적는 API 0) — 설계 §4.2 에 화살표가 없지만 «수동대기»가 끝나는 길이 없으면 piece 가 영원히 awaiting_manual 이고 수익 매칭(piece↔URL)도 안 된다(B3 지적) · **2026-09-23 재측정: ✅** 화면 `public/app/piece.html:654`(«영상 받기»)·`:656`(«올린 주소 적기») → `UI.markPublishedSheet` → 서버 `netlify/functions/piece-video.ts:32` `POST /api/post-mark-published`. 🔴 **끝에서 끝까지 있다**(§4.8) | 직접 올린 글·영상의 URL 을 적을 곳이 없다 | `posts-mark-published { pieceId, url }` + 시트 입력 1칸 → posts 1행·piece published | S |
| 4.2 | 발행중 → 예약(러너 셋업 실패 release·재시도) | ✅ | `runner-jobs.ts:93` status `released` · publisher 재시도 · `awaiting_runner`(30분 · R2 §7-2) | — | — | — |
| 4.2 | 발행완료 → 수익매칭(애드센스/쿠팡/유튜브) | ✅ | `lib/revenue/{adsense,coupang,youtube,aliexpress,linkprice}.ts` · `revenue_daily` piece_id 귀속 | — | — | — |

### §4.3 불변식 5
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 4.3 | 코인 piece 단위 1회 · 재생성·수리·재시도 무료 | ✅ | `coin-ledger.ts consume` 멱등(23505 → ok) · 영상 `videoCoinItem` 1회(이미지 0) · 재렌더·수리·재큐·이어달리기 0 · ➖ 실패 환급분 재차감(R4 결정) | — | — | — |
| 4.3 | 발행 멱등(channel_ref/external_url 있으면 재게시 금지) | ✅ | `finalizePublish` · `posts` channel_ref · `publish-video-background` 멱등 스킵 | — | — | — |
| 4.3 | 계정 캐던스 daily_cap·min_gap | ✅ | `director.ts:151` posts_today<daily_cap · `best-time.ts` minGapMin · 계정 간 30분 | — | — | — |
| 4.3 | 계정 간 중복 0 — **글·대본** | ✅ | 글: `content-gen.ts` SAME_BODY_SIMILARITY → 앵글 바꿔 1회 ✓ · 영상 대본: `content-approve.recheckVideoPiece` 유사도 ✓ · `crossAccountSimilarity`(`lib/content-gen.ts`·`content-approve.ts`) · ⚠️ **라이브에서 실제로 재작성이 일어난 적은 0회** — 그건 «안 돈다»가 아니라 **«임계를 넘은 글이 아직 없었다»**이다(축은 매 생성마다 돈다) | — | — | — |
| 4.3 | 계정 간 중복 0 — **영상 프레임 지문(pHash)** | 🟡 | 🔴 **[2026-09-23 R17 정정 — 한 행에 두 갈래가 묶여 있어서 쪼갰다]** 위 재측정 두 줄이 「프레임 pHash 비교가 실제로 안 돈다 · 러너 보고 0」이라 적었는데 **지금은 거짓이다**: 러너가 `runner/channels/render-video.mjs:804~832` 에서 `thumbGray`(32×32 그레이 1024B → base64)를 **싣고**, 서버가 **두 곳**에서 받아 적고(`lib/runner-jobs.ts:1799` · `lib/video/render-queue.ts:119`), `lib/video/judge.ts:343 similarityAxis` 가 pHash 를 계산해 **다른 계정 영상과 해밍 거리로 견준다**(`PHASH_SIMILAR_MAX_DISTANCE = 10` · `fingerprint.ts:6`). 🔴 **끝까지 이어져 있다.** ⚠️ **그런데 라이브 0회** — 실측 2026-09-23: 영상 piece **3개**뿐이고 `meta.frameHash`·`thumbGray`·`framePhash` **전부 0개**, `meta.judge` 적힌 영상도 **0개**. ⇒ 🟡(코드만 · 라이브 0회). 🔴 **모수 3으로는 «된다»고 말할 수 없다**(AC-141② — 모수 0·소수를 통과로 쓰지 마라). ⚠️ `judge.ts:258` 이 적어 둔 옛 사실(«러너 전송 0 · 2026-09-15 설계감사 19번»)은 **그 뒤에 닫혔다** — 그 주석도 낡았다(B2 영역이라 내가 안 고쳤다) | 영상이 3편뿐이라 **겹칠 상대가 없어** 이 축이 한 번도 실제로 재 본 적이 없다 | 영상이 계정 둘 이상에 쌓이면 그때 **라이브 1회**를 확인한다(B2 와 같이) | L |
| 4.3 | 광고 표시 자동 삽입 + 사람이 지워도 발행 직전 재검사 | ✅ | 글: `writing-contracts.ts:346` 첫 블록 고정 · `piece.html:34` lock/restore · `publish/gate.ts` · 영상: `content-approve.recheckVideoPiece` + `publish-video-background`(2026-09-15 배선) | — | — | — |

📊 §4 소계 25행: ✅ 19 · 🟠 5 · ❌ 1.

---

## 2. §5 디렉터

### §5.1 정의
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5.1 | 디렉터 = 소재+계정 풀+목표 → brief · 엔진은 지시서만 본다(두뇌 1개) | ✅ | `director.ts:2-8` 헤더 «앞뒤만 한다» · `content-gen.ts`·`video/gen.ts` 는 piece.meta 만 읽는다 | — | — | — |
| 5.1 | **소재를 주는 입구**(AI 추천 + 사용자 직접) | ✅ | `topics-add`(2026-09-15 사장님 실측 누락 수리 · `c0fbc2b`) · 화면 A 머지(`d0b186e`) | — | — | — |

### §5.2 지시서 스키마(필드 단위)
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5.2 | goal 6(adsense·adpost·affiliate·ypp·clip_incentive·mixed) | ✅ | `director.ts:36` Goal 6 · `goalOf` | — | — | — |
| 5.2 | pieces[].channel(ChannelKey) | ✅ | `PieceSpec.channel` · `accounts.ts ALL_CHANNELS` 10 | — | — | — |
| 5.2 | pieces[].accountId(계정 풀 배정) | ✅ | `director.ts:151 assignAccount` · 계정 0 이면 null + 화면 «계정 연결 후 배정»(director.html:63) | — | — | — |
| 5.2 | format 9(longform·listicle·qna·compare·story·cardnews·shorts_graphic·shorts_talk·clip_life) | ➖ | R1 계약 §3 이 `FormatKey` 7(story·info·listicle·compare·qna·guide·cardnews)로 · R5 §1.1 이 영상을 `kind:"video"`+`video.format`(graphic·talking·clip)로 재정의(`writing-contracts.ts:11` · `video/types.ts`) | longform→info/guide 로 흡수 · shorts_* 3종은 별 축 | DESIGN §5.2 어휘를 계약 어휘로 갱신 | S |
| 5.2 | emotion(EmotionProfile) | ✅ | `PieceSpec.emotionKey` → `emotion_profiles` 키 · 손보기 감성 칩(director.html:89) | — | — | — |
| 5.2 | composition(구성 5종+수익형) | ✅ | `PieceSpec.composition` · `writing-contracts.structureFor` 블록 시퀀스 · 비교표·체크리스트 구성 있음 | — | — | — |
| 5.2 | lengthHint{words · seconds} | ✅ | words ✓ · seconds 는 `video.seconds`(자리만 다름 · R5 §1.1) | — | — | — |
| 5.2 | images{count · style · heroNeeded} | ✅ | count·style ✓(손보기 «사진 수») · **heroNeeded 는 설정만**(`director.ts:212`) — `content-gen.ts`·`ai-image.ts` 소비처 **0** · **2026-09-23 재측정: 그대로 🟠** — `heroNeeded` 는 `lib/content-gen.ts` 에서 **6곳이 읽는다**(설정만이 아니다). 다만 사진 수·스타일을 **고객이 바꾸는 자리**는 그대로—다음 판에 쪼개야 한다 · 🔴 **[2026-09-23 R17 정정 — 위 «영향»과 «할 일»이 둘 다 이미 해결돼 있었다]** ①«대표 사진을 따로 굽지 않는다» → **거짓**: `lib/stock/plan.ts` 가 **대표는 스톡으로 안 바꾸고 AI 로 굽는다**(`:6`), 대표 자리가 없으면 **한 자리 만들어 준다**(`content-gen.ts:451` `heroPlan.made` — 🔴 막는 게 아니라 **대신 해 주는 것** · 코인 안 는다), 어떻게 됐는지 `meta.hero` 로 남겨 **검수 화면까지 간다**(`pieces.ts:201` · `piece.html:98`). ②«첫 이미지 프롬프트에 «썸네일용·넓은 구도» 반영» → **이미 있다**: `stock/plan.ts:117 HERO_PROMPT_HINT` = 「cover image … also be shown as a small thumbnail in a feed, so keep the subject large and centered with clear margins」 를 `content-gen.ts:528` 이 **대표 자리에만** 붙인다(호출 수 안 는다 = 값 0). ⇒ **✅**. ⚠️ 남은 것은 «사진 수·스타일을 고객이 바꾸는 자리»인데 그건 **이 행이 아니라 화면 행**이다 | ~~대표 사진을 따로 굽지 않는다~~ ⇒ 해결돼 있었다 | ~~heroNeeded 를 첫 이미지 프롬프트에 반영~~ ⇒ 해결돼 있었다 | S |
| 5.2 | monetize.affiliate{provider coupang·ali·linkprice · productQuery · slot} | 🟠 | `director.ts:37` `provider: "coupang"` 만 · `lib/affiliate-coupang.ts` 뿐(ali·linkprice 는 **수익 수집**만 `lib/revenue/`) · **2026-09-23 재측정: 그대로 🟠** — 제휴 제공사는 **쿠팡 하나뿐**(`lib/affiliate-coupang.ts` · ali·linkprice 파일 0). 문서의 세 제공사 중 하나 | 알리·링크프라이스 **제휴 링크 삽입** 0 | affiliate-ali·linkprice 검색·딥링크 어댑터 | M |
| 5.2 | monetize.adDisclosure | ✅ | `PieceSpec.monetize.adDisclosure` · 제휴면 true · 고지 블록 첫 자리 | — | — | — |
| 5.2 | schedule{at · slotReason} | ✅ | `best-time.pickPublishAt` → at · slotReason 사람말(director.html:63 표시) | — | — | — |
| 5.2 | coinCost(표시용 · 정본은 원장) | ✅ | `PieceSpec.coinCost` · 손보기 «적용 · N코인» 재계산 | — | — | — |
| 5.2 | reasons 3줄(사람말) | ✅ | `director.ts:221` LLM reasons×3 · 시스템 용어 금지 | — | — | — |
| 5.2 | mode auto \| reviewed | ✅ | `director.ts:250` 항상 `"reviewed"` · 자동은 `confirm(origin:"auto")` 로 표현(brief.mode 에 auto 가 안 들어간다) · **2026-09-23 재측정: 그대로 🟠** — `lib/director.ts:114` 타입엔 `"auto" | "reviewed"` 가 있지만 **저장할 때 auto 를 쓰는 곳이 여전히 0**. 🔴 그리고 이 행은 **09-15 요약에서도 빠져 있었다**(항목 칸의 `\|` 때문에 칸이 밀렸다) — 오늘 처음 세어졌다 · 🔴 **[2026-09-23 R17 정정 — 바로 위 재측정이 틀렸다]** «auto 를 쓰는 곳이 0»은 **거짓**이다. `lib/cron/director-auto.ts:174` 가 **처음부터** `mode 'auto'` 로 INSERT 하고 있었고 라이브에 행이 있다(실측: `auto/proposed` 2 · `auto/confirmed` 1 · `auto/producing` 1 · 09-13~14). ⚠️ **내가 `lib/director.ts` 한 파일만 보고 «0곳»이라 적었다** — AC-250(«한 과녁으로 «없다»를 말하지 마라») 을 내가 그 항목을 쓴 날 또 밟았다. ⇒ 진짜 구멍은 **다른 것**이었다: `propose()` 가 `"reviewed"` 를 **두 자리에 손으로 박아** `opts.origin` 과 따로 놀았다(오늘은 부르는 곳이 하나라 값이 맞았지만 **맞는 까닭이 우연**). `origin` 에서 파생하게 고쳤다(`director.ts:450`) ⇒ **✅** | brief 행만 보고는 자동/사람을 못 가른다(origin 은 slot 에) ⇒ 🔴 **이 영향도 이미 해소돼 있었다** — 고객 화면은 `schedule.html:218` 이 `origin` 으로 «직접 만든 글 / 자동 편성»을 **이미 그린다** | ~~produce 경로에서 mode:"auto" 로 저장~~ → 한 일: `propose` 의 박힌 글자를 `origin` 파생으로 | S |

### §5.3 자동 디렉터 결정 규칙 6
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5.3-1 | 채널 선택 = channel_hint × 켠 채널 × **목표 매체**(adsense→티스토리 우선…) | 🟡 | `director.ts:4` 채널 = hint ∩ 계정 있는 채널(최대 3) · `goalOf(pieces)` 는 **채널에서 목표를 역산**(director.ts:59) · **2026-09-23 재측정: 그대로 🟠** — `lib/director.ts` 에 `goalOf` 가 있어 목표 매체를 보긴 하나, 설계가 말한 «어느 채널을 먼저»의 **우선순위 표**는 못 찾았다 · ⊘ 더 재야 한다 · 🔴 **[2026-09-23 R17 정정 — 위 ⊘ 가 틀렸다. 표는 있다]** `lib/director-goal.ts targetChannelOrder`(2026-09-16 · `R8CLOSE §B8`)가 **설계 문장 그대로** 구현돼 있고 `director.ts:38` 이 부르고 `:327` 이 쓴다. ⚠️ 나는 `director.ts` 안에서만 «우선순위 표»를 찾았다 — **오늘 세 번째로 같은 실수**다(AC-250). **실행해서 네 갈래 다 확인**: 목표없음→«붙은 광고» 추정 · `adsense`→티스토리 앞 · `adpost`→네이버 앞 · 🔴 **켠 채널을 하나도 안 뺀다**(순서만 · §9). 자동 경로(`cron/director-auto.ts`)가 안 부르는 것은 **맞다** — 슬롯이 채널을 이미 못 박고, 슬롯은 고객이 정한 `schedule_rules.channel` 에서 나온다(`lib/slots.ts:295`). 거기서 목표로 뒤집으면 **고객이 명시한 선택을 덮는 것**이다. ⇒ 🟡 **코드·화면 다 있는데 라이브 0회**(`settings.goal` 실측 **124집 전부 «없음»**). 화면은 `settings.html:28`«수익 — 어디서 벌고 싶으세요» · 서버 화이트리스트 `tenant-settings.ts:22`. ⚠️ 덤으로 `director.ts:4` 의 «힌트 먼저»가 **낡은 줄**이었다(실제 순서는 ①고른 목표 ②붙은 광고 ③힌트) — 목표를 고른 집이 0곳이라 «맞아 보였다»(AC-255 와 같은 모양). 고쳤다. ⚠️ 그리고 **채널이 둘 이상인 집이 9곳**(1채널 30 · 2채널 8 · 3채널 1)이라 나머지 30집엔 순서 자체가 뜻이 없다 | 목표가 채널을 고르지 않는다(방향이 반대) — «adsense 가 목표니 티스토리 먼저»가 없다 ⇒ 🔴 **이 영향은 이미 해소돼 있었다** | ~~테넌트 목표(settings.goal)로 채널 우선순위 가중~~ → 한 일: 낡은 헤더 한 줄 정정 · 남은 것은 **고객이 실제로 고르는 것**(우리 손 밖) | M |
| 5.3-2 | 계정 배정: active · posts_today<daily_cap · health 순 · 같은 소재 두 계정이면 앵글 가르기 | ✅ | `director.ts:151` · 앵글 가르기 LLM 1콜 `director.ts:221` | — | — | — |
| 5.3-2 | 계정 배정에 **페르소나-소재 적합도(LLM 1콜)** 반영 | ✅ | grep `persona.*fit`·적합도 = 0 · 디렉터 LLM 콜은 앵글·reasons 만 · **2026-09-23 재측정: ✅** `lib/persona-fit.ts` · 🔴 **«점수를 재다»가 아니라 «배정을 바꿈»까지 확인**: `lib/director.ts:250` 이 `healthScore + bonus` 로 정렬하고, 사람(`director.ts:334`)·자동(`lib/cron/director-auto.ts:93`) **두 경로가 같은 판정기**를 쓴다. ⚠️ 설계의 «(LLM 1콜)»은 **글자 대조로 바꿈**(그 파일 머리말에 메인 지시 근거 · 돈이 두 배) | 페르소나가 소재와 안 맞아도 그 계정에 배정된다 | 앵글 LLM 콜에 «계정 적합도 0~1» 출력을 더해 배정 순위에 곱한다 | S |
| 5.3-1 | «켠 채널·종류»를 고객이 바꾸는 화면(온보딩 **뒤** 영상 켜기) | ✅ | `public/onboarding.html:49` 만 kinds 저장 · `settings.html` 의 kinds 칩은 내보내기용 · 기존 테넌트 87/99 가 video 없음(라이브 SELECT) · **2026-09-23 재측정: ✅** `public/app/settings.html:22`·`:23` «만드는 것» 그룹 + 영상 토글 — 온보딩 **뒤에도** 바�꿀 수 있다 | 온보딩을 지난 고객은 영상을 켤 길이 없다 | 설정에 «만들 것(글·영상)» 토글 1행 | S |
| 5.3-3 | 구성 로테이션(직전 글과 같은 구성 금지) | ✅ | `writing-contracts.pickFormat` · `director.ts:87 recentFormats` · 영상 `pickVideoFormat` | — | — | — |
| 5.3-4 | 일정 = 골든타임 × min_gap × 하루 분산 | ✅ | `lib/best-time.ts` 표 + 계정 goldenHours · 계정 간 30분 · 지난 시각이면 내일 | — | — | — |
| 5.3-5 | 수익 슬롯(상업 의도 ≥ 임계 → 쿠팡 후보 3) | ✅ | `director.ts:181` intent commercial/mixed → affiliate · `content-gen.ts:226 searchProducts(…,3)` | — | — | — |
| 5.3-6 | 코인 미리보기(piece 합계 · 확정 전) | ✅ | brief.coinCost · director.html «적용 · N코인» · 잔액 선검사 | — | — | — |

### §5.4 감성 프로파일
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5.4 | 채널 7행 기본 감성(네이버·티스토리·블로거WP·쓰레드·쇼츠·클립·릴스) | ✅ | `writing-contracts.ts` naver_blog·tistory·blogger·wordpress·threads + `shortsContract(channel)` 이 youtube_shorts·naver_clip(15~30·상단 자막)·reels(BGM 무드) 분기(`:251`) | — | — | — |
| 5.4 | DB `emotion_profiles` 오버레이 + 코드 기본값 | ✅ | `writing-contracts.ts:276-288 contractFor` 필드 단위 덮어쓰기 · AC-6 shape 가드 | — | — | — |
| 5.4 | **운영자가 화면에서 조정**(재배포 0) | ✅ | `emotion_profiles` 를 쓰는 API **0**(`grep -rl emotion_profiles netlify/functions/` = 0) · `public/ops/*.html` 에 감성 화면 0 · **2026-09-23 재측정: 그대로 🟡** — `emotion` 을 쓰는 문이 `netlify/functions/pieces.ts` 하나뿐이고 **운영 화면에서 바꾸는 자리는 여전히 0** · 🔴 **[2026-09-23 R17 · AC-258 — 만들었다]** `netlify/functions/ops-emotion.ts`(GET 목록 · `-save` · `-reset`) + 운영센터 화면(`public/ops/_tpl.txt` → `ops/ai.html` «집필 계약 · 감성» 묶음 · `loadEmo`/`emoSheet`). 🔴 **판정기는 한 벌**이다 — 얹는 규칙을 `lib/writing-contracts.ts applyOverlay` 로 빼고 **생성 경로(`contractFor`)가 같은 함수를 부른다**(두 벌이면 «화면은 먹었다는데 글은 그대로»가 난다 · PITFALLS #11-b). 🔴 **안 먹은 칸을 말해 준다** — 종전 merge 는 모양이 다르면 `continue` 로 **소리 없이** 넘어가서, `length:"1200"` 을 저장하면 저장은 성공하고 글은 그대로였다(조용한 0건). 이제 `ignored[{key,why,expected,got}]` 로 뜨고 **한 칸도 안 먹으면 저장 자체를 안 한다**(아무 일도 안 하는 저장을 «했다»고 말하지 않는다). 🔴 «즉시»라고 안 적는다 — `OVERLAY_CACHE_SECONDS`(60)를 **서버가 줘서** 화면이 «최대 60초 뒤부터»라 말한다(AC-52). 되돌리기 = 오버레이 행 삭제(§9-③). 자 `scripts/verify-emotion-overlay.mts` 10갈래 통과. ⚠️ **설계는 «집필 계약» «페이지»라 했는데 AI 엔진 화면의 한 묶음으로 넣었다** — 레일 목록이 `public/js/ops.js`(A 파일)라 페이지를 늘리면 남의 영역을 건드린다. **내용은 하나도 안 줄였고**, 페이지로 올리려면 레일 한 줄 + 파일 옮기기다(메인 판단). 🔴 **첫 실행에서 유령 행 하나를 잡았다** — 라이브 `emotion_profiles` 에 `youtube_shorts.graphic` 이 있는데 코드 열쇠는 `youtube_shorts.script` 라 **`contractFor` 가 영영 안 읽는다**. `orphans` 로 화면에 뜬다(«아무도 안 읽어요») | ~~운영자는 SQL 로만 고칠 수 있다~~ ⇒ 화면에서 바꾼다 | ~~`ops-emotion` GET/PUT + 운영센터 «집필 계약» 페이지~~ ⇒ 만들었다(페이지 대신 묶음 · 사유 위) | M |

### §5.5 디렉터 화면(토스형)
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5.5 | 자동안 카드 한 장(채널·계정·구성·코인·시각) | ✅ | `public/app/director.html:63` targets 행(채널 마크·@계정·구성·시각·코인) | — | — | — |
| 5.5 | CTA 2(«이대로 만들기»/«손보기») | ✅ | `director.html:20` 손보기·만들기 | — | — | — |
| 5.5 | 손보기 바텀시트 7(채널 토글·계정·감성·구성·이미지 수·제휴 상품·시각) + 영상 섹션 | ✅ | `director.html:70-96` data-drop 토글 · 계정 seg · 감성 chips · 포맷 · 사진 수 · 제휴 검색어+slot · 시각 · 영상(포맷3·길이·목소리·변주 읽기전용·clampedFrom 배너) · 서버 `PieceSpecPatch` 7키 | — | — | — |
| 5.5 | «디렉터에게 맡기기» 스위치(화면 건너뜀 · 신뢰 계정만 자동승인) | ➖ | R2 계약(편성표)이 흡수: `settings.autoSchedule` + `reviewPolicy` (`lib/slots.ts:20` · `lib/cron/produce.ts`) · 화면 = 편성표 «자동 편성» 토글 | «신뢰 계정만» 은 §4.2 행과 같이 테넌트 단위(🟠 참조) | DESIGN §5.5 문구를 편성표 어휘로 갱신 | S |

📊 §5 소계 31행: ✅ 22 · 🟡 2 · 🟠 3 · ❌ 1 · ➖ 2. — **진행률 30%** 지점.

---

## 3. §5C 글 고도화

### §5C.0 원칙 4
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5C.0-1 | 채널 = 다른 독자 · 집필 계약을 코드에(프롬프트 한 줄 X) | ✅ | `lib/writing-contracts.ts WRITING_CONTRACTS` 6 + shorts · `content-gen.ts:81` ①역할 = 계약 원문 | — | — | — |
| 5C.0-2 | 사람 흔적 넣기 + AI티 코드로 세어 반려 | ✅ | `content-gen.ts personaMaterial`(1~2) · `ai-tell-gate.ts runGate` 12키 · 실패 1회 재작성 → in_review | — | — | — |
| 5C.0-3 | 가독성은 시각 요소 · 구성 템플릿 배치 · 텍스트 벽 금지 | ✅ | `structureFor` 블록 시퀀스 · `visual_min` 게이트 | — | — | — |
| 5C.0-4 | 한국 시장(브랜드·원·계절·검색 습관·번역투 금지) | ✅ | `content-gen.ts:92-100` ④⑤ · `kr-calendar.ts` · TRANSLATIONESE | — | — | — |

### §5C.1 채널별 집필 계약(행 × 열)
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5C.1 네이버 | 말투·구조 5·시각(사진 6~10·인용구·구분선·체크리스트·소제목·해시태그·링크카드)·1,500~2,500·제목 네이버형 | ✅ | `writing-contracts.ts:50-75` 전부 · 링크카드 = 러너 se-oglink ✓ | — | — | — |
| 5C.1 네이버 | 시각 요소 «장소 카드» | 🟠 | 러너 `placesMap`·장소 모듈 grep 0 · 블록 타입에도 없음 · · **2026-09-23 재측정(정정 · 같은 날 두 번째): 🟠** — 🔴 **앞서 «만드는 곳이 0»이라 ❌ 로 적은 것은 틀렸다.** `lib/content-gen.ts:117` 에 **모델 프롬프트 어휘로 있다**(«소재나 재료에 실제로 나온 장소일 때만 한 개까지 · 지어낸 상호 절대 금지»까지 적혀 있다) · `lib/blocks.ts:206` 이 렌더하고 `channel-registry.ts:43,44` 에도 있다. ⚠️ 내가 `'"place"'`(따옴표 포함)로 찾아 프롬프트의 `place{place:{…}}` 를 못 봤다(AC-250 ③). 🔴 **그런데 한 칸은 진짜로 비었다**: `CAPS_NAVER`(`channel-registry.ts:115`)에 `place` 가 없어 **`null`(모른다)**이다 — «실제 장소 카드 **요소**로 뜨나»를 못 재다는 뜻이고, 🔴 **`null` 을 «안 된다»로 읽으면 거짓 빨강이다**(AC-9). ⇒ 글엔 들어간다(✅) · «시각 요소»인가는 **모른다** ⇒ 🟠 · **러너 못(B2)**으로 넘긴다 | 장소 후기 글에 지도 카드가 안 붙는다 | 블록 `place` + SE ONE 장소 모듈(러너) | S |
| 5C.1 네이버 | 사진 **캡션** | ➖ | 2026-09-15 사장님 실측(piece 329 «~놓여 있는 모습» 발행) → 메인 결정 «기본 캡션 없음 · captionRate 0.3 · 글쓴이 말투 ≤25자» (`84a2372`) | **DESIGN §5C.1·§5C.3 본문은 아직 «캡션 필수»** — CLAUDE §8 «설계를 바꿀 땐 DESIGN 먼저» 미이행 | DESIGN 문구 갱신 | S |
| 5C.1 티스토리 | «~합니다» · 구조 4 · 목차·H2·표·요약·강조·이미지 2~4·**애드센스 자리 2곳**·FAQ · 1,800~3,000 · 구글형 제목 | ✅ | `writing-contracts.ts:77-102` · `visualMin{h2:3,tableOrList:1,adsense:2}` · 러너 티스토리 HTML 모드는 B2 R6 §3.2 | — | — | — |
| 5C.1 워드프레스 | 티스토리와 같되 **영문 slug·메타** · H2·표·FAQ(AEO)·alt · 1,200~2,000 | ✅ | 계약 ✓(`:103-125`, 181) · alt ✓ · `lib/publish/blogger.ts`·`wordpress.ts` 에 slug·meta description 전송 **0**(grep 0) · · **2026-09-23 재측정(R17 에 행을 쪼개다): ✅** — `lib/publish/wordpress.ts:137 slugOf` · `:165` 이 `slug` 를 보낸다 | 검색용 slug·메타 설명이 안 나간다 | 발행 payload 에 slug·excerpt 추가(B2) | S |
| 5C.1 블로거 | 영문 slug(주소) | 🔒 | **2026-09-23 R17**: 🔴 **블로거 API 는 permalink 를 못 받는다 — 필드 자체가 없다**(메인이 구글 문서를 찾아 확인: `Posts` 리소스에 `slug`·`permalink` 속성이 없고 `url` 은 응답 전용). ⚠️ 나는 그걸 **못 확인해서 필드를 안 박았다** — 박았으면 «다 만들었다»고 적힌 채 **키 꾂는 날 무너졌다**(트리거 §1 «틀린 ㈟»). 🔴 **우회는 있다**: 제목을 원하는 slug 로 넣어 올리고 **그다음 제목을 고친다**(주소는 첫 제목으로 굳는다) — 자료가 있으니 ㈟ 다. ⚠️ **그런데 안 만들기로 했다 · 까닭 셋**(메인 권고 · 내가 동의): ① 문서에 없는 동작이라 **남의 버그에 우리 계약을 거는 꼴**이다 ② 글이 **두 번 저장**된다 — 발행 멱등(§4.7 `channel_ref`)과 부딪치고, 중간에 실패하면 **제목이 slug 인 글**이 고객 블로그에 남는다 ③ 🔴 **§9 — 막지도 말고 몰래 하지도 마라.** 하려면 고객이 «영문 주소로 올라가요»를 **알고 고르는** 자리여야 한다 | 주소가 한글로 굳는다(검색엔 큰 문제 아니다) | 사장님이 «영문 주소가 중요하다» 하시면 위 우회를 만든다(고객에게 알리고 고르게) | S |
| 5C.1 쓰레드 | «~임/~함» · 훅 1줄→3~5줄 · 500자 | ✅ | 계약 `writing-contracts.ts:127-145`(length 120~500 · 해시태그 0~2) | — | — | — |
| 5C.1 쓰레드 | **스레드 2~3개 연결** · 링크는 **댓글에** | ✅ | `lib/publish/threads.ts` 단일 포스트만(reply/chain grep 0) · 댓글 링크 0 · **2026-09-23 재측정: ✅** `lib/publish/thread-chain.ts`(순수 함수 · 2~3개 분할 · B2 R8CLOSE §B5) | 긴 글이 한 포스트에 잘린다 · 링크가 본문에 못 가고 댓글 경로도 없다 | threads 연쇄 답글 + 첫 댓글 링크(B2) | M |
| 5C.1 인스타 카드뉴스 | 표지→핵심 5→CTA · 카드 6~8 · 30자 | ✅ | 계약 ✓(`instagram` infographic 6~8 · `:158`) · **발행 경로 0** — `lib/publish/instagram.ts` 는 `publishReels` 뿐 · **2026-09-23 재측정: ✅** `lib/publish/instagram.ts:162`(캐러셀 발행 경로 · P1R8 §3.4) | 카드뉴스를 만들어도 올릴 데가 없다 | 인스타 캐러셀 발행 어댑터 | L |
| 5C.1 쇼츠·클립·릴스 대본 | 구어체·3초 훅 · 반전/문제해결/목록/비포애프터 · 자막 4~10·컷 4~8·엔드카드 · 15/30/60 · 무음 전제 | ✅ | `shortsContract`(`:251`) · `video/script.ts` 훅 게이트·포맷 3 · 엔드카드 payload+화면(R6 §2.3) | — | — | — |

### §5C.2 «사람이 쓴 것처럼»
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5C.2 넣기 | 실제 사정 1~2(지역·가족·직업·집·브랜드·계절) | ✅ | `content-gen.ts:48 personaMaterial`(2개 · seed 로 회전) · `personas.profile` | — | — | — |
| 5C.2 넣기 | 구체 숫자·실패담 · 개인 판단 1 · 문장 길이 분산 | ✅ | `writing-contracts.ts:55-58` rules(실패·판단·짧은 문장 30%) → 프롬프트 ① | — | — | — |
| 5C.2 세기 | 상투 표현 사전 60 = 0 | ✅ | `ai-tell-gate.ts CLICHES`(+2026-09-15 캡션 묘사문 7패턴) | — | — | — |
| 5C.2 세기 | 문단 시작 반복 ≤1 | ✅ | `runGate para_repeat` | — | — | — |
| 5C.2 세기 | 불릿 비율 ≤30% | ✅ | `runGate bullet_ratio` | — | — | — |
| 5C.2 세기 | 문장 길이 표준편차 ≥ 임계 | ✅ | `runGate sentence_variance` SD_MIN 9 | — | — | — |
| 5C.2 세기 | 번역투 ≤3 | ✅ | `TRANSLATIONESE` 10패턴 | — | — | — |
| 5C.2 세기 | 근거 없는 수치·최상급 0 | ✅ | `superlative` + 프롬프트 ⑤ + `topics.hasSuperlative` | — | — | — |
| 5C.2 세기 | 페르소나 사정 삽입 ≥1 | ✅ | `runGate persona`(personaTerms) | — | — | — |
| 5C.2 세기 | 채널 시각 요소 최소치 | ✅ | `visual_min`(네이버 quote1·divider1·image6 / 티스토리 h2 3·tableOrList 1) | — | — | — |
| 5C.2 세기 | 걸리면 1회 재작성 · 재반려는 in_review | ✅ | `content-gen.ts buildRewriteInstruction` 1회 → in_review + gate_report · 승인은 하드 5키만 막음(`content-approve.ts`) | — | — | — |

### §5C.3 구성 템플릿
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5C.3 | composition → 블록 시퀀스(예: 네이버 경험담 11블록) | ✅ | `writing-contracts.ts:47 NAVER_STORY` 15블록 · `structureFor` | — | — | — |
| 5C.3 | 러너/커넥터가 채널 에디터 **실제 요소**로 변환(SE ONE 인용구·구분선·체크리스트 모듈 · «눈검사» 스냅샷) | 🟠 | 네이버 러너: 인용구 `se-quotation` ✓ · 구분선 모듈 ✓(셀렉터 실측 수리 `naver-blog.mjs:146`) · 링크카드 ✓ · **체크리스트·표 = 텍스트 «•» 폴백**(`:477` list 타이핑 · table case 0) · 스냅샷 ✓ · **2026-09-23 재측정: 그대로 🟠** — `lib/format-marks.ts` 에 `no_editor_op` 가 4곳(«못 내렸다»를 **적는** 길은 산다). 실제 요소 변환은 채널마다 다르고 B2 영역 | 체크리스트·표가 모듈이 아니라 평문으로 나간다(§5C.0-3 «텍스트 벽 금지»에 반쯤 걸린다) | SE ONE 체크리스트·표 모듈 셀렉터 실측(B2 러너) | M |
| 5C.3 | 이미지 캡션 **필수** · 한글은 굽지 않고 오버레이 · 인물 실사·로고 금지 | ➖ | 캡션 «필수» → 2026-09-15 결정으로 «대부분 없음»(위 5C.1 행) · 한글 굽기 금지·인물·로고 금지 ✓(`ai-image.ts:22-23`) | DESIGN 문구 미갱신 | 위와 같음 | S |

### §5C.4 한국 시장 규칙 4
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 5C.4 | 원화·부가세 포함 · 한국 단위(평/㎡·ℓ·cm) | ✅ | `content-gen.ts:93` ④ · 영상 `tts.ts UNIT_READ` | — | — | — |
| 5C.4 | 제목 2벌(네이버형/구글형) · channel_hint 가 정함 | ✅ | `titleStyle` naver/google/hook/card/script · `content-gen.ts:94` | — | — | — |
| 5C.4 | 계절·명절 캘린더(`kr-calendar.ts`) | ✅ | `lib/kr-calendar.ts seasonalFor·seasonLine` · 소재 점수·프롬프트 ③ | — | — | — |
| 5C.4 | 존댓말 등급을 채널 계약이 고정 | ✅ | `register`·rules(네이버 «~했어요» · 티스토리 «~합니다» · 쓰레드 반말) | — | — | — |
| 5C.4 | **신조어 화이트리스트(페르소나 연령대)** | ✅ | grep 신조어·whitelist = 0 · `personas.profile` 에 연령 필드도 없음 · **2026-09-23 재측정: ✅** `lib/banned-words.ts:107`(계정별 신조어 화이트리스트 · R8CLOSE-B1 §B3) | 연령대에 안 맞는 신조어를 거르지 못한다 | 연령대별 사전 + persona.age | S |

📊 §5C 소계 32행: ✅ 24 · 🟡 1 · 🟠 2 · ❌ 3 · ➖ 2.

---

## 4. §16B 광고·제휴 정책 준수

| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 16B 정본 | `lib/disclosure.ts` + `lib/ad-policy-gate.ts` · 위반 시 발행 차단 | ➖ | `disclosure.ts` ✓ · 게이트는 `lib/publish/gate.ts`(발행 직전) + `content-approve.ts`(승인) 로 이름만 다름(R1 계약 §3) · 차단 ✓ | — | DESIGN 파일명 갱신 | S |
| 16B.1 | 블로그·글: 본문 첫머리 블록 · 구분되는 블록 · 같은 크기 이상 · 회색 금지 | ✅ | `writing-contracts.ts:346` unshift disclosure · `blocks.ts` div.disclosure 첫 요소 · `piece.html:34` lock · 러너 첫 블록 타이핑 | — | — | — |
| 16B.1 | 영상: 시작 3초 자막 + 우상단 상시 배지 + 설명란 첫 줄 | ✅ | `video/gen.ts:238-239` badge tr · disclosureCaption 3000ms · description 첫 줄 · `checkVideoDisclosure` 3종 | — | — | — |
| 16B.1 | 쓰레드·인스타: 첫 줄 «#광고» «#쿠팡파트너스» | ✅ | `publish/threads.ts:13` 첫 줄 고지 · `publish/instagram.ts:64` #광고 | — | — | — |
| 16B.1 | 문구는 경제적 대가가 명확(«활동의 일환»만 불충분) | ✅ | `DISCLOSURE_TEXT.coupang` 공식 전문(«…일정액의 수수료를 제공받습니다») | — | — | — |
| 16B.2 | 쿠팡 공식 문구 | ✅ | `disclosure.ts:12` 글자 그대로 | — | — | — |
| 16B.2 | 일반 제휴 문구 | ✅ | `disclosure.ts:13` | — | — | — |
| 16B.2 | 영상 배지 짧은 형 + 설명란 전문 | ✅ | `disclosure.ts:14` videoBadge · `videoDescriptionFirstLine` | — | — | — |
| 16B.2 | 협찬·유료광고 «유료 광고 포함» 문구 | ✅ | 문구 `disclosure.ts:15 sponsored` ✓ · **소비처 0**(지시서에 협찬 필드 없음) · **2026-09-23 재측정: 그대로 🟡** — `lib/disclosure.ts` 에 `sponsored` 16곳으로 문구는 산다. 소셜(인스타·쓰레드) 쪽은 위 §16B.3 라벨 행으로 닫혔다 · 🔴 **[2026-09-23 R17 정정 — 위 «소비처 0»·«지시서에 협찬 필드 없음»이 둘 다 거짓이다]** 필드는 **있다**(`lib/director.ts:68 monetize.{affiliate,sponsored,gift,adDisclosure}`), 손보기가 **받는다**(`:545` · `:550` 이 `adDisclosure` 를 셋에서 파생), 고지 블록이 **갈라진다**(`lib/content-gen.ts:279 ensureDisclosureFirst({affiliate, sponsored, gift})` — 🔴 **막는 게 아니라 넣어 주는 것** · §9-④), 결과가 `meta.paid` 로 남고(`:644`) 관문이 셋을 다 안다(`ai-tell-gate.ts:228`). **화면도 있다**(§4.8): `public/app/piece.html:292 COMP_ASK` «원고료·PPL을 받았어요»·«제품·서비스를 무상으로 받았어요» · `write.html:31~32` 직접 쓰기에도 같은 두 스위치. 🔴 그리고 발행까지 간다 — 내가 오늘 붙인 `compensationKinds`(AC-253)가 유튜브 `paidPromotion` 을 **`sponsored` 일 때만** 켠다(`disclosure` 유무로 켜면 **거짓 신고**가 된다). ⇒ **✅** | ~~협찬 글에 표시가 안 나간다~~ ⇒ 나간다 | ~~monetize.sponsored 필드 + 고지 블록 분기~~ ⇒ 둘 다 있었다 | S |
| 16B.2 | 유튜브 «유료 프로모션 포함» 설정 | ✅ | `publish/youtube.ts` `paidPromotion`·`hasPaidProductPlacement` grep 0 · **2026-09-23 재측정: 그대로 ❌** — 🔴 다만 **까닭은 고친다**: 앞서 적은 «유튜브 커넥터 없음»은 **틀렸다**(AC-250). 커넥터도 합성 플래그도 있고, 이 행이 없는 것은 **`paidPromotion` 칸 그 자체**다 — `youtube.ts` 에 grep 0(재확인) · 🔴 **[2026-09-23 R17 — 그날 오후에 내가 만들었고, 행을 안 고쳤다]** `lib/publish/youtube.ts:190` 이 `status.paidPromotion` 을 **`sponsored` 일 때만** 켠다(AC-253 · 커밋 `2288977`). 🔴 **`disclosure` 유무로 켜면 «거짓 신고»가 된다** — 제휴만 붙은 글에 «유료 광고 포함»을 켜는 것은 사실이 아니고, 그건 우리가 고객 계정 이름으로 하는 **거짓 신고**다. 그래서 대가 3종 중 **`sponsored` 하나**에만 건다(`lib/publish/contract.ts:90 compensationKinds` · `index.ts:130`). 거부되면 **플래그만 빼고 다시 올린다**(`:226` · 감사 남김). 정책 감시도 있다(`lib/cron/policy-review.ts:34 youtube_paid`). ⚠️ 🔴 **이 행은 앞의 여섯과 종류가 다르다** — 잘못 잰 게 아니라 **내가 같은 날 오전에 만들어 놓고 오후에 그 행을 안 닫았다.** 「고치는 판에서는 «내가 방금 무엇을 닫았나»도 세야 한다」 ⇒ **✅** | ~~협찬 영상이 유튜브 규정 위반으로 올라간다~~ ⇒ 해결 | ~~videos.insert status 플래그~~ ⇒ 만들었다 | S |
| 16B.3 쿠팡 | 고지 첫머리 · 문구 일치 | ✅ | 위 16B.1 · `checkDisclosureHtml` isDisclosureText | — | — | — |
| 16B.3 쿠팡 | 채널명/도메인에 «쿠팡» 금지 → 핸들 검사 | ✅ | `netlify/functions/accounts.ts:125,172` handle_policy | — | — | — |
| 16B.3 쿠팡 | 상품 이미지 API 제공분만 · 가격은 API 값만 · 링크 API(subId) | ✅ | `affiliate-coupang.ts:4-7` 검색·딥링크·subId=piece · `blocks.ts affiliate` imageUrl/price 는 API 응답 | — | — | — |
| 16B.3 쿠팡 | 카톡 대량 발송·스팸 금지 | ➖ | 카톡 발송 기능 자체가 없다(해당 없음) | — | — | — |
| 16B.3 애드포스트 | 글당 제휴 링크 ≤2 · 유사도 · 광고법 금칙어 | ✅ | `content-approve.ts affiliate_count`(≤2) · similarity · `banned-words.ts findBannedWords` · 소재 단계 `banned-categories` | — | — | — |
| 16B.3 애드센스 | AI티 게이트 필수 · 이미지 생성물/자기 사진만 · 광고 자리 라벨 «광고» | ✅ | `runGate` · 이미지 = `ai-image.ts` 생성물만 · `div.adsense` «광고» 라벨(A CSS) | — | — | — |
| 16B.3 유튜브 | 설명란 고지 · **합성 콘텐츠 플래그** · 계정 간 변주 게이트 | 🟡 | 고지 ✓ · `publish/youtube.ts:123 containsSyntheticMedia:true` ✓ · 변주(훅·팔레트·보이스) ✓ · **프레임 지문 미작동**(§4.3 행) · **2026-09-23 재측정: 그대로 🟠** — 🔴 **[정정 · 같은 날] 앞서 «유튜브 커넥터가 없다»고 적은 것은 틀렸다**(B2 가 뒤집었고 메인이 재서 확인). `lib/publish/` 는 **23개**이고 `youtube.ts`(20,435B · `be8bf74` 09-15)가 **있으며** `index.ts:31`·`:60`·`:61` 로 배선돼 있다. 합성 플래그도 `youtube.ts:181 containsSyntheticMedia: true` 로 **있다**. ⇒ 🔴 **남은 것은 구글 앱(OAuth) 하나**이고, 이 행은 아래 🔒 행과 한 덩어리로 **덩어리가 훨씬 작다**. ⚠️ 틀린 까닭: `ls lib/publish/ | head -20` 의 출력을 «전부»로 읽었다 — 잘린 뒤가 `wp-advanced·x·youtube` 였다(AC-250) · 🔴 **[2026-09-23 R17 · 🟠 → 🟡]** 이 행이 걸려 있던 까닭은 «프레임 지문 미작동» 하나였는데, 오늘 §4.3 을 쪼개 재 보니 **코드 경로는 끝까지 이어져 있다**(러너 `render-video.mjs:832 thumbGray` → `runner-jobs.ts:1799`·`video/render-queue.ts:119` → `video/judge.ts:343 similarityAxis` 해밍 거리 ≤10). ⚠️ **다만 라이브 도착이 0이다**(영상 3편 · 지문 0행 · B2 실측: `runner_jobs` 에 `render.video` **0행**) ⇒ «안 만들었다»가 아니라 **«아직 한 번도 안 와 봤다»**라 🟡 다. 🔴 «러너 코드에 있다» ≠ «러너가 실제로 보냈다»(B2). ⇒ 이 행은 §4.3 영상 지문 행과 **같이 움직인다** | 반복 콘텐츠 판정 회피가 대본·변주까지만 | 프레임 지문(§4.3) | L(동일) |
| 16B.3 유튜브 | 유튜브 OAuth 앱(키·심사) — 연결·비공개 업로드 전제 | 🔒 | `oauth-providers.ts:3` GOOGLE_OAUTH_CLIENT_ID/SECRET · 로컬 .env 0 · 없으면 `provider_not_configured` → 화면 «준비 중이에요»(accounts.html:74) · **키 꽂으면 즉시 가동**(연결·`publishYoutubeShorts` 비공개 업로드까지 코드 완비 · R5 §7 사장님 액션) · **2026-09-23 재측정: 그대로 🔒** — 외부 선결(앱 심사). 오늘 바뀜 것 없다 · 🔴 **[2026-09-23 R17 — «키 꽂으면 즉시»의 넷을 B2 가 재서 채웠다 · B2 실측 `1fb4684`(내가 잰 게 아니다)]** ①**«열쇠가 없어요»와 «불렀는데 실패했어요»가 갈린다**: `lib/publish/youtube.ts` 가 **다섯**으로 가른다 — `:165 provider_not_configured`(앱 키 없음 «준비 중이에요») · `:166 no_creds` · `:128 auth_failed`(401 만료) · `:130 auth_failed`(403 «업로드» 권한) · `:123/:126/:132 channel_error`(쿼터·한도·장애 · 전부 retriable) · `:133 config`. ②화면이 미리 안다(`accounts.html:74`). ③키 꽂는 자리 = env(`GOOGLE_OAUTH_*`). ④**`planned → active` 가 손 SQL 없이 된다** — 길 셋(화면이 문을 부른다 · 서버가 화이트리스트 지나 `status` 를 **쓴다** · 고객 `listChannels()` 가 **같은 칸을 읽는다**)을 B2 가 변이로 확인(서버 쓰기만 떼면 빨강). ⚠️ 🔴 **라이브 토글은 미실증** — 실제로 `active` 로 뒤집으면 고객 연결 그리드가 바뀌는 **공개 토글**이라 사장님 승인 사안(§4.5)이다. **«코드 경로 실증 · 라이브 토글 미실증»**으로 적는다(«실증했다»로 읽히면 안 된다). ⑤🔴 **쿼터를 미리 말해 준다**(B2 가 오늘 만들었다 — 종전엔 `DAILY_CAP` 이 `youtube.ts` 밖 **0곳**이라 **실패할 때만** 말했다): `youtubeDailyCap()` → `accounts-list channels[].dailyPublishCap` → 계정 시트 «하루에 N개까지 올라가요». **막지 않는다 · 미리 말해 준다**(§9 «있는데 좁은 길»). ⚠️ 🔴 **설계 §2.2 는 «≈6건/일»인데 코드 기본값은 5** — 쿼터가 채널이 아니라 **구글 프로젝트 단위**라 전 고객이 한 통을 나눠 쓰기 때문이고, B2 가 **까닭만 상수 주석에 적고 값은 안 건드렸다**. **정할 일(메인·사장님)** 로 남는다 | 유튜브 계정을 못 붙인다 | 앱 키 발급·심사(사장님) — **그 앞 아홉 칸은 다 찼다** | L(외부) |
| 16B.3 네이버 클립 | 원본 생성물만 · 제휴 고지 | ✅ | 생성물만 · 고지 공용 · (발행 자체는 러너 스텁 → §A) | — | — | — |
| 16B.3 인스타·쓰레드 | «#광고» 첫 줄 | ✅ | `publish/instagram.ts:64` · `publish/threads.ts:13` | — | — | — |
| 16B.3 인스타·쓰레드 | **유료 파트너십 라벨**(브랜드 협찬 시) | ✅ | Graph API branded_content grep 0 · **2026-09-23 재측정: ✅** `lib/publish/instagram.ts:293`(유료 파트너십 라벨 · 거부되면 태그만 빼고 재시도+감사) | 협찬 시 라벨이 안 붙는다(향후) | 16B.2 협찬 행과 묶음 | S |
| 16B.4 | 지시서 adDisclosure=true → 첫 블록 고정 · 손보기 이동/삭제 불가 | ✅ | `structureFor` unshift · `piece.html:32-35` lockDisc/restoreDisc(«지울 수 없어요» 토스트) | — | — | — |
| 16B.4 | 쇼츠 배지 = 오버레이 트랙(자막 별도 · 세이프존 우상단 · 전 구간) | ✅ | payload `overlay.badge{corner:"tr"}` · 러너 `render-video.mjs` 오버레이 레이어 · safeZone 220/300 | — | — | — |
| 16B.4 | 발행 직전 재검사(사람이 수정해도) → awaiting_manual + **알림** | ✅ | 글 `publish/gate.ts:81` · 영상 `publish-video-background`(2026-09-15) · notifications `publish_manual` | — | — | — |
| 16B.4 | 그 사유가 **홈 «해야 할 일»**에 뜬다 | ✅ | `netlify/functions/home-summary.ts:32-45` todo = readonly/suspended · 재로그인 계정 · in_review 수 · 첫 계정/규칙 · 이메일 인증뿐 — **awaiting_manual 항목 0**(B3 지적 · 내가 처음에 ✅로 잘못 적었던 행) · **2026-09-23 재측정: ✅** `netlify/functions/home-summary.ts:14 summarizeSlotBlocks` + `:16 GATE_FAIL_LABEL`(라벨 정본은 서버 · AC-52) | 발행 직전에 막힌 글이 홈에서 안 보이고 알림함에만 있다 | home-summary todo 에 awaiting_manual 1행(«올리지 못한 글 N건 → 직접 올리기») | S |
| 16B.4 | 정책 원문 링크·확인일을 `disclosure.ts` 헤더에 | ✅ | `disclosure.ts:3-6`(확인일 2026-09-14 · 쿠팡·공정위 링크) | — | — | — |
| 16B.4 | **분기 1회 «정책 재확인» 운영 체크리스트** | ✅ | ops 화면·크론 알림 grep(정책 재확인·quarter) 0 · **2026-09-23 재측정: ✅** `lib/cron/policy-review.ts` + 우산 등록(`lib/cron/runner.ts` STEPS) — 분기 1회(1·4·7·10월 KST 09시) 감사로 남긴다 | 분기가 지나도 아무도 안 찔린다 | 운영센터 «운영진·감사»에 «정책 재확인 D-N» 한 줄 + 분기 알림 | S |

📊 §16B 소계 27행: ✅ 18 · 🟡 1 · 🟠 2 · 🔒 1 · ❌ 3 · ➖ 2. — **진행률 60%** 지점.

---

## 5. §6 쇼츠 공장

### §6.1 이식 체인 8
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 6.1 | shorts-topics 소재 뱅크 | ➖ | R5 §0.1-3: AC `topics` 흡수 + `shorts_templates`(레퍼런스 구조) · `topics-reference` 저작권 게이트 | — | — | — |
| 6.1 | shorts-script 대본 + Google 검색 팩트체크 왕복 + 광고법 게이트 | ✅ | `video/script.ts` buildVideoScript · `factcheckRoundTrip`(googleSearch · 정정 1회) · `checkScriptGates` · `ai.ts:100` google_search | — | — | — |
| 6.1 | shorts-scenes 컷 계획·충돌 점검 | ✅ | `video/scenes.ts` planCutWindows·buildCutPlans·checkCutConflicts · 토킹 12컷(§1.3 정합 `5e02186`) | — | — | — |
| 6.1 | 이미지/클립 생성(video-providers i2v · CHAIN_IMAGE) | ✅ | `providers/{registry,omni,veo,fal,index}.ts` · 정지컷 `generateStill`(CHAIN_IMAGE) · 스텁 분기 | — | — | — |
| 6.1 | creative-tts(자막 본체·나레이션 보조·읽기 전처리) | ✅ | `tts.ts`(전처리·예산·Gemini 폴백) · `tts-typecast.ts`(어절 시각) · `captions.ts` 구절 분할 · 키 세대(AC-39) | — | — | — |
| 6.1 | creative-render(러너 크롬 캡처 + ffmpeg 1080×1920) | ✅ | B2 `runner/channels/render-video.mjs` · `render-queue.ts` · R2 HEAD 실존 확인 · 실측 mp4(BGM 믹스 mean -16.6dB) | — | — | — |
| 6.1 | 8축 심사 | ✅ | `video/judge.ts` 11축 3등급(P0 차단) · 2026-09-15 C 수리(계획값→ffprobe 실측 · 꼬리 판정) | — | — | — |
| 6.1 | shorts-loop 주 N편 무개입 편성 | ➖ | R5 §-1: 편성표 `kind:'shorts'` 규칙·슬롯이 흡수(새 루프 0) · `produce` 크론이 shorts 슬롯도 집음(v5.4) | — | — | — |

### §6.2 디벨롭 6
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 6.2 | 포맷 3(그래픽·토킹·클립형) | ✅ | `VideoFormat` graphic·talking·clip · `shortsFormOf` 표 · 디렉터 손보기 세그먼트 3 | — | — | — |
| 6.2 | 채널 규격: 쇼츠 ≤60 · 클립 15~30 · **릴스 90** · 세이프존 | ➖ | `VIDEO_CHANNEL_MAX_SEC` 60/30/60/60 · 세이프존 220/300 ✓ · 릴스 90 은 R5 §7-3 «R5 제외 · Phase 5»(메인 확정) · 표 한 줄이면 열린다 | **DESIGN §6.2 본문은 아직 «릴스 90»** | DESIGN 문구 갱신(또는 표 값 90) | S |
| 6.2 | 수익 슬롯: 엔드카드 «설명란 링크» 컷 · 설명란 쿠팡 링크+고지 | ✅ | 엔드카드 text ✓ + url(제휴 시) + 검수 화면(R6 §2.3) · `gen.ts` affiliateMeta 설명란 링크 + 첫 줄 고지 | — | — | — |
| 6.2 | 수익 슬롯: **쇼핑 태그 메타** | ✅ | grep shoppingTag·쇼핑 태그 = 0 — 외부 승인(유튜브 쇼핑 제휴) 전이지만 **«키 꽂으면 즉시» 상태가 아니다**(코드 0) · **2026-09-23 재측정: ✅** `lib/publish/instagram.ts:195`(쇼핑 태그 · 거부 시 태그만 떨괴 · 라벨보다 나중) | 승인이 나도 붙일 코드가 없다 | 쇼핑 태그 메타 필드·API 배선(승인 후 활성) | M |
| 6.2 | 다계정 변주(훅·팔레트·보이스) + **중복 업로드 판정 회피(프레임 지문)** | ✅ | 변주 결정론 ✓(`director.ts variantFor` · 사람말 이름 `PALETTE_LABELS_KO`) · 대본 유사도 ✓ · 프레임 지문 **러너 미보고**(§4.3) · **2026-09-23 재측정: ✅** `lib/video/fingerprint.ts`(pHash 64bit) + 🔴 **러너가 지문을 보낸다**: `runner/channels/render-video.mjs:800` `thumbGray`. 09-15 판정이 «러너가 안 보내 안 돌간다»였는데 그 자리가 메워졌다 | 지문 축이 통과로 접힌다 | §4.3 과 동일 | L(동일) |
| 6.2 | 렌더 러너 필수 · **관리형 러너 팜 병렬** | 🔒 | 내 PC 러너 ✓ · 관리형 팜 = R6 §3.1 신청만(B2) · 실기기 프로비저닝 Phase 4 · `publish.youtube_shorts` 잡 kind 어휘 예약 · **2026-09-23 재측정: 그대로 🔒** — 관리형 러너 팜은 Phase 4 | 팜이 없다(코드는 신청·큐까지) | Phase 4 팜 | L |
| 6.2 | 검수 화면에서 **영상 내려받기**(직접 올리기 경로의 마지막 조각) | ✅ | `piece.html:52` `<video controls>` · `:56` SRT `download=` ✓ · **mp4 `download=` 0** · `posts.html` «직접 올리기» 탭도 «영상 보기» 링크뿐 · **2026-09-23 재측정: 그대로 🟠 — 다만 화면은 생겼다**: `public/app/piece.html:654` «영상 받기». 위 §4.2 행(주소 적기)이 닫혔으니 **이 행과 같은 덩어리**다 — 다음 판에 두 행을 합치거나 같이 닫는 것이 맞다 · 🔴 **[2026-09-23 R17 정정 — «모바일 사파리는 못 받는다»가 거짓이다]** 서버가 **파일이름을 서명 안에 담은** 프리사인을 준다(`netlify/functions/piece-video.ts:96 r2PresignGet(key, DOWNLOAD_TTL_SEC, { filename })`)고, 화면은 `piece.html:654` «영상 받기» → `location.href = r.url` 로 **그 주소로 바로 간다.** 🔴 `<video controls>` 메뉴가 아니라 **`Content-Disposition: attachment` 응답으로 내려받는 것**이라 모바일 사파리에서도 받는다. 그리고 이름이 서명 안에 있어(`lib/r2.ts:87~110`) 🔴 **한글 제목이 살아서 저장된다**(RFC 5987 `filename*` + ASCII 폴백 두 벌) — 프리사인은 교차 출처라 화면의 `<a download>` 는 **무시되므로 헤더 말고 방법이 없다.** ⇒ **✅** | ~~데스크톱 크롬 컨트롤로만 받는다 · 모바일 사파리는 못 받는다~~ ⇒ 둘 다 아니다 | ~~«영상 내려받기» 버튼 1개~~ ⇒ 있다 | S |
| 6.2 | 저작권: BGM 게이트 + FreePD R2 시드 | ✅ | `video/bgm.ts resolveBgm`(BGM_LICENSE_VERIFIED 없으면 무음) · `scripts/seed-bgm.mjs`(archive.org 미러 12곡 시드 완료 · env 등록 완료) · 실측 믹스 | — | — | — |

### §6.3 코인
| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 6.3 | video_15=6 · video_30=12 · video_60=28 · 재렌더 무료 | ✅ | `coin-table.ts:11` 6/12/28(+video_clip 2) · `videoCoinItem` 구간제 · 재렌더·수리·재큐 0 | — | — | — |

📊 §6 소계 17행: ✅ 10 · 🟠 2 · 🔒 1 · ❌ 1 · ➖ 3. — **진행률 85%** 지점.

---

## 6. §10 AI 버전 관리

| DESIGN § | 항목 | 상태 | 증거 | 안 되는 것 | 남은 일 | 크기 |
|---|---|---|---|---|---|---|
| 10.1 | 모델 이름은 `lib/ai-models.ts` 에만 | ✅ | `ai-models.ts` MODEL_*·CHAIN_*·VIDEO_MODELS · 다른 파일은 import(grep 모델 문자열 = 0) | — | — | — |
| 10.1 | 목록에 있다고 쓰지 않는다 — 우리 키로 실측 후 등재 | ✅ | `scripts/verify-ai-models.mjs` · AC-26 thinking 프로브 수리 | — | — | — |
| 10.2 | `ai_model_overrides`(role→chain · applied_by/at · canary_pct) | ✅ | `db/schema.ts:533,715` | — | — | — |
| 10.2 | cron 주 1회 models.list diff | ✅ | `lib/cron/ai-model-watch.ts:4` 월 06:00 KST · `runner.ts:64` 등록 | — | — | — |
| 10.2 | 신규 후보 실측 4종(text·json·googleSearch·image/tts) | ✅ | `ai-model-watch.ts:5` text·json·googleSearch·image | — | — | — |
| 10.2 | 후보 → 운영자 알림 | ✅ | `ai-model-watch.ts` ai_settings.candidates + 운영 알림 | — | — | — |
| 10.2 | manual → 운영 «적용» 버튼(즉시/카나리 10%) | ✅ | `public/ops/ai.html:42` «적용 · 카나리» | — | — | — |
| 10.2 | auto → 카나리 10% 24h → 승격/롤백 → 감사 | ✅ | `ai-model-watch.ts:7-8` · `ai.html:47` 문구 | — | — | — |
| 10.2 | 롤백 = 오버레이 삭제 | ✅ | `ai.html:42` 롤백 버튼 · prev_chain 보존 | — | — | — |
| 10.2 | 운영 콘솔 «AI 엔진» 한 페이지(역할별 현재·후보·카나리·롤백) | ✅ | `public/ops/ai.html` · `netlify/functions/ops-ai.ts` | — | — | — |
| 10.2 | 분기마다 오버레이를 파일에 굽는 «정본 동기화» 버튼 = **PR 생성** | ✅ | 🔴 **[2026-09-23 R17 정정 — 위 재측정의 «PR 생성은 없다»가 거짓이다]** `lib/ai-models-sync.ts`(214줄 · `4589d4d` 2026-09-16)가 **PR 생성을 다 하고 있었고** `netlify/functions/ops-ai-sync.ts` 도 있었다. ⚠️ 나는 **화면(`ai.html`)만 보고** «없다»고 적었다 — 같은 날 `mode auto` 에서 밟은 것과 **같은 실수**다(AC-250). ⇒ 진짜 구멍은 **넷**이었고 넷 다 닫았다(AC-256): ①🔴 **화면이 서버를 한 번도 안 불렀다** — 제 손으로 글자를 만들어 복사시키고 있었다(§4.8). 이제 `ai.html:100 syncSheet()` 가 GET 으로 갈림을 보여 주고 POST 로 PR 을 연다 ②🔴 **동기화가 5역할만 구웠다** — AC-252 에서 오버레이를 11역할로 넓히면서 굽는 쪽을 안 넓혔다(**내가 만든 구멍**). 닻을 «env 이름»에서 «상수 이름»으로 바꿔 11/11 실증(`scripts/verify-ai-models-sync.mts` 13갈래) ③`.env.example` 에 `GITHUB_TOKEN`·`GITHUB_REPO`·`GITHUB_BRANCH` 가 **없었다**(R17 §3③ «키 꽂는 자리») ④**분기 알림이 없었다** — `ai.model_watch` 에 `SYNC_REMIND_DAYS=90` 멱등 알림. ⚠️ 라이브 오버레이가 **0행**이라 갈림 경로는 «모수 0»이다 — 합성으로 쟀다(AC-141②) \| 옛 증거: `ai.html:48,59` «정본 동기화 내려받기» = 코드 텍스트 **복사**(«PR 은 사람이») · **2026-09-23 재측정: 🔵 반만 왔다** — `public/ops/ai.html:49`·`:100` 에 «정본 동기화 **내려받기**» 가 있고 복사까지 된다. 🔴 그러나 설계가 말한 **PR 생성은 없다** — 화면이 제 입으로 «PR 은 사람이»라고 적어 둔다(정직하다) | PR 이 자동으로 안 생긴다 · 분기 알림도 없다 | GitHub API PR 생성(선택) + 분기 알림 | S |

📊 §10 소계 11행: ✅ 10 · 🟠 1. — **진행률 100%**.

---

## A. 특별 항목 — 영상 축이 고객 화면에서 실제로 쓰이는가

**라이브 실측(읽기 SELECT · 2026-09-15)**
```
channel_registry:  youtube_shorts=planned · naver_clip=planned · reels=planned · tiktok=planned  (threads·instagram 도 planned · 글 4채널만 active)
tenants 99 중 settings.kinds 에 video 있는 곳 = 12(대부분 테스트·온보딩 기본값) · 영상 채널 계정 = youtube_shorts 4 · **영상 piece = 0건**
```

**고객 경로를 따라간 결과**
1. **계정 연결 그리드** — `public/app/accounts.html:38` `.filter((c) => !c.status || c.status === "active" || cnt[c.key])` = «가동 전(planned) 채널은 숨긴다». 레지스트리가 영상 4채널 전부 `planned` 라 **고객은 영상 채널을 볼 수도 연결할 수도 없다.** 운영센터 `public/ops/channels.html:48` 상태 세그먼트로 `active` 로 바꿀 수 있고 힌트도 «가동이면 고객이 바로 연결할 수 있어요»인데 **한 번도 켜지지 않았다.** → 이것이 «영상이 전부 빠져 있다»의 직접 원인(코드가 없어서가 아니라 **문이 잠겨 있어서**).
2. **디렉터** — `director.ts:154-157` `wantVideo = kinds.includes("video")` **그리고** 살아 있는 영상 계정이 있어야 영상 piece 를 낸다. 1 때문에 계정이 0 → 영상 제안 0 → `pieces kind='video'` 0.
3. **kinds 스위치** — `public/onboarding.html:49` 는 기본 `["text","video"]` 로 저장하고 영상 채널이 안 열렸으면 «곧 열려요» 칩(정직). 그러나 **온보딩 뒤에 켜고 끄는 화면이 없다**(settings.html 의 kinds 칩은 내보내기용). 기존 테넌트 87/99 는 영상이 꺼진 채로 있고 켤 길이 없다 → 🟡.
4. **유튜브 OAuth 없이 «만들기 → 심사 → 내려받기 → 직접 올리기»** — 코드상 열려 있다: `youtube_shorts` 는 OAuth 앱 키(`GOOGLE_OAUTH_CLIENT_ID`) 없으면 `provider_not_configured` → 계정 연결에서 «준비 중이에요»(accounts.html:74) 🔒. **`naver_clip` 은 `connectMethod:"session"`** 이라 OAuth 없이 계정을 붙일 수 있고(accounts.ts:133 · 네이버 블로그와 같은 세션) → 디렉터가 클립 30초 영상을 내고 → `piece.html` 에서 `<video controls>` 재생 + **SRT 내려받기**(`:56`) → 발행은 러너 스텁 `runner/channels/naver-clip.mjs`(`not_supported_yet`) → `awaiting_manual` + 알림 «클립은 앱에서 올려 주세요» → `posts.html:17` «직접 올리기» 탭. **이 경로가 OAuth 없는 실경로다.** 그런데 naver_clip 도 1 에서 숨겨져 있어 시작조차 못 한다.
5. **내려받기의 마지막 조각** — `piece.html` 에 **mp4 «영상 내려받기» 버튼이 없다**(`download=` 는 SRT 뿐). 데스크톱 크롬은 플레이어 컨트롤로 받을 수 있지만 **모바일 사파리는 못 받는다** — «직접 올리기»는 폰에서 하는 일이다 → 🟠.
6. **직접 올린 뒤 주소를 적을 곳이 없다** — `posts.html:77` «직접 올리기» 시트는 «본문 복사하기»뿐이고 `posts.ts` 에 URL 을 적는 API 가 없다 → piece 가 영원히 `awaiting_manual` 이고 수익 매칭(piece↔URL)도 안 된다(B3 지적 · §4.2 행 추가) → ❌.
7. **레지스트리 `active` 로 켜면 무엇이 열리나**(코드 판정 · 실측 아님): naver_clip 연결 → 30초 영상 제작 → 검수 → SRT 받고 플레이어로 저장 → 앱에서 직접 올리기. youtube_shorts 는 OAuth 키·앱 심사(R5 §7 사장님 액션) 뒤 비공개 업로드까지. reels 는 Meta 앱 키 뒤.

**결론 한 문단**: 영상 축은 R5 계약 §1 기준 **코드 100% · 화면 8장 존재 · 라이브 렌더·BGM 믹스 실측까지 끝난** 상태다. 그러나 고객 경로의 **첫 문(계정 연결 그리드)** 이 `channel_registry.planned` 로 잠겨 있고 그 문을 여는 손잡이는 운영센터에만 있다. 사장님이 «영상 관련이 전부 빠져 있다»고 보신 것은 정확한 관찰이며, 원인은 **누락이 아니라 미개통**이다 — 운영센터에서 `naver_clip`(과 원하면 `youtube_shorts`)을 `active` 로 바꾸는 순간 OAuth 없는 경로가 그대로 열린다. 그 뒤에 남는 실제 결함은 4개: **mp4 내려받기 버튼 없음(🟠 · S)** · **직접 올린 주소 적기 없음(❌ · S · B3 지적)** · **온보딩 뒤 영상 켜기 화면 없음(🟡 · S)** · **프레임 지문 미작동(🟠 · L)**. 즉 «앱에서 직접 올리기» 경로는 문을 열어도 **뒤 두 칸(받기·적기)이 막혀 있다.**

## B. 상태 집계

| 상태 | 개수 |
|---|---|
| ✅ | 103 |
| 🟡 | 4 (온보딩 뒤 영상 켜기 화면 · 감성 프로파일 운영 화면 · 인스타 카드뉴스 발행 · 협찬 «유료 광고 포함» 소비처) |
| 🟠 | 15 |
| 🔒 | 2 (유튜브 OAuth 앱 — «키 꽂으면 즉시» · 관리형 러너 팜 — Phase 4) |
| ❌ | 9 (직접 올린 주소 적기 · 페르소나 적합도 LLM · 장소 카드 · 스레드 연결/댓글 링크 · 신조어 화이트리스트 · 유튜브 유료 프로모션 플래그 · 인스타 유료 파트너십 라벨 · 분기 정책 재확인 · 쇼핑 태그 메타) |
| ➖ | 9 (format 어휘 · 맡기기 스위치 · 캡션 필수 ×2 · ad-policy-gate 파일명 · 카톡 스팸 · 소재 뱅크 · 루프 · 릴스 90) |
| ⬜ | 0 |

> 🔴 **DESIGN.md 미갱신 2건**(§5C.1/5C.3 캡션 «필수» · §6.2 «릴스 90»)은 결정은 났지만 문서가 뒤처졌다 — CLAUDE §8 «설계를 바꿀 땐 DESIGN 먼저» 순서가 뒤집힌 상태. 메인이 DESIGN 을 고쳐야 ➖ 가 닫힌다.

## C. 남은 일 크기 합
- **S 24**: brief 상태 정리 · piece draft/approved 정리 · draft 전이 · 신뢰 계정 자동승인 · format 어휘 DESIGN 갱신 · heroNeeded 소비 · 페르소나 적합도 · **온보딩 뒤 영상 켜기** · 맡기기 문구 · 장소 카드 · 캡션 DESIGN 갱신 ×2 · 블로거 slug/메타 · 신조어 · ad-policy-gate 파일명 · 협찬 문구 소비처 · 유튜브 유료 프로모션 · 인스타 유료 파트너십 · 정책 재확인 알림 · 릴스 DESIGN 갱신 · **mp4 내려받기 버튼** · **직접 올린 주소 적기** · 홈 해야 할 일 awaiting_manual 행 · 정본 동기화 PR
- **M 6**: ali/linkprice 제휴 삽입 · 목표→채널 가중 · 감성 프로파일 운영 화면 · 쓰레드 연쇄+댓글 링크 · SE ONE 체크리스트/표 모듈 · 쇼핑 태그 메타
- **L 4**(행 6 · 프레임 지문 3행 = 같은 일): **프레임 지문(러너 보고 + judge)** · 인스타 카드뉴스 발행 · 관리형 러너 팜(Phase 4) · 유튜브 OAuth 앱 심사(외부)

## D. 부록 — 실측한 것
- 라이브 DB **읽기 SELECT 4건**(channel_registry 전체 · tenants.settings.kinds 집계 · 영상 채널 계정 수 · 영상 piece 상태 집계) · 행 생성 0 · 보존 테넌트 무접촉.
- 코드 판정은 전부 `grep`·읽기(파일:줄 기재). 화면은 `public/app/*.html`·`public/ops/*.html`·`public/onboarding.html` 소스 판독(눌러 보는 실측은 특별 항목 b = B3 세션 몫이라 여기서 하지 않았다).
- 이 조사에서 코드를 고친 것 0 · 실호출 0.
