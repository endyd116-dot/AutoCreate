# P1R2-B2 트리거 — 러너(클라이언트+서버 큐) · 발행 커넥터 · 발행 멱등 (B2)

> 발부 2026-09-14 · 베이스 = main `bbc4d6e`(P1R1 머지본 + R2 계약서) · 워크트리 `../AutoCreate-B2` · 브랜치 `feature/p1r2-back2`
> 🔴 이 라운드의 한 줄: **«글이 실제로 네이버 블로그·티스토리·블로거·워드프레스에 올라간다.»** 서버리스에서 못 하는 일(브라우저 자동화)을 러너가 한다.
> 🔴 B 가 같은 라운드에서 크론·슬롯·계정 전이를 만든다. 경계는 계약 §10 — **`publish()` 와 `finalizePublish()` 두 함수를 네가 만들고**, B 는 그것만 호출한다. 시그니처를 **가장 먼저(착수 30분 내)** 확정해 메인에 알려라(B 가 기다린다).

```
P1R2-B2다. 베이스 = main bbc4d6e. 워크트리 ../AutoCreate-B2 · 브랜치 feature/p1r2-back2(이미 체크아웃됨 · npm ci·.env 완료).
읽을 것(순서대로): docs/rules/PARALLEL_GUIDE.md(§2.6) · CLAUDE.md §3~§8 · docs/rules/PITFALLS.md §0(특히 AC-3 세션 섞임·AC-8)·#1·#4 ·
        docs/active/2026-09-14-P1R2-contract.md(🔴 정본 — §2 러너 서버측·§3 발행 커넥터·§7 러너 클라이언트·§9 담당·§10 경계) ·
        docs/active/2026-09-14-P1R1-contract.md §4B(bodyHtml class 계약 — 러너가 올릴 본문의 모양) ·
        docs/DESIGN.md §8(러너 전체)·§2.1(채널별 발행 방식)·§4.3(발행 멱등)·§7.3(격리)·§16B(발행 직전 게이트)·§19(카나리) ·
        코드: lib/accounts.ts · lib/creds-crypto.ts · lib/oauth-providers.ts · lib/blocks.ts(renderBlocksHtml) · lib/disclosure.ts · lib/banned-words.ts · db/schema.ts(runner_devices·runner_jobs·posts) ·
        AM 원본(읽기 전용·복사 출처 헤더 기록 필수): ../AutoMarketing/lib/content-runner.ts(큐 상태기계·claim/report/release) · lib/runner-block.ts(실패 분류) · lib/runner-jobs.ts · scripts/content-runner-core.mjs(러너 코어·세션·이미지 다운로드) · scripts/naver-blog-runner.mjs(스마트에디터 ONE 셀렉터·다중 폴백·스냅샷) · scripts/runner-start.bat(런처) · lib/publish-instagram.ts/publish-threads.ts(커넥터 관례)
[자율주행 — 완전 자율]
- 파일 R/W/Edit, git, bash·PowerShell, npm/npx(러너 폴더는 자체 package.json + playwright 설치 가능), curl — 묻지 말고 자율 실행.
- 커밋 자유. push 차단 — 메인이 머지·배포. 끝나면 [브랜치·해시·변경요약] 보고.
- ①애매한 로직 ②되돌리기 어려운 결정 ③비가역 작업일 때만 질문. 막히면 30분 안에 보고.
- ⚠️ **실제 네이버/티스토리 계정으로 «발행»하지 마라**. 검증은 **임시저장까지**(드라이런) 또는 자사 테스트 계정. 남의 계정·실서비스에 글을 남기지 않는다.
[🔴 전본 규칙 — CLAUDE §8] 계약 §-1 «이번(R2)» 열이 네 범위. «축소판·생략» 금지. 못 만들면 사유 보고.
[진행률 — 의무] "📊 진행률 X%/100 — 방금: … / 다음: …" 최소 8회.
[보고 대상] 메인 세션(autocreate-ca) — SendMessage. 🔴 착수 30분 내 §10 두 함수 시그니처를 먼저 보고.
```

■ 지금 있는 것 / 없는 것
  있음: 계정·자격(암호화 저장 · `account_creds` kind password/app_password/oauth) · `oauth-providers.ts`(인가 URL·토큰 교환 — 갱신만 추가하면 됨) · `blocks.renderBlocksHtml`(bodyHtml 정본) · `disclosure`·`banned-words`(게이트 재료) · 스키마 `runner_devices`·`runner_jobs`·`posts` 전부 존재(**DDL 불필요**) · `RUNNER_TOKEN_SECRET` env.
  없음: 러너 클라이언트 전부 · 서버 큐 API · 발행 커넥터 전부 · finalize. → B2-1~B2-6.

■ B2-1 (0→8%) 🔴 **경계 먼저** — `lib/publish/contract.ts` + 빈 구현
  `publish(piece, account, opts)` · `finalizePublish(pieceId, result)` 의 타입과 반환을 확정해 파일로 올리고 **즉시 메인에 보고**(B 가 이 시그니처로 publisher 를 짠다). 구현은 뒤 섹션에서 채운다.

■ B2-2 (8→26%) 서버 큐 — `lib/runner-jobs.ts` · `lib/runner-block.ts` · `netlify/functions/runner.ts`(계약 §2 · 5경로)
  register(토큰 생성 · sha256 저장 · 평문 1회 반환 · 설치 명령 문자열) · list · remove · heartbeat(last_seen_at·version·status online → 5분 무응답이면 조회 시 offline 계산) · queue(claim/report/release).
  claim: `FOR UPDATE SKIP LOCKED` 원자 선점 · 우선순위(발행 10 > 세션 20 > 통계 50 · 숫자 작을수록 먼저) · 테넌트 스코프 · **계정 자격 복호화해서 payload 에 실어 준다**(러너 claim 응답 = 평문 자격이 나가는 유일한 표면 · 그 외 0 · 감사 남김).
  report: 성공이면 `finalizePublish` 호출 · 실패면 `classifyRunnerBlock(errorKind, detail)` → 계정 전이 신호를 `accounts.last_error_kind` 에 기록(전이 로직 자체는 B 의 `account-health.ts` · 없으면 필드만 쓰고 넘어간다 — 두 곳에서 상태를 쓰지 않는다).
  release: 큐 되돌림(attempts++).

■ B2-3 (26→46%) 발행 커넥터 — `lib/publish/index.ts` · `blogger.ts` · `wordpress.ts` · `finalize.ts`
  `publish()`: 채널별 분기 → API 채널이면 직접 발행 후 `finalizePublish` 까지 하고 `via:"api"` · 러너 채널이면 `runner_jobs` 적재 후 `via:"runner"` 반환.
  **발행 직전 최종 게이트**(§16B): bodyHtml 첫 요소 `<div class="disclosure">` 존재(없으면 삽입) · 금칙어 · 제휴 링크 ≤2 → 실패면 발행 금지 + `{ ok:false, reason:"gate" }`(B 가 awaiting_manual 처리).
  `blogger.ts`: Blogger v3 `POST /blogs/{blogId}/posts`(OAuth access token · 만료면 refresh) · 라벨=tags · 본문은 bodyHtml 그대로.
  `wordpress.ts`: 이미지 먼저 `POST /wp-json/wp/v2/media`(Basic) → 반환 URL 로 bodyHtml 의 R2 URL 치환 → `POST /wp-json/wp/v2/posts`(status publish · title·content·tags).
  `finalize.ts finalizePublish(pieceId, { externalUrl, channelRef, via })`: **멱등**(이미 external_url 있으면 로그만) → `posts` 행 + piece `published`·published_at + slot `published` + 계정 `posts_today++`·`last_post_at` + 알림 1행. 이 함수만 그 상태들을 쓴다.

■ B2-4 (46→76%) 러너 클라이언트 — `runner/**` (계약 §7) 🔴 분량 최대
  `runner/package.json`(playwright) · `ac-runner.mjs`(엔트리 · `--peek` `--once` `--headed` `--canary` · 하트비트 루프 · sleepSec 존중) · `core.mjs`(claim→실행→report · 계정별 영구 프로필 `runner/profiles/{profileKey}` · **피스마다 새 컨텍스트**(AC-3) · 이미지 다운로드 · `RUNNER_SHOTS=1` 이면 단계 스냅샷 → 실패 보고에 shotKey) · 채널 4개:
  - `channels/naver-blog.mjs` — 스마트에디터 ONE. AM `naver-blog-runner.mjs` 의 셀렉터·다중 폴백·이미지 첨부를 **이식**하고 §4B class 를 에디터 요소로 변환(인용구·구분선·체크리스트·사진+캡션·해시태그). 실패는 정직 분류.
  - `channels/tistory.mjs` — 신규. 에디터(HTML 모드 우선 — bodyHtml 을 그대로 넣을 수 있으면 그 경로가 가장 안전) · 카테고리·태그·공개 설정.
  - `channels/session-login.mjs` — 헤드풀 창을 띄워 사람이 로그인 → 쿠키 회수 → `runner-session-upload` 로 업로드(평문은 HTTPS 본문에만).
  - `channels/post-alive.mjs` — 발행 URL 생존·조회수 회수(`verify.post_alive`·`revenue.stats`).
  - `run.bat`(Windows 런처 · 토큰 1회 입력 · node/playwright 자동 설치 안내 · ASCII only — AM `runner-start.bat` 관례) · `install.md`.
  🔴 **카나리**(`--canary`): 자사 테스트 계정으로 «임시저장까지» 드라이런 → 셀렉터 변경을 고객보다 먼저 감지(DESIGN §19). 결과를 `runner-queue report` 가 아니라 `runner-heartbeat` 의 `canary` 필드로 보고(서버는 운영 알림만).

■ B2-5 (76→86%) OAuth 토큰 갱신 — `lib/oauth-providers.ts` 확장
  access token 만료 시 refresh(Google·Meta/Threads 장기 토큰 교환) · 갱신값 재암호화 저장 · 실패면 계정 `disconnected` + 알림(«다시 연결해 주세요»).

■ 검증 (86→97%)
  `npx tsc --noEmit` 0 · 함수 `export const config` · 자격 평문은 claim 응답 외 0(응답·로그 grep) · 발행 멱등(같은 piece 두 번 → posts 1행).
  러너: 로컬에서 `node runner/ac-runner.mjs --peek`(큐 조회) · `--once --headed` 로 **네이버 임시저장까지** 드라이런(자사 테스트 계정 · 발행 금지) · 스냅샷 확인 · 실패 분류가 서버에 기록되는지.
  워드프레스: 실사이트가 없으면 **로컬 WP 또는 wordpress.com 테스트**로 인증·발행 1회 시도 → 불가하면 «키/사이트 없음»으로 정직 보고(코드는 완성).
  블로거: OAuth 앱 키 없으면 `provider_not_configured` 경로 확인(코드 완성).
  ⚠️ 하니스 초록은 증거가 아니다(PITFALLS #9) — 스냅샷·큐 행 id·응답 본문으로 되짚어라.

■ 보고 (97→100%)
  [브랜치·해시·변경요약 · §10 두 함수 최종 시그니처 · 러너 드라이런 증거(스냅샷 경로·job id) · 밟은 함정 · 🔴 설계 대비 못 만든 것과 사유(없으면 «0»)]
