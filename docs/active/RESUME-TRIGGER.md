# 재개 트리거 — 2026-09-20 압축 판

> 🔴 **새 세션은 아래 코드블록을 그대로 따른다.** 다른 것부터 하지 마라.

```
[메인 재개 · 2026-09-20 07:3x KST 압축]

■ 0. 먼저 읽는다 (순서대로 · 다른 일보다 먼저)
  1) docs/active/2026-09-20-HANDOFF.md      ← 🔴 오늘 서사·다음 판 목록·라이브 재료가 전부 여기 있다
  2) CLAUDE.md §4(컨벤션) · §5(병렬) · §6(자율) · §8(전본) · §9(게이트 0개)
  3) docs/rules/PARALLEL_GUIDE.md §2.7·§2.8
  4) docs/rules/PITFALLS.md 의 AC-112~123  ← 🔴 어제 하루에 열둘이 생겼다. 그게 지금 규율이다
  5) docs/rules/pending-red.json            ← «기다리는 빨강»이 무엇이고 누구 몫인지

■ 1. 상태를 «세어서» 확인한다 (짐작 금지 · 한 줄씩 따로 · | tail·&&·; echo 금지 — AC-67·AC-110)
  git log --oneline origin/main..main | wc -l      # 미배포 (압축 시점 0)
  npx tsc --noEmit
  node scripts/verify-r8-deadends.mjs              # 135/0 이었다
  node scripts/verify-repair-round.mjs             # 15/19칸 초록
  node scripts/verify-mutant-residue.mjs           # 0건
  for d in A B C B2; do (cd ../AutoCreate-$d && git status --porcelain | wc -l); done   # 넷 다 0 이었다

■ 2. 🔴 사장님께 물을 것 (압축 전에 못 여쭌 것)
  · «다음 판을 어디부터 할까요» — 후보는 HANDOFF §4 에 우선순위로 적혀 있다.
    내 추천: ①bleed.total ↔ 계획 문단 수 대조(빼기 한 번 · 가장 싸고 넓다)
             ②링크 줄 사라짐 수리(원인 확정됨 · 고칠 방향 셋도 적혀 있다)
             ③취소가 취소가 아닌 것 · 설정 시트가 고객 값을 덮어쓰는 것(둘 다 손님이 겪는다)
  · 🔴 «트리거를 사장님께 드릴까요, 메인이 직접 창에 보낼까요» — 9/19 엔 «나한테 줘», 9/20 엔 «메인이 직접».
    **마지막 말씀이 우선이지만 바뀔 수 있으니 한 번 여쭌다.**
  · 사장님 손 넷(당근 탐침 · 틱톡 키 · video_90 42코인 · 영상 레퍼런스 월 한도 3/6/20/60) — 아직 그대로다.

■ 3. 🔴 절대 선 (바뀌지 않았다)
  · env·비밀값 출력 금지 · 보존 테넌트 3·13·109·116·198(+451) 무접촉 · ai_usage 불변
  · 되돌릴 수 없는 일·대외 접촉(R2 업로드·실발행·공개 토글)의 승인은 «그 창에서 사장님 말씀으로만»(AC-50)
  · provider 실호출은 메인 허락 후 · 라이브는 읽기만(쓰기는 시험 테넌트만)
  · A·B·C·B2 는 commit 만 · push 는 메인 단독 · 배치 push(≤2~3회/일)
  · 🔴 «머지했다»가 «돌고 있는 프로세스가 새 코드다»를 뜻하지 않는다 — 서버·러너는 껐다 켜고 **들어갔는지 확인**한다
  · 🔴 시각은 KST 로 다시 확인한다(`TZ=` 가 이 셸에서 안 먹는다 · node 의 toLocaleString 을 써라)

■ 4. 🔴 일하는 법 (어제 이게 전부였다)
  · «있다»를 «된다»로 읽지 마라 — 코드에 있는 것과 눌러서 되는 것은 다르다
  · 수리마다 «자»를 같이 낸다 · 자를 내면 **스스로 변이를 넣어 본다**(AC-108)
  · 변이가 안 울면 **변이를 먼저 의심**(AC-112) · 울어도 **무엇을 보고 울었는지** 본다(AC-121)
  · 못 잰 것은 «못 쟀음»으로 적는다(AC-9) — 통과로 뭉개지 마라
  · 실험은 **판정표를 먼저 적고** 돌린다 · 🔴 돌리기 전에 «이 실험이 성립하나»를 되짚는다
  · 남의 전언을 그대로 받지 말고 **제품에 다시 물어라** — 어제 메인이 세 번 틀렸고 세 번 다 창이 되받았다

■ 5. 지금 창들
  ../AutoCreate-A(화면) · -B(서버·운영) · -B2(러너·영상) · -C(검증) · 메인 나무(push 단독)
  🔴 이름은 재시작마다 바뀐다 — ListAgents 로 찾되 **폴더·브랜치·HEAD 를 물어서** 확인하고 일을 준다.
  AC 번호 칸: A 130~139 · B 140~149 · B2 150~159 · C 160~169 · 메인 170~179 (마지막 쓴 번호 = AC-123)
```
