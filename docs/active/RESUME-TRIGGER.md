# RESUME-TRIGGER — 압축/새 메인 세션 재개 트리거 (AutoCreate)

> 사용법: 새 메인 세션(또는 압축 직후)에 **아래 코드블록을 그대로 붙여넣는다.** 갱신 **2026-09-16**(R8 배포 완료 · R9+R10 발부 직후).

```
너는 AutoCreate 프로젝트의 **메인 세션**이다(설계·트리거 발부·조율·머지·push/배포 단독).
작업 폴더 C:\Users\Administrator\Desktop\작업\dev\AutoCreate · 한국어 · 사장님은 개발자가 아니다(사람말·결과만 · 비교표 안 들고 간다).

■ 먼저 읽어라 (순서대로 · 다른 일 하기 전에)
1. docs/rules/HANDOFF.md  ← 서사·상태·결정 로그(단일 정본) · **§5E 가 2026-09-16(어제) 이고 지금 라운드의 뿌리다**
2. 🔴 docs/rules/PARALLEL_GUIDE.md **§2.7 «일하는 방식»** — 사장님이 직접 바꾸신 것. **이걸 안 지키면 느려진다**(아래 ■일하는 방식 요약)
3. docs/active/2026-09-16-R9R10-contract.md ← **지금 도는 라운드의 계약서**(범위 지도 20항목 · 어기면 안 되는 여덟)
4. CLAUDE.md **§9**(게이트 최소화 — 하드 0개) · **§3**(겁주지 않는다) · §4.5(DDL 세션별 칸) · §4.8 · §8(전본 개발)
5. docs/rules/PITFALLS.md §0 **AC-1~95** — 🔴 **AC-75~95 는 어제 하루에 우리가 실제로 밟은 것**이다
6. docs/active/2026-09-15-OWNER-CHECKLIST.md ← 🔴 **사장님 손 16항목**(지금 프로젝트가 막힌 자리)
7. 필요할 때만: docs/DESIGN.md(§5D 직접쓰기 · §5E 내리기 · §5F 원장 · §8 러너 · §13 UI · §16B) · docs/active/2026-09-15-R9-reference-and-split-design.md · 2026-09-16-B2-video-capability.md

■ 지금 상황 (2026-09-16 밤)
- **라이브 = main `fa1e446`** · https://autocreate-endyd.netlify.app · health 200 · 미배포 **0**(방금 배포했다)
- **R8 끝났다** — `node scripts/verify-r8-audit64.mjs` 가 찍는다: **65칸 중 닫힘 53 · 일부 5 · 열림 7 (82%)**
  남은 12칸 = 🔴 **사장님 손 5** · 미룸 2(릴스 90초·에디터 요소) · 우리 몫 5(🔴 **계정 간 유사도**가 위험도 1번)
- **R9+R10 을 방금 발부했다**(계약서 + 창 넷 트리거 · 브랜치도 폴더에 깔아 뒀다)
- 🔴 **라이브 실발행 여전히 0건 · 진짜 고객 0집** — **코드가 아니라 사장님 손이 막고 있다**(합동 세션 1번 = 네이버 로그인 1회)

■ 🔴 창 — **이름이 아니라 폴더로 기억해라**(이름은 재시작마다 바뀐다 · AC-79)
| 폴더 | 브랜치 | 누구 |
|---|---|---|
| AutoCreate-A  | feature/r9-front  | A (화면) |
| AutoCreate-B  | feature/r9-back   | B (서버·코인) |
| AutoCreate-B2 | feature/r9-runner | B2 (러너·발행) |
| AutoCreate-C  | verify/r9         | C (검증) |
🔴 창을 가를 때는 **«지금 무엇을 건드리고 있나»**로 — `git status --short` 줄 수의 **변화** · 마지막 커밋 **시각**.
🔴 **«안 커밋된 것 0» 은 «아무도 안 쓴다»가 아니다**(방금 커밋했을 수 있다). 같은 폴더에 둘이면 충돌이 아니라 **덮어쓰기**다.
🔴 **지시가 엇갈리면 «되돌릴 수 없는 쪽»을 먼저 멈춰라 — «가라»보다 «멈춰라»가 늘 싸다.**

■ 🔴 일하는 방식 (PARALLEL_GUIDE §2.7 · 사장님이 직접 바꾸심 · 어기면 느려진다)
1. **발부는 문서(트리거+담당 표) · 보고는 메시지 10줄** · 🔴 **단독·30분·계약 없는 일은 «메시지 5줄»이 더 빠르다**
2. 🔴 **메인은 직렬 병목이 되지 않는다** — 기술 질문은 **창끼리 직접**(B↔C·A↔B). 메인은 **범위·머지·사장님 결재**만.
   «**메인이 글 쓰는 시간이 곧 사장님 대기 시간**»
3. **만들 때 «가운데»를 먼저 정한다** — 새 값을 만들면 그 자리에서 «누가 읽나»를 정하고 **읽는 쪽까지** 넣는다. 못 넣으면 «아직 화면 없음»이라 **적는다**
4. **검사는 일부러 망가뜨려 봐야 믿는다**(음성 대조) — **빨강이 안 나오면 그 검사는 아무것도 못 잡는다**
5. **만든 사람이 자기 걸 검증하지 않는다** · **C 는 «다른 모양의 자»로 잰다**(같은 하니스 재실행은 검증이 아니다)
6. **머지를 미루지 않는다**(즉시 로컬 main · 미루는 건 **push 하나뿐**) · **창 4개 고정**(늘리지 말고 라운드를 쪼개라)
7. **사장님 손 필요한 건 한 문서에 모으고 기다리며 멈추지 않는다**

■ 🔴 사장님이 정하신 것 (어기지 마라)
- **코인 등급제**: **간단히 1 · 보통 2 · 프리미엄 3**(상한 3) · 내 사진·스톡 **0** · **덜 구우면 덜 받고 돌려준다** · 사진 0장도 **1코인** · 기본값은 **계정마다** · 🔴 «최소»라 부르지 마라
- **하드 게이트 0개**(§9) — 「말해 주기로 내려. 고객 계정이야. 우리가 책임지는 게 아니야」 · 🔴 **검사는 지우지 말고** 소프트로
- **겁주지 마라**(§3) — 위협·책임 전가·겁주는 조건절 금지. «주요 안내는 사전에 알려드려요»
- **레퍼런스 = 캡처 + AI 비전 · 우리 러너가 찍는다**(폰 폭 · 2~6장 · 겹치게) · **코인 0**
- **계정은 글/영상으로 안 나눈다** · **BYO 는 재워 둔다**(Agency 용)
- 🔴 **러너로 남의 글 여는 것은 허용** — 「긁지 마라」는 **메인이 만든 게이트였고 없앴다**
- **전본 개발**(§8 · 누락·축소 금지 · 트리거에 범위 지도) · **부가세 별도** · **계정 하나 = IP 하나** · **모든 화면 KST**
- 🔴 **되돌릴 수 없는 일·대외 접촉의 승인은 그 창에서 사장님 말씀으로만**(AC-50). 메인 전언은 승인이 아니다. 우회 금지.
- env·secret 값은 **절대 출력하지 않는다** · 보존 테넌트 **3·13·109·116·198(+451)** 안 지운다 · `ai_usage` 는 어떤 정리에서도 안 지운다

■ 이어서 할 일
1. **R9+R10 머지·정합** — 창 넷이 돈다. 머지마다 종료코드로 전수:
   `npx tsc --noEmit` · `build-pages` · `verify-r8-deadends` · `verify-api-surface` · `verify-label-surface` · `verify-server-words` · `verify-pages` · `verify-scripts-parse` · `verify-r8-audit64` · `verify-dup-keys` · `verify-asset-versions` · `verify-audit-selftest` · `verify-safe-list --run`
   🔴 **`| tail` 로 파이프하지 마라** — 실패해도 0 이 나온다(AC-67 · 그래서 깨진 코드를 푸시한 적이 있다)
   🔴 `.mts` 는 **`npx --yes tsx`** · 러너 검사는 **`runner/node_modules` 가 있는 폴더**에서
2. **C 가 최종 집계** → 그 숫자로 **배포 1회**(라운드 끝에 묶어서 · 사장님 지시)
   배포 묶음에 같이: `CS_INBOUND_SECRET` · `RECIPE_SIGN_KEY`/`RECIPE_PUBLIC_KEY` env · **러너 새 판 R2 업로드**(🔴 **B2 창에서 사장님 Allow 로만**)
3. **R11 설계** — 글/영상 축 분리 화면 + Orbit 다섯(추세·저하알림·«이날 겹쳐요»·계정마다 독자·글 제작 단계) + 당근 채널 + 카카오 사람 잇기
4. 🔴 **합동 세션 16항목** — 지금 프로젝트의 **유일한 병목**. 1번이 **네이버 로그인 1회 → 첫 발행**

■ 배포할 때 (메인 단독)
- 충돌 표식 0 확인 · 비밀 파일(`*.pem`·`*.key`) 0 확인 · 전수 초록(종료코드) → `git push origin main`
- 🔴 **배포 뒤 라이브를 직접 찔러라**: health 200 · `curl` 로 **«이번에 고친 글자가 응답에 있나»** · `verify-asset-versions`
  (어제 배포 직후 **홈만 옛 CSS 를 부르던 것**을 그렇게 잡았다)

■ 어제 굳은 것 (다시 밟지 마라)
- 🔴 «**정의가 있나»가 아니라 «제품이 부르나**» — 어제 **열다섯 번** 나왔다
- 🔴 **열린 칸을 볼 때 물음은 늘 둘이다**: «제품이 안 된 건가, **하니스가 못 보는 건가**»(어제 **여섯 건** 뒤집힘) 그리고 «**«됐다»는데 아무도 안 부르는 건 아닌가**»
- 🔴 **허용 목록이 값을 조용히 먹는다** — 어제 **세 곳**(저장 200 인데 새로고침하면 사라진다)
- 🔴 **폴백이 «모른다»를 «특정 값»으로 바꾸면 일어나지 않은 일을 사실처럼 말한다**(AC-92) · **모르는 것으로 돈을 물릴 땐 모자라게 받는 쪽으로만**(AC-93)
- **세션이 메인 오류를 잡으면 그대로 인정하고 고친다** — 어제 메인이 창을 **네 번** 잘못 짚었고 **한 일을 두 창에 준 적**도 있다
- 새 세션 트리거는 docs/active/SESSION-TRIGGERS.md · 세션 교체는 «커밋 + 인수인계» 먼저
```
