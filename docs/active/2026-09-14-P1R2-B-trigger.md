# P1R2-B 트리거 — 편성 크론 6스텝 · 슬롯 게이트 · 계정 전이/승계 · 슬롯·발행함 API (B)

> 발부 2026-09-14 · 베이스 = main `afa9466`(P1R1 머지본 + R2 계약서) · 워크트리 `../AutoCreate-B` · 브랜치 `feature/p1r2-back`
> 🔴 이 라운드의 한 줄: **«사람이 아무것도 안 눌러도 편성표가 스스로 돌아 글이 만들어지고 발행까지 간다.»** R1 은 사람이 눌러서 한 편을 만들었고, R2 는 그걸 크론이 대신한다.
> 🔴 B2 가 **같은 라운드에서 러너·발행 커넥터**를 만든다. 경계는 계약 §10 함수 2개(`publish()` 호출만 · 상태 기록은 `finalizePublish()` 한 곳). 그 파일들은 B2 것이다 — 만들지 마라.

```
P1R2-B다. 베이스 = main afa9466. 워크트리 ../AutoCreate-B · 브랜치 feature/p1r2-back(이미 체크아웃됨 · npm ci·.env 완료).
읽을 것(순서대로): docs/rules/PARALLEL_GUIDE.md(§2.6 파이프라이닝 포함) · CLAUDE.md §3~§8 · docs/rules/PITFALLS.md §0(AC-1~8)·#1·#4 ·
        docs/active/2026-09-14-P1R2-contract.md(🔴 정본 — §-1 범위 지도·§1 크론·§4 계정 전이·§6 API·§9 담당 분리·§10 B↔B2 경계) ·
        docs/active/2026-09-14-P1R1-contract.md(v1.3 — 기존 키 이름) ·
        docs/DESIGN.md §4(상태기계)·§5B(편성 전체)·§7.2(계정 전이)·§12(코인)·§19(운영) ·
        코드: lib/slots.ts(rollSlots·ScheduleSettings 이미 있음) · lib/director.ts(propose/confirm/triggerGenerate) · lib/coin-ledger.ts · lib/topics.ts · lib/accounts.ts · lib/audit.ts · netlify/functions/rules.ts·pieces.ts·tenant-settings.ts(mergeSettings) · netlify.toml ·
        AM 원본(읽기 전용·복사 출처 헤더 기록): ../AutoMarketing/netlify/functions/cron-tick-5m.ts·cron-tick-hourly.ts(스텝 우산 구조) · lib/content-slot-gate.ts · lib/content-approve.ts · lib/editorial-board-slots.ts(슬롯 원장 관례)
[자율주행 — 완전 자율]
- 파일 R/W/Edit, git(add·commit·checkout·merge·fetch·restore), bash·PowerShell, npm, npx, curl(검증) — 묻지 말고 자율 실행.
- 커밋 자유. push 차단됨 — 메인이 머지·배포. 끝나면 [브랜치·커밋해시·변경요약]만 보고.
- ①진짜 애매한 로직 ②되돌리기 어려운 결정 ③비가역(DB drop·reset --hard)일 때만 질문. 막히면 30분 안에 보고.
[🔴 전본 규칙 — CLAUDE §8] 계약 §-1 범위 지도의 «이번(R2)» 열이 네 범위다. «간단히·축소판·이번엔 생략» 금지. 못 만들면 사유 보고.
[진행률 — 의무] "📊 진행률 X%/100 — 방금: … / 다음: …" 최소 8회(섹션 착수·완료).
[보고 대상] 메인 세션(autocreate-ca) — SendMessage.
```

■ 지금 있는 것 / 없는 것 — 메인 실측(다시 재지 마라)
  있음: `lib/slots.ts`(SCHEDULE_DEFAULTS·scheduleSettingsOf·readScheduleSettings·rollSlots·listSlots·coinsPerWeek·spreadWeekdays/MonthDays·ruleHitsDate) · `lib/director.ts`(propose/confirm/triggerGenerate — 사람이 부르는 경로) · `lib/coin-ledger.ts`(consume 멱등·grant·refundPiece) · `lib/topics.ts`(refresh/pick) · `lib/content-gen.ts` + `generate-piece-background` · 40표 스키마(slots·runner_jobs·posts·notifications 전부 존재 · **DDL 추가 불필요**) · `CRON_SECRET` env 등록됨.
  없음: 크론 함수 2개 · 스텝 7개 · 슬롯 게이트 · 계정 전이/승계 · 슬롯 3경로·발행함 API. → B1~B6.
  ⚠️ 함정: Date 바인딩 금지(AC-5) · jsonb 는 `jsonb()`(AC/PITFALLS #1) · timestamp 는 `utcDate()` · KST 계산은 SQL 안에서(#4) · emotion_profiles 같은 DB 오버레이는 shape 가드(AC-6).

■ B1 (0→14%) 크론 우산 2개 — `netlify/functions/cron-tick-5m.ts` · `cron-tick-hourly.ts` + `lib/cron/runner.ts`
  [만들 것] 스텝 등록기(`STEPS = [{ key, every:"5m"|"hourly", run(tenant, settings) }]`) · 테넌트 루프(활성 테넌트 = status trial|active) · **테넌트 단위 try/catch 격리** · 스텝별 `{ tenants, changed, skipped, errors }` 집계 · 전건 `writeAudit`(action `cron_<step>`) · 응답 = 계약 §1 모양.
  게이트: 헤더 `x-nf-event: schedule` 이거나 `x-cron-secret`/`?secret=` 일치. 아니면 401.
  `netlify.toml` 에 `[[scheduled]]` 2개(*/5 · 0 * * * *) 등록.
  ⚠️ 26초 벽: 테넌트가 많아지면 스텝 안에서 `Date.now()` 예산(20초)을 보고 남은 테넌트는 다음 주기로 미룬다(skipped 로 집계 · 조용한 누락 0).

■ B2 (14→30%) 소재 배정 — `lib/cron/assign-topics.ts`
  `topicLeadDays` 안의 `planned` 슬롯마다: 그 채널에 맞는 `candidate` 소재 중 최고 점수를 배정(30일 내 같은 norm_key 사용 이력 회피) → `topic_assigned` + `topics.status='picked'`.
  후보가 없으면 `topics.refresh` **자동 1회**(하루 3회 상한과 별개 계정: `origin:"cron"` 으로 감사) → 그래도 없으면 슬롯 `no_topic` + `notifications`(«소재가 떨어졌어요 — 만들기에서 새로 뽑아 주세요») · 다음 주기에 재시도.

■ B3 (30→52%) D-3 제작 — `lib/cron/produce.ts` 🔴 이 라운드의 심장
  실행 조건: KST 현재 시각의 `HH:00` == `produceHour` 의 시(hour) · `produceLeadDays` 안의 `topic_assigned` 슬롯.
  절차: ① **주간 코인 상한 검사**(`weeklyCoinCap` != null 이면 이번 주(월~일 KST) consume 합 + 예상 비용 > cap → 슬롯 `coin_short` + 알림 1회(중복 알림 금지) · 원장 무접촉) ② 디렉터 자동 모드: `director.propose(topicId)` 의 **결정론 부분만** 쓰되 그 슬롯의 채널·계정으로 고정(슬롯이 이미 «무엇을 언제»를 정했다 — 디렉터는 «어떻게») ③ `confirm` 과 같은 경로로 piece + 코인 consume(멱등 ref 그대로) ④ 슬롯 `producing` · piece 생성 후 `in_review` 되면 슬롯도 `in_review`(배경 함수가 piece 를 바꾼 뒤 슬롯 동기화는 이 스텝의 다음 주기 또는 배경 함수 말미 — **한 곳에서만** 쓰도록 정하고 헤더에 적어라) ⑤ 코인 부족(실제 잔액) → `coin_short` + 알림 ⑥ 생성 실패(failed) → 슬롯 `failed` + 알림(환급은 R1 경로 그대로).
  🔴 **슬롯 게이트**(`lib/slot-gate.ts` · AM `content-slot-gate` 이식): 자동 경로가 piece 를 만들 때 `slotId` 를 반드시 실어야 한다. 없으면 거부 + `audit(action:"piece_slotless_blocked", risk:"high")` + 홈 todo 로 노출(조용한 0건 금지). 사람 경로(`origin:"manual"`)는 통과.

■ B4 (52→64%) 검수창 마감 — `lib/cron/review-deadline.ts`
  `reviewPolicy==="silence_approves"`: 발행일 **02:00 KST** 까지 `in_review` 이고 반려 없음 → `pieces-approve` 와 **같은 게이트**를 태운 뒤 approved→scheduled(슬롯도) · 게이트 실패면 `awaiting_manual` + 알림(자동 승인으로 정책 위반이 나가는 일 0).
  `require_confirm`: D-0 08:00 KST 에 «오늘 나갈 글 확인해 주세요» 알림 1회. 미확인이면 발행하지 않고 슬롯 `awaiting_manual`.

■ B5 (64→80%) 발행 디스패처 + 학습 — `lib/cron/publisher.ts` · `lib/cron/learn.ts`
  publisher(5분): due(`scheduled` ∧ publish_at ≤ now) piece 마다 **`publish(piece, account)` 하나만 호출**(B2 의 `lib/publish/index.ts`) → 반환 `via:"api"` 면 그 함수가 이미 finalize 까지 한 상태 · `via:"runner"` 면 잡이 적재된 상태이므로 슬롯·piece 를 `publishing` 으로 · `{ ok:false, retriable }` 이면 다음 주기 재시도(3회 초과 → `awaiting_manual` + 알림) · **러너 오프라인 30분+ 이고 러너 채널**이면 발행 시도 대신 `awaiting_runner` 표기(취소 아님 · 온라인 되면 다음 주기 정상 진행).
  ⚠️ B2 가 `lib/publish/index.ts` 를 아직 안 올렸으면 **인터페이스만 import 하는 얇은 어댑터**(`lib/publish/contract.ts` 타입 선언 — B2 와 이름 합의는 계약 §10)로 컴파일되게 두고, 실제 파일은 B2 것이다. 네가 만들지 마라.
  learn(hourly): 발행 후 6·24·72h 지난 post 에 대해 API 채널은 커넥터의 stats 조회(B2 제공 · 없으면 스킵) · 러너 채널은 `revenue.stats` 잡 적재 → `posts.stats` 갱신 · `topics.factors.performance` 되먹임(그 소재의 조회수 정규화 값).
  runner.reap(5분): claim 후 15분 무보고 job → release(attempts++ · 3회 초과면 `awaiting_manual`).

■ B6 (80→90%) 계정 전이·승계 + 고객 API — `lib/account-health.ts` · `netlify/functions/slots.ts`(신규 경로 3) · `posts.ts`
  계정 전이(계약 §4): `classifyAndApply` · `reassignSlots`(정지 → 같은 채널·그룹의 건강한 계정으로 예약 슬롯 이전 · 코인 0 · 알림 «@a 정지 → @b 로 옮겼어요») · health_score 산식(헤더에 근거) · `pending_login` 이면 `session.login` 잡 적재(잡 적재 헬퍼는 B2 의 `lib/runner-jobs.ts` · 없으면 타입만 두고 B2 머지 후 연결 — 메인이 순서를 알린다).
  API: `slots-assign-topic` · `slots-reschedule` · `slots-produce-now`(코인 즉시 차감 · 배경 생성) · `posts-list`(계약 §6 모양 그대로).

■ 검증 (90→97%)
  `npx tsc --noEmit` 0 · 모든 함수 `export const config` · Date 바인딩 0 · tenant 스코프 전수 · jsonb 헬퍼.
  로컬 `netlify dev`(포트 충돌 시 8901 등 · netlify.toml 은 원복): 테스트 테넌트에 코인 grant → 규칙 저장 → **크론 수동 호출**(`?secret=`)로 hourly 강제 1회 → 슬롯 배정 확인 → `produceHour` 를 지금 시각으로 바꿔 produce 강제 → piece in_review → review_deadline(시각 조작은 DB 의 publish_at 을 과거로) → publisher due → (B2 미머지 상태면 publish 어댑터가 «미구현» 을 정직 반환하는지) → 상태 전이·알림·감사 확인 · **재실행 멱등**(같은 슬롯 두 번 돌려 changed 0).
  ⚠️ 크론은 «예외 없이 돌았다»가 통과가 아니다(PITFALLS #7) — changed/skipped 숫자를 세어 보고하라.

■ 보고 (97→100%)
  [브랜치·해시·변경요약 · 스모크 수치(스텝별 changed/skipped) · 밟은 함정(PITFALLS §0 AC-N) · B2 와 합의가 필요한 지점 · 🔴 설계 대비 못 만든 것과 사유(없으면 «0»)]
