# R8CLOSE-B1 트리거 — R8 을 닫는 마지막 덩어리: 글 품질 6칸 (B-1)

> 발부 2026-09-16 · 베이스 = 지금 **로컬 main**(푸시 안 한다 · 미배포 65) · 브랜치 `feature/r8-b1-3` · 워크트리 `../AutoCreate-B1B`
> 🔴 사장님 결정: **«R8 을 닫고 배포한 다음 3라운드»**. 지금 **54%**(67칸 중 36). A·B·B2 가 하는 걸 다 끝내도 **14칸이 남고 그중 제일 큰 덩어리가 이것**이다.

## 읽을 것
`docs/rules/PARALLEL_GUIDE.md` · `CLAUDE.md` **§3 말투(겁주지 않는다)·§4.5(DDL 칸 `0041~0049`)·§4.8(«완료»=화면에서 쓸 수 있을 때)·§8 전본·§9 게이트 최소화** · `docs/rules/PITFALLS.md` §0 **AC-69·AC-72·AC-75·AC-78** · `docs/DESIGN.md` §4.3·§5.3(419·422·530 줄) · `scripts/verify-r8-audit64.mjs`(네 진척을 세는 자
)

[자율주행 — 완전 자율] 파일 R/W·git(커밋까지)·npm/npx·bash 자율. 🔴 **push 금지**(사장님 지시 «푸시는 모아서»).
[진행률] 착수·완료마다 `📊 진행률 X%/100 — 방금: … / 다음: …` 최소 6회.

## 🔴 시작 전에
```
git -C ../AutoCreate-B1B merge main       # 로컬 main 을 당긴다(원격 아니다 — 오늘은 푸시를 안 했다)
node scripts/verify-r8-audit64.mjs        # 네 칸 6개가 B2·B3·B4·B5·B8·B9 다
```

---

## 공통 규율 (여섯 개 전부에 걸린다)

- 🔴 **«정의가 있나»가 아니라 «제품이 부르나»** — 오늘 이 프로젝트를 **열세 번** 관통한 문장이다. 만들면 **`scripts/verify-r8-deadends.mjs` 에 줄을 박아라.** 안 박으면 다음 조사에서 또 열림이다(AC-69).
- 🔴 **막지 마라**(CLAUDE §9) — 여섯 개 다 «잰다·보여 준다»이지 «막는다»가 아니다. 하드 게이트는 **0개**다.
- 🔴 **못 잰 것은 «못 쟀어요»로 적는다**(AC-9). 표본이 없어 판정 못 하는 축을 «통과»로도 «실패»로도 적지 마라.
- 🔴 **검사가 값을 베끼지 마라**(AC-78) — 오늘 두 번 데였다(코인 `7`·말투 «알려만 드려요»). **정본 상수를 따라가거나 구조를 재라.**
- **실호출 금지** — provider 실호출은 내 허락이 필요하다. 프롬프트 변경은 **가짜 응답**으로 재라.
- 검사는 🔴 **종료코드로** 본다: `node scripts/x.mjs > /dev/null 2>&1; echo $?` — **`| tail` 로 파이프하면 실패해도 0 이 나온다**(AC-67 · 오늘 내가 그래서 깨진 코드를 푸시했다).

---

## B1 (0→20%) 🔴 **`images.heroNeeded` — 죽은 통로부터**

**여섯 중 이것부터 해라. 유일하게 «이미 만들어 놓고 안 쓰는» 것이고, 고객 눈에 바로 보인다.**

[실측 — 다시 재지 마라]
- **적는 곳**: `lib/director.ts:322` · `lib/cron/director-auto.ts:132` — `heroNeeded: ch === "naver_blog" || ch === "tistory"`
- **나르는 곳**: `lib/director.ts:579` 가 `piece.meta` 로 싣는다
- 🔴 **읽는 곳: 0** — `lib/stock/plan.ts:28` 이 「`heroNeeded` 가 꺼져 있어도 첫 장은 대표다」라고 **주석만** 적어 뒀다
⇒ **네이버·티스토리 대표 이미지가 «필요하다»고 적히기만 하고 아무 일도 안 일어난다.**

[만들 것]
ⓐ `heroNeeded: true` 면 **첫 사진을 대표로 세우는 일이 실제로 일어나게** 한다 — 무엇이 달라지는지 **네가 정하고 한 줄로 적어라**(가로세로비·최소 크기·위치·alt·발행 커넥터에 대표로 넘기기 중 **우리가 지금 실제로 할 수 있는 것**만).
ⓑ 🔴 **못 하는 것은 «못 한다»고 적어라** — 채널이 대표 이미지 지정을 안 받으면 그건 «없는 길»이다(§9). 억지로 만들지 마라.
ⓒ `verify-r8-deadends` 에 사슬 한 줄.
⚠️ `lib/stock/plan.ts:28` 의 주석이 **코드보다 앞서 있는지 뒤처져 있는지** 먼저 봐라(AC-59).

## B2 (20→40%) 목표 매체 → **채널 선택**

[설계] `docs/DESIGN.md:262` — 「소재의 `channel_hint` × 사용자가 켠 채널 × **목표 매체**. `goal=adsense` → 티스토리·블로거 우선 · `adpost` → 네이버 블로그」
[실측] 그 반영이 **0건**. 지금은 목표가 무엇이든 채널이 같게 골라진다.
[만들 것] `lib/director.ts` 의 채널 고르는 자리에 **목표 매체 가중치**. 🔴 **켜지 않은 채널은 절대 안 고른다**(그건 «없는 길»이다). 그리고 **왜 이 채널인지 한 줄**을 `meta` 에 남겨라(고객이 «왜 티스토리?»를 물으면 답할 수 있어야 한다 · AC-72 «폴백으로 떨어졌으면 떨어졌다고 남겨라»).
⚠️ 🔴 **AC-72 를 그대로 밟지 마라** — `goalOf()` 가 채널이 둘 이상이면 `"mixed"` 를 넣는데 그 열쇠를 가진 표가 **0개**여서 라이브 brief 의 **56%** 가 규칙을 한 줄도 못 받았다. **라이브에 실제로 들어 있는 값으로 돌려 보고** 규칙을 받는지 확인해라.

## B3 (40→55%) 페르소나 **적합도**

[설계] `DESIGN.md:246` — 계정 배정이 「건강도·캐던스·**페르소나 적합도**」를 본다.
[실측] 적합도 **0건**(건강도·캐던스만 본다).
[만들 것] «이 소재가 이 계정 말투·주제에 맞나»를 재서 **배정에 쓴다**.
🔴 **LLM 호출을 새로 만들지 마라** — 돈이 두 배가 된다. 먼저 **글자로 잴 수 있는 것**(계정 주제어 ↔ 소재 키워드 겹침 · 이미 있는 `personaTerms`)으로 재고, 그걸로 안 되는 것만 남겨서 **«LLM 이 필요하다»고 보고**해라. 내가 값을 보고 정한다.
🔴 **막지 마라** — 적합도가 낮아도 **배정은 된다**. 순위만 바뀌고, 낮으면 «이 계정엔 좀 안 맞아요»라고 **말해 준다**.

## B4 (55→70%) 신조어 **화이트리스트**

[설계] `DESIGN.md:530` — 「신조어는 **페르소나 연령대에 맞게** 사전 화이트리스트」
[왜 필요한가] 지금 금칙어·번역투 검사가 **멀쩡한 요즘 말을 잡을 수 있다.** 그러면 재작성이 돌고 **돈이 두 배**가 된다(2026-09-15 `visualMin` 사고와 같은 모양).
[만들 것] 연령대별 허용 낱말 표 + 그 표를 **검사가 실제로 본다**. 🔴 표만 만들고 검사가 안 보면 이 칸은 안 닫힌다.
⚠️ 표는 **한 곳**에만 둬라(`lib/` 상수 한 파일). 두 곳에 두면 갈린다(오늘 코인이 그랬다).

## B5 (70→85%) 네이버 **장소 카드**

[설계] `DESIGN.md:419` — 네이버 블로그 시각 요소에 「**장소/링크카드**」가 적혀 있다.
[실측] **0건**.
[만들 것] 블록 어휘에 장소 카드 한 종 + 렌더 + 계약의 시각 요소 목록에 연결.
🔴 ⚠️ **`visualMin` 과 싸우게 만들지 마라** — 2026-09-15 에 blogger·wordpress 의 `visualMin.faq: 1` 이 `tiers.optional` 과 싸워 **네이버 글 10편 중 8편이 재작성**됐다(돈이 두 배). 새 블록을 **필수로 넣지 마라**. 넣고 나서 `contractSelfConflicts` 를 **반드시** 돌려라.
🔴 그리고 **러너가 그 카드를 실제로 넣을 수 있나**를 먼저 봐라 — 못 넣으면 «있는 척»이 된다. 못 넣으면 **본문 링크로 내려앉히고 «장소 카드는 아직 못 넣어요»라고 적어라**(§9 «없는 길»).

## B6 (85→95%) 쓰레드 **연결글**

[설계] `DESIGN.md:422` — 쓰레드는 「**스레드 2~3개 연결**」이 계약이다.
[실측] **0건** — 지금은 한 덩이로 나간다.
[만들 것] 500자 넘으면 2~3개로 **끊어서 잇는다**. 🔴 **끊는 자리가 문장 한가운데면 안 된다.**
⚠️ 🔴 **AC-73 을 보라** — 쓰레드가 인스타와 **같은 캡션 빌더**를 쓰는 바람에 계약은 «태그 0~1개»인데 실제 캡션엔 **10개**가 실렸다. **연결글도 발행 커넥터가 실제로 이어 올리나**를 확인해라. 계약 문장만 고치면 아무 일도 안 일어난다.

## 검증 (95→100%)
```
npx tsc --noEmit > /dev/null 2>&1; echo $?
node scripts/verify-r8-deadends.mjs > /dev/null 2>&1; echo $?
node scripts/verify-r8-audit64.mjs                      # 🔴 네 칸 6개가 닫혔는지 이 출력으로 본다
node scripts/verify-api-surface.mjs · verify-label-surface.mjs · verify-server-words.mjs
npx tsx scripts/verify-cardnews.mts · verify-fact-claims.mts · verify-seo-keyword.mts
```
🔴 **`verify-r8-audit64` 가 네 칸을 닫혔다고 세는 것까지가 «완료»다.** 그 하니스는 **«제품이 부르나»**를 본다 — 정의만 만들면 안 닫힌다.

## 보고 (100%)
`[브랜치 · 커밋해시 · 변경요약 · 검증 증거(종료코드) · 밟은 함정]` · **commit 만 · push 금지.**
막히면 30분 안에 한 줄. 사장님 손이 필요한 건 멈추지 말고 나한테 넘겨라(합동 세션으로 모은다).

## 설계 대비 범위 지도 (CLAUDE §8)
- **이번**: ③ B2·B3·B4·B5·B8·B9 여섯 칸
- **다음 라운드(R10)**: 🔴 **B7 «에디터 실제 요소»** — 이건 R10 의 «AM 발행 러너 이식(서식)»과 **같은 일**이라 그때 같이 한다(`docs/active/2026-09-15-R9-reference-and-split-design.md` §2.1d 「AM op 7종 ↔ 우리 블록 18종」).
- **사장님 손**: A1 클립 게시물형 · A5 브런치 · A10 `publish.brunch` — 계정을 만들고 화면을 한 번 열어 주셔야 열린다(합동 세션 15번).
- **안 만들기로 한 것**: I 러너 트레이 앱 2칸 — 결정 끝(`docs/active/2026-09-15-runner-tray-decision.md` · DESIGN §8.1). **하니스에서 «안 만듦»으로 바꿔야 하는데 그건 내가 한다.**
