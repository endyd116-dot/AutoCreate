# 재부팅 정지 — 2026-09-21 (메인)

> 사장님이 재부팅하신다. **재개 신호가 오면 이 문서부터 읽는다.**

## 지금 상태
```
main = 2aaea31 · 안 커밋 0 · 미배포 7(전부 오늘 것) · 변이 잔재 0
```

## 오늘 main 에 들어간 것 (전부 머지 완료)
- **계정 0개로 영상 만들기** — 서버는 원래 됐고(`lib/director.ts` §1.2) 화면만 없었다. `kinds`/`kindsSet`/`selfUpload` 를 응답에 실었다(`lib/tenant-kinds.ts` 신규)
- 🔴 **«28코인 쓰고 발행 실패한 뒤에야 «직접 올리세요»를 처음 듣던 것»** — `lib/manual-upload.ts` 가 `naver_clip` 하나만 알았다. 영상 채널 넷으로 넓혔다
- **코인 단가 한 벌** — 전엔 화면=코드값 / 차감=DB오버레이 **두 벌**. `plans-list`·`accounts-list` 가 `loadPacksAndTable()` 로. 🔴 라이브 `coin_price_overrides` **0행**이라 아직 안 갈렸었다
- 🔴 **컷 수 정본화** — 90초가 «3컷»이라 원가가 60초보다 싸게 나왔다. 계약 `cutCountFor` 로 통일 → 90초 13컷 · 추정 ₩1,863 → **₩12,923**
- 🔴 **`ai_usage` 새는 자리 여섯** — 영상 3(제공사 실패·내려받기 실패·저장 실패) · 이미지 1 · TTS 2. `${purpose}:fail` + `fail_kind` + `cost_usd_maybe`. **DDL 0084 라이브 적용 확인**(글은 원래 막혀 있었다)
- 프록시 fail-closed 자 · 운영 화면 칸 자

## 🔴 사장님 결재 (이번 판에 내려온 것)
| | 결재 | 상태 |
|---|---|---|
| `video_90` | **42코인 확정**(임시값 아님) | B 에게 보냈다 — 주석 «결재 대기» 를 «2026-09-21 결재» 로 고치는 중 |
| 실패분 상한 | **«글처럼 맞춰라»** = 영상·이미지·TTS 실패도 `cost_usd` 로 물게 | B 진행 중 · 🔴 **아는 금액만**(`download_failed`·`store_failed`) · 모르는 것은 NULL 유지 |

## 🔴 사장님 지시 — 지금 방침
- **영상을 실제로 만들지 마라**(«AM 에서 지겹도록 테스트했다 · 검증된 걸 가져왔으니 문제없다»). **provider 실호출 0**
- **키 꽂기·OAuth 등록은 나중에 한 번에 날 잡고** 한다. **그전까지는 개발만**
- ⇒ 사장님 손이 필요한 몫은 **전부 보류**. 키 없이 되는 것만 돌린다

## 창별 몫 (재부팅으로 다 사라진다 — 새 창에 다시 내야 한다)
| 창 | 몫 | 비고 |
|---|---|---|
| **A** | 영상 보이게 ⑴온보딩이 «영상도?»를 묻기 ⑵만들기·편성표가 `kinds` 보기 ⑶**«만들고 내가 직접 올리기»** 화면 | 작업 중이었다 |
| **B** | 위 결재 둘 | 작업 중이었다 |
| **B2** | 🔴 ⑴**중복 게시**(채널에 «이미 있나» 안 묻는다) ⑵**교차 발행**(`naver-blog.mjs:1146` 이 handle 을 그대로 믿는다) | **받자마자 정지 — 시작 안 했다** |
| **C** | ⑴오늘 것 재기(특히 B 의 ALLOW 9줄) ⑵**«품질 최고등급»을 자로** ⑶AC-172 «실험이 성립하나» 축 | **받자마자 정지 — 시작 안 했다** |

## 아직 안 한 «팔기 전 8개» 중 남은 것
내리기 콘솔(3) · 프록시 콘솔(2) · 수익 연결 시작 버튼(1) — **전부 A 몫이고 키가 필요 없다**. 중복 게시·교차 발행은 위 B2.

## 🔴 다음 사람이 밟지 말아야 할 것 (오늘 메인이 틀린 것)
- **로컬 `.env` 를 보고 «라이브에 키가 없다»고 말하지 마라** — 라이브 키는 넷리파이에 있고 **API 로 읽으면 마스킹된다**(별표16+뒤4자=20자). PITFALLS **AC-170·171·172**
- **코드에 적힌 추정표를 «실제 원가»라고 말하지 마라** — 원장(`ai_usage`)이 진짜다. AM 도 **청구서와 맞춘 적이 없다**
- **`pickProvider` 는 아무도 안 부른다** — FAL 키를 꽂아도 영상 원가는 안 내려간다(계약이 `omni|veo_lite` 로 못 박았다)
