# P1R2 계약서 — «편성표가 스스로 돌아 실제로 발행된다» (A·B·B2·C 공통 · 키 이름 정본)

> 라운드 **P1R2 «자동 편성 크론 → D-3 제작 → 검수창 → 발행(API·러너) → 발행함·정지 승계»** · 베이스 main `afa9466`(P1R1 머지본 + R2 계약) · 발부 2026-09-14
> 🔴 키 이름은 **글자 그대로**. 없는 값은 키를 싣지 않는다. 고객 API 는 `requireUser` → `auth.tid` 스코프.
> 🔴 전본 규칙(CLAUDE §8): 아래 §-1 범위 지도가 Phase 1 잔여 + DESIGN §5B·§8·§7.2 전 항목을 배정한다. 지도 밖 = 누락.
> 🔴 파이프라이닝(PARALLEL_GUIDE §2.6): C 는 P1R1 검증·수리 중이다. **C 가 만지는 파일과 겹치지 마라** — 겹침 목록은 §12(메인이 C 보고를 받아 채운다). 겹치면 메인에 물어라.

## -1. 설계 대비 범위 지도 (P1R2)

| DESIGN | 항목 | 이번(R2) | 다음(R3/Phase 2) | 외부 선결조건 |
|---|---|---|---|---|
| §5B.2 흐름 | D-7 소재 편성 · D-3 제작 · 검수창 · D-0 발행 · D+1 수익 | 소재 편성·제작·검수창·발행 ✅ B | 수익 매칭(Phase 2) | — |
| §5B.7 크론 6스텝 | roll · assign_topics · produce · review_deadline · publisher · learn | **6스텝 전부** ✅ B(learn 은 발행 후 통계 회수까지 · 수익 되먹임은 Phase 2) | — | — |
| §5B.4 변수 | topicLeadDays·produceLeadDays·produceHour·reviewPolicy·bestTimeMode·weeklyCoinCap·quietDays·horizonDays | **크론이 전부 실제로 읽는다** ✅ B | — | — |
| §5B.6 슬롯 상태기계 | planned→topic_assigned→producing→in_review→approved→scheduled→publishing→published / skipped·no_topic·coin_short·awaiting_manual·awaiting_runner·reassigned·failed | ✅ B(전 상태) | — | — |
| §4.7 슬롯 게이트 | 슬롯 없는 자동 생성 거부 + 감사 + 홈 노출 | ✅ B(`lib/slot-gate.ts`) | — | — |
| §2.1 발행(API 채널) | 블로거(Blogger v3)·워드프레스(REST) 실발행 | ✅ B2(코드 완성 · WP 는 실사이트로 실증) | — | 블로거 OAuth 앱 |
| §2.1 발행(러너 채널) | 네이버 블로그 발행 · 티스토리 발행 | 네이버 블로그 ✅ B2 · 티스토리 ✅ B2(셀렉터 신규) | 클립·브런치 | — |
| §8 러너 | 큐 상태기계(claim/report/release) · 잡 10종 · 하트비트 · 우선순위 · 계정별 프로필 · 세션 로그인(헤드풀) · 눈검사 스냅샷 · 런처 | ✅ B2 전부(관리형 러너 팜은 Phase 4) | 렌더·수익 스크랩 잡(Phase 2·3) | — |
| §7.2 계정 전이 | 실패 분류 6종 · suspended→승계 · pending_login → 사용자 알림 · cooldown · health_score | ✅ B | — | — |
| §4.3 발행 멱등 | channel_ref/external_url 있으면 재게시 0 · post 행 생성 | ✅ B2 | — | — |
| §16B | 발행 직전 고지·금칙어 최종 재검사(발행층) | ✅ B2 | 영상 배지(Phase 3) | — |
| §5B.8 화면 | 검수창 알림·달력 상태·슬롯 시트 4동작(소재 바꾸기·시각 바꾸기·지금 만들기·건너뛰기) | ✅ A | — | — |
| §8 화면 | 러너 연결(내 PC 설치 안내·토큰 발급·온라인 칩)·오프라인 경고 | ✅ A | 관리형 러너 신청(Phase 4) | — |
| §2.3 발행함 | 발행 결과 목록(외부 링크·조회수) | ✅ A(링크·상태) · 조회수는 B learn 이 채운다 | 수익 연결(Phase 2) | — |
| §7.2 화면 | 계정 재로그인 흐름(러너 세션 로그인 요청) | ✅ A/B2 | — | — |

## 0. 공통
- 응답/에러 모양·시각 규칙·채널 키: P1R1 계약 §0 그대로.
- 새 상태 어휘 없음(§5B.6·§4 이미 정의된 것만 쓴다).
- 크론은 **한 우산**: `cron-tick-5m`(publisher·러너 타임아웃 회수) · `cron-tick-hourly`(roll·assign_topics·produce·review_deadline·learn) 두 함수만 만든다(AM 관례 — 스텝 추가는 함수 추가가 아니다). `netlify.toml [[scheduled]]` 로 등록.

## 1. 크론 (B · 서버 자동화)
```
POST /api/cron-tick-5m      (Netlify scheduled · 수동 호출은 x-cron-secret)
POST /api/cron-tick-hourly
  → { ok:true, ran:[{ step:string, tenants:number, changed:number, skipped:number, errors:number, detail?:object }] }
```
스텝 계약(각 스텝은 테넌트 단위 격리 · 한 집 실패가 전체를 굶기지 않는다 · 전건 audit):
| step | 주기 | 하는 일 |
|---|---|---|
| `slots.roll` | hourly(하루 1회분 멱등) | `rollSlots(tid, horizonDays)` — R1 함수 그대로 · quietDays·bestTimeMode 반영 |
| `slots.assign_topics` | hourly | `topicLeadDays` 안의 `planned` 슬롯에 소재 배정(없으면 `topics.refresh` 자동 1회 → 그래도 없으면 status `no_topic` + 알림) · 30일 중복 회피 |
| `slots.produce` | hourly(`produceHour` 시각에만) | `produceLeadDays` 안의 `topic_assigned` → 디렉터 brief(자동 모드) → piece 생성 + 코인 consume(**weeklyCoinCap 초과면 status `coin_short` + 알림 · 원장 무접촉**) → 배경 생성 호출 → 슬롯 `producing` |
| `slots.review_deadline` | hourly | `reviewPolicy==="silence_approves"` 면 발행일 02:00(KST) 까지 미반려 in_review → approved+scheduled · `require_confirm` 이면 D-0 08:00 알림만 |
| `publisher` | 5분 | due(`scheduled` ∧ publish_at ≤ now) → API 채널이면 즉시 발행(B2 커넥터) · 러너 채널이면 `runner_jobs` 적재 후 `publishing` · 러너 오프라인 30분+ 면 `awaiting_runner` 표기(발행 취소 아님) |
| `slots.learn` | hourly | 발행 후 6·24·72h 지난 post 의 조회·좋아요·댓글 회수(API 채널=API · 러너 채널=`revenue.stats` 잡) → `posts.stats` · topic factors.performance 갱신 |
| `runner.reap` | 5분 | claim 후 15분 무보고 job 회수(release) · 계정 `pending_login` 판정 유지 |

## 2. 러너 서버측 (B2)
```
POST /api/runner-register  body { name, kind:"own" }  (로그인 세션)    → { ok:true, device:{ id, name, token }, install:{ cmd, url } }   // token 은 이때 1회만 평문
GET  /api/runner-list                                                   → { ok:true, devices:[RunnerDevice] }
POST /api/runner-remove    body { id }                                  → { ok:true }
POST /api/runner-heartbeat header x-runner-token  body { version, jobs:number }
  → { ok:true, sleepSec:number }
POST /api/runner-queue     header x-runner-token  body { action:"claim"|"report"|"release", ... }
  claim   { action:"claim", kinds:[RunnerJobKind], max:number }         → { ok:true, jobs:[RunnerJob] }
  report  { action:"report", jobId, result:{ ok:true, externalUrl?, channelRef?, stats? } | { ok:false, errorKind:RunnerErrorKind, detail?, shotKey? } } → { ok:true }
  release { action:"release", jobId, reason }                           → { ok:true }
POST /api/runner-session-upload header x-runner-token body { accountId, cookies:[...], verifiedAt } → { ok:true }   // 헤드풀 로그인 결과 · 암호화 저장 · 계정 active 승격
RunnerDevice = { id, name, kind, status:"online"|"offline", lastSeenAt?, version?, jobsWaiting:number }
RunnerJob = { id, kind, accountId?, pieceId?, payload:RunnerPayload, account:{ handle, channel, profileKey, proxyUrl?, cookies?:[...] }, priority }
RunnerJobKind = "publish.naver_blog" | "publish.tistory" | "session.login" | "session.verify" | "verify.post_alive" | "revenue.stats"
RunnerErrorKind = "login_fail" | "captcha" | "rate_limited" | "suspended" | "selector_changed" | "network" | "unknown"
RunnerPayload(publish.*) = { title, bodyHtml, blocks, images:[{ url, caption }], tags:[string], disclosure:string|null, scheduledFor }
```
- 러너 토큰: `runner_devices.token_hash`(sha256) · 헤더 `x-runner-token` · 테넌트 스코프.
- claim 은 원자 선점(`FOR UPDATE SKIP LOCKED`) · 우선순위 발행 > 세션 > 통계 · report 는 러너 주장을 믿지 않는다(URL HEAD/본문 확인 후 `published`).
- 실패 분류(`lib/runner-block.ts` — AM 이식) → 계정 상태 전이(§4).

## 3. 발행 커넥터 (B2)
```
lib/publish/index.ts  publish(piece, account) → { ok:true, externalUrl, channelRef, via:"api"|"runner" } | { ok:false, reason, retriable }
  · blogger.ts    Blogger v3 posts.insert(OAuth 토큰 · 리프레시 자동)
  · wordpress.ts  POST {site}/wp-json/wp/v2/posts (Basic · App Password) · 이미지는 media 업로드 후 본문 치환
  · naver_blog.ts · tistory.ts  = 러너 잡 적재(서버에서 직접 발행 불가)
```
- 발행 직전 최종 게이트(§16B): 고지 첫 요소 존재 · 금칙어 · 제휴 링크 ≤2 — 실패면 `awaiting_manual` + 알림(발행 금지).
- 멱등: piece.channel_ref/external_url 있으면 재발행 0(로그만) · 성공 시 `posts` 행 + piece `published` + slot `published` + 계정 `posts_today++`·`last_post_at`.

## 4. 계정 전이·승계 (B)
```
lib/account-health.ts
  classifyAndApply(accountId, errorKind) → { status, action:"cooldown"|"relogin"|"suspend"|"none" }
  reassignSlots(accountId) → { moved:number, targets:[{ slotId, toAccountId }] }   // suspended 시 같은 채널·그룹의 건강한 계정으로 · 코인 0 · 앵글 재변주는 produce 가 처리
```
- `suspended` → 예약 슬롯 재배정 + 알림 «@a 계정이 정지됐어요 · 글 N건을 @b 로 옮겼어요»
- `captcha|login_fail` → `pending_login` + `session.login` 잡 적재 + 홈 todo
- `rate_limited` → `cooldown` 24h + daily_cap −1
- health_score = 최근 30일 성공률 − 경고 − 발행 후 삭제(verify.post_alive) · 0~100.

## 5. 화면 (A)
| 파일 | 내용 |
|---|---|
| `public/app/runner.html` | 러너 연결: «내 PC에서 켜기» → 이름 입력 → 등록 → **토큰 1회 표시 + 복사** + 설치 한 줄 명령 + 상태 칩(온라인/오프라인·마지막 응답) · 기기 목록·해제 |
| `public/app/schedule.html` | 슬롯 시트 4동작 완성: 소재 바꾸기(후보 목록) · 시각 바꾸기(시간 칩) · 지금 만들기 · 건너뛰기 · 상태 점에 `no_topic`·`coin_short`·`awaiting_runner` 색 추가 |
| `public/app/posts.html` | 발행함: 날짜별 목록(마크·제목·@계정·외부 링크 열기·조회수) · 실패(`awaiting_manual`)는 «직접 올리기» 안내 + 본문 복사 |
| `public/app/home.html` | 러너 오프라인 경고 todo · «@a 정지 → @b 승계» 알림 행 · 발행 실패 todo |
| `public/app/accounts.html` | 재로그인 흐름: `pending_login` 계정 행 → «다시 로그인» → 러너가 창을 연다는 안내 + 진행 상태(잡 상태 폴링) |
| 신규 API 소비 | `runner-list/register/remove` · `slots-assign-topic`(소재 바꾸기) · `slots-reschedule` · `slots-produce-now` · `posts-list` |

## 6. 추가 고객 API (B)
```
POST /api/slots-assign-topic  body { slotId, topicId }        → { ok:true, slot:Slot }
POST /api/slots-reschedule    body { slotId, at:ISO }          → { ok:true, slot:Slot }
POST /api/slots-produce-now   body { slotId }                  → { ok:true, pieceId }   // 코인 즉시 차감 · 배경 생성
GET  /api/posts-list?from=&to=&status=                          → { ok:true, posts:[PostRow] }
PostRow = { id, pieceId, channel, accountHandle, title, externalUrl?, publishedVia:"api"|"runner", publishedAt, stats:{ views?, likes?, comments?, lastSyncAt? }, alive:boolean }
```

## 7. 러너 클라이언트 (B2 · `runner/`)
```
runner/
  package.json(playwright)  ac-runner.mjs(엔트리 · --peek --once --headed)  core.mjs(큐 소비·claim/report·하트비트·프로필)
  channels/naver-blog.mjs  channels/tistory.mjs  channels/session-login.mjs  channels/post-alive.mjs
  install.md  run.bat(Windows 런처 · 토큰 입력 1회 · 자동 업데이트 안내)
```
- 계정마다 **영구 브라우저 프로필**(`runner/profiles/{profileKey}`) · 피스마다 새 컨텍스트(세션 섞임 0 · AC-3).
- `RUNNER_SHOTS=1` 이면 단계별 스냅샷 저장(눈검사) · 실패 보고에 `shotKey`.
- 셀렉터는 다중 폴백 · 실패는 정직 분류(`RunnerErrorKind`) · 임시저장까지 성공했는데 발행 실패면 `awaiting_manual`.
- **카나리**(`--canary`): 자사 테스트 계정으로 «임시저장까지» 드라이런 → 셀렉터 변경 조기 감지(DESIGN §19 운영 항목).

## 8. C 시나리오(R3 에서 C 가 검증)
규칙 저장 → hourly 1회 강제 실행 → 슬롯 소재 배정 → produceHour 강제 → piece 생성·in_review → review_deadline 자동 승인 → publisher due → (블로거/WP 실발행 or 러너 잡 적재 → 로컬 러너로 네이버 임시저장 드라이런) → posts 행·발행함 표시 → 계정 정지 시뮬 → 승계 · 멱등(같은 슬롯 재실행 0건) · weeklyCoinCap 초과 시 coin_short.

## 9. 담당 분리 (파일 겹침 0 목표)
| | 파일 |
|---|---|
| **B** | `netlify/functions/cron-tick-5m.ts`·`cron-tick-hourly.ts`·`slots.ts`(신규 경로 3)·`posts.ts` · `lib/cron/*.ts` · `lib/account-health.ts` · `lib/slot-gate.ts` · `lib/slots.ts`(확장) · `lib/director.ts`(자동 모드 진입점만) · `netlify.toml`([[scheduled]]) |
| **B2** | `runner/**` · `lib/publish/**` · `lib/runner-jobs.ts` · `lib/runner-block.ts` · `netlify/functions/runner.ts`(5경로) · `lib/oauth-providers.ts`(토큰 갱신만) |
| **A** | `public/**` (runner.html·posts.html 신규 · schedule/home/accounts 확장 · `_tpl.txt`·`build-pages.mjs`) |
| **C(P1R1 수리 중)** | §12 참조 — 겹치면 메인에 물어라 |

## 10. 공용 계약(B↔B2 경계)
- B 의 `publisher` 스텝은 **`lib/publish/index.ts publish()` 하나만 호출**한다. 러너 여부 판단·잡 적재는 B2 의 publish() 안에서.
- B2 의 report 성공 처리는 **`lib/publish/finalize.ts finalizePublish(pieceId, { externalUrl, channelRef, via })`** 하나로 — post 행·piece·slot·계정 카운터를 이 함수만 건드린다(두 곳에서 상태를 쓰지 않는다).
- 두 함수의 시그니처는 B2 가 먼저 만들고(30분 내) B 에 알린다.

## 11. 크론 시크릿
`CRON_SECRET`(Netlify env · 메인이 등록) — 수동 호출 `?secret=` 또는 헤더 `x-cron-secret`. Netlify scheduled 호출은 헤더 없이도 통과(내부 호출 판정: `x-nf-event: schedule`).

## 12. C 가 만지는 파일 (P1R1 수리 · 메인이 C4 보고 후 채움)
- (대기 중)
