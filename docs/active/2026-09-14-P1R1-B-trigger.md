# P1R1-B 트리거 — 계정·소재·디렉터·글 생성·검수·편성 규칙의 **백엔드** (B)

> 발부 2026-09-14 · 베이스 = 지금 main(`2cd2101`) · 워크트리 `../AutoCreate-B` · 브랜치 `feature/p1-back`
> 🔴 Phase 0 뼈대가 라이브다(https://autocreate-endyd.netlify.app). 이 라운드는 «글 한 편이 실제로 만들어져 검수함에 들어오는» 첫 라운드다. 사장님 지시: **«고도화를 먼저» — 채널 감성·사람이 쓴 흔적·시각 요소·정책 고지가 이번에 다 들어간다.**

```
P1R1-B다. 베이스 = 지금 main(2cd2101). 워크트리 ../AutoCreate-B · 브랜치 feature/p1-back(이미 체크아웃됨).
읽을 것(순서대로): docs/rules/PARALLEL_GUIDE.md · CLAUDE.md §3~§6 · docs/rules/PITFALLS.md §0(AC-1~5)·#1·#4 ·
        docs/active/2026-09-14-P1R1-contract.md(🔴 키 이름 정본 — 글자 그대로) ·
        docs/DESIGN.md §4(상태기계) §5(디렉터) §5B(편성) §5C(글 고도화) §7(다계정) §12(코인) §16B(정책) ·
        lib/auth-service.ts(rows() 헬퍼·utcDate·jsonb 관례) · lib/coin-table.ts · lib/ai-models.ts · db/schema.ts · drizzle/0001-init.sql ·
        AM 원본(읽기 전용·복사 출처 기록): ../AutoMarketing/lib/ai.ts · coin-ledger.ts(§1~§3) · channel-creds.ts(암호화 §) · content-tone.ts · content-director.ts(§5-7 AI티 검사) · naver-keyword-acquire.ts(HMAC·keywordstool 조회부) · content-gen.ts(블록→HTML 관례) · ../tbfa-mis/lib/r2-client.ts
[자율주행 — 완전 자율]
- 파일 R/W/Edit, git(add·commit·checkout·merge·rebase·fetch·restore), bash·PowerShell, npm install/run, npx, curl(검증) — 전부 묻지 말고 자율 실행. 도구 허락 구하지 말 것.
- 커밋 자유. push 불필요·차단됨 — 메인이 공유 git에서 feature/p1-back 을 직접 머지·배포함. 끝나면 [브랜치·커밋해시·변경요약]만 보고. "push 막혔다/직접 해달라" ❌.
- 오직 ①진짜 애매한 로직 해석 ②되돌리기 어려운 아키텍처 결정 ③비가역 위험작업(DB drop·reset --hard)일 때만 멈추고 질문. 막히면 30분 안에 보고.
[🔴 전본 규칙 — CLAUDE §8] 계약서 §-1 «설계 대비 범위 지도»의 R1 열이 네 범위다. «간단히·축소판·폴백 대체·이번엔 생략» 금지. 못 만들면 사유 보고.
[진행률 — 의무] 시작 0% · 섹션 착수/완료마다 "📊 진행률 X%/100 — 방금: … / 다음: …" 한 줄. %는 아래 제목에서 읽는다. 최소 12회.
[보고 대상] 이 세션의 보고는 메인 세션(autocreate-ca)이 읽는다 — 마지막 보고에 브랜치·해시·변경요약·검증 증거·밟은 함정을 넣어라.
```

■ 지금 무엇이 있고 무엇이 없나 — 메인 실측(다시 재지 마라)
  있음: 인증·가드(`requireUser`→`auth.tid`) · 스키마 40표(accounts·account_creds·topics·briefs·pieces·piece_assets·slots·cadence_rules·coin_ledger·personas·emotion_profiles 전부 있음 · **DDL 추가 불필요**) · `lib/coin-table.ts`(단가) · `lib/ai-models.ts`(모델 체인) · `lib/db-util.ts`(jsonb·utcDate) · Netlify env(GEMINI_API_KEY·CREDS_ENC_KEY·R2_*·NAVER_SEARCHAD_*·NAVER_OPENAPI_*) · 로컬 `.env` 동일.
  없음: `lib/ai.ts`(Gemini 호출) · 코인 원장 로직 · 계정 API · 소재·디렉터·생성·게이트·고지·편성 로직 전부. → 이 트리거의 B1~B8.
  🔴 원칙: 두뇌를 하나 더 만들지 않는다 — 생성기는 `content-gen` 하나, 디렉터는 «앞뒤»(재료 모으기·게이트)만(AM §7 통폐합 관례).
  ⚠️ 함정: Date 객체 바인딩 금지(AC-5 · `.toISOString()`+`::timestamptz AT TIME ZONE 'UTC'`) · jsonb 는 `jsonb()` 헬퍼 · timestamp 문자열은 `utcDate()`.

■ 계약 (정본 = docs/active/2026-09-14-P1R1-contract.md — 그 파일을 열어 그대로 따른다. 키 이름을 여기 다시 적지 않는다)

■ B1 (0→10%) `lib/ai.ts` + `lib/ai-image.ts` + `lib/r2.ts`
  [만들 것] `callGemini({ purpose, chain, system, user, json?, tenantId?, ref?, maxOutputTokens? })` — 체인 순서대로 폴백(400/429/5xx) · JSON 모드는 `responseMimeType:"application/json"` · 응답 파싱 실패 시 1회 재요청 · `ai_usage` 1행(purpose·model·토큰·cost_usd — 단가는 AM `ai-cost.ts` 표 복사).
           `generateImage({ prompt, aspect:"1:1"|"4:3"|"9:16", tenantId, ref })` → `CHAIN_IMAGE` 순서 폴백 → PNG bytes → `r2.put(key, bytes, "image/png")` → 공개 URL(`R2_PUBLIC_BASE` 없으면 `/api/r2-image?key=` 서빙 함수 신설 · tbfa 패턴).
  [정본] AM `lib/ai.ts` 의 fetch 본문·에러 분류·thinkingBudget 처리 그대로(plan-gate·BYO 키·캐시는 뺀다). 모델명 문자열 금지 — import.
  ⚠️ 이미지 프롬프트에 **한글 텍스트 요구 금지** · 실존 인물·로고 금지 문구를 시스템 프롬프트에 고정(DESIGN §5C.3·§19).

■ B2 (10→18%) `lib/coin-ledger.ts` + `netlify/functions/coins.ts`
  [만들 것] `balance(tid)` = SUM(delta) (만료 제외) · `consume(tid, item, ref, { allowPurchased?:false })` → `{ ok, charged, balance }` 또는 `{ ok:false, reason:"insufficient", need, have }` — **같은 (tid,'consume',ref,bucket) 이 있으면 charged 0·ok true**(멱등은 DB 유니크 `coin_ledger_idem` 이 최종 보증 → 23505 잡아서 ok 처리) · `grant(tid, coins, reason, actorId)`.
  버킷 규칙(AM §1): included 우선 소진 → purchased 는 옵트인(tenants.settings.coinAutoUsePurchased===true)일 때만. delta 음수 행은 bucket 을 실제 소진한 버킷으로.
  GET /api/coins-balance 응답은 계약 §6.
  ⚠️ 잔액 컬럼 만들지 마라(원장 합산이 정본 · AM 약속 ①).

■ B3 (18→28%) 계정 — `lib/creds-crypto.ts` + `lib/oauth-providers.ts` + `netlify/functions/accounts.ts` + `personas.ts`
  [만들 것] AES-256-GCM `encrypt(obj)→"v1:iv:tag:ct"(base64url)` · `decrypt` · `maskSecret` — `CREDS_ENC_KEY`(hex 32B) 없으면 **정직 500**(폴백 없음 · AM 규율).
  accounts-list / add / remove / update (계약 §1). add: 플랜 한도 `planOf(tenant.plan_key).limits.maxAccounts`(체험 5) 초과 → step:"limit" · 자격은 `{ loginId, password }` 암호화 → account_creds(kind "password") · status `pending_login` · `browser_profile_key` = `t{tid}-a{id}` · audit `account_add`(자격 평문 0).
  연결 방식은 계약 §1 그대로: session(naver_blog·tistory) · wordpress = App Password(siteUrl·loginId·appPassword → `GET {siteUrl}/wp-json/wp/v2/users/me` Basic 인증 확인 후 active) · OAuth 채널은 `accounts-oauth-start/return`(`lib/oauth-providers.ts`: Google(blogger·youtube)·Meta(instagram·reels)·Threads·TikTok 인가 URL·토큰 교환·장기 토큰 — AM `publish-threads.ts` 관례 이식 · state 서명 · 토큰은 account_creds kind "oauth" 암호화) — env 앱 키(`GOOGLE_OAUTH_CLIENT_ID/SECRET`·`META_APP_ID/SECRET`·`THREADS_APP_ID/SECRET`·`TIKTOK_CLIENT_KEY/SECRET`) 없으면 step:"provider_not_configured"(코드 완성 · 키 꽂으면 가동).
  accounts-update 로 proxy_url·golden_hours·persona_id·monetize 키(쿠팡 access/secret = account_creds kind "coupang" 암호화 · 애드포스트 미디어 id·애드센스 pub id = accounts.monetize jsonb) 저장.
  personas-list/save: DESIGN §5C.2 profile 필드 전부(region·family·job·home·brands·tone·interests·banned·signature).
  🔴 응답·로그 어디에도 password 평문 0. `hasCreds` 만.

■ B4 (28→40%) 소재 — `lib/naver-volume.ts` + `lib/topics.ts` + `netlify/functions/topics.ts`
  [만들 것] ① 키워드툴 조회: AM `naver-keyword-acquire.ts` 의 서명(HMAC-SHA256 · `X-Timestamp/X-API-KEY/X-Customer/X-Signature`)과 `GET /keywordstool?hintKeywords=…&showDetail=1` 그대로 → `{ keyword, pcVolume, mobileVolume, compIdx }[]`. 키 없거나 실패 → 빈 배열(graceful).
           ② 후보 생성: Gemini JSON — 입력 = 테넌트 settings.kinds/channels + 계정 페르소나(있으면) + 한국 캘린더(이달·다음달 절기·명절 — `lib/kr-calendar.ts` 상수 30줄) + 최근 30일 사용 소재(norm_key 제외) → 후보 15개 `{ title, angle, seedKeywords:[3], channelHint, intent }`.
           ③ 검색량 결합: seedKeywords 를 키워드툴로 조회 → volume=max(PC+모바일) · **growthPct = `lib/naver-datalab.ts`**(데이터랩 검색어 트렌드 · 최근 4주 vs 직전 4주 · 키워드 그룹 ≤5/호출) · competition = compIdx 매핑 · intent/pain = LLM 판정 · seasonal = `lib/kr-calendar.ts`(연간표 전부) · score = DESIGN §5 팩터식 그대로(demand×intent×pain×compGap÷difficulty×seasonal — AM `content-topics.ts` 가중치 복사 · 헤더에 수식 명시).
           ④ upsert topics(norm_key = 공백제거·NFC·lower · 30일 내 used/picked 와 같으면 버림) · expires_at = +7일 · 하루 3회 rate limit(audit 기반 COUNT).
  topics-list / refresh / pick / skip (계약 §2).
  ⚠️ 환각 0 원칙: 수치는 키워드툴 값만. 없으면 키 생략. «최고·1위» 류 앵글 금지(프롬프트 + 코드 필터).

■ B5 (40→52%) 디렉터 — `lib/director.ts` + `lib/best-time.ts` + `lib/writing-contracts.ts` + `netlify/functions/director.ts`
  [만들 것] DESIGN §5.3 을 **결정론 함수**로: 채널 = topic.channelHint ∩ 테넌트가 연결한 채널(계정 있는 채널만) · 계정 배정 = 같은 채널에서 status active|pending_login · posts_today<daily_cap · health 높은 순(pending_login 도 배정 — 러너 R2 에서 검증) · 구성 = 직전 piece 와 다른 format(writing-contracts 로테이션) · 일정 = `best-time.ts`(DESIGN §5B.5 표 · 계정 간 30분 간격 · 오늘 이미 지난 시각이면 내일) · 제휴 = intent commercial 이면 monetize.affiliate={ provider:"coupang", productQuery, slot } — **계정에 쿠팡 키가 있으면 `lib/affiliate-coupang.ts` 로 상품 검색(3후보 → LLM 1선택)·딥링크(subId=`piece:{id}`)·affiliate_links 행·본문 슬롯에 링크 블록 삽입** · 키 없으면 고지 블록 + meta.affiliateHint · 이미지 수 = 채널 계약 기본(네이버 6 · 티스토리 3) · coinCost = blog 1 + image×imageCount.
           LLM 1콜: 소재 앵글을 채널별로 «갈라» 준다(같은 소재 두 계정이면 다른 앵글 — 유사도 회피의 첫 수).
           briefs 행 저장(pieces jsonb = PieceSpec[] · reasons 3줄 사람말).
  director-confirm: patch 적용(drop·계정·format·imageCount·affiliate·scheduleAt) → piece 행 N개(status generating · meta.emotionKey·format·imageCount·affiliate) + slot 행(origin manual · producing · slot_date=scheduleAt KST 날짜 · publish_at) → `consume(tid,"blog","piece:{id}")` + `consume(tid,"image","piece:{id}:img{i}")` 순서로 — 하나라도 insufficient 면 **전부 되돌리고**(piece 삭제) step:"coin_short" → 성공 시 `generate-piece-background` 를 piece 마다 fetch(POST · `x-internal-secret`).
  `lib/writing-contracts.ts`: DESIGN §5C.1 표 **7행 전부**를 코드 객체로(독자·말투·서사 로테이션·시각 요소·분량·제목 스타일 필드 전부) + `emotion_profiles` DB 오버레이(있으면 덮어씀 · graceful). 이번 라운드 생성 대상은 글 채널이지만 계약은 전부 있어야 한다(전본 규칙).

■ B6 (52→74%) 글 생성 — `lib/content-gen.ts` + `lib/ai-tell-gate.ts` + `lib/disclosure.ts` + `netlify/functions/generate-piece-background.ts` 🔴 이 라운드의 심장
  [입력] piece + topic + 계약(채널) + 페르소나(계정 persona_id → profile · 없으면 테넌트 기본 페르소나 1개 자동 생성: 지역=«서울» 등 **가짜 구체값 금지** → 없으면 «1인 가구·30대·자취» 같은 넓은 사정만) + 제휴 여부.
  [출력] `blocks:Block[]`(계약 §4 Block 타입) → `renderBlocksHtml(blocks, channel)` → pieces.body(HTML)·blocks(jsonb)·title·meta.tags.
  [프롬프트 골격 — 6칸 순서 고정] ①역할(채널 독자·말투 계약 원문) ②구성(블록 시퀀스 — 계약의 structure 배열을 그대로 요구) ③재료(소재·앵글·검색 키워드·페르소나 사정 1~2개·계절) ④한국 규칙(원화·단위·제목 2벌 중 채널 것) ⑤금지(상투 표현 사전 60개 원문 · 최상급 · 근거 없는 수치 · 번역투) ⑥출력(JSON 스키마).
  [유사도] `lib/similarity.ts`(AM `ad-copy-similarity.ts` 이식) — 같은 brief 의 다른 piece·같은 계정 최근 30일 글과 본문 유사도 > 임계면 앵글을 바꿔 1회 재생성(DESIGN §4.3 «계정 간 중복 0»).
  [게이트] `ai-tell-gate.ts` — DESIGN §5C.2 표 **전부** 코드로: 상투 표현 60개 사전(파일 상수 · 「~에 대해 알아보겠습니다」「마무리하며」「오늘은 ~를 소개」「~하는 것이 중요합니다」「~에 있어서」「~함에 따라」「~을 통해」 포함) 0건 · 문단 시작 반복 ≤1 · 불릿 비율 ≤30% · 문장 길이 표준편차 ≥ 임계(헤더에 값·근거) · 번역투 ≤3 · 근거 없는 최상급 0 · 페르소나 사정 ≥1 · 채널 최소 시각 요소(네이버: quote 1·divider 1·image 6 / 티스토리: h2 3·(table|list) 1·adsense 2). 실패 항목을 **재작성 지시문**으로 만들어 1회 재생성 → 재실패는 그대로 in_review(gate_report 에 남긴다 · 사람이 본다).
  [고지] `disclosure.ts` — DESIGN §16B.2 문구 상수 · affiliate=true 면 blocks[0] 이 `disclosure` 블록(이동·삭제 불가는 pieces-update 에서 강제: 사용자가 지워도 다시 넣는다) · `checkDisclosure()` 를 approve 직전 재검사.
  [이미지] blocks 의 image 블록마다 `generateImage(캡션 기반 프롬프트 · 채널 비율)` → piece_assets(kind image · sort) · 캡션은 본문 문맥에서 LLM 이 쓴 것을 그대로(블록에 caption 필수).
  [상태] generating → (게이트 통과/재실패) in_review · 예외 → failed + meta.failReason + notifications 1행(«글을 만들지 못했어요 · 코인은 돌려드렸어요» — 실패 시 `grant` 로 환급 · ref `refund:piece:{id}`).
  [배경 함수] 파일명 `-background.ts` 로 Netlify background(202 즉시 반환 · 15분) · 내부 시크릿 헤더(`INTERNAL_SECRET` env — 없으면 `JWT_SECRET` 앞 16자 폴백 X → 정직 500) · 멱등(이미 in_review/failed 면 스킵).
  ⚠️ 26초 동기 함수에서 생성하지 마라. director-confirm 은 202 로 즉시 답하고 화면이 폴링한다.

■ B7 (74→84%) 검수 — `netlify/functions/pieces.ts`
  pieces-list / get / approve / reject / regenerate / update (계약 §4).
  approve: `checkDisclosure` + 금칙어(AM `ad-law-banned.ts` 의 `findBannedWord` 사전만 발췌 이식 · `lib/banned-words.ts`) 재검사 → 실패면 step:"gate" 와 사유 · 통과면 status scheduled · scheduled_for = piece.meta.scheduleAt · slot 도 scheduled.
  update: title/bodyHtml 저장 → blocks 는 HTML 에서 다시 못 만드니 **bodyHtml 을 정본으로 승격**(meta.editedByUser=true · blocks 유지) → 게이트 재검사 결과 반환(차단 아님 · 정보).
  regenerate: regenCount<1 이면 generating 으로 되돌리고 배경 함수 재호출(코인 0 — 같은 ref).

■ B8 (84→92%) 편성 규칙·슬롯 — `lib/slots.ts` + `netlify/functions/rules.ts`
  rules-list / save / settings · slots-list / skip (계약 §5). save 는 전체 교체(있는 id 갱신 · 없는 id 비활성) → `rollSlots(tid, horizonDays)` 1회 → slotsCreated.
  `rollSlots`: 오늘(KST)~+horizonDays · 규칙마다 weekdays(비면 count 를 주 안에 균등 분산 · 결정론) · (rule_id, slot_date) 중복 0 · quietDays 제외 · publish_at = best-time(계정 미정이면 채널 첫 후보) · status planned · origin auto.
  `coinsPerWeek(rules)` = Σ(count 주환산 × (1 + 계약 기본 imageCount)).
  ⚠️ 소재 배정·D-3 제작·발행 크론은 **이 라운드 밖**(R2). 슬롯이 생기고 화면에 점이 찍히는 것까지.

■ 검증 (92→97%)
  - `npx tsc --noEmit` 0 에러 · 모든 함수 `export const config = { path }` · jsonb 쓰기 직후 `jsonb_typeof` 1회 확인(콘솔) · Date 바인딩 0(grep `\${.*new Date(` 로 자기 점검) · tenant 스코프 전수(요청 body tenantId 0건).
  - 로컬 `netlify dev --port 8899` 로 시나리오: 가입 → 코인 grant(SQL 직접 30) → 계정 add 2 → topics-refresh(검색량 숫자 확인·키 없으면 graceful 로그) → director-propose → confirm(코인 차감 · 재확정 재차감 0) → 배경 생성 완료(in_review · 이미지 URL 200) → gate_report 확인 → approve → rules-save → slots-list 7일치.
  - 증거: 로컬 재현이면 «스모크»라고 쓴다. 라이브 실증은 C 몫.

■ 보고 (97→100%)
  [브랜치 feature/p1-back · 커밋해시 · 변경요약(파일 목록) · 스모크 결과(응답 본문 발췌) · 밟은 함정(PITFALLS §0 AC-N 추가 커밋) · A 에게 알릴 계약 이탈(원칙 0) · 🔴 **설계 대비 못 만든 것과 사유(없으면 «0»)**]
  ❌ "push 해달라" · 증거 없는 «실증»
