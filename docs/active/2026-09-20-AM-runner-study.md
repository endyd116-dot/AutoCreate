# AM 네이버 러너 분석 — 무엇을 배울 것인가 (2026-09-20 · B2)

> 사장님 지시: «이미 AM 에서 **모든 것들이 되는 러너**를 쓰고 있으니, 그 러너를 분석해서 우리 시스템을 디벨롭하면 좋겠다.»
> 트리거: [`2026-09-20-AM-RUNNER-port-trigger.md`](2026-09-20-AM-RUNNER-port-trigger.md) · 🔴 **AM 레포는 읽기만 했다**(쓰기 0 · 커밋 0).

## 0. 🔴 먼저 — 읽자마자 우리 버그가 하나 나왔고, 이미 고쳤다

**사진 주소의 `&amp;`** — AM 실물 #722 가 «사진 5장 중 2장이 통째로 빠졌다. 진범은 `&` 하나»라고 적어 뒀다.
🔴 **우리도 똑같았다**(실행으로 확인):

```
보낸 주소      : …/p.jpg?fm=webp&w=800&fit=cover
러너가 받을 주소: …/p.jpg?fm=webp&amp;w=800&amp;fit=cover   ← 이 주소로는 못 받는다
```

`renderBlocksHtml` 이 `&`→`&amp;` 로 적는데 폴백 파서가 글자 그대로 꺼내 `fetch` 에 넘겼고, 그 실패는 `null` 로 **조용히 삼켜진다**(오류도 `demoted` 도 없다). 🔴 **우리 R2 프리사인드 주소는 파라미터가 여럿이라 «항상» 걸린다.** 그리고 이 경로는 오늘부터 **«손님이 고친 글»의 정본 경로**다.
⚠️ 오늘 시험 글이 멀쩡했던 건 주소가 `?key=…` **파라미터 하나**여서다 — **운이었다.**
⇒ 고침 `0eb9629`(`decodeAttrUrl`) · 자 `verify-edited-wins` 30축 · 변이 6.

**이 한 건이 사장님 말씀의 값을 이미 넘겼다고 본다.**

---

## 1. 🔴 메인이 제일 궁금해한 것 — «우리 굵게 ↔ 네이버 굵게»를 AM 은 어떻게 푸나

> B2 가 짚은 것: «마크업만 보면 **«우리가 칠한 굵게»와 «네이버가 소제목에 쓴 굵게»를 가를 수 없다.»**

**답: AM 도 «풀지» 않았다. 피해 간다 — 그리고 그 피하는 방법이 우리가 배울 것이다.**

AM 은 **발행된 글을 되읽지 않는다.** 대신 **칠하기 직전에** «내가 잡은 게 내가 치려던 글자인가»를 확인한다(`colorLastTyped`):

- `window.getSelection()` 은 🔴 **항상 빈 문자열**이다(AM 실물 #737 — 다섯 번 다 `""`). 스마트에디터가 **자체 선택 모델**을 써서 DOM 이 그 선택을 못 읽는다.
- ⇒ 선택 대신 **마지막 문단의 꼬리**를 읽어 «방금 친 조각으로 끝나는가»를 본다. 글자는 실제로 문서에 있으니 **DOM 으로 정확히 읽힌다.**
- 어긋나면 🔴 **안 칠하고 물러난다.** AM 의 말: «**안 칠한 강조는 아쉬울 뿐이지만, 잘못 칠한 강조는 글을 망가뜨린다.**»

🔴 **즉 «짝 대조»를 «발행 뒤»가 아니라 «칠하는 순간»에 한다.** 그 순간엔 **의도를 알고 있으니** 애매함이 없다.

### 🔴 우리는 이미 있다 — 그리고 한 자리는 더 갔다
`runner/channels/naver-blog.mjs:264 tailMatches` 가 **같은 것**이고, 어긋나면 `caret_drift` 로 **`demoted` 에 적는다**(고객 문장까지 있다). 주석에는 이렇게 적혀 있다:

> «AM 에는 이 대조가 없었다(`colorLastTyped` 의 꼬리 확인은 그 사고 **뒤에** 생겼다).»

⇒ **이 축은 «받을 것»이 아니라 «이미 있다»**이다. 다만 **되읽기 자**(`verify-published-format`)의 한계는 그대로 남는다 — 발행 뒤 HTML 만으로는 여전히 못 가른다. **그건 AM 도 못 한다.**

---

## 2. 받을 것 (뜻이 우리한테도 맞다 · 무엇을 어디에)

| # | AM 이 하는 것 | 우리는 지금 | 어디에 |
|---|---|---|---|
| ① | **표 → `▪ 항목: 값` 불릿**(`tableToBullets` · 머리행으로 각 칸에 **라벨**을 붙인다) | `머리1 · 머리2` / `칸1 · 칸2` 로 평평하게 — 🔴 **머리행이 뜻 없는 한 줄이 되고 데이터가 라벨을 잃는다** | `runner/lib/plan.mjs` 표 갈래 둘(블록 `:341` · 폴백 `:570`) |
| ② | **목록 표식을 붙인다** — 체크 성격(«확인·체크·준비물»)은 `✓`, 나머지는 `·`. 판정에 **목록 바로 앞 문단 120자**까지 본다 | 우리도 글머리 기호는 나간다(오늘 실물 `• 목록 첫째 줄` 확인) — **체크/일반 구분은 못 봤다** | 같은 파일 목록 갈래 |
| ③ | **값 강조 ↔ 줄 강조 상한을 나눈다**(값 60자 · 줄 140자) + **형광펜/글자색을 번갈아** 칠해 «칠한 티»를 없앤다(참고 블로그 실측 배경 14 : 글자색 10) | 상한은 같은 값이 있다. **번갈아 칠하기**는 `MARK_COLOR_FIXED` 로 있다 — **실물로 확인 안 됨** | 확인 먼저 |
| ④ | **링크 주소 보존**(`deferAnchorUrls`) — 일괄 태그 제거가 `<a href>` 를 지워 **랜딩 URL 이 0건**이던 사고(#라이브 실측) | 🔴 **본문 안 인라인 `<a>` 는 글자만 남고 주소가 사라진다**(오늘 확인 · `demoted` 에도 안 적힌다) | `opsFromHtml` — **§4 로 별도 보고** |

---

## 3. 🔴 «네이버가 원래 안 되는 것» — 수리할 게 아니라 `demoted` 로 말해 줄 것 (§9)

| 사실 | 근거 | 우리가 할 일 |
|---|---|---|
| **스마트에디터엔 색 단축키가 없다** — AM 은 `<mark>` 를 **굵게로 옮긴다**(`:188`) | AM 주석(실측) | ⚠️ **우리는 도구모음 버튼을 누른다**(`clickToolbarItem`) — 단축키가 아니다. ⇒ **«색이 안 된다»는 아직 확정 아니다.** 고친 코드로 한 번 재야 갈린다 |
| **평문 URL 은 자동 링크가 안 된다**(앵커 0개) | AM `:112` 실측 | 링크는 **우리가 명시로** 넣어야 한다 |
| **에디터에 표 요소가 없다** | 양쪽 공통 | 이미 `note` 로 말한다 — ①처럼 **읽기 좋게** 바꾸는 것이 남은 일 |

---

## 4. ⊘ 못 가름 / 아직 못 잼

| # | 무엇 | 왜 |
|---|---|---|
| ⊘① | **사진 여러 장·정렬·본문 중간 배치** | 계획(ops) 단계에서는 **문서 순서대로 image op 가 끼어 들어간다**(오늘 실물 플랜에서 확인). 🔴 그런데 **에디터가 실제로 그 자리에 넣는지**는 한 장짜리로 한 번 올려 봤을 뿐 — **여러 장·정렬은 한 번도 못 쟀다** |
| ⊘② | 형광펜·글자색이 네이버에 **실제로** 실리나 | 오늘 실물은 **우리가 안 보냈다**(옛 `plan.mjs` · `planned 0`). 고친 코드로 다시 올려야 답이 나온다 |
| ⊘③ | AM 의 «굵게로 옮긴다»가 **지금도 맞나** | AM 은 단축키 기준이고 우리는 도구모음 기준이다. **같은 결론일 이유가 없다** — 짐작으로 «해당 없음»에 넣지 않는다 |

---

## 5. 🔴 이번에 새로 안 사실 하나 (AM 도 우리도 값이 있다)

**`window.getSelection()` 이 스마트에디터에서 빈 문자열이라는 것** — 이건 «쓰기만 안 되는 것»이 아니라 **«읽기도 안 된다»**는 뜻이다(AM 이 자기 가정이 틀렸다고 주석에 적어 뒀다). 그래서 **선택 기반 검증은 이 에디터에서 원천적으로 불가**하고, **문단 꼬리 읽기**가 유일한 길이다.
⇒ 이 사실은 **티스토리·다른 에디터로 넓힐 때 제일 먼저 확인할 것**이다(에디터마다 다를 수 있다).

---

## 6. 다음 순서 (제안)

1. 🔴 **고친 코드로 서식 글 둘째를 올리고 되읽는다** — ⊘②가 그때 갈린다. (오늘 자리는 **2/2 로 꽉 찼다** · 내일 열린다)
2. ①②④(표 불릿 · 체크 표식 · 링크 주소) — 셋 다 `plan.mjs` 한 파일이고 순수 함수라 **자 먼저 쓰고** 고칠 수 있다
3. ⊘① 사진 여러 장 — 둘째 글에 **사진 2~3장**을 넣어 한 번에 잰다

---

## 7. 2차 읽기 — 사진 여러 장 · 그리고 «얼마나 봤나»

### 🔴 사진 여러 장 — AM 의 제일 비싼 실측, **우리도 이미 있다**
AM `attachImages`(:1674)가 실사고 둘을 적어 뒀다:
1. 여러 장을 올리면 네이버가 **«사진 첨부 방식»(개별사진/콜라주/슬라이드) 팝업**을 띄운다.
   🔴 **레이아웃을 안 고르면 이미지가 한 장도 안 들어간다** — ✕ 로 닫으면 **취소**다(2026-07-28 실측: 그래서 **0장 게시**).
2. «개별사진»은 `button` 이 아니라 **카드/라벨** 구조라 `button:has-text` 로 안 잡힌다.
   그러면 팝업이 안 닫히고 **발행 버튼을 가려 발행 자체가 실패**한다(`naver-fail-143.png`).

**우리 쪽**(`runner/channels/naver-blog.mjs:11·354-356`): 🔴 **둘 다 이미 있다** — 머리말에 «✕ 로 닫으면 0장 삽입»이 적혀 있고,
`getByText("개별사진", { exact: true })` + 클릭 가능한 조상 폴백까지 같다. ⇒ **«이미 있다»**.
⚠️ 다만 **한 번도 실물로 못 쟀다**(오늘 글은 사진 한 장이라 팝업이 안 떴다). **내일 2~3장으로 그 자리를 처음 밟는다.**

### 링크 — §2 ④는 **고쳤다**(`6d8ad03`)
문장에는 라벨만, 주소는 **줄 끝 독립 줄**로. 🔴 고치다가 **내가 `plan.mjs` 에 백스페이스를 박았고**(파이썬 층),
`<a\b` → `<a` 가 되어 **링크 걷기가 통째로 안 먹었다 — 문법 오류도 안 났다.**
⇒ 0x08 검사를 **나무 전체**로 넓혔다(한 파일만 보는 축은 다음 파일에서 그대로 뚫린다 · 오늘 두 번 났고 **둘째가 나였다**).

### 🔴 얼마나 봤나 — 세어서 적는다(짐작 금지)
AM 러너는 함수 **43개 · 2,715줄**이다. 내가 **자세히 읽은 것은 10개**(`tableToBullets` · `deferAnchorUrls` ·
`appendDeferredUrls` · `parseBodyBlocks` · `colorLastTyped` · `attachImages` + 머리말·상수 구역).
⇒ **대략 400~500줄 ≈ 15~18%.** 🔴 **남은 82% 에 더 있을 것이다** — 특히 아직 안 본 곳:
`insertPara`(:1077 · 한 문단을 안전하게 넣기) · `writeParts`(:1045) · `moveCaretToEnd`(:1189) ·
`sweepFormatting`(:1730) · `measureFormatBleed`(:935) · `writeQuote`(:1367) · `setFontSize`/`sizeLastTyped`.
**이 목록이 다음 읽기의 과녁이다.**
