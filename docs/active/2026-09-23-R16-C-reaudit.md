# R16 다시 재기 — C 몫(`docs/active/audit/2026-09-15-B.md` · §7·§11·§12·§14·§15·§16)

> 브랜치 `audit/2026-09-23-C` · **commit 만 · push 0 · 제품 나무 무접촉 · 고친 것 0**(재는 판이다)
> AC 칸 240~249(240 하나 씀 · 이 판에 새로 안 씀) · DDL 0

---

## ■ 여섯 줄(새 양식 — 🔵 «반만 왔다» 칸이 이번에 생겼다)

```
■ 모수: 열린 행 20개 (내가 세어서 — 메인 수와 같다)
■ ✅ 로 닫혔다: 10개  (전부 «파일:줄» 근거)
■ 아직 열려 있다: 10개
■ 🔴 더 나빠졌다: 0개
■ 🔵 반만 왔다: 2개   ← ❌ 가 ✅ 가 아니라 🟡(서버는 됐는데 화면 0곳)
■ ⊘ 못 쟀다: 0개 (판정을 안 바꾼 «모르는 것» 셋은 아래에)
   10 + 10 + 0 + 0 = 20 ✔ 맞는다  (🔵 2 는 «아직 열려 있다 10» 안에 든다 — 겹쳐 세지 않는다)
```

**어떻게 쟀나**: `grep` 으로 서버 쪽 존재 → `export const config` 로 **경로 선언** → `public/**` 에서 **부르는 화면** 확인.
🔴 **세 겹을 다 본 것만 ✅ 로 올렸다**(§4.8 · AC-178 «API 가 있다 ≠ 화면에서 쓸 수 있다»).

---

## ■ 🔵 반만 왔다: **2개** — ❌ 가 ✅ 가 아니라 🟡 로 갔다

더 나빠진 건 아니지만 **✅ 로 세면 안 되는 것**이라 따로 적는다. 서버는 받는데 **화면이 0곳**이다.

| 행 | 서버 | 화면 | 판정 |
|---|---|---|---|
| §7.1 `avatar` | `db/schema.ts:248 avatarUrl` · `netlify/functions/accounts.ts:258` | 🔴 **0곳**(`grep avatarUrl public/` = 0) | ❌ → 🟡 |
| §7.1 `account_groups` | `lib/accounts.ts` · `accounts.ts:9` (`groupName?|groupId?`) | 🔴 **0곳**(`grep groupName public/` = 0) | ❌ → 🟡 |

🔴 **이게 이 파일이 09-15 에 스스로 적어 둔 그 오판의 거울이다** — 그때는 «A 화면은 있는데 서버가 없음»을 ✅ 로 적었고, 지금은 그 반대가 둘 있다. **한쪽만 보면 반드시 틀린다.**

---

## ■ ✅ 닫힌 10개 (근거)

| § | 항목 | 근거 |
|---|---|---|
| 11.0 · 15 | 팀 초대/수락 | `netlify/functions/team.ts:27` · 화면 `team.html:110`(초대) · `team-accept.html:63`(받기) |
| 11.4 | 세금계산서·영수증·회사 정보 | `invoice.ts:21`(`/api/invoice`·`/api/tax-profile`·`/api/tax-invoice-request`) · 운영 `ops-billing.ts:33` · 화면 `plan.html` |
| 11.4 | CS 이메일·카카오 유입 | `cs-inbound.ts:24` `/api/cs-inbound` |
| 12.0 | AM↔AC 코인 이전 | `coin-transfer.ts:19` `/api/coin-transfer` |
| 12.2 | 플랜별 채널 게이트 | `lib/plans.ts:230 planChannelsOf()` · `:256` 에서 허용 채널을 가른다 |
| 12.2 | 리포트 내보내기 게이트 | `netlify/functions/export.ts:45 requireFeature(tid, "exportZip")` |
| 12.3 · 16 | **30일 파기** | 서버 `lib/account-close.ts:109` · 크론 `lib/cron/tenant-purge.ts`(KST 04:00) · API `account-close.ts:14` · 🔴 **화면** `settings.html:326` |
| 15 | `publish-now` | `publish-now.ts:32` · 화면 `piece.html:580` |

🔴 **§12.3 파기가 제일 크다** — 09-15 에 «L(라운드)»로 적힌 두 건이 **서버·크론·화면까지** 다 왔다.
그 화면 주석이 스스로 적어 뒀다: «이용자 권리는 **누를 자리가 있어야** 기능이다(§4.8 · 서버·크론은 R7 에 다 있었는데 화면이 없었다)».

---

## ■ 아직 열린 10개

| 줄 | 상태 | 항목 | 지금 무엇이 없나(재서) |
|---|---|---|---|
| 20 | 🟡 | §7.1 `avatar` | 화면 0곳(위) |
| 29 | 🟡 | §7.1 `account_groups` | 화면 0곳(위) |
| 62 | 🔒 | §11.2 MIS 허브 카드 | 우리 손 밖(그대로) |
| 68 | 🟠 | §11.4 AI 원가 세 갈래 | 🔴 **까닭을 고쳐 적는다**(메인이 갈라 줬고 내가 세어 확인했다) — 문제는 «원가를 안 가른다»가 아니라 **«무배포 갱신이 영상·음성에는 없다»**다. `ops-ai.ts:35 CODE_CHAIN` 은 `high·low·director·landing·image` **5개**뿐이고 `lib/ai-models.ts` 의 모델 상수는 **17개** ⇒ TTS·OMNI·VEO 3종·VISION·VIDEO_READ·FAL 3종 = **영상·음성 전부**를 운영센터에서 **못 바꾼다**. CLAUDE §2 의 «무배포 갱신» 약속이 **글·사진에만 참**이다 |
| 72 | 🟠 | §11.4 추천인 코인 | `lib/referral.ts:107 referralRewardCoins()` 는 있다 — **운영 이벤트 화면 쪽은 못 쟀다**(아래 ⊘) |
| 80 | 🟠 | §11.4 CS 한 화면 | 그대로 |
| 107 | 🟠 | §12.2 디렉터 플랜 갈래 | `lib/plans.ts:25 PlanFeatures` 에 `directorEdit`·`teamApproval` 은 **있다**. «Starter 자동만»을 **가르는 자리**는 못 찾았다 |
| 155 | ❌ | §15 `director-settings` | 없다 — `director.ts:18` 은 `propose`·`confirm`·`estimate` 셋뿐 |
| 162 | ❌ | §15 `accounts-health` | 없다(경로 0) |
| 179 | 🟠 | §16 팩트체크 그라운딩 | `lib/fact-claims.ts` 가 있다 — **«쇼츠 대본 검색 그라운딩»까지인지는 못 쟀다** |

---

## ■ ⊘ 못 쟀다 — 무엇을 모르는지

- **§12.2 «러너 스크랩 수익»** — 수익 쪽에 `requireFeature` 가 **0곳**이다. 그런데 그게 «게이트가 없다»인지 «게이트가 필요 없는 설계»인지 **내가 안 쟀다**(설계 문장을 다시 읽어야 한다).
- **§11.4 추천인 코인의 «운영 화면»** — 서버(`lib/referral.ts`)는 봤는데 **운영센터 이벤트 화면에서 무엇을 하는지** 안 봤다.
- **§16 팩트체크의 «검색 그라운딩»** — `fact-claims.ts` 가 **수치 주장 표시**까지인지, **검색 그라운딩**까지인지 안 갈랐다.
  🔴 셋 다 **판정을 안 바꿨다**(🟠 그대로) — 모르는 채로 ✅ 로 올리지 않는다.

---

## ■ 🔴 재면서 지킨 것 (규칙 여섯)

1. **모수 먼저** — 시작할 때 20 을 **내가 세었고**(메인 수와 같았다) 끝에 10+10+0+0=20 으로 닫았다.
2. **«재서» 말한다** — 전부 `grep` + `export const config` + `public/**` 세 겹.
3. **남이 잰 숫자엔 출처** — 이 판엔 남의 숫자를 안 썼다(메인의 «20» 도 **내가 다시 세어** 확인했다).
4. 🔴 **대용물 조심** — `avatar`·`account_groups` 가 그 함정이었다. 서버만 보고 ✅ 를 매겼으면 **둘을 잘못 닫을 뻔했다.**
5. **⊘ 를 통과로 안 썼다** — 위 셋은 🟠 로 남겼다.
6. 🔴 **고치지 않았다** — `avatar` 화면은 «5분이면 될 것»처럼 보였지만 **적어만 뒀다**. 제품 나무 변경 **0**.


---

## ■ 🔴 메인이 내 까닭을 고쳐 줬다 — 받되 **내가 세어** 확인했다

내 첫 판정: «원장엔 `image`·`video_clip`·`tts` 가 다 남는데 화면 `ROLE` 이 넷뿐이라 **안 가른다**».
메인이 재서 갈랐다: **그 화면은 «원가 화면»이 아니라 «모델 고르는 화면»**이다(`ops-ai.ts:35 CODE_CHAIN`).

**내가 다시 세었다**(남이 잰 숫자를 그대로 안 쓴다 · 규칙 ③):
- `ops-ai.ts:35 CODE_CHAIN` = `high · low · director · landing · image` → **5개**
- `lib/ai-models.ts` 의 `MODEL_*`·`CHAIN_*`·`FAL_*` 상수 → **17개**
- ⇒ 못 바꾸는 것: `MODEL_TTS` · `MODEL_OMNI` · `MODEL_VEO`/`_FAST`/`_LITE` · `MODEL_VISION` · `MODEL_VIDEO_READ` · `FAL_MODEL_*` 3종 = **영상·음성 전부**

🔴 **CLAUDE §2 가 «모델명은 `lib/ai-models.ts` 한 파일에만 · DB 오버레이로 무배포 갱신»을 약속하는데, 그 약속이 글·사진에만 참이다.**
⇒ **영상 모델이 바뀌거나 죽으면 배포해야 한다** — 영상이 제일 비싼 축인데 그렇다.

**판정(🟠)은 안 바꿨다. 까닭만 바꿨다.** 🔴 내가 ㉯에서 쓴 말이 나한테 돌아왔다 — **«까닭이 다르면 다음에 할 일이 다르다.»**
«화면에 칸을 더한다»와 «오버레이를 영상까지 넓힌다»는 **완전히 다른 일**이고, 뒤엣것이 훨씬 크고 훨씬 값지다.
