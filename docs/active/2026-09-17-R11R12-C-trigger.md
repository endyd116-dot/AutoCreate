# R11+R12-C 트리거 — 다른 모양의 자로 잰다 · 그리고 **자를 재는 일**을 이어받는다 (C)

> 발부 2026-09-17 · 베이스 = `main` **`05cc828`** · 폴더 **`C:\Users\Administrator\Desktop\작업\dev\AutoCreate-C`** · 브랜치 **`verify/r11`**
> 계약서: `docs/active/2026-09-17-R11R12-contract.md` — **먼저 읽어라.**

## 읽을 것
계약서 · R11 설계 · R12 설계 · `CLAUDE.md` **§3·§4.7·§4.8·§8·§9** · `docs/rules/PARALLEL_GUIDE.md` **§2.7** ·
`docs/rules/PITFALLS.md` **§0 전체**(특히 **AC-96~101** — 어제 하루에 실제로 밟은 것) ·
`docs/active/2026-09-16-audit-measure-review.md`(b-49 가 **자를 잰** 보고서) · `docs/active/2026-09-16-deadends-measure-review.md`

[자율주행 — 완전 자율] 🔴 **push 금지** · 🔴 **실호출 금지** · 🔴 **라이브는 읽기만**(쓰기는 그 창에서 사장님 Allow · AC-50).
[진행률] `📊 진행률 X%/100` · 보고 **10줄 이내**. **DDL 칸 `0101~0109`**(쓸 일이 거의 없다).

---

## 🔴 네 역할은 «다시 돌리기»가 아니라 «다른 자로 재기»다 — 어제 그게 값을 했다

어제 B2 가 **`55/0` 초록 · 변이 `6/6` 빨강**을 내밀고 «닫았다»고 보고했다. **그때 «번짐 끊기»가 통째로 무력했다.**
🔴 **네가 진짜 Chromium 으로 안 쟀으면 그대로 배포됐고, «꾸밈 있는 글은 전부 발행 중단»이 됐다.**
까닭은 하나다: **B2 의 변이 여섯이 전부 «자기가 상상한 고장»만 찔렀다**(AC-99 ①).

⇒ **이번에도 같다.** 네 자는 **아직 아무도 상상 못 한 자리**를 찔러야 한다.

## 🔴 그리고 **자를 먼저 변이로 찔러라**
«전부 통과»가 «아무것도 안 쟀다»와 구별되게, **일부러 깨뜨린 판**을 같은 격자에 넣어 **빨강이 나오는 것**을 보고서야 «통과»를 믿어라.
⚠️ 🔴 어제 새로 배운 것: **변이표 맨 윗줄은 «원판»이어야 하고, 그게 초록이 아니면 표 전체를 버린다**(AC-100 ⑦).
**모든 칸이 빨강인 표는 «다 잡는다»가 아니라 «아무것도 안 돈다»**일 수 있다.

---

# C1 — 🔴 **영상이 «어제와 같은가»** (0→25%) · **이 라운드의 본체**

R12 의 절반은 **무회귀**다. B2 가 모션·전환·속도를 여는데, **안 보내면 지금과 같은 영상이 나와야** 한다.
- 🔴 **네 자로 «안 보냈을 때»를 재라** — 프레임·전체 길이·자막 시각이 **지금 판과 같나**
  ⚠️ B2 도 이 축을 세운다. 🔴 **같은 축을 두 벌 만들지 말고 «다른 층»에서 재라** — B2 는 필터 문자열을, **너는 나온 파일**을 봐라(ffprobe · 프레임 해시)
- 🔴 **전체 길이가 안 밀리나** — 전환은 컷 «안»에서 빌리기로 했다. **밀리면 코인이 틀어진다**(길이 구간제)
- 🔴 **모션 `none` 인 구절에 모션이 안 걸리나**(대조군 · 안 걸어야 할 데 걸면 더 나쁘다 · AC-68)
- 🔴 **ffmpeg 가 낮을 때** `none` 으로 내려앉고 **«못 냈어요»가 적히나** — 낮은 판을 흉내 내서 재라

# C2 — **서식이 목록·표 «안»에서도 안 번지나** (25→40%)
R9 는 문단까지였다. R12-5 가 항목 안으로 들어간다 — 🔴 **번짐이 거기서 다시 난다.**
- 🔴 **네 브라우저 자에 축을 더해라**: «목록 3항목 중 **2번째만** 밑줄 · 1·3번은 평문»
- 표 칸도 같다. 🔴 **B2 가 부탁할 것이다 — 네 자로는 또 못 볼 수 있다고 스스로 말했다**(R9 에서 두 번 그랬다)
- 🔴 **거짓 양성도 같이 봐라** — 안 번졌는데 «번졌다»가 뜨면 **멀쩡한 글이 안 나간다**(못 잡는 것보다 아프다)

# C3 — **축이 진짜로 갈리나** (40→55%)
- 🔴 **수익 «어디서 났나»** — `piece_id` → `account_id` → «그 밖» 3단이 **정말 그 순서로** 도나. 🔴 **«그 밖»을 한쪽에 몰지 않나**(AC-92)
- **종류 배지** — 🔴 **카드뉴스인데 배지가 없으면 빨개지나**(낱말이 아니라 **브라우저로**)
- **축 칩** — 🔴 **축이 하나뿐인 고객에게 안 보이나** · 🔴 **새로고침하면 «전체»로 돌아가나**(저장하면 안 된다)
- 🔴 **`axis` 가 진짜로 불리나** — `TEXT_AXIS_KEYS`·`VIDEO_CHANNEL_KEYS` 를 **가려도 초록이면** 그 배선은 가짜다

# C4 — **말해 주기가 정말 닿나** (55→68%)
- **«이날 겹쳐요»** — 🔴 **막지 않나**(§9). 그리고 **되돌릴 길(시각 바꾸기)이 같이 있나**
- **성과 저하 알림** — 🔴 **겁주지 않나**(§3 금칙어를 검사로 돌려라) · 🔴 **표본이 적을 때 «아직 몰라요»가 나오나**
- **당근** — 🔴 «**이 채널은 수익이 안 붙어요**»가 **고장으로 안 보이나** · 서식 0 이 «못 냈어요»로 적히나
- 🔴 **자동 승인 경로에서도 닿나** — 사람이 안 보는 길이 §9-②의 전부다

# C5 — 🔴 **«자를 재는 일»을 이어받는다** (68→85%)

b-49 가 어제 감사와 배선 검사의 자를 쟀고, **고칠 자리 넷을 남겼다**(`deadends-measure-review.md` §7). 메인이 그중 **넷을 고쳤고 넷이 남았다**:

| 남은 것 | 무엇 |
|---|---|
| ② | 🔴 **짧고 흔한 심볼 거부** — 박을 때 «제품에 몇 번 나오나»를 세서 흔하면 **박는 사람에게 돌려준다**(`place` 가 그 예였다) |
| ③ | **문자열 리터럴 제외** ⚠️ 🔴 **`SURFACES` 는 빼라** — 거긴 일부러 `/api/…` 문자열을 찾는 자리다. **두 셈법을 가른 채로 둬라** |
| ⑥ | **빌드 어긋남 한 줄** — 🔴 «끊겨도 초록»이 아니라 «**낡아도 초록**»이다(생성물만 읽는다) |
| ⑨ | **«적는 곳/읽는 곳»이 갈리는 줄**(`heroNeeded` 처럼 적는 곳만으로 초록) |

🔴 그리고 b-49 가 **못 했다고 적은 것**을 네가 이어라:
> «`TARGETS` 68줄 중 **걸린 4줄만 직접 돌렸다.** 나머지 64줄은 «돌려서» 확인 안 했다 — 내 분류기가 두 줄을 틀렸으니 **같은 오차가 있을 수 있다.** «64줄은 깨끗하다»고 말하지 않는다.»
⇒ 🔴 **그 64줄을 돌려서 확인해라.** 물음은 하나다: «**그 줄이 가리키는 진짜 소비처를 다 가렸을 때, 여전히 초록인가?**»

# C5b — 🔴 **«막는 손 ↔ 푸는 손» 짝 검사** (새로 · AM 실사고에서 받았다)

2026-09-17 AM 에서 **고객 3곳이 갇혔다.** 모양이 이렇다:
> 온보딩 게이트가 «아직 안 끝냈다»면 **위저드 밖 모든 경로를 되돌리는데**, 그 상태를 푸는 손이 **한 경로에만** 있었다.
> 게다가 화면이 «저 메뉴에서 하세요»라고 **안내하는데 게이트가 그 메뉴를 막았다.**

🔴 **일반형(메인이 세우고 AM 이 그대로 받아 갔다 · AC-102 · AM #1767)**:
> **막고 · 어디로 가라 말하고 · 그 길이 열려 있다 — 셋이 다 있어야 갇히지 않는다.**
> 🔴 **셋째가 제일 안 보인다** — 첫째·둘째는 코드에 보이는데 셋째는 «**그 상태에서 실제로 눌러 봐야**» 안다.

## 만들 것
**세 칸 표**를 소스에서 뽑아 맞댄다: ①**무엇을 막나** ②**어디로 가라 말하나** ③🔴 **그 길이 «그 상태에서» 열려 있나**

🔴 **손으로 적은 목록끼리 비교하지 마라 — 둘 다 틀려도 초록이다**(AC-97). **기계가 소스를 읽어 세라.**

## 지금 AC 상태 (메인이 미리 쟀다 — 기준선으로 써라)
- 게이트는 `requireWritable(tid)` 하나 · **거는 함수 18개가 전부 «만들기·발행·내보내기»**
- 🔴 **`plans-list`·`coins`·`coin-purchase`·`coin-transfer` 는 0번** — 막혀도 **푸는 길이 열려 있다**
- 안내: `home-summary.ts:61-62` 가 홈 «해야 할 일» **첫 줄**에 «체험이 끝났어요 — 요금제를 골라 주세요» / «결제가 안 돼서 잠시 멈췄어요»
⇒ **지금은 안 갇힌다.** 🔴 **그런데 «잘해서»가 아니다** — 온보딩은 «푸는 손 1 · **막는 손 0**»이라 갇힐 구조가 없었을 뿐이다.
🔴 **다음에 누가 «온보딩 안 끝낸 사람은 막자»를 넣는 순간 우리도 같은 사고를 낸다.** 그 «아직 안 난 사고»를 막는 축이다.

## 🔴 반드시
- **변이로 증명해라** — «푸는 길을 일부러 막았을 때» 빨개지나. 안 빨개지면 그 축은 아무것도 안 잡는다
- 🔴 **네가 만든 것을 네가만 검증하지 마라** — **B 에게 변이를 한 번 부탁해라**(무엇을 망가뜨릴지 남이 정해 주면 내가 상상 못 한 고장이 잡힌다 · AC-100 ⑩)
- ⚠️ **§9 와 헷갈리지 마라** — «돈·계약으로 막는 것»은 게이트가 아니라 **계약**이다(CLAUDE §9 «이 규칙 밖인 것»). 🔴 **막는 걸 없애라는 축이 아니라, «막았으면 나갈 문도 있나»를 재는 축**이다

# C6 — 최종 집계 + 배포 판정 (85→100%)
- `verify-r8-audit64.mjs` 한 줄이 최종 숫자 · 🔴 **분모를 만지면 %가 공짜로 오른다**
- 🔴 **전수는 `verify-safe-list.mjs --run`** 으로(AC-94 — 글로브로 돌리지 마라)
  ⚠️ 그 판은 이제 **exit 2 를 «⊘ 못 쟀음»으로 따로 센다** — «실패»와 다른 말이다
- 🔴 **`| tail` 도 `&&` 사슬도 쓰지 마라**(둘 다 앞 명령의 코드를 물고 온다)
- 🔴 **편성 다중 규칙 화면**(감사 ③ 마지막 🟠) — **A 와 같이** 라이브에서 띄워 보고 판정해라
- 남은 «열림·일부» 칸마다 **무엇이 빠졌는지 한 줄** — 사장님께 드릴 목록이다
- 🔴 **«이 판을 라이브에 올려도 되나»를 한 줄로** 답해라. 네가 «아니»라고 하면 안 올린다

---

## 🔴 네 원칙 셋 (그대로 지켜라)
1. **네가 만든 것을 네가 검증하지 마라.** 새로 만들 게 나오면 **범위만 보고**하고 메인이 다른 창에 준다.
2. **판정은 세 갈래**(닫힘 · 🟠일부 · 열림) + 「결정으로 미룸」. 둘로 뭉치면 어느 쪽으로든 거짓말한다. **파일을 지목**해라 — 전역 grep 은 **가짜 닫힘**을 만든다.
3. 🔴 «**안 만들 것을 미개발로 세면 거짓말이고, 만들 것을 분모에서 빼도 거짓말이다.**»

## 🔴 어제 네가 세운 것 — 잊지 마라
«**번짐 0 인 글만 표본에 있으면 `measure` 를 통째로 무력화해도 아무 변이도 안 빨개진다**» (AC-99 ⑨)
⇒ **«잡아야 할 것»을 표본에 먼저 넣어라.** 오늘 그 병이 **세 창에서 세 번** 났다.

## 보고
`[폴더 · 브랜치 · 커밋해시 · 근거(파일:줄) · 최종 한 줄]` **10줄 이내**.
🔴 **A·B·B2 와 직접 물어라**(메인 거치지 마라). 메인은 범위·머지·사장님 결재만.
🔴 **해시는 `git log -1 --format=%H` 로 직접 읽어라**(AC-98 — 메인이 어제 없는 해시를 두 창에 말했다).
