# R11-15 변이 확인 — «막는 손 ↔ 푸는 손» 자가 진짜 우는가 (B · 2026-09-19)

> 자 = `scripts/verify-gate-pair.mjs` (메인이 만들었다 · 커밋 `74a3b75`) · 재는 사람 = B(만든 사람이 자기 자를 재면 안 된다).
> 변이는 전부 **되돌렸다** — 커밋에 남지 않았다(`git status` 깨끗). 하니스는 스크래치패드에 두었다(레포 밖).

## 0. 한 줄

**자는 운다 — 단, «푸는 손의 이름이 파일에서 사라질 때»만 운다.**
«그 손이 하는 일이 없어질 때»·«그 손에 닿는 길이 없어질 때»·**«푸는 문이 다른 담 뒤로 들어갈 때»** 는 **안 운다.**
내가 새로 짠 변이 넷 중 **넷 다 초록**이었다.

## 1. 바닥값

```
node scripts/verify-gate-pair.mjs   → exit 0 · 담 18개 · PASS 19 · FAIL 0
```

## 2. 변이 다섯

| # | 무엇을 고장 냈나 | 결과 | 뜻 |
|---|---|---|---|
| **M0**(양성 대조) | 로그인 잠금의 푸는 손 **문자열**(`locked_until = NULL`)을 지운다 | 🔴 **exit 1 · PASS 18 · FAIL 1** | 자는 살아 있다 |
| **M1** | `restoreAccount` 의 **몸통만** 비운다(이름·시그니처 그대로 · 첫 줄에서 «되돌릴 수 없어요» 반환) | ⚠️ exit 0 · 19/0 | **안 운다** |
| **M2** | readonly 를 푸는 **유일한 길**(요금제 고르기 = `netlify/functions/subscription.ts`)을 `requireWritable` 담 뒤로 옮긴다 | ⚠️ exit 0 · **담 18→19 로 세어 놓고** 19/0 | **안 운다** |
| **M3** | 로그인 잠금을 실제로 푸는 HTTP 길(`netlify/functions/auth-reset.ts`)을 **통째로 치운다** | ⚠️ exit 0 · 19/0 | **안 운다** |
| **M4** | 계정을 깨우는 손을 **부르는 유일한 곳**(`lib/cron/reap.ts` 의 `sweepAccountStates`)을 끊는다 | ⚠️ exit 0 · 19/0 | **안 운다** |

## 3. 왜 안 우는가 — 축별로

### ② 갇힘 축이 **6쌍 중 1쌍**에만 걸려 있다  ← 제일 큰 구멍(M2)
`TENANT_WIDE = new Set(["탈퇴 readonly"])` 한 줄이라, 갇힘은 그 한 쌍만 본다.
그런데 서버의 담(`lib/guards.ts` `NON_WRITABLE`)은 **`readonly`·`suspended`·`cancelled` 셋**이다.
⇒ «체험이 끝난 집»(readonly)과 «결제가 밀린 집»(suspended)이 **그 상태를 푸는 문**(요금제·결제·충전) 뒤에 갇히는
**AM 사고와 똑같은 모양**이 들어와도 자는 그냥 지나간다. M2 에서 자는 담이 하나 늘어난 것을 **세어 놓고도**(18→19)
판정은 19/0 초록이었다 — 세는 것과 재는 것이 이어져 있지 않다.

> 참고(지금 라이브는 안전하다): `subscription.ts`·`coin-purchase.ts` 에는 `requireWritable` 가 **없다**(2026-09-19 실측).
> 막힌 집이 나올 문은 오늘은 열려 있다. 🔴 **그 사실을 이 자가 지켜 주고 있지는 않다**는 것이 이 항목의 요지다.

### ③ 표면 축이 «화면에서 닿나»를 안 잰다 (M3)
- 여섯 쌍 중 셋(`계정 연결 끊김`·`계정 칸 멈춤`·`내 AI 키 무효`)의 표면 검사가 그 파일에 **`export const config` 가 있나**뿐이다.
  그 파일에 아무 경로나 하나 있으면 초록이라, **푸는 경로만 골라 지워도** 안 운다.
- `로그인 잠금` 의 표면 검사는 아예 **lib 파일**(`lib/auth-service.ts` 의 `reset_nonce = NULL`)을 본다 — 표면이 아니다.
  그래서 `/api/auth-reset` 함수 파일을 통째로 치워도 19/0 이었다.

### ①·②·③ 전부 «문자열이 있나»까지만 (M1·M4)
푸는 손의 **몸통을 비우거나**(M1) **부르는 데를 끊어도**(M4) 문자열은 파일에 남아 있어 초록이다.
🔴 AC-99 가 말한 모양 — «통째로 무력한데 초록».

## 4. 그래서 이 자의 값은 얼마인가

- **값 있다**: 누가 푸는 손을 **지우면** 잡는다(M0). 담 목록을 폴더에서 **세는** 것도 값이다(손으로 박아 두면 새 담을 못 본다 · AC-108).
- **값 0인 축**: 갇힘(6쌍 중 5쌍) · 표면(6쌍 중 4쌍) · 몸통 · 호출자.
- 🔴 **초록을 «나올 문이 있다»로 읽으면 안 된다.** 지금 이 자가 보증하는 문장은
  **«푸는 손의 이름이 아직 그 파일에 적혀 있다»** 한 줄이다.

## 5. 다음 판에 하면 되는 것 (한 줄씩 · 이번 판에 고치라는 말이 아니다)

1. 갇힘 축을 `NON_WRITABLE` **세 상태 전부**로 넓히고, «그 상태를 푸는 표면»(`subscription.ts`·`coin-purchase.ts`·`account-close.ts`)을
   **이름으로 못 박아** 그 파일에 `requireWritable` 가 붙으면 빨강. (서버의 `NON_WRITABLE` 를 읽어 오면 둘이 갈라지지 않는다.)
2. 표면 축은 `export const config` 존재가 아니라 **그 경로 문자열**(`/api/auth-reset` 처럼)로 재기.
3. 몸통·호출자는 정적 문자열로 못 잰다 — **«못 쟀어요»로 적어 두는 편이 정직하다**(AC-9). 재려면 스모크의 몫이다.

---

# 2판 재측정 (B · 2026-09-19 · 메인 `012f137` 위에서)

> 자 2판 = 19축 → **28축**. 바닥값 `node scripts/verify-gate-pair.mjs` → **exit 0 · 담 18개 · PASS 28 · FAIL 0**.
> 같은 하니스로 **M0~M4 를 그대로** 다시 넣고, **새 변이 넷(M5~M8)** 을 더 짰다. 변이는 전부 되돌렸다(워킹트리 깨끗).

## 6. 다시 잰 값 — 1판 넷 중 **셋이 울게 됐다**

| # | 변이 | 1판 | **2판** |
|---|---|---|---|
| **M0** | 푸는 손 문자열 삭제(양성 대조) | 🔴 운다 | 🔴 **운다** (27/1) |
| **M1** | `restoreAccount` **몸통만** 비우기 | ⚠️ 안 운다 | ⚠️ **안 운다 — 예상대로** |
| **M2** | 푸는 문을 `requireWritable` 담 뒤로 | ⚠️ 안 운다 | 🔴 **운다** (담 18→19 · 25/3) |
| **M3** | 푸는 표면 파일 제거 | ⚠️ 안 운다 | 🔴 **운다** (27/1) |
| **M4** | 푸는 손의 **호출자** 끊기 | ⚠️ 안 운다 | 🔴 **운다** (27/1) |

🔴 **M1 이 안 우는 것은 «못 고친 것»이 아니라 «못 잰다»고 적은 것이다 — 그 판단에 동의한다.**
자가 스스로 찍는 ⊘ 두 줄(몸통이 일을 하나 · 화면에 실제로 보이나)이 M1 을 정확히 덮는다.
정적으로 이름·경로만 보는 자에게 «속이 비었다»를 시키면, 그 다음은 «속이 채워졌나»를 문자열로 재게 되고 그게 더 나쁘다.

## 7. 새로 짠 변이 넷 — **둘이 가짜 초록이다**

| # | 변이 | 결과 |
|---|---|---|
| **M5** | 서버 `NON_WRITABLE` 에 막는 상태 `"frozen"` 추가(나올 문은 안 적음) | 🔴 **운다** (28/1) — 축 ①은 산다 |
| **M6** | 같은 것을 **낙타표기** `"pastDue"` 로 추가 | ⚠️ **안 운다** |
| **M7** | 같은 것을 **붙임표** `"past-due"` 로 추가 | ⚠️ **안 운다** |
| **M8** | 로그인 잠금을 푸는 **URL 을 갈아 404** 로(`config.path` 만 바꾸고 머리 주석은 그대로) | ⚠️ **안 운다** |
| **M9**(원인 확정용) | M8 과 같되 **머리 주석의 옛 경로까지** 삭제 | 🔴 **운다** (27/1) |

### 7-① 상태 이름이 `[a-z_]+` 가 아니면 **조용히 빠진다** (M6·M7)
`nonWritableStates()` 가 `seg.split('"')` 한 조각을 `/^[a-z_]+$/` 로 거른다.
⇒ `pastDue`·`past-due` 같은 이름은 **목록에서 사라지고**, 새 막음이 늘었는데도 «나올 문을 안 적었다»가 안 울린다.
🔴 축 ①의 값이 **상태 이름의 철자에 달려 있다.** 고칠 자리 한 줄: 거르개를 `/^[A-Za-z0-9_-]+$/` 로(또는 빈 조각·구분자만 버리기).

### 7-② 경로 축이 **머리 주석에 걸려 초록이 된다** (M8 ↔ M9)
`hit(surface, "/api/...")` 가 **파일 전체**를 본다. 그런데 이 레포는 함수마다 **머리 주석에 경로를 적어 둔다** —
문 7개의 표면 파일을 세어 보니 **7개 전부** 그 경로 문자열을 «주석 + `config.path`» **두 군데**에 갖고 있다.
⇒ 라우트를 갈아 **URL 이 실제로 404 가 돼도**(CLAUDE §4.2 «config.path 누락 시 404») 자는 안 운다.
M9 가 주석까지 지우자 바로 울었으므로 **원인은 주석 하나로 확정**이다.
고칠 자리 한 줄: 파일 전체가 아니라 **`export const config` 가 든 줄(또는 그 객체 안)** 에서만 찾기.

## 8. 2판에 대한 총평

- 1판이 보증하던 문장(«푸는 손의 이름이 아직 적혀 있다»)에서 **«그 문이 담 뒤에 있지 않고, 표면 파일이 남아 있고,
  부르는 데가 살아 있다»** 까지 넓어졌다. M2·M3·M4 가 그것을 실제로 증명한다.
- 남은 가짜 초록 둘(M6·M7 · M8)은 **축이 없어서가 아니라 «읽는 자리»가 넓어서** 생긴다 — 둘 다 한 줄 고침이다.
- 🔴 ⊘ 두 줄(못 재는 것)을 자가 스스로 찍는 것이 이 2판의 제일 큰 값이다. **초록의 뜻을 자가 스스로 좁혀 말한다.**
