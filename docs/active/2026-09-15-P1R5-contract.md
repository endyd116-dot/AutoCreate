# P1R5 계약서 v5.6 — «소재 하나가 계정별 쇼츠가 된다» (A·B·B2·C 공통 · 키 이름 정본 · 발부 2026-09-14 · 베이스 main `0f3ec44` = R4 배포본)

> 정본: `docs/DESIGN.md` **§6**(쇼츠 공장 · 6.1 이식 체인 · 6.2 디벨롭 6항목 · 6.3 코인) · **§2.2**(영상 채널 매트릭스) · **§16B**(영상 광고 표시 · 우상단 배지 · 설명란) · **§5C.1**(쇼츠·클립·릴스 대본 계약 1행) · **§5.4**(영상 감성 3행) · **§8.2**(`render.video`·`publish.naver_clip`) · **§17 Phase 3**
> 조사 정본: `docs/active/2026-09-14-R5-presurvey-video.md`(B-1 조사 A~G + 메인 결정 §D 8건) — 이 계약은 그 문서를 **계약 문장**으로 옮긴 것이다. §D 와 어긋나면 §D 가 이긴다.
> 규칙: `CLAUDE.md` §4·§4.5b(KST)·§4.7(절대 게이트)·§8(전본) · `docs/rules/PARALLEL_GUIDE.md` · `docs/rules/PITFALLS.md` §0 **AC-1~AC-26**(특히 AC-2 슬롯 게이트 · AC-4 비공개 고정 · AC-16 착수 실패 삼킴 · AC-17 순환 import · AC-18 동적 import · AC-21 칸 폭 · AC-26 thinking 본문 0)
> 로드맵: **Phase 3 «영상»** — 완료 정의 = «소재 1개 → 60초 쇼츠가 계정별 변주로 예약·업로드(심사 전엔 비공개 업로드)»
> 베이스 main **`930f8ef`**(R4 진행 중 · R1~R3 배포됨) · 초안 2026-09-15 · 🔴 코드 0 — 이 문서는 계약뿐이다.

---

## -1. 설계 대비 범위 지도 (P1R5) — 🔴 전본 개발 규칙(CLAUDE §8)

| DESIGN | 항목 | 이번(R5) | 다음(R6/Phase 5) | 외부 선결조건 |
|---|---|---|---|---|
| §6.1 이식 체인 | 소재 뱅크 → 대본(+**Google 검색 팩트체크 왕복**+광고법 게이트) → 컷 계획·충돌 점검 → 클립 생성(provider) → TTS(자막 본체·나레이션 보조·읽기 전처리) → 러너 렌더(크롬 프레임 캡처+ffmpeg 1080×1920) → 8축 심사 → 편성 루프 | ✅ B(대본~심사·배경 함수) / B2(러너 렌더) / 편성 루프 = **R2 슬롯·크론 흡수**(새 루프 0) | — | ffmpeg(러너 PC) |
| §6.2 포맷 3종 | 그래픽 스토리 · 토킹(TTS+자막+B-roll) · 클립형(15~30초 생활밀착) | ✅ B(`format` 3 · 계약 표 1벌) / A(손보기 «영상» 행) | — | — |
| §6.2 채널 규격 | 쇼츠 ≤60 · 클립 15~30 · 릴스 ≤90 · 세이프존(상하 UI 가림) | ✅ B(프리셋 표) / B2(레이아웃 좌표) | — | — |
| §6.2 수익 슬롯 | 엔드카드 «설명란 링크» 컷 · 쇼핑 태그 메타 · 설명란 쿠팡 링크 + 고지 | ✅ B(엔드카드 컷·설명란 문구) · 쇼핑 태그 메타 = **키 후**(유튜브 쇼핑 제휴 승인) | — | 유튜브 쇼핑 제휴 |
| §6.2 다계정 변주 | 같은 대본 → 계정별 다른 훅·팔레트·보이스 + 중복 업로드 판정 회피(**프레임 지문**) | ✅ B(`variant` · 프레임 지문 게이트) / A(변주 미리보기) | — | — |
| §6.2 렌더 | 러너 필수(서버리스 불가) · 관리형 러너 팜 병렬 | ✅ B2(내 PC 러너 `render.video`) · 관리형 팜 = **R4 후반 팜 프로비저닝 후** | ✅ 팜 | — |
| §6.2 저작권 | BGM 게이트 `BGM_LICENSE_VERIFIED` + FreePD R2 시드 | ✅ B(관문) / B2(시드 스크립트) | — | env 1 · R2 시드 1회 |
| §6.3 코인 | `video_15=6` · `video_30=12` · `video_60=28` · 재렌더 무료 | ✅ B(소비처·구간제) | — | — |
| §2.2 유튜브 쇼츠 | Data API v3 `videos.insert`(resumable) · **심사 전 비공개 고정** · 쿼터 10,000u/일 · `containsSyntheticMedia` | ✅ B2(어댑터·private·쿼터 회로) — **공개 전환은 심사 후** | 공개 업로드 | Google OAuth 앱 **민감 스코프 심사**(사장님) |
| §2.2 네이버 클립 | 러너 PC 업로드 → 실패 시 Android 에뮬 | **잡 kind `publish.naver_clip` 예약 + 러너 스텁(정직 `awaiting_manual`)** ✅ B2 | PC 경로 실측·에뮬 | tv.naver.com 스튜디오 실측 |
| §2.2 릴스 | REELS 컨테이너·폴링 | ✅ B2(`publish/instagram.ts` REELS) | — | Meta 앱 키 |
| §2.2 쓰레드 영상 | VIDEO 확장 | ✅ B2(`publish/threads.ts` VIDEO) | — | Threads 앱 키 |
| §2.2 틱톡·페북 릴스·롱폼 | — | ❌ **Phase 5** | ✅ | 앱 심사 |
| §16B.1 영상 표시 | 시작 3초 자막 + **우상단 상시 배지** + 설명란 첫 줄 공식 문구 + 유튜브 유료 프로모션·합성 콘텐츠 플래그 | ✅ B(문구·배지 트랙 spec) / B2(렌더 오버레이·API 플래그) / A(미리보기) | — | — |
| §16B.3 유튜브 정책 | 반복 콘텐츠 YPP 제외 위험 → 계정 간 변주 게이트 | ✅ B(프레임 지문 유사도) | — | — |
| §5C.1 대본 계약 | 3초 훅 · 문장 4~10 · 컷 4~8 · 엔드카드 · 무음 시청 전제 | ✅ B(`writing-contracts` shorts 4행 보강) | — | — |
| §5.4 영상 감성 | 쇼츠(3초 훅·자막 본체) · 클립(생활밀착·네이버 톤) · 릴스(감성 b-roll·BGM 무드) | ✅ B(`emotionKey` 3) | — | — |
| §8.2 잡 | `render.video` · `publish.naver_clip` | ✅ B2 | — | — |
| §19 제품 | 콘텐츠 내보내기 ZIP · 결과 공유 카드 · 추천인 · §12 AM↔AC 코인 이전 | ❌ **R6**(R4 지도의 «R5» 표기를 R6 로 민다 — 영상이 먼저) | ✅ R6 | — |

> 지도에 없는 §6·§2.2·§16B(영상) 항목 = 메인의 작성 미준수. «R6/Phase 5» 는 순서만 뒤.

---

## 0. 공통

- 응답·에러·config path·tenant 스코프·KST·`requireWritable`: R1~R4 계약 §0 그대로. 모든 고객 API 는 `requireUser` → `auth.tid`.
- 🔴 **생성은 서버 배경 함수 · 합성은 러너 잡**(조사 §0-①). Netlify 함수에 ffmpeg 를 넣지 않는다(조사 C-4).
- 🔴 **15분 벽 3종 세트를 처음부터**(조사 §0-②): 하트비트 `meta.chainStage` · 이어달리기 `meta.chainResume` · 잠금 `meta.chainLock` + 스위퍼(§1.5).
- 🔴 **두 척추 금지**: `creative_assets`·`video_assets`·`shorts_topics` 표를 만들지 않는다. 영상 piece = `pieces(kind 'video')` + `piece_assets` + `runner_jobs` + `posts`.
- 🔴 **슬롯 게이트 fail-closed**(AC-2): 영상도 `content-slot-gate` 를 탄다. 수동 «만들기»는 `origin:"manual"` 슬롯을 만든다(R1 확정과 같다).
- 🔴 **코인 = piece 당 1회**(ref `piece:{id}` · 구간제 item) **+ 달러 캡은 별개 관문**(§1.6). 재렌더·수리·심사 재큐·편집 재생성·이어달리기 = 코인 0.
- 🔴 **비공개 업로드 정직 표기**(AC-4): 심사 전 유튜브 = `privacyStatus:"private"` 고정 → `posts.status = 'uploaded_private'`.

### 0.1 [B-1 조사 반영] 정본 결정 — 다시 묻지 않는다 (= 조사 §D 8건 그대로)
| # | 항목 | 결정 |
|---|---|---|
| 1 | provider 기본 티어 | **Omni 기본 + Veo Lite 폴백**(동기·편집 재생성·같은 `GEMINI_API_KEY`). fal(wan·hailuo·kling)은 `FAL_KEY` 있을 때만 · kling 은 명시 지정 시만. **`video_15` 는 Lite/이미지 위주 티어 강제**(원가 역전 방지) |
| 2 | TTS | **타입캐스트 기본 · Gemini TTS 폴백**(어절 타임스탬프 = 자막 싱크 품질). 폴백 시 자막 균등 분할. `TYPECAST_API_KEY` 없으면 정직 폴백 + `meta.tts.provider:"gemini"` |
| 3 | 소재 뱅크 | **AC `topics` 흡수**(channelHint 영상 채널 · `factors.structureTemplateId?`) + **`shorts_templates` 표만 유지**(레퍼런스 구조 · 추가형 DDL) · 새 뱅크·새 루프 크론 0 |
| 4 | 포맷·코인 어휘 | `format: "graphic"\|"talking"\|"clip"` · `seconds: 15\|30\|60` · 코인 item = `videoCoinItem(seconds)`(≤5 `video_clip` · ≤15 `video_15` · ≤35 `video_30` · 그 이상 `video_60` — 초 산식 금지) · 재렌더·수리·재큐 = 코인 0 |
| 5 | 상태 어휘 | piece `generating` 안의 **`meta.stage: "script"\|"tts"\|"clips"\|"render"\|"judging"\|"done"\|"failed"`** → `in_review`. 러너 렌더 대기 = piece `generating` + slot `producing`. 심사 3회 미달 = `in_review` + `gate_report`(**새 piece status 0** · AC-21) |
| 6 | 공개 상태 | `posts.status` 에 **`uploaded_private`** 1개 추가(칸 폭 확인 AC-21 · 화면 «비공개 업로드됨 · 공개 전환 필요») · 공개 전환은 사용자가 유튜브에서(R5) · 심사 통과 후 `publishVideo` 가 `public` 으로 |
| 7 | 심사 등급 | AM SHORTS5 **게이트 3등급 채택**: P0(정책 위반·프레임 공백·루프 이음새 파손) 차단 · P1(저품질 컷) 1회 편집 재생성 후 통과 표시 · P2 기록만. 재큐 상한 `RENDER_MAX_RETRY=2` · 3회째 `in_review`(사람) |
| 8 | 달러 캡 | 테넌트 월 **$30** · 전역 **$300** · `ai_usage(purpose IN ('video_clip','tts','video_judge'))` 당월 합 · **fail-closed**(조회 실패 = 차단) · kill switch = `feature_flags.key='video'`(있는 표) |

### 0.2 어휘 정본(글자 그대로)
```
VideoFormat  = "graphic" | "talking" | "clip"
VideoSeconds = 15 | 30 | 60
VideoChannel = "youtube_shorts" | "naver_clip" | "reels" | "threads"        // R5 생성 대상 · 발행 = youtube_shorts·reels·threads(API) · naver_clip(스텁)
ProviderKey  = "omni" | "veo_lite" | "veo_fast" | "veo" | "wan" | "hailuo" | "kling"   // AM registry 그대로 · 금지 목록 Sora·Hunyuan·LTX 는 코드가 거부
ClipTier     = "filler" | "standard" | "money"
VideoStage   = "script" | "tts" | "clips" | "render" | "judging" | "done" | "failed"
JudgeGrade   = "P0" | "P1" | "P2"
PostStatus  += "uploaded_private"                                              // 기존 어휘에 1개 추가
RunnerJobKind += "render.video" | "publish.youtube_shorts" | "publish.naver_clip"   // 우선순위 render 70 · publish 10 · 발행>세션>수익>렌더
```

---

## 1. B — 생성 두뇌 (`lib/video/**` · `netlify/functions/generate-video-background.ts` · 디렉터 확장 · 코인 소비처 · 스위퍼)

### 1.1 디렉터 확장 (`lib/director.ts` · 계약 R1 §3 PieceSpec 에 **키 추가** · 기존 키 무변경)
```
PieceSpec += {
  kind: "post" | "video",                                   // 기본 "post"(R1 호환 · 없으면 post)
  video?: {                                                 // kind video 일 때만
    format: VideoFormat, seconds: VideoSeconds,
    provider: { tier: ClipTier, key: ProviderKey },         // §0.1-1 규칙으로 디렉터가 정한다(15초 = "veo_lite" 강제)
    voice: { provider: "typecast" | "gemini", voiceId: string },
    variant: { hookType: string, palette: string, voiceId: string },   // 다계정 변주 — 같은 brief 의 piece 끼리 서로 다르다
    cuts: number,                                           // 포맷·초 표(§1.3)의 기본값
    disclosure: { badge: boolean, descriptionFirstLine: boolean }      // 제휴/유료면 둘 다 true
  },
  coinCost: number                                          // = coinCostOf(videoCoinItem(seconds)) — 이미지 코인 없음
}
PieceSpecPatch += { video?: { format?, seconds?, voiceId?, palette?, hookType?, cuts? } }
```
- 채널 선택(§5.3-1): topic.channelHint 가 영상 채널이거나 테넌트 `settings.kinds` 에 `video` 가 있고 영상 계정이 있으면 영상 piece 를 낸다. 같은 brief 에 글 piece 와 영상 piece 가 섞일 수 있다(예: 네이버 글 + 쇼츠).
- 계정 배정·일정·제휴 = R1 규칙 그대로(best-time 표에 쇼츠 18/21 · 클립 19/22 · 릴스 12/19 이미 있음).
- `variant` 결정 규칙: 같은 brief 안 영상 piece 를 i=0,1,2… 로 세어 `hookType = HOOK_ROTATION[i]` · `palette = PALETTES[(accountId + i) % n]` · `voiceId = 계정 persona 의 voice ?? VOICES[i % n]` — 결정론(재제안 시 같은 값).
- reasons 3줄에 영상이면 «쇼츠 60초 · 그래픽 스토리 · @a 는 훅 «반전»·@b 는 훅 «질문» 으로 갈랐어요» 를 포함.

### 1.2 director-confirm(영상 분기)
- piece 행: `kind='video'` · `status='generating'` · `meta = { stage:"script", video:PieceSpec.video, angle, emotionKey, scheduleAt, chainStage:null, chainLock:null, chainResume:{count:0} }`.
- 코인: `consume(tid, videoCoinItem(seconds), "piece:{id}")` **1회**(이미지 ref 없음). 선검사·롤백 = R1 v1.1 규칙 그대로.
- 달러 캡(§1.6) 선검사: 추정 원가 `estimateVideoCostUsd(format, seconds, provider)` 를 더해 캡 초과면 **코인 차감 전** `{ ok:false, step:"budget", error:"이번 달 영상 제작 한도에 닿았어요. 다음 달에 다시 만들 수 있어요." }`.
- 배경 함수 호출: `POST /api/generate-video-background { pieceId, tenantId }`(x-internal-secret · AC-16: 호출 실패는 즉시 `failed`+환급).

### 1.3 포맷·초 표 (`lib/writing-contracts.ts` shorts 4행 보강 · 코드 정본 · `emotion_profiles` 오버레이 AC-6 shape 가드)
| format | seconds | cuts(min~max·기본) | 컷 길이 | provider 기본 | TTS | 자막 | 이미지 대체 |
|---|---|---|---|---|---|---|---|
| graphic | 60 | 6~12 · 9 | 5~8s | omni(폴백 veo_lite) | 타입캐스트 | 키워드 중앙 + 구절 | 없음 |
| graphic | 30 | 4~6 · 5 | 5~8s | omni(폴백 veo_lite) | 타입캐스트 | 〃 | 없음 |
| talking | 30/60 | B-roll 3~4 · 나머지 정지 이미지 | 5s | veo_lite(폴백 이미지 CHAIN_IMAGE) | 타입캐스트(본체) | 구절 자막 큰 글씨 | ✅ 정지 이미지 Ken Burns |
| clip | 15/30 | 3~4 · 3 | 5s | **veo_lite 강제**(15초) · omni(30초) | 타입캐스트 짧게 | 상단 큰 자막(클립 규격) | 없음 |
- 각 행에 `visualMin`(엔드카드 1 · 배지 트랙 1(제휴 시) · 자막 문장 4~10) · `titleStyle:"script"` · `length:{min,max}`(음절) 이 붙는다. `shortsFormOf(format, seconds)` 한 함수가 이 표를 낸다.

### 1.4 배경 함수 `generate-video-background` (파일명 `-background` · 15분 · 멱등)
단계(= `VideoStage` · 단계마다 `meta.chainStage={stage,at,cutsDone?,cutsTotal?}` + `updated_at`):
1. **script** — `lib/video/script.ts`(AM `shorts-script.ts` 이식): 훅 검사 `checkHook`(3초·음절) · 대본 JSON `{ lines:[{ text, role, seconds, cutIdx }], hook, closing, youtube:{ title, description, tags } }` · **팩트체크 왕복**: `hasFactualClaims` 면 `callGemini({ googleSearch:true })`(§1.7 ai.ts 옵션 복원)로 claims 판정 → `unverifiedTokens` 가 있으면 1회 수정 요청 → `correctionLanded` 확인 → 그래도 남으면 P0 차단(`failed` · 사유 «확인 안 되는 사실이 남아 있어요»). 광고법 금칙(`banned-words`)·수익 약속(AM INCOME-CLAIM 축 이식) 재검사.
2. **tts** — `lib/video/tts.ts`(AM `creative-tts.ts` 순수부 + `synthesizeNarration`) + `lib/video/tts-typecast.ts`(AM 그대로): 읽기 전처리 → 문장별 합성(타입캐스트 `/with-timestamps` · `audio_tempo` 1.1) → `piece_assets(kind 'audio' · sort=문장idx · meta:{ durationMs, words:[{text,startMs,endMs}], provider })` **문장마다 즉시 저장** · 실패 시 Gemini TTS 폴백(어절 시각 없음 → 자막 균등 분할 · `meta.tts.provider`). 비용 `ai_usage(purpose 'tts')`.
3. **clips** — `lib/video/scenes.ts`(AM `shorts-scenes.ts` 이식: `planGraphicCutWindows`·`buildGraphicShotPrompt`·`checkCutConflicts`·붉은 표식·굽는 글자 게이트) → 컷마다 `lib/video/providers/{registry,omni,veo,fal}.ts`(AM 이식 · 키 = `GEMINI_API_KEY`·`FAL_KEY`) → **컷마다 즉시** `piece_assets(kind 'clip' · sort=컷번호 · r2_key · meta:{ interactionId, provider, model, durationMs, costUsd, judge? })`. 이미 있는 컷(sort 존재·r2 HEAD 200)은 **다시 만들지 않는다**(이어받기 · 돈 두 배 금지). P1 컷은 `omniEditRetry`(previous_interaction_id) 1회 후 통과 표시. `variant.palette` 는 프롬프트 COLOR 절에 · 무인물 규칙(personGeneration) · PIPA(인물 식별 사진은 i2v 입력 금지) 게이트. 비용 `ai_usage(purpose 'video_clip')`.
   - 컷 루프마다 경과 > `CHAIN_BUDGET_MS = 11분` 이면 **스스로 멈추고** `meta.chainResume.count+1` · 같은 함수 재디스패치(`resume:true`) · 상한 3회 → `failed`(«제작이 너무 오래 걸려 멈췄어요(N단계까지 진행)») + 환급.
4. **render** — `RenderPayload`(§5) 조립 → `enqueueRender(pieceId, payload)`(B2 함수 · §5) → `runner_jobs(kind 'render.video')` · piece 는 `generating`+`stage:"render"` 유지 · slot `producing`.
5. **judging** — 러너 `report` 뒤 B2 가 `finalizeRender(pieceId, report)` 를 부르면 B 의 `lib/video/judge.ts`(AM `creative-judge.ts` 8축 + `creative-polish.ts` 6축 + 프레임 지문) 가 판정: P0 → `repairSpecDeterministic` 후 재큐(≤2) · 3회째 `in_review`+`gate_report` · 통과 → **done** → `in_review`(slot `in_review`). 비용 `ai_usage(purpose 'video_judge')`. AC-26: 비전 콜 «본문 0» 을 실패로 세지 않는다(finishReason·parts 검사).
6. **done** — `pieces.body = 설명란 텍스트(첫 줄 고지)` · `pieces.blocks = [{type:"video", assetId}, {type:"srt", assetId}, {type:"hashtags",…}]` · `piece_assets(kind 'video' r2_key mp4 · kind 'thumb' poster · kind 'srt')` · `meta.stage:"done"` · `gate_report`(GateKey 12 중 영상에 해당하는 것 + `judge:{grade, axes:[…]}`).
- 예외 → `failed` + `meta.failReason` + notifications 1행(«영상을 만들지 못했어요 · 코인은 돌려드렸어요») + `refundPiece`.
- 멱등: piece 가 `generating` 이 아니면 스킵 · `meta.chainLock={at,by}` 가 20분 안이면 즉시 반환(로그만) · 20분 넘은 잠금은 뺏는다 · 종결(성공·실패·이어달리기 넘김)에서 **반드시** 잠금 해제 · 성공 시 `failReason` 비운다.

#### 1.4b [v5.2] 검증 손잡이(로컬·수동 전용 · 프로덕션 설정 금지 · C 하니스가 쓴다)
- `VIDEO_PROVIDER_STUB=1` — provider·TTS·심사 비전 호출 스텁(고정 응답 · `ai_usage` 에 «스텁» 표기 · 원가 0). 실호출은 변수 없을 때만.
- `CHAIN_BUDGET_MS`(기본 660000=11분) — 이어달리기 재현용 덮어쓰기(`CRON_BUDGET_MS` 관례).
- 슬롯 없는 자동 생성 재현 = 새 손잡이 없음 · `confirm({origin:"auto"})` slotId 없이 → `piece_slotless_blocked`(R2 fail-closed) · 영상 분기도 같은 게이트.

#### 1.4c [v5.4 · B-1 인수인계 미결 3건 → 메인 확정]
**(1) 자동 편성이 영상도 만든다 — 좁히지 않는다.** `produce` 크론은 `kind='shorts'` 슬롯도 집는다(고객이 영상 규칙을 **직접 만든 것**이 옵트인 · DESIGN §5B+§6).
🔴 **[v5.5 재정정 — 문을 둘로 만들지 않는다]** R4 에 **이미** 원가 관문이 있다: `lib/billing/ai-cost-cap.ts checkAiCostCap`(플랜별 **일일 KRW** 상한 `PLAN_CAP_KRW` trial 3,000 / starter 5,000 / pro 20,000 / agency 60,000 · `ai_usage.cost_usd` 의 **오늘(KST)** 합 × `fxToKrw`). 영상 원가도 `ai_usage(purpose video_clip|tts|vision)` 로 들어가므로 **같은 함수가 이미 센다.**
⇒ `lib/video/cost.ts` 는 **새 캡을 만들지 않는다.** `checkAiCostCap` 을 부르고 **판정만 영상용으로 두 단계**로 나눈다:
- **소프트 = 플랜 일일 상한 초과** → **막지 않는다**(고객은 이미 코인을 냈다) · **운영 알림 1건/일**(`ai_cost_soft` · risk normal) · 감사에 `usedKrw/capKrw` 기록.
- **하드 = 플랜 일일 상한 × 3 초과** → piece `failed` + **코인 환급** + 고객 알림(«오늘은 여기까지예요 · 내일 이어서 만들어 드려요») + 운영 알림 `risk high`.
- **전역 월 상한 ₩1,400,000** → 하드(전 테넌트 생성 정지) + 운영 알림 `risk high`. 이것만 새로 잰다(`ai_usage` 월 합 · 테넌트 무관).
- 🔴 **환율이 없으면 못 재는 것이므로 막지 않는다**(`fxMissing:true` 를 응답·감사에 · 기존 규칙 그대로).
- 🔴 **`FX_USD_KRW` 기본값을 코드에 박지 마라**(`lib/revenue/common.ts fxToKrw` 헤더: «환율을 대충 1,300 으로 박지 않는다 — 그 숫자가 화면의 오늘 번 돈이 된다»). 환율은 env 등록이 메인 몫.
- 포함 코인 × 단가로 임계를 유도하던 v5.4 안은 **폐기**(같은 일을 하는 문이 둘이 된다 · `PLAN_CAP_KRW` 한 곳이 정본).
- `feature_flags.video=false` kill switch 는 그대로(운영센터 AI 메뉴).

**(1-b) [v5.6 · C 라이브 실측] 하드 판정에서 «이번 편 원가»를 빼라 — «한 편은 언제나 만들 수 있다».**
실측(FX 1400): graphic/60s/omni/9컷 = **₩8,989** · trial 하드 = `PLAN_CAP_KRW.trial 3,000 × 3` = **₩9,000** → 그날 LLM 으로 ₩697만 써도 **60초 1편도 못 만든다**(starter 는 2편째부터). 고객은 28코인(₩14,000)을 이미 냈는데 «내일 오세요»를 본다 — v5.4 를 폐기하며 막으려던 사고가 플랜만 바꿔 남았다.
```
전: usedKrw + addKrw > hardKrw   → 1편 원가 ≥ 하드인 플랜에서 첫 편부터 차단
후: usedKrw          > hardKrw   → 한 편은 언제나 통과 · 폭주는 다음 편부터 즉시 차단
```
- 근거: 하드는 **폭주·남용 방어**지 «편당 원가 심사»가 아니다. 초과분은 1편치(≤₩9,000)로 유한하고, **그 1편도 고객이 코인으로 값을 치렀다**(60초 = 28코인 ₩14,000 vs 원가 ₩8,989 — 편당 흑자다. 캡의 목적은 마진 보호가 아니라 **버그 루프** 방어).
- **소프트는 그대로 `used + add`**(어차피 안 막고 운영 알림만 빨라진다).
- 전역 월 ₩1,400,000 하드는 그대로(`used` 기준).

**(2) 토킹 포맷의 still 컷 — 만든다(스킵 금지).** 지금 «구간이 비는» 것은 설계 축소다(CLAUDE §8). B-1 조사 F 의 `CHAIN_IMAGE $0.04/장` 경로로 **정지 이미지 컷 + kenburns** 를 붙인다(`scenes.ts` 의 `imageKey` 자리 · 러너는 이미 `imageKey`+`motion:"kenburns"` 를 받는다 · §2.1 payload 무변경).
**(3) BGM 시드 스크립트 — R5 안에 만든다.** `scripts/seed-bgm.mjs`(FreePD 카탈로그 → R2 `autocreate/bgm/` · 멱등 · 라이선스 메타 동봉). `BGM_LICENSE_VERIFIED=1` 은 메인이 env 에 등록(시드 확인 뒤). 그 전까지는 무음이 정직 경로.

### 1.5 스위퍼(새 함수 0 · `cron-tick-5m` 스텝 `video.sweep`)
```
대상: pieces kind='video' AND status='generating' AND updated_at < now() - interval '20 minutes'
판정: meta.chainStage 있음 → 이어달리기 재디스패치(상한 3 안) · 없음 → 처음부터 · 상한 초과 → failed(«제작이 멈춰서 멈춤 처리했어요(마지막 단계: …)») + 환급
잠금 먼저(§1.4 멱등) · 회차 상한 5건 · stage 'render' 는 runner_jobs 가 살아 있으면 건드리지 않는다(reap 는 B2 reapStaleJobs 몫)
```

### 1.6 비용 게이트 `lib/video/cost.ts`(AM `video-cost.ts` 이식 · 표는 AC 것)
```
estimateVideoCostUsd(format, seconds, providerKey) → number          // registry costPer5sUsd × 컷 수 + TTS + 심사 상수
checkVideoBudget(tid, addUsd) → { allowed, reason?:"killed"|"tenant_cap"|"global_cap"|"lookup_failed", tenantUsed, globalUsed, tenantCap:30, globalCap:300 }
```
- 합계 원천 = `ai_usage.cost_usd WHERE purpose IN ('video_clip','tts','video_judge')` 당월(KST) · kill switch = `feature_flags(key='video', enabled=false)`(테넌트 또는 전역 NULL). 조회 실패 = **차단**.
- env `VIDEO_TENANT_MONTHLY_CAP_USD`(기본 30) · `VIDEO_GLOBAL_MONTHLY_CAP_USD`(기본 300).

### 1.7 `lib/ai.ts` 옵션 복원(B · 소)
`callGemini({ googleSearch?: boolean })` — AM 원본 그대로: `tools:[{google_search:{}}]` · jsonMode 와 병용 불가(텍스트 모드 후 `parseJsonLoose`) · `sources:[{url,title}]` 반환. 다른 호출부 무영향.

### 1.8 고지(B · `lib/disclosure.ts` 소비처 추가 · 문구 무변경)
```
videoBadgeText()            → DISCLOSURE_TEXT.videoBadge  («광고 포함 · 파트너스 수수료»)
videoDescriptionFirstLine(provider) → 공식 문구 전문(첫 줄) · 유튜브 설명란·릴스 캡션·쓰레드 첫 줄 공통
checkVideoDisclosure(payload, meta) → { ok, detail? }   // 제휴면 badge 트랙 + 시작 3초 자막 + 설명란 첫 줄 셋 다 있어야 통과 · approve·publish 직전 재검사
```

### 1.9 영상 계정 변주 게이트 `lib/video/fingerprint.ts`(신규 · 순수)
- 같은 테넌트 **다른 계정**의 최근 14일 영상(`piece_assets kind 'thumb'` + 컷 대표 프레임)과 pHash(64bit) 해밍 거리 ≤ 10 이면 유사 → 팔레트·훅 바꿔 컷 1회 재생성(P1 취급) · 재실패는 `gate_report.similarity` 실패로 `in_review`. 텍스트 유사도(`similarity.ts` `CROSS_ACCOUNT_SIMILARITY`)는 대본에 그대로 적용.

### 1.10 코인(B · `lib/coin-table.ts` 무변경 · `lib/coin-ledger.ts` 소비처)
- `videoCoinItem(seconds)` 를 `coin-table.ts` 에 추가(AM 원본 함수 이식 · 값 표는 이미 있음). `coinsPerWeek(rules)` 는 `kind 'shorts'` 규칙이면 `video_60`(기본 60초) 단가로.

### 1.11 소재(B · `lib/topics.ts` 소)
- 후보 생성 프롬프트에 영상 채널 힌트(`youtube_shorts`·`naver_clip`·`reels`)를 허용(테넌트 `settings.kinds` 에 video 있을 때) · `factors.structureTemplateId?`(레퍼런스 구조 · `shorts_templates`) · 검색량은 그대로 키워드툴.
- `POST /api/topics-reference { url }` → Gemini 가 URL 을 훅·서사·스타일로 분해 → `shorts_templates` 1행 → `{ ok:true, template:{ id, name, structure:[string], hook, style } }`(AM `shorts-reference.ts` 이식 · 코인 0 · 하루 3회).

### 1.12 DDL(B · 추가형 · `drizzle/0007-r5-video.sql` + `db/schema.ts` append)
```
shorts_templates (id, tenant_id NULL=내장, name, source_url, structure jsonb, hook_type, style jsonb, created_at)
posts.status  — 어휘 추가만(uploaded_private · 칸 폭 varchar(20) 확인 · AC-21)
piece_assets.kind — 'clip'|'audio'|'srt' 사용(varchar(12) 안) · 컬럼 추가 0
```

---

## 2. B2 — 러너 렌더 · 출구 (`lib/runner-jobs.ts` kind 추가 · `runner/channels/render-video.mjs` · `lib/publish/{youtube,instagram,threads}.ts` · `lib/video/render-queue.ts`)

### 2.1 잡 kind `render.video`(우선순위 70) · payload 정본
```
RunnerRenderPayload = {
  pieceId, tenantId,
  out: { w:1080, h:1920, fps:30, maxSeconds:VideoSeconds, crf:20 },
  scenes: [{ idx, startMs, endMs, clipKey?:string, imageKey?:string, motion?:"kenburns"|"none", captionIdx:[number] }],   // clipKey|imageKey 중 하나
  captions: { preset:"keyword_center"|"talking_big"|"clip_top", phrases:[{ idx, text, startMs, endMs, keyword?:string }], srtKey:string },
  audio: { narration:[{ key, startMs }], bgm:{ key, gainDb:-18 }|null, sfx:[]|null, loudnorm:{ I:-16, TP:-1.5, LRA:11 } },
  overlay: { badge:{ text:string, corner:"tr" }|null, safeZone:{ top:220, bottom:300 }, endcard:{ text:string, url?:string }|null },
  disclosureCaption: { text:string, untilMs:3000 }|null,                    // 시작 3초 자막(§16B)
  upload: { putUrl:string, key:string, posterPutUrl:string, posterKey:string }   // R2 presigned PUT(6MB 본문 우회)
}
```
- 러너는 payload 만 보고 굽는다(서버에 다시 묻지 않는다). `clipKey`/`imageKey`/`audio.*.key` 는 서버가 **presigned GET URL 로 치환해 내려준다**(러너에 R2 자격 0).
- `RunnerReportOk += { render:{ key, posterKey, durationMs, bytes, frameCount, ffmpegVersion } }` · 서버는 **R2 HEAD 로 실존 확인 후에만** 성공(러너 주장 불신) · 실패 report `{ errorKind:"ffmpeg_missing"|"font_missing"|"clip_fetch"|"encode"|"upload", detail, shotKey? }`.
- `claim` 시 `render.video` 는 회당 1건 타임박스 · 발행 잡이 있으면 발행 먼저(우선순위 표).

### 2.2 러너 `runner/channels/render-video.mjs`(AM `content-runner-core.mjs` §render **파일 분리 이식** · `core.mjs` 무접촉 · AC-18 정적 import 목록에 등록)
- 크롬: CDP attach → playwright launch(헤드리스) · 오버레이 페이지(`buildRenderPage` 이식 — 자막·키워드·배지·엔드카드 레이어) 프레임 캡처(30fps) · 클립은 ffmpeg 입력으로 직접(재인코딩 최소).
- ffmpeg 사다리 `FFMPEG_CANDIDATES`(env `FFMPEG_PATH` > PATH > winget shim > 표준 경로 > 리포 동봉 `ffmpeg-static`) · 없으면 잡을 **claim 하지 않고** heartbeat 에 `caps.ffmpeg:false` 보고(서버가 화면 «ffmpeg 없음» 칩 · 침묵 금지).
- 필터: concat → 자막 번인(폰트 = 러너 동봉 Pretendard) → `amix=inputs=N:normalize=0`(**N=0 분기 방어 · AM #907**) → `loudnorm` → libx264 CRF 20 · 1080×1920 · yuv420p · faststart. 포스터 = 가장 선명한 프레임 1장(`pickSharpestFrame` 이식).
- 업로드 = payload.upload presigned PUT · 성공 report 구조화.
- `runner/install.md` 에 ffmpeg 설치(winget) + `FFMPEG_PATH` + 폰트 안내 · `run.bat` 은 무변경.

### 2.3 발행 어댑터
```
publish/youtube.ts   publishYoutubeShorts(piece, account) — Data API v3 videos.insert(resumable · 3단계) ·
                     snippet:{ title, description(첫 줄 고지), tags, categoryId:"22" } · status:{ privacyStatus:"private"(심사 전 고정 · env YOUTUBE_PUBLIC_ALLOWED=1 이면 "public"),
                     selfDeclaredMadeForKids:false, containsSyntheticMedia:true } · 쿼터 회로(일 1,600u × N · env YOUTUBE_DAILY_INSERT_CAP 기본 5 · 초과 → retriable 내일) ·
                     성공 → posts(status 'uploaded_private' | 'published', channel_ref=videoId, external_url) · 실패 분류 quotaExceeded|forbidden(스코프)|uploadLimitExceeded|invalid
publish/instagram.ts publishReels(piece, account) — AM publish-instagram REELS 이식: 컨테이너 {media_type:"REELS", video_url(R2 공개 URL), caption(첫 줄 고지·#광고)} → status_code 폴링(3×4s) → media_publish · IN_PROGRESS 면 { ok:false, reason:"video_processing", retriable:true } + creation_id 영속(멱등 · 중복 게시 0)
publish/threads.ts   publishThreadsVideo(piece, account) — media_type:"VIDEO" · 컨테이너 30초 대기 규칙 = 같은 retriable 관례
publish.naver_clip   러너 잡 kind 예약 + 러너 스텁: claim 하면 즉시 { ok:false, errorKind:"not_supported_yet" } → piece awaiting_manual + 알림 «클립은 앱에서 올려 주세요»(딥링크 문구) — 조용한 0건 금지
```
- `finalizePublish` 는 R2 함수 그대로 · `uploaded_private` 는 `status` 만 다르고 posts 1행·멱등 동일.
- 발행 직전 재검사 = `checkVideoDisclosure`(B 함수 import) + `publish/gate.ts` 기존 게이트.

### 2.3b [v5.3 · B2 구멍 → 메인 결정] 유튜브 업로드는 **API(서버)** — `publish.youtube_shorts` kind 는 어휘 예약만
- 이유: OAuth 토큰을 러너 PC 에 내려보내면 **자격 평문 표면**이 하나 늘고(§16 · 러너 claim 의 계정 쿠키와 다른 급), 관리형 러너 팜(Phase 4)에선 더 위험. 서버가 R2 → YouTube resumable 로 **스트리밍**(함수 6MB 한도는 요청 본문에만 적용 · outbound 스트림 무관) — `netlify/functions/publish-video-background.ts`(15분) 에서. 러너는 mp4 를 R2 에 올리고 끝.
- 따라서 `publishJobKindOf(youtube_shorts) = null`(B2 구현 그대로) · `publish.youtube_shorts` 는 Phase 4 관리형 팜에서 «러너 직접 업로드»를 열 때 쓰는 예약 어휘.
- 같은 이유로 릴스·쓰레드도 서버 API. 러너가 올리는 건 `publish.naver_clip` 하나(PC 업로드 · 스텁).
- `lib/r2.ts` 에 B2 가 `r2Head·r2PresignPut·r2PresignGet` **추가만**(기존 무변경) — §4 담당표에 «B2 추가 가능(append-only)» 등재. `runner_devices.caps` DDL 은 B2 적용 · **schema.ts 선언은 B-1 이 `*R5` 블록에**(4.4).

### 2.4 러너 큐 API(기존 `/api/runner-queue` · 변경 = kinds 에 `render.video` 허용 · heartbeat body `caps:{ ffmpeg:boolean, ffmpegVersion? }`)

---

## 3. A — 화면 (`public/app/**` · 12 컴포넌트 안 · 헌장 §13.0)

| 화면 | 패턴 | 이번 |
|---|---|---|
| `director.html` | 송금 확인 | 대상 행에 **«영상» 행**(채널 마크 · «쇼츠 60초 · 그래픽 스토리» · 코인 28) · 손보기 시트에 영상 섹션: 포맷 세그먼트 3 · 길이 칩 15/30/60 · 보이스 선택(타입캐스트 목록·미리듣기 없음 R5) · 변주 미리보기(계정별 훅·팔레트 칩 · 읽기 전용) · 코인 재계산 |
| `pieces.html` | 만드는 중 스텝 바 | 영상 스텝 6(대본→목소리→장면→합성→검사→완료 · `meta.stage` 매핑) · «합성» 단계는 «내 PC 러너가 굽는 중»(러너 오프라인이면 «러너를 켜 주세요» 칩) |
| `piece.html` | 검수 | `<video>` 플레이어(포스터) · 자막 SRT 다운로드 · 배지·시작 자막 미리보기(제휴 시) · 설명란 편집(제목·설명·태그 · 첫 줄 고지는 잠금) · 심사 축 목록(gate_report.judge.axes · label 만) · «이대로 예약» / «다시 만들기(1회)» / «버리기» |
| `schedule.html` | 소비 캘린더 | `kind:"shorts"` 규칙 행(주 N회 · 채널 유튜브/클립/릴스) · 슬롯 점 상태색 동일 · 코인/주 미리보기에 video_60 단가 |
| `posts.html`(발행함) | 내 소비 | 상태 칩 **«비공개 업로드됨 · 공개 전환 필요»**(`uploaded_private`) + 유튜브 스튜디오 링크 · 릴스 «처리 중» |
| `accounts.html` | 계좌 연결 | 유튜브 연결 카드에 «심사 전엔 비공개로 올라가요» 한 문장 |
| `runner.html`(러너) | 상태 | «ffmpeg 없음» 경고 칩 + 설치 안내 시트(`caps.ffmpeg`) |
| `create.html` | 추천 카드 | «레퍼런스로 구조 배우기»(URL 한 칸 → `topics-reference`) — 결과는 소재 카드에 «구조: …» 부제 |

- API 폴링·상태 어휘는 §0.2 글자 그대로 · 새 컴포넌트 0 · 영상 미리보기는 `<video controls playsinline>` 표준 태그.

---

## 4. 담당 분리 (파일 겹침 0 목표)
| | 파일 |
|---|---|
| **B** | `lib/video/{script,scenes,tts,tts-typecast,captions,judge,cost,fingerprint,gen,providers/*}.ts` · `netlify/functions/{generate-video-background,topics-reference}.ts`(topics.ts 에 경로 추가 가능) · `lib/director.ts`(video 분기) · `lib/writing-contracts.ts`(shorts 4행) · `lib/coin-table.ts`(videoCoinItem) · `lib/disclosure.ts`(영상 소비처) · `lib/ai.ts`(googleSearch) · `lib/topics.ts`(영상 힌트·structureTemplateId) · `lib/cron/video-sweep.ts`(+tick 등록 1줄) · `db/schema.ts`(append) · `drizzle/0007-r5-video.sql` · R2 BGM 시드 스크립트 `scripts/seed-bgm.mjs` |
| **B2** | `lib/runner-jobs.ts`(kind 3 · payload 타입 · heartbeat caps) · `lib/video/render-queue.ts`(`enqueueRender`·`finalizeRender` — §5) · `runner/channels/render-video.mjs` · `runner/channels/naver-clip.mjs`(스텁) · `runner/install.md` · `lib/publish/{youtube,instagram,threads}.ts` · `lib/publish/index.ts`(채널→함수 배선) · `lib/cron/publisher.ts`(영상 채널 디스패치 1줄) |
| **A** | `public/app/{director,pieces,piece,schedule,posts,accounts,runner,create}.html` · `_tpl.txt` · `build-pages.mjs` · `mock.js`(§0.2 어휘) |
| **C** | 검증·수리 |
- 🔴 `lib/runner-jobs.ts` 는 B2 소유 · B 는 `enqueueRender` 만 import(AC-17 순환 0: `lib/video/render-queue.ts` → `runner-jobs.ts` 방향 한쪽 · `judge.ts` 는 render-queue 를 보지 않는다 — `finalizeRender` 가 `judge` 를 **동적이 아닌 정적** import(AC-18)).
- 🔴 `db/schema.ts` 는 B 만 · B2 DDL 은 SQL 만(있으면).
- 🔴 `lib/director.ts`·`lib/disclosure.ts` 는 B 소유 · B2 무접촉(문구는 import).

## 5. 경계 — B↔B2 함수 한 벌(글자 그대로)
```
// lib/video/render-queue.ts (B2 소유 · B 가 부른다)
enqueueRender(pieceId:number, payload:RenderPayload) → Promise<{ jobId:number, created:boolean }>
  // RenderPayload = RunnerRenderPayload 에서 upload·presigned 치환 «전» 모양(키는 r2_key) — B2 가 claim 시 presign 한다. 멱등(piece 당 열린 render.video 1개).
finalizeRender(pieceId:number, report:{ key, posterKey, durationMs, bytes, frameCount }) → Promise<{ ok:boolean, next:"judging"|"requeued"|"in_review"|"failed", retry:number }>
  // B2 reportJob 이 R2 HEAD 확인 뒤 부른다 → 안에서 B 의 judgeVideo(pieceId) 를 부르고 piece·slot·piece_assets(video/thumb) 를 쓴다. piece 상태를 쓰는 유일한 자리(렌더 이후).
// lib/video/judge.ts (B 소유 · B2 가 finalizeRender 안에서 부른다)
judgeVideo(pieceId:number) → Promise<{ grade:JudgeGrade, pass:boolean, axes:[{ key, label, pass, detail? }], repaired:boolean }>
```
- 상태를 쓰는 자리 분담(R2 §10 «두 곳에서 상태를 쓰지 않는다»): 생성 단계(script~clips) = B 배경 함수 · render 이후 = `finalizeRender` 한 벌 · 발행 = `finalizePublish`(R2).
- B 는 30분 안에 `RenderPayload`·`judgeVideo` 시그니처를 B2·A 에게 알린다 · B2 는 `enqueueRender`·`finalizeRender` 를.

## 6. C 시나리오(R5 검증 · 로컬 스모크 + 라이브 실증 분리)
1. 테넌트 `settings.kinds=["text","video"]` · 유튜브 계정 OAuth(키 있으면 실왕복 · 없으면 provider_not_configured 정직) · 코인 60 → 소재 뽑기(영상 힌트 후보 존재) → 레퍼런스 URL 1개 → `shorts_templates` 1행 → 디렉터 제안(글 1 + 쇼츠 1 · 변주 칩 · 코인 1+6+28) → 확정(코인 1회 · 재확정 0 · 달러 캡 선검사 통과) → 배경 생성: `meta.chainStage` 가 script→tts→clips 로 **컷마다** 갱신 · `piece_assets(clip)` 컷마다 즉시 · 11분 넘기면 `chainResume.count` 1 · 재디스패치에서 **이미 만든 컷 재생성 0**(ai_usage 행 수로 증명) → `render.video` 잡 1개(멱등) → 러너(ffmpeg 있음)가 굽고 report → R2 HEAD 200 → 심사(등급·축 목록) → `in_review` · 플레이어 재생 · SRT 다운로드 · 배지·시작 자막·설명란 첫 줄 고지(제휴 소재) → 승인 → 예약 → 발행 → 유튜브 `uploaded_private` + 화면 «비공개 업로드됨» + 유튜브 스튜디오 링크(키 있을 때) / 릴스 `video_processing` retriable → 재시도 멱등(creation_id 재사용).
2. 실패 경로: `TYPECAST_API_KEY` 없음 → Gemini 폴백 · `meta.tts.provider:"gemini"` · 자막 균등 분할 / 러너 ffmpeg 없음 → `caps.ffmpeg:false` · 잡 미claim · 화면 칩 / 달러 캡 초과 → `step:"budget"` 코인 무접촉 / 배경 함수 20분 침묵 → 스위퍼 재디스패치 · 3회 상한 → failed+환급+알림 / P0 정책 위반 → failed+환급 / 잠금 20분 안 중복 호출 → 두 번째 즉시 반환(ai_usage 중복 0).
3. 다계정: 같은 brief 유튜브 계정 2 → 훅·팔레트·보이스 다름 · 프레임 pHash 거리 > 10 · 대본 유사도 < 0.6.
4. 규칙 `kind shorts` 주 2회 → 슬롯 점 · coinsPerWeek = 2×28 · 슬롯 없는 자동 생성 시도 → `piece_slotless_blocked` 감사(AC-2).
5. 회귀: 글 파이프(R1~R3) 무변경 · 타 테넌트 404 · 자격 평문 0 · KST.
6. 증거 어휘: 라이브 URL·videoId·row id·스샷 없이 «실증» 금지. 유튜브 실업로드는 **테스트 채널에 비공개**로만.

---

## 7. 열린 결정 — 메인 확정(2026-09-14)
1. `uploaded_private` 는 **`posts.status` 에만**(piece 어휘 불변 · AC-21). 화면은 posts 로 «비공개 업로드됨 · 공개 전환 필요».
2. 내 PC 러너가 꺼져 있으면 렌더도 **`awaiting_runner`(30분 · R2 규칙 그대로)** — 침묵 금지 · 홈 «해야 할 일»에.
3. 릴스 90초는 **R5 제외**(`seconds` 상한 60 · 90 은 Phase 5). 릴스 출구는 60초 이하로 발행.
4. §-1 의 «R4 지도 R5 항목(ZIP·공유 카드·추천인·코인 이전) → R6» **승인** — 로드맵 Phase 3 영상이 먼저.
> 발부: 2026-09-14 R4 배포(#3 `0f3ec44`) 직후. 담당 = B-1(`autocreate-b-8a` · `../AutoCreate-B1` · `feature/p1r5-back`) · B2(`feature/p1r5-back2`) · A(`feature/p1r5-front`) · 검수 «다시 만들기» 잔여 1회는 버튼 글자 아닌 비활성 전환(A 시안). 사장님 액션 선결 = TYPECAST_API_KEY · 유튜브 앱 심사 착수(심사 전엔 비공개 업로드로 전본 개발 가능).
