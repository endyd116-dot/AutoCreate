# P1R1-A 트리거 — 계정 연결·소재·디렉터·검수·편성 규칙의 **화면** (A)

> 발부 2026-09-14 · 베이스 = 지금 main(`2cd2101`) · 워크트리 `../AutoCreate-A` · 브랜치 `feature/p1-front`
> 🔴 B 와 **동시에** 돈다. B 의 API 가 아직 없어도 계약서의 모양대로 **mock 응답(`public/js/mock.js` · `?mock=1` 일 때만)**으로 화면을 끝까지 만든다. 키 이름은 계약서 글자 그대로 — 이름이 어긋나면 메인이 머지에서 봉합하지만, 어긋나지 않는 것이 규칙이다.

```
P1R1-A다. 베이스 = 지금 main(2cd2101). 워크트리 ../AutoCreate-A · 브랜치 feature/p1-front(이미 체크아웃됨).
읽을 것(순서대로): docs/rules/PARALLEL_GUIDE.md · CLAUDE.md §3(UX 표준)·§5·§6 · docs/rules/PITFALLS.md §0 ·
        docs/active/2026-09-14-P1R1-contract.md(🔴 키 이름 정본 · §9 화면 자리) ·
        docs/DESIGN.md §13.0(헌장)·§13.0b(토스 패턴 사전)·§13.3b(반응형)·§5.5(디렉터 화면)·§5B.8(편성표 화면) ·
        docs/screens-v3.html(시안 정본 — 브라우저로 열어 픽셀을 본다) ·
        public/css/ac.css(토큰·12 컴포넌트) · public/js/ui.js(UI.api·sheet·toast·done·toggle·countUp·mark) · public/app/_tpl.txt + scripts/build-pages.mjs(화면 생성 방식) · public/app/home.html(실제 화면 예)
[자율주행 — 완전 자율]
- 파일 R/W/Edit, git(add·commit·checkout·merge·rebase·fetch·restore), bash·PowerShell, npm run, npx, curl — 전부 묻지 말고 자율 실행. 도구 허락 구하지 말 것.
- 커밋 자유. push 불필요·차단됨 — 메인이 feature/p1-front 를 직접 머지·배포함. 끝나면 [브랜치·커밋해시·변경요약]만 보고.
- 오직 ①진짜 애매한 화면 해석 ②계약 이탈이 불가피할 때 ③비가역 작업일 때만 질문. 막히면 30분 안에 보고.
[🔴 전본 규칙 — CLAUDE §8] 계약서 §-1 «설계 대비 범위 지도»의 R1 열이 네 범위다. DESIGN §5.5·§5B.8·§7.1·§13.0b 의 화면 항목을 하나도 빼지 않는다. «간단히·이번엔 생략» 금지. 못 만들면 사유 보고.
[진행률 — 의무] 시작 0% · 섹션마다 "📊 진행률 X%/100 — 방금: … / 다음: …" 최소 10회.
[보고 대상] 메인 세션(autocreate-ca).
```

■ 규칙(어기면 반려 — DESIGN §13.0 헌장 그대로)
  화면당 색 3(회색·잉크·초록 — 컬러는 채널 마크만) · 카드 대신 그룹 섹션(`.group`) · 첫 화면 큰 숫자 1 · Primary 버튼 1 · 옵션은 바텀시트(`UI.sheet`) · 설명 한 문장 · 이모지 아이콘 금지 · 팝업 모달 금지 · 시스템 용어(«테넌트» «piece» «슬롯») 화면에 0 · 새 컴포넌트 금지(12개 안에서 — 꼭 필요하면 보고서에 사유).
  말: «편성표» «내 계정» «봐주세요» «디렉터에게» «이대로 발행 예약». 버튼은 동작을 말한다(«만들기» 후 토스트 «만들기 시작했어요»).
  반응형: 400px 폰 + 1280px 데스크톱 둘 다 확인(스크린샷 2장씩 `_shots/`에 남긴다 — 커밋 제외).

■ 계약 (정본 = docs/active/2026-09-14-P1R1-contract.md §1~§7 — 그대로)

■ A0 (0→8%) mock 층 + 빌드 파이프
  `public/js/mock.js`: `location.search` 에 `mock=1` 이면 `UI.api` 를 감싸 계약서 모양의 가짜 응답(계정 2·소재 6·brief 1·piece 2(in_review)·rules 2·slots 14일치·coins 30) 반환. 실 API 가 오면 자동으로 안 쓰인다(운영 코드 무접촉 — 스크립트 태그는 `_tpl.txt` 에서 `?v=` 뒤에 붙이되 mock 파라미터 없으면 즉시 return).
  `_tpl.txt` 에 화면 블록을 추가하고 `node scripts/build-pages.mjs` 로 생성 · 생성물 커밋. 뒤로가기 화면 목록(`BACK`)에 새 파일 추가.

■ A1 (8→22%) 계정 연결 — `accounts.html` + `account.html` 실데이터
  토스 «계좌 연결»: 채널 그리드(`.cg` · 연결 수 small) → 탭 → 채널 방식별 시트(계약 §1): session(네이버·티스토리) = 아이디·비밀번호(`autocomplete="off"`)·표시명 / wordpress = 사이트 주소·아이디·앱 비밀번호(+«앱 비밀번호 만드는 법» 한 줄) / OAuth 채널(블로거·쓰레드·인스타·유튜브·릴스·틱톡) = «OO 계정으로 연결» 버튼 → `accounts-oauth-start` → url 이동 · `provider_not_configured` 면 시트 «준비 중이에요 · 준비되면 홈에 알려드릴게요». 성공 → `UI.done("연결했어요")` → 목록 갱신 · `?connected=` 로 돌아오면 토스트.
  연결된 계정 목록(그룹 섹션): 마크 · @핸들 · «오늘 0/2 · 건강 100» · 상태 알약(pending_login=«확인 중»·active=«정상»·suspended=«정지»·disconnected=«끊김»·cooldown=«쉬는 중»·limited=«제한»). 행 탭 → 계정 상세 시트(DESIGN §7.1 필드 전부): 표시명 · 하루 최대(스테퍼) · 글 간격(칩 60/180/360분) · 활동 시간대(시간 칩 다중) · 페르소나(세그먼트 목록 + «새로 만들기» → 페르소나 시트: 지역·가족·직업·집·쓰는 브랜드·말투·관심사·금지어·서명 — «넓게 적어도 돼요» 안내) · 프록시 주소(선택) · **수익 매체**(쿠팡 파트너스 access/secret · 애드포스트 미디어 id · 애드센스 pub id — 저장 후 boolean 알약만 표시) · «연결 해제» danger(확인 한 번 더).
  한도 초과(step limit) → 시트 «이 요금제에서는 N개까지 · 요금제 보기».
  🔴 비밀번호 필드 값은 요청 후 즉시 비운다 · 화면 어디에도 자격을 되그리지 않는다(hasCreds 만).

■ A2 (22→34%) 소재 — `create.html`
  추천 카드 패턴: 상단 «오늘의 소재 N» + 우측 «새로 뽑기»(하루 3회 · step rate_limit 토스트). 소재 행: 제목(2줄 말줄임) · 부제 «검색 32,000 · 경쟁 낮음»(factors 에 없는 값은 안 그린다) · 우측 «디렉터에게» 버튼(`.btn sm primary`) · 좌측 스와이프 또는 ⋯ → «넘김». «디렉터에게» → `/app/director.html?topicId=`.
  빈 상태: «소재를 뽑아 볼까요?» + «새로 뽑기». 계정 0개면 빈 상태가 «먼저 계정을 연결해 주세요 → 계정 연결».

■ A3 (34→50%) 디렉터 — `director.html?topicId=`
  송금 확인 패턴(시안 4번 화면 그대로): 질문 «이 소재로 / 에어프라이어 청소법, 3곳에 만들까요?» · 대상 행(마크·@계정·«경험담 · 사진 8장 · 제휴 링크»·우측 «내일 07:30») · kv(쓰는 코인·남는 코인·검수 «3일 전 보여드려요») · 하단 «손보기 / 만들기».
  손보기 시트(DESIGN §5.5 항목 전부): piece 별 섹션 — 채널 토글(drop) · 계정 세그먼트 · 감성 칩(emotionKey) · 구성 칩(경험담·정보·목록·비교·문답·가이드·카드뉴스) · 이미지 수 스테퍼(0~10) + 스타일 세그먼트(사진/일러스트/인포그래픽) · 제휴(토글 + 상품 검색어 + 자리 세그먼트 중간/끝/둘 다) · 시각(시간 칩 07:30/12:00/21:00 + «내일/모레» 세그먼트 · `datetime-local` 금지) → «적용» → 코인 합 재계산(1 + images.count). 제안 카드에 `reasons` 3줄과 `schedule.slotReason` 을 그대로 보인다.
  «만들기» → confirm → 202 → `UI.done("만들기 시작했어요")` → `/app/pieces.html`. coin_short → 시트 «코인이 N개 부족해요 · 충전하기(/app/coins.html)».

■ A4 (50→70%) 만드는 중 · 검수 — `pieces.html` + `piece.html?id=`
  pieces.html: 세그먼트 «만드는 중 / 봐주세요 / 예약됨 / 발행됨». 만드는 중 = 스텝 바(글 쓰는 중 → 사진 만드는 중 → 검사 중 · 상태 문자열로 추정) + 5초 폴링(`generating` 이 없어지면 폴링 중단). 봐주세요 = 행(마크·제목·@계정·«봐주세요» warn 알약) → piece.html.
  piece.html(검수): 상단 채널 마크+@계정+예약 시각 · **채널 렌더 미리보기**(bodyHtml 을 그대로 — 네이버 계약의 인용구/구분선/체크리스트/사진+캡션이 실제 네이버 글처럼 보이게 `.preview.naver_blog` / `.preview.tistory` 스코프 CSS 30줄 — 새 컴포넌트가 아니라 콘텐츠 스타일) · 게이트 체크 목록(`gate.checks` → ✓/✗ 행 · rewritten 이면 «한 번 고쳐 썼어요») · 제목/본문 인라인 수정(contenteditable · 저장 시 pieces-update · 게이트 재검사 결과 토스트) · 하단 «이대로 발행 예약»(primary) + «수정 저장»(secondary · 수정 중일 때만) · 우상단 ⋯ → 시트 «다시 만들기(1회) / 버리기».
  고지 블록(`disclosure`)은 미리보기에서 **눈에 띄게**(첫 블록 · 회색 박스 · 삭제 불가 안내) — 정책(§16B) 은 화면에서도 지킨다.
  approve 성공 → `UI.done("발행을 예약했어요")` → pieces.html «예약됨».

■ A5 (70→86%) 편성표 — `schedule.html`
  규칙 0개: 빈 상태 → «자동 편성 켜기» → 3스텝 시트(한 화면 한 질문):
    ① «얼마나 자주 올릴까요?» 채널 행(연결된 채널만) 마다 «주 N회» 스테퍼(0~7 · 0 = 안 함)
    ② «어느 계정으로?» 채널마다 «자동(건강한 계정 순) / 고르기» 세그먼트
    ③ «3일 전에 만들어 두고, 조용히 두면 그대로 나가요» — 검수 방식 세그먼트(조용하면 발행 / 반드시 확인) + produceLeadDays 칩(1·3·5)
    끝: «이 편성이면 주 N코인 · 지금 잔여 M코인» → «켜기» → rules-save + rules-settings(autoSchedule true) → `UI.done` → 달력.
  규칙 있음: 상단 «9월 · 이번 주 12건 · 주 84코인» · 달력(`.cal` · 날짜 아래 상태 점 ≤3: published=초록·in_review=주황·planned/scheduled=회색) · 날짜 탭 → 그날 목록(마크·제목 or «소재 정하는 중»·@계정·시각·알약) · 슬롯 행 탭 → 시트(«건너뛰기» + 정보) · 우상단 톱니 → 규칙 시트 재진입(현재 값 채워서).
  홈 `자동 편성` 토글은 이제 rules 가 있으면 켜진다(기존 home.html 로직 유지).

■ A6 (86→94%) 홈·내 계정 연결
  home.html: todo `kind:"review"` 링크 → `/app/pieces.html` · «오늘 편성» 행 → `/app/schedule.html`(있음) · 코인 잔액 표기(체험 배너)에 `/api/coins-balance` 대신 me.coins 그대로(호출 최소).
  account.html: «연결한 계정» 실데이터(A1 과 같은 행) · 코인 행 → coins.html(기존).
  create.html 빈 상태 ↔ accounts.html 왕복이 자연스러운지 한 번 걸어본다.

■ 검증 (94→98%)
  - `?mock=1` 로 전 화면 왕복(폰 400·데스크톱 1280 스크린샷 각 1) · 콘솔 에러 0 · 헌장 체크(색 3·숫자 1·Primary 1·그룹 섹션·시스템 용어 0) · 12 컴포넌트 외 신설 0.
  - B 가 로컬에 올라오면(메인이 알려줌) mock 없이 실 API 로 A1→A5 한 번 더.
  - 증거: 스크린샷 경로. «실증»은 라이브에서만.

■ 보고 (98→100%)
  [브랜치 feature/p1-front · 커밋해시 · 변경 파일 · 스크린샷 경로 · 계약과 다르게 가정한 것(원칙 0) · 새 컴포넌트(원칙 0 · 있으면 사유) · 🔴 **설계 대비 못 만든 것과 사유(없으면 «0»)**]
