# R9+R10-C 상태 — «다른 모양의 자» (C · 2026-09-16)

> 폴더 `AutoCreate-C` · 브랜치 `verify/r9` · 베이스 main `2088610`(R8 배포 82%) · 트리거 `docs/active/2026-09-16-R9R10-C-trigger.md`
> 🔴 push 금지 · 실호출 0 · 라이브 읽기만(이 라운드 지금까지 라이브 접속 0).

## 0. 지금 어디까지 (📊 진행률 25%/100)

| 칸 | 상태 | 한 줄 |
|---|---|---|
| 자 짓기 · 자 찌르기 | **끝** | 자 다섯 전부 «옛 판/옛 식»에 빨강이 난다(⓪ 축) — 통과가 «안 쟀다»와 구별된다 |
| C1 서식·번짐 | **열림(제품 미착)** | 옛 러너를 같은 격자에 태움: 평범한 글 번짐 0 · 서식 글은 «부분 서식 0 = 그렸다는 증명 없음» |
| C2 레퍼런스 누출 | **열림(제품 미착)** | 영상 소독기는 이 격자에 잡힌다(원문 25자 남음 · 최장 121자) — 글 소독기는 이보다 짧아야 한다 |
| C3 코인 등급 | **열림(제품 미착)** | 옛 식은 상한 3 을 넘어 잡힌다 |
| C4 두 방향 | 부분 | 옛 판에서 R9-6 결함 3건 발견(§3) · 주석닫힘 0 · 같은칸두번 0 |
| C5 집계 | 기준선 | audit64 **82%**(닫힘 53·일부 5·열림 7) · safe 47 / live 30 / needs 2 |

B(`autocreate-b-f3`)·B2(`autocreate-b2-71`)·A(`autocreate-a-6f`)는 **2026-09-16 04:40 KST 무렵 시작 · 커밋 0**. 셋 다 이름·이음새를 직접 답했다(§4). 커밋이 오면 §2 자를 그대로 대고 §5 로 판정한다.

## 1. 자 다섯 (전부 `scripts/` · safe 갈래 · 종료코드로 본다)

| 파일 | 무엇을 · 어떻게 «다른 모양»인가 | ⓪ 찌르기 |
|---|---|---|
| `verify-r9-bleed-browser.mjs` | 🔴 **진짜 Chromium + 가짜 스마트에디터**(contenteditable · se-* · 툴바 6종 · 실제 색 팔레트 · «본문 추가»)에 러너 `playOps` 를 그대로 연주 → **문단마다 «계획이 안 시킨 서식이 붙었나»** DOM 대조(AM 의 % 가 아니라 계획 대 실물) · 변이 `--mutate=break\|dirty\|measure`(스크래치 사본에서만) | 가짜 에디터가 정말 번진다(색·밑줄) · 새 칸은 안 물려받는다 · 인용은 안 센다 |
| `verify-r9-blocks-to-ops.mts` | 블록 어휘를 `lib/blocks.ts` 타입 줄에서 읽어 **19종 전종** → op 또는 note(조용한 삭제 0) · 두 경로(블록·bodyHtml 폴백) · 꾸밈 표 전 채널 `boolean\|null` · 쓰레드에 서식 안 밀기 · `formatUnused.field` ⊆ 화면 `FMT_FIELD_SAY` | 옛 판에서 3종 빨강(§3) |
| `verify-r10-ref-leak.mts` | 소독기 이름을 모른 채 `lib/**` 에서 찾아 **가짜 응답 6모양**(금지 키·허용 칸 통짜·중첩·배열·구조 어휘 자리·숫자 칸 문자열) → 🔴 **원문 25자 연속이 저장물 어디에도 없다** + 최장 저장 문자열 < 22자 + 구조 어휘 닫힘 + 저장 자리마다 소독기 경유 + 러너 캡처 finally 삭제·R2 0·로그 URL 0 | 영상 소독기(캡 120~240)가 잡힌다 |
| `verify-r10-capture-slices.mjs` | 높이 축 19개(0·-1·NaN·"abc"·300·경계 1,398·…·12,000·100,000) → 페이지 밖 0 · 구멍 0 · 겹침 10~15% · 2~6장(뷰포트보다 짧으면 1) · 끝에 붙음 · 결정적 · why 필수 | «겹침 0 4등분» 옛 식이 47건 잡힌다 |
| `verify-coin-tier-honesty.mts` | 등급 × 채널 × 포맷 × AI 0~12 → 🔴 **낸 값 ≤ 말한 값** · 0장=1 · 상한 3 · 덜 구우면 덜 받음 · 내 사진·스톡 무관 · 모르는 tier 10종 → 1 · 정산 ≤ 첫 차감(계획×실제 전 조합) · 🔴 **호출부 6곳 전부 tier 동반**(안 넘기면 «1 을 말하고 3 을 뺀다») · piecesByTier 서버 셈 | 옛 식이 상한 3 을 넘는다 |

돌리는 법: `.mjs` 는 `node`, `.mts` 는 `npx --yes tsx` · `| tail` 금지(AC-67) · 전수는 `node scripts/verify-safe-list.mjs --run`.

## 2. 기준선 (main `2088610` · 제품 미착 상태)

```
verify-r9-bleed-browser      exit 1  ⓪ 4/4 ✓ · playOps export 없음(사본 강제 export 로 옛 판 측정) · 평범한 글 11문단 번짐 0 · 서식 글 부분 서식 0(빨강)
verify-r9-blocks-to-ops      exit 1  19종 읽음 · hashtags·adsense 조용히 사라짐 · place 폴백 경로 사라짐 · 꾸밈 표 없음 · <u>/<mark> 벗겨짐 · formatUnused 없음
verify-r10-ref-leak          exit 1  ⓪ 영상 소독기 잡힘(2모양 · 최장 121자) · 글 소독기 없음 · 러너 캡처 없음
verify-r10-capture-slices    exit 1  ⓪ 옛 식 47건 잡힘 · capture-slice.mjs 없음
verify-coin-tier-honesty     exit 1  ⓪ 옛 식 9장수 잡힘 · COIN_TIERS 없음
verify-r8-audit64            82% (65칸 중 닫힘 53 · 일부 5 · 열림 7)  · verify-audit-selftest 0 · verify-dup-keys 0 · verify-safe-list safe 47/live 30/needs 2
```

## 3. 옛 판에서 이미 보인 결함 (B2 에 보냈다 · R9-6 몫)

1. 🔴 `place` 카드가 **bodyHtml 폴백**(검수창에서 고친 글)에서 통째로 사라진다 — `runner/lib/plan.mjs opsFromHtml` 태그 목록에 `aside` 가 없다.
2. 🟠 `hashtags` 블록(items 만)이 블록 정본 경로에서 op 0·note 0 — `case "hashtags": if (text)` 가 items 를 안 본다.
3. 🟠 `adsense` 가 두 경로 모두 op 0·note 0 — «못 냈어요» 한 줄이 없다(AC-9).

## 4. 창끼리 맞춘 이음새 (메인 거치지 않음 · PARALLEL_GUIDE §2.7)

- **B2**: `runner/lib/format-bleed.mjs`(`FORMAT_BLEED_MAX_PCT`·`createFormatState`·`markFormatDirty`·`breakFormatBeforePara(state, freshBlock)`·`measureFormatBleedIn(env)`·`measureFormatBleed`·`bleedVerdict`) · `playOps(page, ctx, plan, files, shotKey, missed, fmt)` **export 약속** · `runner/lib/capture-slice.mjs planCaptureSlices(pageHeight, opts)`·`PHONE_WIDTH=430` · 캡처는 `runner/tmp` 에 쓰고 finally 삭제·R2 0·로그 장수·크기만.
- **B**: `COIN_TIERS`·`COIN_TIER_KEYS`·`coinsForAiImages`·`pieceCoinCost(kind, ai, {tier})`(tier 없으면 1) · `post_simple|standard|premium` · `lib/text-style.ts sanitizeTextStyleForStorage`(자유 문자열 칸 0) · `formatCaps: Record<key, boolean|null>`·`formatCapsOf` · `meta.formatUnused=[{field,label,why}]`(`lib/format-marks.ts formatUnusedOf`) · 계정 간 유사도 `cross_account`·`meta.crossSimilarity{measured,…}`.
- **A**: 검수 = `public/app/_tpl.txt` `=== piece.html` · `#fmtunused` · `FMT_FIELD_SAY` · `?mock=1&fmt=1` · 등급 `accounts-list.tiers[]`·`accounts[].defaultTier`·`pieces[].tier`·`meta.tier`·`meta.coins.line`·`plans[].piecesByTier` · A 포트 4187 / **C 포트 4179**.
- C 가 미리 넘긴 위험: B 에 «tier 안 넘기는 호출부 6곳(content-gen 518·526 · director-auto 38 · director 93 · slots 101·321)» · B2 에 §3 셋.

## 5. 커밋이 오면 (순서)

1. `git log fa1e446..feature/r9-runner|r9-back|r9-front` 로 **폴더·브랜치 확인**(AC-79) → 로컬에서 세 브랜치를 verify/r9 에 **읽기용으로만** 합쳐 본다(임시 브랜치 · 충돌 나면 그 자리 보고).
2. 자 다섯 원판 + 변이 3종(`--mutate=break|dirty|measure`) → **원판 초록 · 변이 빨강**이어야 «통과». 둘 중 하나라도 아니면 «자가 못 본다».
3. C1 브라우저: `piece.html?mock=1&fmt=1` 을 4179 에서 띄워 `#fmtunused` 칩이 **보이나**(display·예외) · 라벨이 사람말인가(AC-87·AC-91).
4. C4: `verify-r8-deadends` 에 새 정의(`formatUnusedOf`·`sanitizeTextStyleForStorage`·`planCaptureSlices`·`coinsForAiImages`·`crossSimilarity`) 호출 줄 추가 · `verify-audit-selftest`·`verify-dup-keys` 재실행 · R8-A 되돌림: 학습 템플릿이 `structure_repeat` 대상에서 빠졌나(`compareToRecent` 호출부에 styleId 필터가 생겼나 grep) · `contractSelfConflicts` 가 `visualMin` 변경에 돌았나.
5. C5: `verify-r8-audit64` 한 줄(분모 변경 시 사유) · `verify-safe-list --run` 종료코드 · 남은 열림 칸마다 «무엇이 빠졌나» 한 줄.

## 6. 밟은/피한 함정
- AC-94: 전수는 safe 갈래만(새 자 다섯은 safe 로 분류됨 · 확인함). AC-95: `.mts` 는 `npx --yes tsx`. AC-67: `| tail` 0.
- 🔴 새 함정 후보: **가짜 에디터의 «번짐 성질» 자체를 먼저 증명하지 않으면** 러너 축 초록이 «에디터가 안 번져서»인지 «러너가 끊어서»인지 구별이 안 된다 — 그래서 ⓪a~d 가 매번 먼저 돈다.
