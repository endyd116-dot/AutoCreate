# R9+R10-C 상태 — «다른 모양의 자» (C · 2026-09-16 · 최종)

> 폴더 `AutoCreate-C` · 브랜치 **`verify/r9`**(main `84e389f` 위 재구성 · 커밋 1개 · 충돌 0) · 트리거 `docs/active/2026-09-16-R9R10-C-trigger.md`
> 🔴 push 0 · 실호출 0 · 라이브 접속 0(이 라운드 전체).

## 0. 최종 한 줄 (📊 100%/100)

| 칸 | 판정 | 근거(파일:줄 · 숫자) |
|---|---|---|
| **C1 서식·번짐** | **닫힘 — 가짜 에디터 범위** | `verify-r9-bleed-browser` 19/19 · 변이 break 4·dirty 3·measure 1 빨강 · 화면 `verify-r9-screen-browser` 10/10 · 블록 `verify-r9-blocks-to-ops` 12/12 |
| **C2 레퍼런스 누출** | **닫힘** | `verify-r10-ref-leak` 8/8(원문 25자 0 · 최장 저장 문자열 **5자** · 문이 하나 `lib/text-style-store.ts:51→56` · 캡처 디스크 0·R2 0·로그 URL 0) · `verify-r10-capture-slices` 7/7 |
| **C3 코인 등급** | **닫힘** | `verify-coin-tier-honesty` 12/12(1,872조합 «낸 값 ≤ 말한 값» · 모르는 tier 10종 → 1 · 정산 507조합 · 호출부 9곳 tier 동반 · DDL 0035) |
| **C4 두 방향** | **닫힘** | deadends 106/0(새 정의 12개 전부 «제품이 부른다») · selftest 0(예외 1칸 출력) · dup-keys 0 · R8-A 되돌림 없음(`applyTextStyleShape` 는 골격에 최대 2블록만 얹음 · `content-approve.ts` 에 styleId 제외 0 · visualMin 변경 0) |
| **C5 집계** | **91%** | audit64 **65칸 중 닫힘 59 · 일부 2 · 열림 4** · `verify-safe-list --run` 59개 중 58 통과 · 빨강 1 = `verify-format-notice.mts`(B2 R9-11 알림 · 진행 중) |

## 1. 이 라운드에 «다른 자»가 잡은 것 (전부 창끼리 직접 · 전부 수리됨)

| # | 누구 | 무엇 | 어떻게 잡았나 | 수리 |
|---|---|---|---|---|
| 1 | 🔴 B2 | **끊기 무력** — `breakFormatBeforePara` 의 fresh = `moveCaretToEnd` 는 마지막이 글이면 새 칸을 안 만들고 클릭만 · `breaks` 는 «클릭 성공»을 셌다 | 진짜 Chromium: 마크 다음 문단이 **같은 컴포넌트** · `--mutate=break` 로 끊기를 빼도 결과 동일 | `freshTextBlock`(조건 없음) · breaks = 컴포넌트 수 증가로 판정 |
| 2 | 🔴 B2 | **꼬리 번짐** — 마크 뒤 ArrowRight 캐럿이 span 안에 남아 같은 문단 다음 조각이 서식을 물려받음(«밑줄 한 토막» 5자 → 21자) | 글자 수 축(계획 대 실물) + DOM 덤프 | 문단 전부 평문 → 되짚어 칠하기 |
| 3 | 🟠 B2 | 마크 문단마다 **빈 서식 문단**이 남음(Enter 뒤 새 칸) | 빈 서식 문단 세기 | 새 칸 만들면 Enter 생략 |
| 4 | 🟠 B2 | 자가검사가 **굵게**를 안 셈(AM #800·#801 이 굵게 번짐) | value 마크 뒤 통문단 굵게가 verdict 0% | 굵게 ∧ 본문 크기 = 번짐 |
| 5 | 🟠 B2 | 자가검사가 `<p>` 밑 **맨 텍스트**를 안 세 «앞머리 평문 + 서식 span»을 통문단으로(멀쩡한 서식 글이 발행 멈춤 · AC-68) | 3/8 거짓 양성 | 텍스트 노드도 센다 |
| 6 | 🔴 B2 | `place` 카드가 bodyHtml 폴백에서 통째로 사라짐 · `hashtags`(items) op 0 · `adsense` note 0 | 19종 전종 × 두 경로 | 셋 다 수리 + 폴백 11종 하니스 |
| 7 | 🟠 B2 | 티스토리 폴백 «깎였다»가 `notes` 로만 가 서버가 버림(R9-5 안 닿음) | 사슬 추적(core.mjs:225 ↔ runner-jobs.ts:929) | `formatMarks.demoted` + `degradedFieldOf` 순수 함수 |
| 8 | 🔴 B | `pieceCoinCost(…, {tier:"abc"})` **던짐** — 정산 try/catch 가 삼켜 «환급 0 = 더 받는 쪽»(AC-93 반대) | 모르는 입력 10종 격자 | `toCoinTier` 를 식 안에서 한 번 더 |
| 9 | 🟠 A | 화면/모의 `UI.MARK_LABEL` ≠ 서버 `MARK_LABEL`(«핵심 강조» ≠ «핵심 숫자·낱말 강조») · `style_learned` 아이콘 없음 · plan.html 라벨 화면 상수 | A+B 합친 판에서 `verify-label-surface` | A 반영(fec2a8c·40a7b31) |
| 10 | 검사 | `verify-r8-deadends` 「화면이 부르나」가 공용 시트(`ui.js UI.styleSheet`) 경유를 죽은 통로로 오판 · 정본 파일명 `account-styles.ts` 없음 | 화면 3곳이 `UI.styleSheet(` 를 부름 | viaUi 경유 인정(정의만 있고 화면이 안 부르면 여전히 빨강) |
| 11 | 검사 | `verify-r8-audit64` B1 이 옛 자리(content-gen)만 봐 B 의 `lib/cross-account.ts` 를 못 봄(AC-75) | deadends 호출 2곳 + `account_id <>` + measured | «다른 계정을 보나 + 두 경로가 부르나 + measured:false»로 판정 → 닫힘(89→91%) |
| 12 | 검사 | `verify-audit-selftest` C5(출처 헤더 = 산출물이 주석) 빨강 | 메인 질문 → C 결정 ⓐ | 이름 붙인 예외 1칸 · 출력에 찍음 · `SELFTEST_NO_EXCEPTION=1` 부정 대조(끄면 다시 빨강 확인) · 손으로 든 목록의 없는 파일 = 실패 |

그리고 **B7 «에디터 실제 요소»** — 감사 줄이 저장소에 없는 이름(`editorElements`)을 찾고 있었다. C 의 행동 축(«인용·구분선이 `.se-quotation`·`.se-horizontalLine` 진짜 컴포넌트로 선다 · 텍스트 폴백 0»)으로 **ⓐ 닫힘**에 동의(B2 의 넷과 독립).

## 2. 자 여섯 — 무엇을 어떻게 «다른 모양»으로 (전부 ⓪ 축에서 옛 판/옛 식이 빨개진다)

| 파일 | 자 | ⓪ 찌르기 |
|---|---|---|
| `verify-r9-bleed-browser.mjs` | 진짜 Chromium + 가짜 스마트에디터(se-* · 툴바 6종 · 실측 색 팔레트 · «본문 추가»)에 러너 `playOps` 연주 → 문단마다 «계획이 안 시킨 서식» 0 · 꼬리 글자 수 · 새 컴포넌트 · 빈 서식 문단 0 · 자가검사 자체(일부러 번지게 만든 판을 잡나) · B7 컴포넌트 · 변이 3종(스크래치 사본) | 가짜 에디터가 정말 번진다(색·밑줄) · 새 칸은 안 물려받는다 · 인용은 안 센다 |
| `verify-r9-screen-browser.mjs` | 4179 정적 서버 · `piece.html?mock=1&fmt=1` #fmtunused 보임·칩=서버 label·영어 키/러너 어휘 0·겁주는 말 0·손잡이 없으면 안 그림·영상 안 그림 · plan «글로 치면» 서버 숫자 · 계정 시트 등급 = tiers[] (모의는 `UI.api` 에서 가로챈다) | AC-90: 응답에 «이번 글자» 없으면 시작 안 함 |
| `verify-r9-blocks-to-ops.mts` | 블록 어휘를 타입 줄에서 읽어 19종 × 두 경로 → op 또는 note/demoted · 꾸밈 표 전 채널 boolean|null · 쓰레드에 서식 0 · formatUnused 어휘 ↔ 화면 · 티스토리 폴백 규칙 14op 격자 | 옛 판에서 3종 빨강 |
| `verify-r10-ref-leak.mts` | 소독기 이름을 모른 채 찾아 가짜 응답 6모양 → 원문 25자 0 · 최장 저장 문자열 < 22자 · 구조 어휘 닫힘 · B 예시 JSON 생존 · 표에 쓰는 문장마다 소독기 · 러너 캡처 디스크 0·R2 0·로그 URL 0 | 영상 소독기(캡 120~240)가 잡힌다 |
| `verify-r10-capture-slices.mjs` | 높이 축 19개 → 페이지 밖 0·구멍 0·겹침 10~15%·2~6장(짧으면 1)·끝에 붙음·결정적·why | 겹침 0 4등분 옛 식 47건 |
| `verify-coin-tier-honesty.mts` | 등급×채널×포맷×AI 0~12 → 낸 값 ≤ 말한 값 · 0장=1 · 상한 3 · 덜 구우면 덜 받음 · 모르는 tier → 1 · 정규화는 아는 값 · 정산 ≤ 첫 차감 · 호출부 tier · DDL/schema · piecesByTier 서버 셈 | 옛 식 상한 3 초과 |

돌리는 법: `.mjs` 는 `node`, `.mts` 는 `npx --yes tsx` · `| tail` 금지 · 전수는 `node scripts/verify-safe-list.mjs --run` · 화면 자는 `live` 갈래(localhost fetch)라 따로 `node scripts/verify-r9-screen-browser.mjs`.

## 3. 남은 것 (사장님께 드릴 목록 · 무엇이 빠졌나 한 줄)

| 칸 | 상태 | 빠진 것 |
|---|---|---|
| **R9-2·3 네이버 실물** | 확인됨 — **가짜 에디터 범위** | 네이버 스마트에디터 실물 카나리 1회(dryRun · 임시저장) — 합동 세션 17 |
| **R9-11 알림(자동 승인 경로)** | 🟠 반쯤 | 검수 칩은 닫힘 · 알림 경로는 B2 `a639eb0` 진행 중 — `verify-format-notice.mts` 11/1 |
| ③ A1 클립 게시물형 · A5 브런치 · A10 `publish.brunch` | 열림 | 사장님 계정·화면 한 번(외부 선결 · 합동 세션 15) |
| ③ A12 릴스 90초 | 열림 | 설계가 Phase 5 로 잡아 둔 것(`VIDEO_SECONDS` 에 90 없음) |
| ④-12 클립 «앱에서 올리기» 딥링크 | 🟠 일부 | URL 스킴을 지어내지 않는다(DESIGN §17) — 화면 스위치 |
| ③ 편성 다중 규칙 UI | 🟠 일부 | 배포 뒤 라이브에서 화면 한 번 |
| 티스토리 실물 | 미측정 | 가짜 에디터는 네이버 쪽 — 티스토리는 `degradedFieldOf` 순수 격자만(HTML 모드 실물은 라이브 몫) |

## 4. 밟은 함정 (이 라운드 C)
- 🔴 **편집 도중 DOM 을 감싸면 캐럿이 튄다** — 자가검사 거짓 양성을 가리려고 span 감싸기를 MutationObserver 에 넣었더니 가짜 에디터가 망가졌다(글자가 엉뚱한 문단에). 감싸기는 **측정 직전 한 번**. 그리고 그 거짓 양성은 가리지 않고 B2 에 알렸다 — 제품 버그였다.
- 🔴 **변이가 «번짐 0 판»에서는 안 보인다** — measure 변이가 처음엔 0 빨강. 자가검사 자체를 재는 축(일부러 번지게 만든 판)을 더해서야 잡혔다.
- AC-67 을 두 번 더 밟았다(sed 이스케이프 · heredoc 안 `\\n`) — 정확한 편집은 Edit 도구로.
- 두 케이스 덤프를 한 grep 으로 읽고 «태그 줄 중복»이라 착각했다(② 것이 섞였다) — 덤프는 케이스 이름과 함께 읽는다.
- 옥토퍼스 머지로 probe 가 main 과 어긋났다 → verify/r9 를 main 위에 다시 세우는 편이 싸다(메인이 충돌 없이 받는다).

## 5. 창끼리 맞춘 이음새(기록)
B2 `runner/lib/format-bleed.mjs`·`playOps(page, ctx, plan, files, shotKey, missed, fmt, applied)`·`capture-slice.mjs planCaptureSlices`·`tistory.mjs degradedFieldOf` / B `COIN_TIERS`·`pieceCoinCost(…,{tier})`·`lib/text-style.ts sanitizeTextStyleForStorage`·`TEXT_STYLE_RAW_EXAMPLE`·`formatCaps`·`meta.formatUnused=[{field,label,why}]`·`cross_account` / A `#fmtunused`·`accounts-list.tiers[]`·`plans-list.tiers[]`·`?mock=1&fmt=1`·A 포트 4187 / C 포트 4179.
