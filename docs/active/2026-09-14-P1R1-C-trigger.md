# P1R1-C 트리거 — 검증·fix (C)

> 발부 2026-09-14 · 베이스 = 지금 main(`b3c83d4`) · 워크트리 `../AutoCreate-C` · 브랜치 `verify/p1`
> 🔴 C 는 **두 단계**로 돈다. ① 지금(A·B 진행 중): 설계·계약 정합 감사 + 검증 하니스 준비 ② 메인이 «B·A 머지됨» 을 알리면: 라이브 시나리오 + fix.
> 이 라운드의 C 는 AM 규칙대로 **진단→보고**가 기본이고, 작은 fix 는 `fix/p1-*` 브랜치로 직접(전 영역 허용 · 신규 기능 금지).

```
P1R1-C다. 베이스 = 지금 main(b3c83d4). 워크트리 ../AutoCreate-C · 브랜치 verify/p1(이미 체크아웃됨).
읽을 것(순서대로): docs/rules/PARALLEL_GUIDE.md · CLAUDE.md §4·§6·§8 · docs/rules/PITFALLS.md §0 + «검증·보고 규율»(#5~#9) ·
        docs/active/2026-09-14-P1R1-contract.md(전부 · §-1 범위 지도 · §10 시나리오) · docs/active/2026-09-14-P1R1-A-trigger.md · -B-trigger.md ·
        docs/DESIGN.md §4·§5·§5B·§5C·§7·§12·§16B·§13.0 ·
        lib/auth-service.ts · netlify/functions/ops-center.ts(코인 지급 API — 시나리오 준비에 쓴다) · scripts/seed-admin.mjs
[자율주행 — 완전 자율]
- 파일 R/W/Edit, git(add·commit·checkout·merge·fetch·restore), bash·PowerShell, npm, npx, curl, playwright(../AutoMarketing 의 node_modules 에 있다 — 스크린샷은 거기서 실행) — 전부 묻지 말고 자율 실행.
- 커밋 자유. push 불필요·차단됨. fix 는 fix/p1-<이름> 브랜치(베이스 = 그때의 main). 끝나면 [브랜치·해시·보고서 경로]만 보고.
- 비가역 작업(라이브 실고객 데이터 변경·DROP) 금지. 라이브 실행은 **테스트 테넌트만**(이메일 `c+p1@autocreate.test` 로 직접 가입).
[🔴 전본 규칙 — CLAUDE §8] 계약서 §-1 범위 지도의 R1 열 **전 항목**이 코드·화면에 있는지가 1차 판정 기준이다. «있는데 축소됨»도 결함이다.
[진행률 — 의무] "📊 진행률 X%/100 — 방금: … / 다음: …" 최소 8회.
[보고 대상] 메인 세션(autocreate-ca). 보고서 파일 docs/history/2026-09-14-P1R1-C-report.md (C 만 docs/history 에 보고서 1장 쓸 수 있다).
```

■ 1단계 — 지금 (0→40%)
  C1 (0→15%) 정합 감사(정적): 계약서 §1~§6 의 모든 키 이름을 표로 뽑아 → A 트리거의 화면 항목·B 트리거의 함수 항목과 1:1 대조 → 빠진 키·이름 어긋남·«없으면 키 생략» 규칙 위반 가능 지점을 목록으로. DESIGN §5.2·§5C.1·§5C.2·§7.1·§16B 의 필드/검사 항목이 계약·트리거에 **전부** 배정됐는지(범위 지도 vs 본문) 대조 — 빠진 것은 즉시 메인에 보고(트리거 정정은 메인 몫).
  C2 (15→30%) 검증 하니스: `scripts/verify-p1r1.mjs` — BASE_URL(로컬 8899 / 라이브) + 테스트 계정으로 계약서 §10 시나리오 1을 **API 순서대로 자동 실행**하고 각 단계의 응답 키가 계약과 일치하는지 검사(키 누락·여분·타입). 코인 멱등(confirm 2회 → 원장 consume 행 수 동일) · 타 테넌트 IDOR(두 번째 계정으로 pieces-get) · 자격 평문(응답 본문에 password 문자열 0) · 고지 첫 블록 · AI티 검사 결과 존재 · 이미지 URL 200. 결과는 표로 stdout + `_verify/p1r1-<ts>.json`.
  C3 (30→40%) 화면 하니스: `scripts/shot-p1r1.mjs`(playwright · ../AutoMarketing 에서 실행) — 폰 390/데스크톱 1280 으로 accounts→create→director→pieces→piece→schedule 왕복 스크린샷 + 콘솔 에러 수집 + 헌장 자동 검사(페이지당 `.btn.primary` 개수 ≤1 · `.hl .num` ≤1 · 이모지 문자 0 · «테넌트/piece/슬롯» 문자열 0).
  → 1단계 끝나면 메인에 «하니스 준비됨 + 정합 감사 결과» 보고하고 **대기**(메인이 머지 후 호출).

■ 2단계 — 메인 호출 후 (40→95%)
  C4 (40→65%) 로컬 실행: main 최신으로 `git merge main` → `netlify dev --port 8899` → C2·C3 하니스 실행 → 결함 목록(재현 절차·기대/실제·심각도 P0/P1/P2).
  C5 (65→80%) fix: P0(흐름이 끊김·코인 이중차감·자격 노출·IDOR·게이트 미작동)는 `fix/p1-<이름>` 로 직접 수리 + PITFALLS §0 AC-N 추가. P1/P2 는 보고만(A/B 재분배는 메인).
  C6 (80→95%) 라이브(메인 배포 후): 테스트 테넌트로 §10 시나리오 1~3 실행 · 증거(URL·row id·스크린샷 경로) 첨부 · 회귀(로그인·로그아웃·운영센터 대시보드·고객 상세).

■ 보고 (95→100%)
  docs/history/2026-09-14-P1R1-C-report.md: [범위 지도 대비 누락/축소 목록(없으면 0) · 결함 목록(P0/P1/P2 · 재현 · 수리 여부) · 라이브 증거 · 회귀 결과 · 밟은 함정]. 마지막 줄에 fix 브랜치·해시.
  ❌ «실증» 은 증거 옆에만 · 하니스 초록은 증거가 아니다(실물 되짚기 · PITFALLS #9).
