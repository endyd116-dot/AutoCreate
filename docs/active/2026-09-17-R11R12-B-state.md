# R11+R12 · B 상태 — 서버 몫 끝 (2026-09-17)

> 폴더 `C:\Users\Administrator\Desktop\작업\dev\AutoCreate-B` · 브랜치 `feature/r11-back` · 커밋 **`bbaaf1aea9deb417394b2df962822333e25a8c3c`**(본작업 `b79574b` → 상태 `210a3a0` → 마감 `bbaaf1a`)
> 베이스 = `main` `9d81a2b`(트리거가 적은 `05cc828`보다 앞선 tip · `git log -1` 로 직접 읽었다 · AC-98)
> 트리거 `docs/active/2026-09-17-R11R12-B-trigger.md` · 계약 `docs/active/2026-09-17-R11R12-contract.md`

---

## 0. 🔴 메인이 먼저 볼 것 넷

| # | 무엇 | 왜 지금 |
|---|---|---|
| 1 | 🔴 **DDL 둘을 아직 안 걸었다** — `drizzle/0081-r11-account-reader.sql` · `drizzle/0082-r12-daangn-channel.sql` | 트리거가 «**라이브는 읽기만**»이라고 못 박아서 `neon-migrate` 를 안 돌렸다. ⚠️ 그래서 지금 **`schema.ts` 정의가 DDL 보다 앞서 있다**(CLAUDE §4.4 는 «동시에»다). 머지 전에 둘을 걸어야 `accounts.reader` 를 읽는 `ACCOUNT_SELECT` 가 라이브에서 안 터진다 |
| 2 | 🔴 **`verify-channel-tables` 가 빨갛다** — 당근이 `public/js/ui.js UI.CH` 에 없다 | 내가 채널을 넣어서 난 빨강인데 **A 영역**이다(`public/`). A 에게 글자까지 넘겼다(`daangn: "당근"` 한 줄) |
| 3 | **사장님 결재 대기 숫자 둘** | ① 영상 레퍼런스 월 한도 **3/6/20/60**(체험/Starter/Pro/Agency) ② **`video_90` = 42코인**. 둘 다 코드 주석에 «임시값·결재 대기»로 적어 뒀다 |
| 4 | 🔴 **`slots-reschedule` 의 409 는 그대로 뒀다** | §9(하드 게이트 0) 와 §4.7(캐던스) 이 부딪히는 자리인데 **2026-09-15 게이트 감사가 이미 «미루는 것 · 유지»로 닫았다**(그 문서 §146). 내 판단으로 뒤집지 않고 **겹침 사실만 응답에 실었다**(AC-101) |

---

## 0b. 🔴 라운드 마감에 더 잡은 것 셋 (`bbaaf1a` · A·B2 와 주고받으며)

| # | 무엇 | 누가 찾았나 |
|---|---|---|
| 1 | 🔴 **심사가 «고지 ✅»를 거짓으로 찍고 있었다** — `judge.ts disclosure` 가 **계획서**(`p.overlay.badge.text`)를 본다. 러너 오버레이가 `eof_action=pass` 라 자막·배지·제휴 고지가 **첫 프레임에만** 실렸는데도 초록이었다(AC-33 · `duration_fit` 이 겪은 그것과 같은 모양). **대가 표시는 법**이라 이 거짓말이 제일 비싸다.<br>⇒ 고지가 필요한 글인데 `report.overlayVerified` 가 안 오면 **`pending`**(«아직 못 쟀어요»). 🔴 막지는 않는다(§9). `RenderReport.overlayVerified` 칸을 열었다(러너가 채운다 · **B2 몫 · 다음 판이어도 된다** — 안 보내도 아무것도 안 망가지고 그 축만 pending 으로 뜬다) | **B2·C** 가 ffmpeg 8.1.2 로 실측 → B 가 서버 축을 확인·수리 |
| 2 | 🔴 **«이름표 + 마크 배경은 한 짝»** — `UI.CH` 한 줄만 넣으면 **검사는 초록인데 화면은 빈칸**이다(`.mk` 는 흰 글자만 갖고 배경은 `.mk.<키>` 가 준다). `verify-channel-tables` 에 축으로 박았다 · 변이로 확인(이름표만 있고 배경 없으면 exit 1) · 배경이 0개로 읽히면 «정규식이 빗나간 것»으로 따로 죽인다(AC-100 ⑦) | **A** 가 **눈으로** 찾았다 → B 가 자에 박았다 |
| 3 | `lib/video/reference-apply.ts` 를 **B2 에 돌려줬다**(AC-101 — 그쪽에 `captionMotion`·`transition`·`minCutMs` 가 같이 산다). `gen.ts` 는 `audioTempo` 를 «있으면 읽는다»로 받아 **안 와도 비율 1**(무회귀) | B2 가 정본을 주장 → B 가 물림 |

🔴 **러너 1.3.0(R2 에 이미 올라간 판)은 여전히 `pass` 다** — 새 판을 올려야 고객에게 간다. **사장님 Allow 가 필요한 배포**라 B 가 못 한다(AC-50 · 메인 몫).

🔴 **다음 라운드로 넘기는 것 하나**(조용한 축소 아님): `styleApplied.why` 를 **`piece`·`account` 에도** 서버가 싣는다.
지금은 `learned` 일 때만 온다 — A 가 «서버가 정본이니까 세 값 모두 서버 문장이 맞다»고 짚었고 **그 말이 맞다.**
지금은 A 의 fallback 문장이 그 자리를 메우고 있어 화면이 틀리진 않는다(A 코드는 그날 안 고쳐도 되게 짜여 있다).

---

## 1. 만든 것 — 트리거 항목별

### R11

| # | 무엇 | 어디 | 🔴 지킨 것 |
|---|---|---|---|
| **B-1** | 종류 배지 | `lib/slots.ts ruleKindOfPiece` → `pieces.ts pieceRow.ruleKind` | DB `pieces.kind` 안 건드림 · **다섯 번째 kind 이름 0** · 목록·상세 둘 다 실린다 |
| **B-2** | 수익 «어디서 났나» | `lib/revenue/aggregate.ts buildByAxis`·`byAxis`·`axes` | piece → account → **«그 밖»** 3단 · ③을 한쪽에 안 몬다 · «그 밖» 0원이면 줄 없음 · 라벨·문장 서버 정본 |
| **B-3** | `axis` 를 부르게 | `lib/channel-registry.ts axisOfChannel`·`axesOfChannels` | `TEXT_AXIS_KEYS`·`VIDEO_CHANNEL_KEYS` 를 **실제로 읽는다**(여태 0곳) · 모르는 채널은 `null` |
| **B-4** | 추세 + 저하 알림 | `lib/revenue/trend.ts`(순수) · `lib/outcomes.ts viewsWeekCompare` · `home-summary` | 표본 3 미만 = `unknown` · **«무엇을 셌나»**(편/일)를 같이 말한다 · 겁주는 낱말 0개(검사로 박음) · 아이콘 `gauge`(있는 것) |
| **B-5** | «이날 겹쳐요» + 계정 독자 | `lib/publish-gap.ts crowdOf` · `accounts.reader`(DDL 0081) | `crowdOf` 는 **boolean 을 안 돌려준다**(막을 수 없게 만든 모양) · 독자는 **읽는 쪽 넷을 같은 커밋에** |
| **B-6** | 레퍼런스 한도 단위 | `lib/video/reference.ts videoRefQuota` · `lib/plans.ts videoRefsPerMonth` | 글 쪽과 **글자 그대로 같은 모양**(`{used,limit,left,resetAt}`) · 숫자는 임시값이라 주석에 남김 |
| **B-7** | 수익 학습 되먹임 | `lib/learn-tilt.ts` + `director.ts`·`director-auto.ts`·`slots.ts` | 아래 §2 |

### R12

| # | 무엇 | 어디 | 🔴 지킨 것 |
|---|---|---|---|
| **B-8** | 말 속도(서버 몫) | `script.ts budgetFor(seconds, ratio)` · `gen.ts` 두 호출 · `reference-apply` | 안 배웠으면 비율 **정확히 1**(무회귀) · 규격 초과 문장은 **B2 가 쓴다**(두 벌 금지) |
| **B-9** | 릴스 90초 + 워드프레스 고급 | `video/types.ts`·`writing-contracts.ts`·`coin-table.ts` · `publish/wp-advanced.ts` | 길이표·코인·심사 **셋이 같이** · 60 이하 무회귀 · WP 는 **글을 막지 않는다** |
| **B-10** | 당근 | 레지스트리 행 + 글 계약 + `seed-plans` + DDL 0082 | `formatCaps` **전부 false**(`null` 0개) · `monetizable:false` · `maxPhotos:10` · 수익 없음을 화면에 실어 준다 |
| **B-11** | 목록·표 «안»의 꾸밈 | `blocks.ts itemMarks`/`cellMarks` · `format-marks.ts` | 검증이 **항목마다** 돈다 · 조각 넘나드는 마크는 버린다 · 상한은 **글 전체 합** |

---

## 2. 🔴 R12-10(되먹임)에서 «안 한 것»과 그 까닭 — 여기가 이 라운드의 제일 위험한 자리였다

트리거가 «되먹임이 `structure_repeat` 을 되돌리지 않는지 봐라»라고 했다. 그래서 **기울이는 자리를 일부러 좁혔다.**

| 축 | 어디까지 기울이나 | 🔴 왜 더 안 하나 |
|---|---|---|
| **골격** | `pickFormatByPrint` 의 **② 동점 가르기**(이미 «겹친다»고 판정해 갈아탈 때) + ① 견줄 글이 0편일 때 | 겹침 판정 **앞**에 넣으면 «리스티클이 제일 잘 된다»가 골격을 몰고, 그게 곧 R8-A 의 결론(«이상적인 글 한 벌을 베끼지 마라»)을 무른다. 그리고 곧바로 `structure_repeat` 이 빨개져 **재작성이 돌아 돈이 두 배**다 |
| **시각** | `candidatesFor` 의 **기본표 순서만** | 표 밖 시각은 안 만든다 · 고객 골든타임·규칙 고정 시각이 있으면 **아무 일도 안 한다** |
| **스타일** | 계정이 **안 골랐을 때만** | 고객이 건 옷을 우리가 갈아입히지 않는다. 그리고 입혔으면 **`styleApplied.from:"learned"` 로 말해 준다**(조용히 입히면 고객은 자기가 고른 적 없는 모양을 **고장으로 읽는다**) |

부수 효과 하나: 🔴 **`bestHoursFor` 가 여태 «부르는 곳 0곳»이었다.** 주석엔 «편성이 읽는다»고 적혀 있었는데 아니었다(AC-59). `rollSlots` 가 이제 읽는다.

---

## 3. 검증 — 종료코드

```
tsc                    0     verify-r8-deadends     0     verify-server-words    0
verify-label-surface   0     verify-r8-audit64      0     verify-dup-keys        0
verify-coin-tier       0     verify-coin-honesty    0     verify-format-marks    0
verify-kst-surface     0     verify-r11-axis        0  ← 신설(118축)
verify-channel-tables  1  ← 🔴 A 몫 한 줄(UI.CH 당근)
verify-api-channels    1  ← ⚠️ 내 변경 **전에도 1** 이었다(실호출 ECONNRESET · 스택으로 확인)
```

### 3.1 🔴 «거저 초록»이 아님을 증명한 것 — 변이 3건

| 변이 | 결과 |
|---|---|
| `axisOfChannel` 이 모르는 채널을 `"text"` 로 접게 | **exit 1** |
| `allMarksOf` 가 `itemMarks`/`cellMarks` 를 안 세게 | **exit 1** |
| `trendOf` 의 표본 바닥을 `if (false)` 로 | **exit 1** |
| 원복 | exit 0 |

### 3.2 🔴 이 검사가 **나를** 잡은 것 셋 — 원판 줄을 먼저 찍은 값이다(AC-100 ⑦)

1. `TEXT_AXIS_KEYS`·`VIDEO_CHANNEL_KEYS` 에 `external` 모드를 잘못 달았다 → `verify-r8-deadends` 가 빨갛게 잡았다.
   고쳐 놓고 **왜 `external` 이 아닌지**를 그 파일 주석에 적었다(사슬이 두 마디라 **양쪽을 다 센다**).
2. `trendSince` 기대값을 8/28 로 적었는데 실제는 **9/04** 였다 — 함수가 맞고 내가 틀렸다. 그 사실을 하니스 주석에 남겼다.
3. 목록 렌더 비교에서 `insertAdSlots` 가 붙인 광고 자리를 안 떼서 **멀쩡한 코드가 빨갛게** 나왔다. `noAds()` 를 만들고 그 경위를 주석에 적었다.

### 3.3 🔴 `verify-channel-tables` 가 **당근 때문에** 잡아 준 것 셋(둘은 고쳤다)

| 잡힌 것 | 누구 | 상태 |
|---|---|---|
| `textGen=true` 인데 **글 계약이 없다** | B | ✅ 고침 — 안 고치면 `contractFor` 가 **네이버 블로그 계약**을 돌려줘 1,500자 글이 평문 새소식에 올라간다 |
| `scripts/seed-plans.mjs` 에 없다 | B | ✅ 고침(+ DDL 0082 · 글자 하나까지 같게) |
| `public/js/ui.js UI.CH` 에 없다 | **A** | ⏳ A 에게 넘김 |

---

## 4. 🔴 이 환경의 함정 — 밟았고 우회했다

셸이 **역슬래시를 한 겹 먹는다**(AC-100). `python - <<'PY'` 힙독에서 정규식 `/[\n]|[ \t]*/` 를 넣었더니
**정규식 리터럴 안에 생 줄바꿈**이 들어가 `verify-r11-axis.mts` 가 통째로 안 돌았다(그러고도 `grep` 에 아무것도 안 잡혀 «조용히» 지나갈 뻔했다).

⇒ 우회 둘을 썼고, 이 라운드의 파일 수정은 전부 이 방법으로 했다:
1. **패치 스크립트를 `Write` 로 파일에 쓰고 `python <파일>` 로 실행**(셸을 안 지난다) — 역슬래시가 산다.
2. **역슬래시를 아예 안 쓰는 모양**으로 바꾼다 — `noAds()` 는 정규식을 버리고 `split().join()` 으로 갔다.
3. 넣은 뒤 **제어문자 0개**를 셌다(`lib/blocks.ts`·`lib/format-marks.ts`·`verify-r11-axis.mts` 전부 0).

---

## 5. A·B2 와 맞춘 것(글자 그대로)

- **A** — `ruleKind` · `byAxis`/`axes`/`noRevenueChannels` · `bySource[].trend` · `crowd` · `reader`/`readerFrom` · `quota` · `channels[].axis|monetizable|maxPhotos` · 홈 `kind:"gauge"` · `styleApplied.from` 에 **세 번째 값 `"learned"`**.
- **B2** — `itemMarks{i,marks}`/`cellMarks{r,c,marks}`(항목 문자열 기준 · faq 는 쪼개기 전 한 덩이) · `lib/video/tempo.ts` 는 **B2 소유**(cherry-pick `831c1a4` = 내용 동일) · `jobKind "publish.daangn"` · `maxPhotos 10` · 릴스 90초 컷 수.
- 🔴 **내가 `lib/video/reference-apply.ts` 를 만졌다**(B2 가 claim 안 한 파일) — `audioTempo` 를 `unused` → `applied` 로 옮겼다. B2 에 알렸다.
- 🔴 **`lib/cron/runner.ts` 는 안 건드렸다** — A 의 `fix/piece-sweep` 몫(A 가 먼저 알려 왔다).

---

## 6. 다음 사람이 알아야 할 것

1. 🔴 **`accounts.reader` 를 읽는 자리가 넷**이다 — 쓰기(`accounts-update`) · 읽기(`ACCOUNT_SELECT`+`toAccountRow`) · 프롬프트(`content-gen`) · 검수(`pieces-get`). 「허용 목록이 값을 먹은 자리」가 **다섯 번째가 되지 않게** 한 커밋에 넣었다.
2. 🔴 **마크가 사는 자리가 셋**이 됐다(`marks`·`itemMarks`·`cellMarks`). 훑는 코드는 `format-marks.ts allMarksOf`/`filterBlockMarks` **한 곳**에 모았다 — 네 번째가 생기면 거기만 고친다.
3. 🔴 **`contractFor` 가 돌려주는 객체를 고치지 마라.** 60초 캐시에 든 **같은 참조**라, 고치면 그 뒤 60초 동안 **다른 계정 글에 번진다.** 계정 독자는 복사해서 덮는다(`content-gen`·`pieces-get` 둘 다 그렇게 했다).
4. 🔴 **당근 셀렉터는 AM 2026-07 실측**이고 헤더가 «변경 가능성 高»라고 적어 뒀다 — `channel_registry.status` 를 `planned` 로 둔 까닭이다. **탐침 먼저**(합동 세션 항목).
