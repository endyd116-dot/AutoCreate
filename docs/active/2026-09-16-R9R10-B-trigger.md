# R9+R10-B 트리거 — 서식을 적고 · 레퍼런스를 재고 · 코인을 등급으로 (B)

> 발부 2026-09-16 · 베이스 = `main` **`fa1e446`** · 폴더 **`C:\Users\Administrator\Desktop\작업\dev\AutoCreate-B`** · 브랜치 **`feature/r9-back`**(이미 만들어 뒀다)
> 계약서: `docs/active/2026-09-16-R9R10-contract.md` — **먼저 읽어라.** 🔴 전본 개발(축소·왜곡·요약 금지).

## 읽을 것
계약서 · `docs/active/2026-09-15-R9-reference-and-split-design.md` **§2.1d·§2.1e·§3.4·§3.5·§3.6·§3.7** · `CLAUDE.md` **§3·§4.4·§4.5·§4.7·§8·§9** · `docs/rules/PITFALLS.md` **AC-9·17·47·52·57·63·68·69·71·72·74·78·82·91·92·93**

[자율주행 — 완전 자율] 🔴 **push 금지** · 🔴 **실호출 금지**(프롬프트 변경은 **가짜 응답**으로 재라 · 돈이 든다) · 라이브 **읽기만**(쓰기는 그 창에서 사장님 Allow · AC-50).
[진행률] `📊 진행률 X%/100` 최소 8회 · 보고 **10줄 이내**.
**DDL 칸 `0031~0039`** — 🔴 쓰기 전에 `ls drizzle/ | grep 003` 로 **눈으로** 남은 번호를 봐라.

---

# R9 — 서식을 «적는다» (0→30%)

## B-1 (0→12%) 인라인 꾸밈 — `lib/blocks.ts`
[실측] 지금 낼 수 있는 꾸밈이 **«굵게» 하나뿐**이다(`<strong>` 3곳). **밑줄 0 · 형광펜 0 · 이모지 0 · 기울임 0 · 글자색 0.**
[만들 것] 블록 어휘(`BlockType` 18종+`place`)는 **그대로 두고** **인라인 꾸밈**을 넓힌다: **밑줄 · 하이라이트 · 이모지 ·(기울임)**.
🔴 **뜻을 적어라, 모양을 적지 마라** — AM 러너는 «뜻 마크»(`value`·`line`·`row`)를 쓰고 **보이는 모양은 러너가 정한다.** 그래야 채널이 늘어도 서버가 안 바뀐다. **키 이름은 B2 와 직접 맞춰라**(메인 거치지 마라).

## B-2 (12→20%) 🔴 채널이 **낼 수 있는 꾸밈 표** — `lib/channel-registry.ts`
[근거] 네이버·티스토리는 형광펜이 되고 **쓰레드는 글자뿐**이며 **AM 당근 러너에는 서식이 0건**이다. 「서식은 채널마다 다르다」가 AM 에서도 사실로 서 있다.
[만들 것] 채널별 «낼 수 있는 꾸밈» 칸. 🔴 **모르면 `null`** — 그 파일이 이미 그 사상이다(「추측 폴백 제거 · 모르면 null」).

## B-3 (20→26%) 🔴 **«못 낸 서식»을 남긴다** — 영상 `refUnused` 와 **같은 모양**
[왜] 「배웠는데 이 채널에선 못 낸다」가 글 쪽에도 생긴다(설계 §2.1e).
[만들 것] 발행 때 `meta` 에 «이 채널에서 못 낸 서식»(AM 은 `marksDemoted`).
🔴 **A 는 이미 `refUnused` 패턴을 만들어 뒀다** — 같은 모양으로 내면 A 가 **새로 안 짓는다.**
⚠️ 🔴 **`why` 같은 칸에 내부 용어를 넣지 마라** — `verify-server-words.mjs` 는 `error·message·title·body·desc·subject·note·label·text` 만 «사람이 읽는 칸»으로 본다. **네 새 칸을 그 목록에 넣을지 그 자리에서 정하라**(AC-91).

## B-4 (26→30%) 🔴 **유사도 «계정 간»** — R8 잔여 · **위험도 1번**
[실측] `content-gen.ts:288` 이 비교하는 건 «**같은 brief**» **또는** «**같은 계정** 최근 30일»뿐. **다른 brief·다른 계정끼리는 안 본다.**
[왜 위험한가] 🔴 우리 고객은 **계정 하나 = IP 하나**로 여러 계정을 굴린다. **IP 는 돈 주고 나눠 놓고 글 내용 쪽엔 그 방어가 없다.** 플랫폼이 제일 먼저 잡는 게 그것이다.
[만들 것] 같은 테넌트의 **다른 계정·다른 brief** 최근 글과도 본다. 🔴 **막지 마라**(§9) — 재고 **말해 준다**. 그리고 **못 쟀으면 «못 쟀어요»**(AC-9).
⚠️ 🔴 **교차 테넌트는 보지 마라** — 설계 §5F 가 «교차 테넌트는 **집계된 숫자만**»이라 못 박았다.

---

# R10 — 레퍼런스를 재고 저장한다 (30→62%)

## B-5 (30→45%) 🔴 **AI 비전이 읽는다 → «숫자·목록»만 저장**
B2 가 캡처를 넘긴다(폰 폭 · 2~6장 · 겹치게). 네가 **읽고 저장**한다.

[재는 것 — 🔴 «어떻게 생겼나»는 **전부** 잰다]
- **모양**: 문단 길이(줄·자) · 줄바꿈 리듬 · **이모지**(쓰나·어디에·자주 쓰는 것 상위 몇 개·글당 몇 개) · **강조**(굵게/밑줄/형광펜 중 무엇·글당 몇 곳·무엇에 — 숫자/결론/이름) · **사진**(몇 장·어디에·캡션 있나·실사/캡처/그림) · 목록·표·인용·구분선(쓰나·몇 개)
- **말투**: 문장 끝(해요체/합니다체/반말 비율) · 문장 평균 길이 · 질문 문장 비율 · 제목 꼴(숫자 들어가나·몇 자·질문형인가)
- **구성**: 블록 순서 · 결론이 앞인가 뒤인가 · 소제목 몇 개 · 총 길이

[🔴 저장 안 하는 것] **문장·문구·고유명사 주제.**
- 「"요즘 전기요금 때문에 고민 많으시죠?"」 = **원문 → 저장 안 한다**
- 「이모지를 문단 첫머리마다 · ✅📌💡」 = **원문 아님 → 저장한다**

[🔴 캡처는 즉시 버린다] 남의 글은 **한 장도 안 남는다.**

## B-6 (45→52%) 🔴 **새지 않게 하는 이빨** — AM 방식 + 우리가 더 세다
AM `lib/shorts-reference.ts sanitizeTemplateForStorage` 를 **읽어라**(읽기만 허가 · 쓰기·복사 금지).
1. 🔴 **허용 목록 복사**(금지 목록이 **아니라**) — 모델이 `quotes`·`sentences`·`script`·`transcript` 어느 키에 원문을 실어도 **저장 모양에 그 키가 없다.** 금지 목록이면 «새 키로 새는» 구멍이 영원히 남는다.
2. 🔴 **길이 캡**(두 번째 이빨) — 허용된 칸에 **통짜 본문을 밀어 넣는 우회**까지 막는다. ⚠️ **글 축은 캡을 영상보다 훨씬 짧게** — 영상의 120자 캡은 대본은 못 담아도 **블로그 한 문장은 통째로 담는다**.
3. 🟢 **우리가 더 센 것**: 블록 어휘가 **닫힌 목록**이라 구성을 `BlockType[]` 로만 저장하면 **구조가 산문을 실어 나를 수 없다.**
4. **저장 직전 한 곳**에서만 소독한다(문이 하나) · 🔴 **프롬프트는 부탁이고 이 함수가 강제다**(AC-63).

## B-7 (52→58%) **계정의 옷장** (서버 몫)
- **기본** = 계정에 걸어 둔다 → 그 계정 글에 자동으로 계속
- **이번 글만** = 글마다 덮어쓰기
- **직접 쓰기(§5D)** = 내가 쓰되 **구성만 그 틀로**
🔴 **요금제별 «달에 몇 개» 한도**(코인 **안 받는다**). 참고: 영상 레퍼런스는 이미 «코인 0 · 하루 3개»로 돈다 — **같은 결로 맞춰라.**

## B-8 (58→62%) 🔴 **되먹임 원장이 추천한다**
`lib/outcomes.ts`(§5F 원장)가 «이 스타일로 쓴 글이 반응이 좋았어요»를 말한다. **원장의 첫 실사용**이다.
🔴 **표본이 적으면 «못 쟀어요»**(AC-9) — 추천을 지어내지 마라.

---

# R10 — 코인 등급제 (62→88%)

## B-9 (62→72%) 🔴 등급 셋 — 식은 `pieceCoinCost` **한 곳**
| 등급(화면 말) | AI 사진 | 코인 |
|---|---|---|
| **간단히** | 1장 | **1** |
| **보통** | 2~3장 | **2** |
| **프리미엄** | 4~5장 | **3** |
🔴 **상한 3.** 🔴 **내 사진·스톡은 코인 안 늘린다.** 🔴 **AI 사진 0장이어도 1코인**(글 만드는 데 우리 돈이 든다 — 편당 약 68원).
🔴 **AI 를 덜 구우면 덜 받고 돌려준다** — 정산(`settlePieceCoins`)이 이미 있다. **프리미엄(3)을 골랐는데 내 사진으로 다 채워 AI 0장이면 1코인만 받는다.**
⚠️ 🔴 **«고르는 자리»를 갈라 놓지 마라** — 2026-09-15 에 카드뉴스가 1코인으로 샌 게 그 사고다(`coinFormatOf` 가 그 수리). 등급도 **한 곳**에서 갈려야 한다.
⚠️ 🔴 **AC-93**: 등급을 모를 때 **제일 비싼 값을 물리지 마라** — 모르면 **모자라게 받는 쪽**으로만 틀린다.

## B-10 (72→82%) 🔴 등급마다 **글도 달라진다**
사장님: «**전반적인** 컨텐츠 퀄리티». 사진 수만 다르면 **«프리미엄»이 거짓말**이 된다.
| | 간단히 | 보통 | 프리미엄 |
|---|---|---|---|
| AI 사진 | 1장 | 2~3장 | 4~5장 |
| **분량** | 1,000자 | 1,500자 | **2,000자+** |
| **구성** | 소제목 | +목록·표 | **+FAQ·체크리스트** |
| **검색 최적화** | 기본 | 기본 | **구조화 데이터** |
🟢 **글을 늘리는 값은 거의 공짜다** — 글 한 편 AI 값이 **68원**(사진 한 장 64원과 비슷). 프리미엄에 글을 두 배로 얹어도 마진이 거의 안 준다.
⚠️ 🔴 **`visualMin` 을 건드리면 반드시 `contractSelfConflicts` 를 돌려라** — 2026-09-15 `visualMin.faq:1` 이 `tiers.optional` 과 싸워 **네이버 글 10편 중 8편이 재작성**됐다(돈 두 배).

## B-11 (82→88%) 등급 기본값 · 요금제 다시 말하기 (서버 몫)
- 🔴 기본값은 **계정마다**(같은 사람이 수익 블로그는 프리미엄, 취미 계정은 간단히)
- **요금제 «포함 코인»이 몇 편이 되는지** 다시 셈해서 내려 준다(Pro 150 → 간단히 150편 / 보통 75 / 프리미엄 50)
- 🔴 **견적과 실제 차감이 갈릴 수 없게** — `director-estimate`·`rules-estimate`·`slots-list[].coinCost` 가 **같은 식**을 본다

---

# R9 잔여 (88→95%)
- **piece 상태 `edited`** — 지금 `'edited'` 상태값이 코드에 **0건**(`editedByUser` 는 **다른 것**이다)
- **`lib/*.ts` 출처 헤더 25개** — AC-1(«AM 원본 경로·복사일» 또는 «AC 신규»). 🔴 **지어내지 마라** — 모르면 «출처 불명»이라 적어라

## 검증 (95→100%)
```
npx tsc --noEmit > /dev/null 2>&1; echo $?
node scripts/verify-r8-deadends.mjs · verify-api-surface.mjs · verify-label-surface.mjs · verify-server-words.mjs · verify-r8-audit64.mjs   (전부 종료코드)
npx --yes tsx scripts/verify-coin-honesty.mts   # 🔴 C 가 만든 자 — «고객이 내는 값 ≤ 고객에게 말한 값» 780조합
npx --yes tsx scripts/verify-cardnews.mts · verify-fact-claims.mts · verify-seo-keyword.mts · verify-ai-key.mts
+ 네가 새로 만들 것: verify-reference-sanitize(허용 목록·길이 캡) · verify-coin-tier(등급 셋)
```
🔴 **`| tail` 금지**(AC-67) · 🔴 **DDL 은 `node scripts/neon-migrate.mjs <file.sql>`** · **jsonb 는 `sql.json` + 쓴 직후 `jsonb_typeof` 확인까지**(PITFALLS #1)
🔴 **`verify-coin-honesty` 가 초록이어야 코인을 바꿨다고 말할 수 있다** — 그 자는 «아직 아무도 안 본 조합»을 지킨다.

## 보고
`[폴더 · 브랜치 · 커밋해시 · 변경요약 · 검증 증거(종료코드) · 밟은 함정]` **10줄 이내**.
🔴 **A·B2 와 직접 맞춰라**(서식 마크 키 · 캡처 넘기는 모양 · 등급 화면 값). 메인은 범위·머지만 한다.
