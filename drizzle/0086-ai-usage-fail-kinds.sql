-- 0086-ai-usage-fail-kinds.sql — [B · 2026-09-22 · AC-192] **사유가 다섯 늘었다** — DB 주석을 같이 고친다
--   번호: B 칸 `0086~0089`(메인 지시 2026-09-22 · 마지막 0085). 🔴 `ls drizzle/` 로 눈으로 봤다.
--
--   ══ 왜 ══
--     자(`verify-ai-usage-leaks.mjs`)의 ⑤축이 「`return { ok: false` 앞 **12줄** 안에 `recordAiUsage` 가 있나」로 재고 있었다.
--     🔴 **근접은 «같은 갈래»라는 뜻이 아니다.** 딴 갈래(스텁)의 기록이 12줄 안에 있으면 그것도 통과였다.
--     ⇒ 모수를 찍어 보니 `return { ok: false` **28곳 중 검사한 것은 9곳**이었다 — **19곳이 말없이 빠져 있었다.**
--        면제를 「바로 앞 4줄 · 사이에 딴 갈래 없음」으로 좁히자 **진짜로 한 줄도 안 남던 자리 셋**이 드러났다.
--
--   ══ 늘어난 사유(모두 TTS·이미지) ══
--     · `no_audio`    — 🔴 합성은 **200 으로 끝났는데**(= 확실히 청구) 응답에 오디오가 어디 있는지가 없다 ⇒ 금액을 **안다**
--     · `empty_audio` — 🔴 같은 자리. 받아 온 오디오가 비어 있다 ⇒ 금액을 **안다**
--     · `store_failed`(TTS)   — 다 만들고 R2 에 못 넣었다. 영상엔 있던 갈래가 TTS 에만 없어 «예외» 한 덩이에 섞여 있었다 ⇒ 금액을 **안다**
--     · `store_failed`(이미지) — 같은 자리. 🔴 다만 **금액은 0 으로 적는다** — 바로 앞 성공 줄이 실제 금액을 이미 먹였다.
--                               여기 또 적으면 **상한이 두 번 먹는다.** 이 줄은 «금액»이 아니라 **«사유»를 남기는 줄**이다
--     · `exception`   — TTS 바깥 catch. 🔴 **어디서 터졌는지 모른다**(fetch 전일 수도, 다 만든 뒤일 수도)
--                       ⇒ 금액을 **지어내지 않는다**: `cost_usd = 0` · `cost_usd_maybe` 도 NULL(못 쟀음 · AC-9).
--                       그래도 **행은 남는다** — «몇 번 헛돌았나»는 이제 셀 수 있다
--
--   🔴 칸 자체는 그대로다(`varchar(32)` · CHECK 없음) — **추가형이라 이 파일은 주석만 고친다.**
--      가장 긴 새 값 `empty_audio`(11자)도 32 안이다.

COMMENT ON COLUMN ai_usage.fail_kind IS
  '실패 사유. 금액을 알아 cost_usd 에 들어가 상한을 먹는 것: download_failed·store_failed·no_audio·empty_audio(합성/생성이 끝난 뒤라 확실히 청구된다). 금액을 몰라 cost_usd=0 이라 상한을 안 먹는 것: provider_failed·http·timeout·empty·exception. 예외 하나 — 이미지 store_failed 는 금액을 알지만 바로 앞 성공 행이 이미 먹였으므로 0 이다(두 번 먹지 않게). NULL = 성공한 호출.';
