# dependencies
node_modules/

# env / secrets
.env
.env.*
!.env.example

# netlify
.netlify/

# build output
dist/
build/
.next/
out/

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# runner local state
runner/profiles/
runner/.env

# netlify dev artifacts
deno.lock

# per-worktree overrides
.claude/settings.local.json

# 검증 산출물(PITFALLS #16 · _* 커밋 금지)
_verify/
_shots/
runner/_shots/
runner/profiles/
runner/.token
runner/node_modules/
runner/tmp/

# 로컬 스모크·검증 스크립트(PITFALLS #16 — _* 는 커밋하지 않는다)
scripts/_smoke/
assets/fonts/*.ttf

# 🔴 열쇠 파일은 리포에 두지 않는다 — 빈 자리도 두지 않는다(누가 거기에 개인키를 붙여 넣는다 · 2026-09-15).
#    러너 공개키는 빌드가 env `RECIPE_PUBLIC_KEY` 에서 zip 에 써 넣는다(scripts/build-runner.mts).
*.pem
*.key
!*.pem.example
